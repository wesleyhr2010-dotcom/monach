---
phase: 12-seguranca-e-dependencias
plan: 04
type: execute
wave: 2
depends_on:
  - 12-02
files_modified:
  - src/app/admin/actions-analytics.ts
  - src/__tests__/security/analytics-timezone.test.ts
autonomous: true
requirements:
  - SEC-05

must_haves:
  truths:
    - "getSinceDate(7) às 23:59 UTC retorna midnight do Paraguai (UTC-3), não midnight UTC"
    - "getSinceDate(30) à meia-noite UTC retorna o mesmo valor que getSinceDate(30) às 23:59 UTC do ponto de vista do dia paraguaio"
    - "Todos os 7 call sites de getSinceDate continuam recebendo a data correta automaticamente"
  artifacts:
    - path: "src/app/admin/actions-analytics.ts"
      provides: "getSinceDate corrigida com offset UTC-3 fixo"
      contains: "PY_OFFSET_MS"
    - path: "src/__tests__/security/analytics-timezone.test.ts"
      provides: "Testes de timezone verificando que midnight paraguaio é retornado corretamente"
  key_links:
    - from: "src/app/admin/actions-analytics.ts"
      to: "prisma query"
      via: "getSinceDate retorna UTC timestamp equivalente a midnight Asunción"
      pattern: "PY_OFFSET_MS"

user_setup: []
---

<objective>
Corrigir `getSinceDate` em `src/app/admin/actions-analytics.ts` para usar o timezone do Paraguai (UTC-3) em vez de UTC midnight. A função atual usa `d.setHours(0,0,0,0)` que em ambiente serverless Vercel (UTC) equivale a `setUTCHours(0,0,0,0)` — retornando midnight UTC, que corresponde a 21:00 do dia anterior em Asunción. O fix usa offset manual conforme decisões D-06 a D-09 do CONTEXT.md.

Purpose: Relatórios de analytics exibem intervalos incorretos — "últimos 7 dias" na UI pode incluir dados do dia errado dependendo do horário UTC em que a query é executada. Afetar todos os 7 call sites automaticamente via fix na função central.

Output: `getSinceDate` corrigida com `PY_OFFSET_MS`, testes de timezone verificando comportamento correto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Estado atual da função a corrigir: src/app/admin/actions-analytics.ts linha 72-77 -->
```typescript
// ESTADO ATUAL (bugado — usa local TZ que em Vercel = UTC)
function getSinceDate(days: number) {
  const d = new Date();
  d.setDate(d.getDate() - days);
  d.setHours(0, 0, 0, 0);  // BUG: em Vercel (UTC), isso é midnight UTC, não midnight PY
  return d;
}
```

<!-- Lógica alvo: CONTEXT.md D-06..D-09 -->
```typescript
// Lógica correta (D-09):
// 1. Subtrair 3h do "agora" UTC para obter "agora" no Paraguai
// 2. Subtrair os dias
// 3. Zerar para midnight no horário paraguaio (setUTCHours no objeto já ajustado)
// 4. Converter de volta para UTC somando 3h
const PY_OFFSET_MS = 3 * 60 * 60 * 1000; // UTC-3, sem DST desde 2024 (D-07)

function getSinceDate(days: number): Date {
  const nowPy = new Date(Date.now() - PY_OFFSET_MS);
  nowPy.setUTCDate(nowPy.getUTCDate() - days);
  nowPy.setUTCHours(0, 0, 0, 0);
  return new Date(nowPy.getTime() + PY_OFFSET_MS);
}
```

<!-- Call sites verificados no codebase (RESEARCH.md): -->
<!-- 1. getAnalyticsKPIs — linha ~91 -->
<!-- 2. getAnalyticsFluxoMaletas — linha ~144 -->
<!-- 3. getAnalyticsTopRevendedoras — linha ~217 -->
<!-- 4. getAnalyticsProdutosMaisVendidos — linha ~346 -->
<!-- 5. getVitrinaKPIs — linha ~421 -->
<!-- 6. getVitrinaVisitasSeries — linha ~506 -->
<!-- 7. exportVitrinaAnalyticsCSV — linha ~624 -->
<!-- Todos passam getSinceDate(periodDays) para queries ao banco — fix propaga automaticamente -->

<!-- ATENÇÃO: NÃO usar new Date().getTimezoneOffset() em serverless -->
<!-- Em Vercel (UTC), getTimezoneOffset() == 0, tornando a abordagem inútil -->
<!-- Usar apenas PY_OFFSET_MS constante -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS timestamp → banco de dados | getSinceDate gera parâmetro de data para queries Prisma — valor incorreto expõe dados do dia errado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Information Disclosure | getSinceDate retorna midnight UTC incorreto | mitigate | Fix com PY_OFFSET_MS constante — midnight Asunción = UTC+3h |
| T-12-12 | Tampering | Timezone dinâmico via getTimezoneOffset em serverless | accept | Anti-pattern explicitamente documentado — PY_OFFSET_MS é constante fixa conforme D-07 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar testes de timezone e corrigir getSinceDate com PY_OFFSET_MS</name>
  <files>src/app/admin/actions-analytics.ts, src/__tests__/security/analytics-timezone.test.ts</files>
  <read_first>
    - src/app/admin/actions-analytics.ts — ler o arquivo completo; localizar getSinceDate (linha 72) e as primeiras linhas de imports; confirmar posição exata antes de editar
  </read_first>
  <behavior>
    - Test 1: getSinceDate(0) retorna data com horas = 03:00:00 UTC (equivalente a 00:00 Asunción UTC-3)
    - Test 2: getSinceDate(7) retorna data 7 dias atrás relativa ao horário paraguaio, não UTC
    - Test 3: Às 23:59 UTC (20:59 Asunción), getSinceDate(1) retorna hoje em Asunção como início do dia — não ontem UTC
    - Test 4: A diferença entre getSinceDate(7) e getSinceDate(8) é exatamente 86400000ms (1 dia)
  </behavior>
  <action>
Passo 1 — Criar src/__tests__/security/analytics-timezone.test.ts:

```typescript
import { describe, it, expect, vi, afterEach } from "vitest";

/**
 * Testes de timezone para getSinceDate.
 *
 * getSinceDate é uma função privada de actions-analytics.ts.
 * Testamos seu contrato via comportamento observável: o valor retornado
 * deve ser midnight do dia correto no Paraguai (UTC-3).
 *
 * Invariante: getSinceDate(N) retorna UTC timestamp igual a:
 *   midnight_asuncion(today - N_dias) = midnight_paraguaio convertido para UTC
 *
 * Ref: SEC-05, D-06..D-09
 */

// Reimplementação da lógica para testar isoladamente
// (a função original é privada — testamos a lógica aqui)
const PY_OFFSET_MS = 3 * 60 * 60 * 1000; // UTC-3

function getSinceDatePy(days: number): Date {
  const nowPy = new Date(Date.now() - PY_OFFSET_MS);
  nowPy.setUTCDate(nowPy.getUTCDate() - days);
  nowPy.setUTCHours(0, 0, 0, 0);
  return new Date(nowPy.getTime() + PY_OFFSET_MS);
}

// Função bugada original para comparar
function getSinceDateBuggy(days: number): Date {
  const d = new Date();
  d.setDate(d.getDate() - days);
  d.setHours(0, 0, 0, 0); // Em UTC (serverless), equivale a setUTCHours
  return d;
}

describe("SEC-05 — getSinceDate timezone UTC-3 (Paraguai)", () => {
  afterEach(() => {
    vi.useRealTimers();
  });

  it("retorna hora UTC igual a 03:00:00 (midnight Asunción UTC-3)", () => {
    // Fixar now em 2025-01-15T12:00:00Z (12:00 UTC = 09:00 Asunción)
    vi.useFakeTimers();
    vi.setSystemTime(new Date("2025-01-15T12:00:00.000Z"));

    const result = getSinceDatePy(0);

    // Midnight de 2025-01-15 em Asunción = 2025-01-15T03:00:00.000Z
    expect(result.toISOString()).toBe("2025-01-15T03:00:00.000Z");
  });

  it("retorna o dia correto quando now é às 23:59 UTC (20:59 Asunción)", () => {
    // Às 23:59 UTC = 20:59 Asunción — ainda é o MESMO dia no Paraguai
    vi.useFakeTimers();
    vi.setSystemTime(new Date("2025-01-15T23:59:00.000Z"));

    const result = getSinceDatePy(0);

    // Midnight de 2025-01-15 em Asunción = 2025-01-15T03:00:00.000Z
    expect(result.toISOString()).toBe("2025-01-15T03:00:00.000Z");
  });

  it("a função bugada retornaria dia errado às 23:59 UTC", () => {
    // Demonstra o bug: às 23:59 UTC, setHours(0,0,0,0) dá midnight UTC do DIA SEGUINTE
    vi.useFakeTimers();
    vi.setSystemTime(new Date("2025-01-15T23:59:00.000Z"));

    const buggy = getSinceDateBuggy(0);
    const fixed = getSinceDatePy(0);

    // Bugado: 2025-01-15T00:00:00.000Z (midnight UTC = 21:00 do dia anterior em PY)
    // Correto: 2025-01-15T03:00:00.000Z (midnight PY)
    expect(buggy.toISOString()).not.toBe(fixed.toISOString());
    expect(fixed.getUTCHours()).toBe(3); // Sempre 03:00 UTC = 00:00 Asunción
  });

  it("getSinceDate(7) retorna 7 dias atrás no calendário paraguaio", () => {
    vi.useFakeTimers();
    vi.setSystemTime(new Date("2025-01-15T12:00:00.000Z"));

    const result = getSinceDatePy(7);

    // 7 dias antes de 2025-01-15 = 2025-01-08, midnight PY = 2025-01-08T03:00:00Z
    expect(result.toISOString()).toBe("2025-01-08T03:00:00.000Z");
  });

  it("diferença entre getSinceDate(7) e getSinceDate(8) é exatamente 1 dia", () => {
    vi.useFakeTimers();
    vi.setSystemTime(new Date("2025-01-15T12:00:00.000Z"));

    const d7 = getSinceDatePy(7);
    const d8 = getSinceDatePy(8);

    const diffMs = d7.getTime() - d8.getTime();
    expect(diffMs).toBe(86400000); // 24h em ms
  });
});
```

Passo 2 — Modificar getSinceDate em src/app/admin/actions-analytics.ts:

Localizar as linhas 72-77 (função getSinceDate atual) e substituir por:

```typescript
const PY_OFFSET_MS = 3 * 60 * 60 * 1000; // UTC-3, sem DST desde 2024 (D-07)

function getSinceDate(days: number): Date {
  const nowPy = new Date(Date.now() - PY_OFFSET_MS);
  nowPy.setUTCDate(nowPy.getUTCDate() - days);
  nowPy.setUTCHours(0, 0, 0, 0);
  return new Date(nowPy.getTime() + PY_OFFSET_MS);
}
```

ATENÇÃO: `PY_OFFSET_MS` deve ser declarado FORA da função — é uma constante de módulo, não de função. Declarar na mesma posição onde a função getSinceDate está (linha ~72), logo antes da função.

NÃO modificar nenhuma outra função. NÃO modificar os call sites. NÃO modificar `startOfMonthDate`. Apenas substituir os 5 linhas da função getSinceDate e adicionar a constante acima dela.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && grep -n "PY_OFFSET_MS" src/app/admin/actions-analytics.ts && npx vitest run src/__tests__/security/analytics-timezone.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep "PY_OFFSET_MS" src/app/admin/actions-analytics.ts` retorna resultado
    - `grep "setHours" src/app/admin/actions-analytics.ts` NÃO retorna resultado (função bugada removida)
    - `npx vitest run src/__tests__/security/analytics-timezone.test.ts` — todos os testes passam
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Constante e função corrigida existem
cd /Users/wesleyhudson/Documents/next-monarca
grep -n "PY_OFFSET_MS\|getSinceDate\|setUTCHours" src/app/admin/actions-analytics.ts

# 2. Função bugada removida (setHours sem UTC)
grep -n "\.setHours(" src/app/admin/actions-analytics.ts
# Deve retornar vazio ou não encontrar (setHours substituído por setUTCHours)

# 3. Testes de timezone passam
npx vitest run src/__tests__/security/analytics-timezone.test.ts 2>&1 | grep -E "Tests|passed|failed"

# 4. Suite de segurança completa passa
npx vitest run src/__tests__/security/ 2>&1 | grep -E "Tests|passed|failed"

# 5. TypeScript limpo
npx tsc --noEmit 2>&1 | grep "actions-analytics" | head -5
```
</verification>

<success_criteria>
1. `grep "PY_OFFSET_MS" src/app/admin/actions-analytics.ts` retorna `const PY_OFFSET_MS = 3 * 60 * 60 * 1000`
2. `grep "setUTCHours" src/app/admin/actions-analytics.ts` retorna linha dentro de getSinceDate
3. `grep "\.setHours(0" src/app/admin/actions-analytics.ts` não retorna resultado (função bugada removida)
4. `npx vitest run src/__tests__/security/analytics-timezone.test.ts` — todos os testes passam (5 testes)
5. Todos os 7 call sites de getSinceDate (getAnalyticsKPIs, getAnalyticsFluxoMaletas, etc.) continuam sem alteração — o fix é transparente
6. `npx tsc --noEmit` sem erros em actions-analytics.ts
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-seguranca-e-dependencias/12-04-SUMMARY.md`
</output>
