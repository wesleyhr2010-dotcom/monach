---
phase: 12-seguranca-e-dependencias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/export/route.ts
  - src/app/api/export/pdf/route.ts
  - src/__tests__/security/export-auth.test.ts
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "Request GET /api/export sem sessão retorna status 401"
    - "Request GET /api/export/pdf sem sessão retorna status 401"
    - "Request autenticado como ADMIN ou COLABORADORA continua funcionando normalmente"
  artifacts:
    - path: "src/app/api/export/route.ts"
      provides: "Export xlsx/csv com requireAuth no início do GET handler"
      contains: "requireAuth"
    - path: "src/app/api/export/pdf/route.ts"
      provides: "Export PDF com requireAuth no início do GET handler"
      contains: "requireAuth"
    - path: "src/__tests__/security/export-auth.test.ts"
      provides: "Testes de regressão de autenticação nas rotas de export"
      exports: []
  key_links:
    - from: "src/app/api/export/route.ts"
      to: "src/lib/user.ts"
      via: "import requireAuth"
      pattern: "requireAuth.*ADMIN.*COLABORADORA"
    - from: "src/__tests__/security/export-auth.test.ts"
      to: "src/app/api/export/route.ts"
      via: "import GET handler"
      pattern: "expect.*status.*401"

user_setup: []
---

<objective>
Adicionar guarda de autenticação nas duas rotas de export — `/api/export` (xlsx/csv) e `/api/export/pdf` — que atualmente servem dados de PII (nomes, WhatsApp, email, comissão) sem qualquer verificação de sessão. Criar testes de regressão para garantir que requests sem sessão retornam 401.

Purpose: Vulnerabilidade de exposição de PII mais crítica do sistema — qualquer cliente HTTP sem sessão pode baixar dados completos de revendedoras e colaboradoras. O padrão `requireAuth` já existe no codebase e precisa apenas ser aplicado.

Output: Ambas as rotas protegidas com `requireAuth(["ADMIN", "COLABORADORA"])`, testes de regressão verificando 401.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contrato de requireAuth extraído de src/lib/user.ts -->
<!-- requireAuth LANÇA BusinessError — não retorna null -->
```typescript
// src/lib/user.ts
export async function requireAuth(allowedRoles?: Role[]): Promise<CurrentUser>
// Lança BusinessError("Sesión no válida...") se não há sessão
// Lança BusinessError("No tienes permiso...") se role não está em allowedRoles
// Retorna CurrentUser se autenticado e com role permitida
```

<!-- Padrão canônico de uso em route handlers (src/lib/user.ts verificado) -->
```typescript
import { requireAuth } from "@/lib/user";
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export async function GET(request: NextRequest) {
  try {
    await requireAuth(["ADMIN", "COLABORADORA"]);
  } catch {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 });
  }
  // ... lógica existente SEM alteração
}
```

<!-- Padrão de mock para testes: src/__tests__/security/rbac-regression.test.ts -->
```typescript
vi.mock("@/lib/user", async (importOriginal) => {
  const mod = await importOriginal<typeof import("@/lib/user")>();
  return { ...mod, requireAuth: vi.fn() };
});
import { requireAuth } from "@/lib/user";
// No teste: vi.mocked(requireAuth).mockRejectedValue(new Error("Sesión no válida"));
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/export | HTTP requests sem autenticação cruzam para lógica que lê PII do banco |
| Internet → /api/export/pdf | Mesma superfície — dados de revendedoras, WhatsApp, comissão em PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | GET /api/export | mitigate | Adicionar `requireAuth(["ADMIN","COLABORADORA"])` como primeira operação do handler, retornar 401 se lançar |
| T-12-02 | Information Disclosure | GET /api/export/pdf | mitigate | Mesma mitigação: `requireAuth` com try/catch retornando 401 |
| T-12-03 | Tampering | CSRF em export routes | accept | Next.js 16.1.6 CSRF é mitigado na Wave 1 plano 02 (SEC-02 atualiza Next.js) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar requireAuth em /api/export/route.ts e /api/export/pdf/route.ts</name>
  <files>src/app/api/export/route.ts, src/app/api/export/pdf/route.ts</files>
  <read_first>
    - src/app/api/export/route.ts — arquivo a modificar; ler estado atual antes de qualquer edição
    - src/app/api/export/pdf/route.ts — arquivo a modificar; ler estado atual antes de qualquer edição
    - src/lib/user.ts — contrato de requireAuth: a função LANÇA exceção, não retorna null; o catch deve retornar NextResponse com 401
  </read_first>
  <behavior>
    - Test 1: GET /api/export sem sessão → requireAuth lança → handler retorna 401
    - Test 2: GET /api/export/pdf sem sessão → requireAuth lança → handler retorna 401
    - Test 3: GET /api/export com ADMIN autenticado → requireAuth resolve → handler continua normalmente
  </behavior>
  <action>
Em src/app/api/export/route.ts:

1. Adicionar import no topo do arquivo, logo após os imports existentes:
   ```typescript
   import { requireAuth } from "@/lib/user";
   ```

2. No início da função GET, ANTES do bloco `try` existente que processa a query `type`, inserir o guard:
   ```typescript
   export async function GET(request: NextRequest) {
     try {
       await requireAuth(["ADMIN", "COLABORADORA"]);
     } catch {
       return NextResponse.json({ error: "No autorizado" }, { status: 401 });
     }

     const format = request.nextUrl.searchParams.get("format") || "csv";
     // ... resto do handler SEM ALTERAÇÃO
   ```

   ATENÇÃO: O guard deve ser um try/catch SEPARADO do try/catch já existente no handler. Não aninhar — o catch existente captura erros de banco e retorna 500.

Em src/app/api/export/pdf/route.ts:

1. Adicionar import logo após os imports existentes:
   ```typescript
   import { requireAuth } from "@/lib/user";
   ```

2. No início da função GET, ANTES do bloco `try` existente:
   ```typescript
   export async function GET(request: NextRequest) {
     try {
       await requireAuth(["ADMIN", "COLABORADORA"]);
     } catch {
       return NextResponse.json({ error: "No autorizado" }, { status: 401 });
     }

     const type = request.nextUrl.searchParams.get("type") || "resumo";
     // ... resto do handler SEM ALTERAÇÃO
   ```

Não modificar nenhuma outra linha dos arquivos. Apenas inserir o import e o guard no início.
  </action>
  <verify>
    <automated>grep -n "requireAuth" /Users/wesleyhudson/Documents/next-monarca/src/app/api/export/route.ts && grep -n "requireAuth" /Users/wesleyhudson/Documents/next-monarca/src/app/api/export/pdf/route.ts</automated>
  </verify>
  <done>Ambos os arquivos têm `import { requireAuth } from "@/lib/user"` e chamada `await requireAuth(["ADMIN", "COLABORADORA"])` antes do bloco try existente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar testes de regressão de autenticação nas rotas de export (SEC-01, D-10)</name>
  <files>src/__tests__/security/export-auth.test.ts</files>
  <read_first>
    - src/__tests__/security/rbac-regression.test.ts — padrão de mock e estrutura de teste a replicar
    - src/app/api/export/route.ts — importar GET desta rota nos testes
    - src/app/api/export/pdf/route.ts — importar GET desta rota nos testes
  </read_first>
  <behavior>
    - Test 1: GET /api/export sem sessão → status 401, body.error = "No autorizado"
    - Test 2: GET /api/export/pdf sem sessão → status 401, body.error = "No autorizado"
    - Test 3: GET /api/export com params type=revendedoras&format=csv e ADMIN autenticado → requireAuth resolve; o handler continua (mock prisma para evitar DB real)
  </behavior>
  <action>
Criar o arquivo src/__tests__/security/export-auth.test.ts com o seguinte conteúdo:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { BusinessError } from "@/lib/action-utils";

// Mock requireAuth ANTES de importar os handlers
vi.mock("@/lib/user", async (importOriginal) => {
  const mod = await importOriginal<typeof import("@/lib/user")>();
  return { ...mod, requireAuth: vi.fn() };
});

// Mock prisma para evitar conexão real ao banco
vi.mock("@/lib/prisma", () => ({
  prisma: {
    product: { findMany: vi.fn().mockResolvedValue([]) },
    reseller: { findMany: vi.fn().mockResolvedValue([]) },
    maleta: { findMany: vi.fn().mockResolvedValue([]) },
    analyticsAcesso: { findMany: vi.fn().mockResolvedValue([]) },
  },
}));

import { requireAuth } from "@/lib/user";
import { GET as exportGET } from "@/app/api/export/route";
import { GET as exportPdfGET } from "@/app/api/export/pdf/route";
import type { NextRequest } from "next/server";

const mockedRequireAuth = vi.mocked(requireAuth);

// Helper para criar NextRequest mock
function makeRequest(url: string): NextRequest {
  return new Request(url) as unknown as NextRequest;
}

describe("SEC-01 — Autenticação em rotas de export", () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  describe("GET /api/export", () => {
    it("retorna 401 sem sessão (requireAuth lança)", async () => {
      mockedRequireAuth.mockRejectedValue(
        new BusinessError("Sesión no válida. Inicia sesión nuevamente.")
      );
      const req = makeRequest("http://localhost/api/export?type=produtos&format=csv");
      const res = await exportGET(req);
      expect(res.status).toBe(401);
      const body = await res.json();
      expect(body.error).toBe("No autorizado");
    });

    it("retorna 401 sem sessão para type=revendedoras (PII)", async () => {
      mockedRequireAuth.mockRejectedValue(
        new BusinessError("Sesión no válida. Inicia sesión nuevamente.")
      );
      const req = makeRequest("http://localhost/api/export?type=revendedoras&format=xlsx");
      const res = await exportGET(req);
      expect(res.status).toBe(401);
    });

    it("continua execução quando ADMIN autenticado", async () => {
      mockedRequireAuth.mockResolvedValue({
        id: "u1",
        email: "admin@monarca.com",
        profileId: "p1",
        name: "Admin",
        role: "ADMIN",
        isActive: true,
        colaboradoraId: null,
        rawUser: { id: "u1" },
      } as Awaited<ReturnType<typeof requireAuth>>);

      // type=produtos retorna 404 (sem dados no mock) — mas não 401
      const req = makeRequest("http://localhost/api/export?type=produtos&format=csv");
      const res = await exportGET(req);
      expect(res.status).not.toBe(401);
    });
  });

  describe("GET /api/export/pdf", () => {
    it("retorna 401 sem sessão (requireAuth lança)", async () => {
      mockedRequireAuth.mockRejectedValue(
        new BusinessError("Sesión no válida. Inicia sesión nuevamente.")
      );
      const req = makeRequest("http://localhost/api/export/pdf?type=resumo");
      const res = await exportPdfGET(req);
      expect(res.status).toBe(401);
      const body = await res.json();
      expect(body.error).toBe("No autorizado");
    });

    it("retorna 401 sem sessão para type=revendedoras (PII em PDF)", async () => {
      mockedRequireAuth.mockRejectedValue(
        new BusinessError("Sesión no válida. Inicia sesión nuevamente.")
      );
      const req = makeRequest("http://localhost/api/export/pdf?type=revendedoras");
      const res = await exportPdfGET(req);
      expect(res.status).toBe(401);
    });
  });
});
```

Este arquivo segue exatamente o padrão de mock de src/__tests__/security/rbac-regression.test.ts. O mock de prisma retorna arrays vazios para evitar queries reais ao banco.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && npx vitest run src/__tests__/security/export-auth.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os testes em export-auth.test.ts passam. `npx vitest run src/__tests__/security/export-auth.test.ts` retorna "5 tests passed" (ou equivalente) sem falhas.</done>
</task>

</tasks>

<verification>
```bash
# 1. Verificar guards inseridos
grep -n "requireAuth" /Users/wesleyhudson/Documents/next-monarca/src/app/api/export/route.ts
grep -n "requireAuth" /Users/wesleyhudson/Documents/next-monarca/src/app/api/export/pdf/route.ts

# 2. Verificar que guard vem ANTES do try existente (linha deve ser < linha do try principal)
grep -n "requireAuth\|try {" /Users/wesleyhudson/Documents/next-monarca/src/app/api/export/route.ts

# 3. Rodar testes de regressão
cd /Users/wesleyhudson/Documents/next-monarca && npx vitest run src/__tests__/security/export-auth.test.ts

# 4. Verificar que suite de segurança completa ainda passa
cd /Users/wesleyhudson/Documents/next-monarca && npx vitest run src/__tests__/security/
```
</verification>

<success_criteria>
1. `grep -c "requireAuth" src/app/api/export/route.ts` retorna 2 (import + chamada)
2. `grep -c "requireAuth" src/app/api/export/pdf/route.ts` retorna 2 (import + chamada)
3. `npx vitest run src/__tests__/security/export-auth.test.ts` — todos os testes passam
4. `npx vitest run src/__tests__/security/` — todos os testes de segurança passam (sem regressão)
5. `npx tsc --noEmit` — sem erros de TypeScript nos arquivos modificados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-seguranca-e-dependencias/12-01-SUMMARY.md`
</output>
