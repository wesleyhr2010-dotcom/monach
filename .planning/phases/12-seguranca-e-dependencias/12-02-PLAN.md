---
phase: 12-seguranca-e-dependencias
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "npm audit não lista CVEs para next, @serwist/next, serwist ou brace-expansion"
    - "next está em versão 16.2.5 em package.json e no node_modules"
    - "@serwist/next e serwist estão em versão 9.5.11"
    - "npm run build passa sem erros após as atualizações"
  artifacts:
    - path: "package.json"
      provides: "Versões atualizadas: next@16.2.5, eslint-config-next@16.2.5, @serwist/next@9.5.11, serwist@9.5.11"
      contains: "16.2.5"
  key_links:
    - from: "package.json"
      to: "node_modules/next"
      via: "npm install next@16.2.5"
      pattern: '"next": "16.2.5"'

user_setup: []
---

<objective>
Atualizar Next.js de 16.1.6 para 16.2.5 (fecha 6 CVEs incluindo HTTP request smuggling, CSRF, WebSocket origin e DoS) e atualizar @serwist/next e serwist de 9.5.6 para 9.5.11 (fecha vulnerabilidade brace-expansion com CVSS 8.7 na árvore de dependências do service worker).

Purpose: Fechar vulnerabilidades de segurança com CVEs confirmados via npm audit antes de iniciar as novas features das fases 13-15.

Output: package.json atualizado, npm audit limpo para estas dependências, build verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Versões atuais em package.json verificadas -->
```json
{
  "dependencies": {
    "next": "16.1.6",
    "@serwist/next": "^9.5.6"
  },
  "devDependencies": {
    "eslint-config-next": "16.1.6",
    "serwist": "^9.5.6"
  }
}
```

<!-- Comandos de atualização (RESEARCH.md §Standard Stack) -->
<!-- ATENÇÃO: next está PINNED sem caret — npm install simples NÃO atualiza -->
<!-- eslint-config-next deve ser atualizado junto para evitar peer dep conflict -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Pacotes de terceiros entram na cadeia de build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | Next.js HTTP rewrites (GHSA-ggv3-7p47-pfv8) | mitigate | next@16.2.5 fecha este CVE |
| T-12-05 | Tampering | Next.js CSRF em null origin (GHSA-mq59-m269-xvcx) | mitigate | next@16.2.5 fecha este CVE |
| T-12-06 | Denial of Service | Next.js Server Components (GHSA-q4gf-8mx6-v5v3) | mitigate | next@16.2.5 fecha este CVE |
| T-12-07 | Denial of Service | brace-expansion ReDoS via serwist→glob→minimatch (CVSS 8.7) | mitigate | @serwist/next@9.5.11 usa glob@13 que usa brace-expansion@5.x |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar Next.js para 16.2.5 e serwist para 9.5.11</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json — verificar versões atuais ANTES de executar comandos; confirmar que next está pinned como "16.1.6" (sem caret)
  </read_first>
  <action>
Executar os seguintes comandos na raiz do projeto (/Users/wesleyhudson/Documents/next-monarca):

```bash
# 1. Atualizar Next.js e eslint-config-next (devem ter a mesma versão)
npm install next@16.2.5 eslint-config-next@16.2.5

# 2. Atualizar serwist (atenção: @serwist/next é dependency, serwist é devDependency)
npm install @serwist/next@9.5.11
npm install --save-dev serwist@9.5.11
```

Após cada instalação, verificar:
- `npm ls next` — deve mostrar 16.2.5
- `npm ls @serwist/next` — deve mostrar 9.5.11
- `npm ls serwist` — deve mostrar 9.5.11

Se `npm ls` mostrar versão antiga, executar `npm install` novamente com versão explícita.

NÃO executar `npm audit fix --force` — pode causar breaking changes. Apenas os 4 pacotes listados acima.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && npm ls next 2>/dev/null | grep -v deduped | head -5 && npm ls @serwist/next 2>/dev/null | grep -v deduped | head -3</automated>
  </verify>
  <done>
    - `npm ls next` retorna `next@16.2.5`
    - `npm ls @serwist/next` retorna `@serwist/next@9.5.11`
    - `npm ls serwist` retorna `serwist@9.5.11`
    - package.json contém `"next": "16.2.5"` e `"eslint-config-next": "16.2.5"`
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar npm audit limpo e build passa</name>
  <files>package.json</files>
  <read_first>
    - package.json — confirmar versões atualizadas antes de rodar audit e build
  </read_first>
  <action>
Executar em sequência:

```bash
# 1. Verificar audit para os CVEs alvo
npm audit --json 2>/dev/null | node -e "
  const d = require('fs').readFileSync(0,'utf8');
  const j = JSON.parse(d);
  const vulns = j.vulnerabilities || {};
  const targets = ['next','@serwist/next','serwist','brace-expansion'];
  let found = false;
  targets.forEach(t => {
    if (vulns[t]) { console.log('AINDA VULNERÁVEL:', t); found = true; }
  });
  if (!found) console.log('CLEAN: nenhuma vulnerabilidade nos pacotes alvo');
"

# 2. Verificar build
npm run build 2>&1 | tail -20
```

Se o build falhar por incompatibilidade de tipos ou breaking change no Next.js 16.2.5:
- Verificar se o erro está em `src/` ou em arquivos de config
- Se for erro de config (`next.config.ts`), ler o arquivo e corrigir a API deprecada
- NÃO modificar lógica de negócio — apenas ajustar configurações se a API Next.js mudou

Se `npm audit` ainda mostrar CVEs em next, @serwist/next ou serwist após a instalação:
- Verificar se há versões fantasma com `npm ls next --all`
- Executar `npm dedupe` para deduplicar versões
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && npm audit 2>&1 | grep -E "next|serwist|brace-expansion" | grep -v "info" | head -10</automated>
  </verify>
  <done>
    - `npm audit` não lista next, @serwist/next, serwist ou brace-expansion como vulneráveis
    - `npm run build` completa sem erros (pode ter warnings de TypeScript não-críticos, mas sem erros que impedem o build)
    - CVEs SEC-02 e SEC-03 fechados conforme npm audit
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Versões instaladas
cd /Users/wesleyhudson/Documents/next-monarca
npm ls next 2>/dev/null | grep "^next-monarca" -A1 | head -3
npm ls @serwist/next 2>/dev/null | grep "@serwist/next" | head -3
npm ls serwist 2>/dev/null | grep "^serwist" | head -3

# 2. Audit limpo para pacotes alvo
npm audit 2>&1 | grep -E "(next|serwist|brace-expansion)" | grep -v "^npm"

# 3. package.json atualizado
grep -E '"next"|"eslint-config-next"|"@serwist/next"|"serwist"' package.json

# 4. Build
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `grep '"next"' package.json` retorna `"next": "16.2.5"` (sem caret)
2. `grep '"eslint-config-next"' package.json` retorna `"eslint-config-next": "16.2.5"`
3. `grep '"@serwist/next"' package.json` retorna `"@serwist/next": "^9.5.11"` (ou exato)
4. `grep '"serwist"' package.json` retorna `"serwist": "^9.5.11"` (ou exato)
5. `npm audit` não lista CVEs em next, @serwist/next, serwist ou brace-expansion
6. `npm run build` completa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-seguranca-e-dependencias/12-02-SUMMARY.md`
</output>
