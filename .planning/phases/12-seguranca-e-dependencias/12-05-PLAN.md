---
phase: 12-seguranca-e-dependencias
plan: 05
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
  - 12-04
files_modified:
  - docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md
autonomous: true
requirements:
  - SEC-06

must_haves:
  truths:
    - "xlsx e jspdf têm risco documentado como aceito em SPEC_SECURITY_API_ENDPOINTS.md"
    - "Se jspdf@4.2.1 fecha CVEs: package.json atualizado; se não fecha: risco documentado"
    - "npm audit exibe resultado final documentado"
  artifacts:
    - path: "docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md"
      provides: "Seção de riscos aceitos documentando xlsx e jspdf com CVE IDs, justificativa e mitigações"
      contains: "xlsx"
  key_links:
    - from: "docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md"
      to: "src/app/api/export/route.ts"
      via: "referência ao path de uso restrito"
      pattern: "xlsx.*export"

user_setup: []
---

<objective>
Investigar se jspdf@4.2.1 fecha os CVEs identificados (npm audit indica `fixAvailable: true`) e tomar a ação correta: atualizar se fechar, documentar como risco aceito se não fechar. Em ambos os casos, documentar xlsx como risco aceito em `SPEC_SECURITY_API_ENDPOINTS.md` (xlsx não tem fix upstream). Concluir a fase com npm audit documentado.

Purpose: SEC-06 exige que os riscos de xlsx e jspdf sejam documentados explicitamente — não ignorados silenciosamente. A documentação serve como referência para desenvolvedores futuros e auditorias de segurança.

Output: SPEC_SECURITY_API_ENDPOINTS.md com seção de riscos aceitos; package.json atualizado com jspdf@4.2.1 se os CVEs forem fechados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- CVEs identificados via npm audit (RESEARCH.md §Open Questions) -->
<!-- xlsx: sem fix upstream disponível, uso restrito a write/export path -->
<!-- jspdf: npm audit indica fixAvailable:true, range <=4.2.0, versão 4.2.1 existe -->
<!-- CVEs jspdf: GHSA-7x6v-j9x4-qf24 e GHSA-wfv2-pwc8-crg5 -->

<!-- Uso atual no codebase: -->
<!-- xlsx: src/app/api/export/route.ts — apenas WRITE (json_to_sheet, book_append_sheet) -->
<!-- jspdf: src/app/api/export/pdf/route.ts — apenas WRITE (new jsPDF, doc.output) -->
<!-- Nenhum dos dois faz parse de arquivos externos — risk surface é write-only -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules (xlsx) | xlsx tem CVEs sem fix upstream — risco presente em todo build |
| npm registry → node_modules (jspdf) | jspdf potencialmente corrigido em 4.2.1 — investigar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | Prototype Pollution via xlsx (sem fix upstream) | accept | Uso restrito a write-only path em /api/export; rotas protegidas por requireAuth (SEC-01); nenhum parse de arquivo externo |
| T-12-14 | Tampering/Injection | jspdf CVEs (GHSA-7x6v-j9x4-qf24, GHSA-wfv2-pwc8-crg5) | mitigate (se 4.2.1 fechar) ou accept | Investigar 4.2.1; se não fechar: mesma mitigação que xlsx — write-only, autenticado |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Investigar jspdf@4.2.1 e atualizar se CVEs fechados</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json — verificar versão atual de jspdf (deve ser ^4.2.0)
  </read_first>
  <action>
Executar em sequência:

```bash
cd /Users/wesleyhudson/Documents/next-monarca

# 1. Registrar estado atual do audit para jspdf
npm audit --json 2>/dev/null | node -e "
  const d = require('fs').readFileSync(0,'utf8');
  const j = JSON.parse(d);
  const v = j.vulnerabilities || {};
  if (v.jspdf) {
    console.log('jspdf CVEs:', JSON.stringify(v.jspdf.via?.map(x => x.url || x), null, 2));
  } else {
    console.log('jspdf: sem CVEs no audit atual');
  }
"

# 2. Tentar upgrade para 4.2.1
npm install jspdf@4.2.1

# 3. Verificar se CVEs somem após upgrade
npm audit --json 2>/dev/null | node -e "
  const d = require('fs').readFileSync(0,'utf8');
  const j = JSON.parse(d);
  const v = j.vulnerabilities || {};
  if (v.jspdf) {
    console.log('JSPDF AINDA VULNERÁVEL — risco aceito conforme SEC-06');
    console.log(JSON.stringify(v.jspdf.via?.map(x => x.url || x)));
  } else {
    console.log('JSPDF CLEAN — 4.2.1 fechou os CVEs');
  }
"
```

Cenário A: `npm audit` não lista mais jspdf → CVEs fechados. Manter jspdf@4.2.1 em package.json.
Cenário B: `npm audit` ainda lista jspdf → 4.2.1 não fechou. Manter em 4.2.1 de qualquer forma (é versão mais recente) e documentar como risco aceito.

Em ambos os cenários, prosseguir para Task 2 com o resultado documentado.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && grep '"jspdf"' package.json</automated>
  </verify>
  <done>
    - `grep '"jspdf"' package.json` mostra versão >=4.2.1
    - `npm ls jspdf` confirma versão instalada
    - Resultado do npm audit registrado (CVEs fechados ou ainda presentes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Documentar riscos aceitos de xlsx e jspdf em SPEC_SECURITY_API_ENDPOINTS.md</name>
  <files>docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md</files>
  <read_first>
    - docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md — ler o arquivo completo para entender a estrutura existente antes de adicionar a seção
    - src/app/api/export/route.ts — confirmar que requireAuth foi adicionado (plano 01) antes de documentar as mitigações
  </read_first>
  <action>
Ler docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md e localizar a seção mais adequada para adicionar a documentação de riscos aceitos (provavelmente ao final do arquivo ou em uma seção de "Riscos Aceitos").

Adicionar (ou criar) uma seção chamada `## Riscos Aceitos — Dependências com CVE` com o seguinte conteúdo (adaptar ao estilo existente do arquivo):

```markdown
## Riscos Aceitos — Dependências com CVE

> Última revisão: [DATA-ATUAL]
> Revisor: Phase 12 — Segurança e Dependências

As seguintes dependências possuem CVEs conhecidos sem fix upstream disponível ou sem impacto prático no perfil de uso deste projeto. Cada risco foi analisado, documentado e aceito explicitamente.

---

### xlsx — Prototype Pollution

| Campo | Valor |
|-------|-------|
| Pacote | `xlsx@^0.18.5` |
| CVEs | [Verificar `npm audit` para IDs atuais] |
| Severidade | High (CVSS varia por CVE) |
| Fix upstream | Não disponível |
| Decisão | **ACEITO** |

**Perfil de uso:** `xlsx` é usado **exclusivamente** no path de escrita (write-only) em `src/app/api/export/route.ts`:
- `XLSX.utils.json_to_sheet(data)` — converte array JS para sheet
- `XLSX.utils.book_append_sheet(wb, ws, name)` — adiciona sheet ao workbook  
- `XLSX.write(wb, { type: "buffer", bookType: "xlsx" })` — serializa para buffer

**O projeto NUNCA usa** `XLSX.read()`, `XLSX.readFile()` ou qualquer operação de parse de arquivo externo. A superfície de ataque de prototype pollution via parse não é atingível.

**Mitigações adicionais:**
- Rota `/api/export` protegida por `requireAuth(["ADMIN", "COLABORADORA"])` (SEC-01, Phase 12)
- Dados exportados são queries lidas do banco via Prisma — sem input externo não-confiável na cadeia de serialização

**Próxima revisão:** Verificar npm audit no início de cada milestone se fix upstream surgir.

---

### jspdf — [Descrever CVEs: GHSA-7x6v-j9x4-qf24, GHSA-wfv2-pwc8-crg5]

| Campo | Valor |
|-------|-------|
| Pacote | `jspdf@4.2.1` (atualizado de 4.2.0 em Phase 12) |
| CVEs | GHSA-7x6v-j9x4-qf24, GHSA-wfv2-pwc8-crg5 |
| Fix disponível | [RESULTADO DA TASK 1: "Sim — fechado em 4.2.1" ou "Não — 4.2.1 não fecha"] |
| Decisão | [SE FECHADO: "MITIGADO — atualizado para 4.2.1"] [SE NÃO FECHADO: "ACEITO"] |

**Perfil de uso:** `jspdf` é usado **exclusivamente** no path de escrita em `src/app/api/export/pdf/route.ts`:
- `new jsPDF()` — instancia documento
- `autoTable(doc, {...})` — adiciona tabelas
- `doc.output("arraybuffer")` — serializa para buffer

**O projeto NUNCA** faz parse de PDFs externos. Superfície de ataque via input externo não é atingível.

**Mitigações adicionais:**
- Rota `/api/export/pdf` protegida por `requireAuth(["ADMIN", "COLABORADORA"])` (SEC-01, Phase 12)

**Próxima revisão:** Verificar npm audit no início de cada milestone.
```

Preencher os campos em colchetes com os valores reais obtidos na Task 1 (resultado do npm audit, se CVEs foram fechados, data atual).

NÃO modificar nenhuma outra seção do arquivo.
  </action>
  <verify>
    <automated>grep -c "Riscos Aceitos\|xlsx\|jspdf" /Users/wesleyhudson/Documents/next-monarca/docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md</automated>
  </verify>
  <done>
    - `grep "Riscos Aceitos" docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md` retorna resultado
    - `grep "xlsx" docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md` retorna resultado
    - `grep "jspdf" docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md` retorna resultado
    - npm audit final documentado no arquivo com data e decisão explícita (ACEITO ou MITIGADO)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Todos os 5 planos da Fase 12 foram executados:
    - Plano 01: requireAuth em /api/export e /api/export/pdf + testes de regressão
    - Plano 02: Next.js 16.2.5 + serwist 9.5.11 + build verificado
    - Plano 03: sanitize-html + email-sanitizer.ts + integração em emails.ts + testes
    - Plano 04: getSinceDate com PY_OFFSET_MS + testes de timezone
    - Plano 05: jspdf investigado/atualizado + riscos documentados
  </what-built>
  <how-to-verify>
    Executar o checklist completo da fase:

    ```bash
    cd /Users/wesleyhudson/Documents/next-monarca

    # 1. SEC-01: Auth nas rotas de export
    echo "--- SEC-01: Auth ---"
    grep -c "requireAuth" src/app/api/export/route.ts
    grep -c "requireAuth" src/app/api/export/pdf/route.ts

    # 2. SEC-02/03: Versões de dependências
    echo "--- SEC-02/03: Dependências ---"
    grep -E '"next"|"eslint-config-next"|"@serwist/next"|"serwist"' package.json

    # 3. SEC-04: Sanitização
    echo "--- SEC-04: Sanitização ---"
    test -f src/lib/email-sanitizer.ts && echo "email-sanitizer.ts: EXISTS" || echo "MISSING"
    grep "sanitizeEmailHtml" src/lib/emails.ts

    # 4. SEC-05: Timezone
    echo "--- SEC-05: Timezone ---"
    grep "PY_OFFSET_MS" src/app/admin/actions-analytics.ts

    # 5. SEC-06: Documentação
    echo "--- SEC-06: Docs ---"
    grep -c "Riscos Aceitos" docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md

    # 6. Todos os testes de segurança
    echo "--- Testes ---"
    npx vitest run src/__tests__/security/ 2>&1 | grep -E "Tests|passed|failed"

    # 7. npm audit status
    echo "--- npm audit ---"
    npm audit 2>&1 | grep -E "critical|high" | tail -5
    ```

    Resultado esperado:
    - Itens 1-5: todos retornam valores conforme esperado
    - Testes: todos passam (sem falhas)
    - npm audit: sem critical/high em next, serwist, brace-expansion
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
```bash
# Checklist completo da Fase 12
cd /Users/wesleyhudson/Documents/next-monarca

# SEC-01
grep -c "requireAuth" src/app/api/export/route.ts
grep -c "requireAuth" src/app/api/export/pdf/route.ts

# SEC-02/03
grep '"next"' package.json
grep '"@serwist/next"' package.json

# SEC-04
grep "sanitizeEmailHtml" src/lib/emails.ts

# SEC-05
grep "PY_OFFSET_MS" src/app/admin/actions-analytics.ts

# SEC-06
grep -c "xlsx\|jspdf" docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md

# Todos os testes
npx vitest run src/__tests__/security/ 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `grep "Riscos Aceitos" docs/sistema/SPEC_SECURITY_API_ENDPOINTS.md` retorna resultado
2. A seção documenta xlsx com decisão ACEITO e mitigações (write-only + requireAuth)
3. A seção documenta jspdf com resultado real da investigação de 4.2.1 (MITIGADO ou ACEITO)
4. npm audit final registrado no documento com data
5. `npx vitest run src/__tests__/security/` — todos os testes passam (export-auth + email-sanitizer + analytics-timezone + testes existentes)
6. `npm run build` passa sem erros (validação final de toda a fase)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-seguranca-e-dependencias/12-05-SUMMARY.md`
</output>
