---
phase: 12-seguranca-e-dependencias
plan: 03
type: execute
wave: 2
depends_on:
  - 12-02
files_modified:
  - src/lib/email-sanitizer.ts
  - src/lib/emails.ts
  - src/__tests__/security/email-sanitizer.test.ts
autonomous: true
requirements:
  - SEC-04

must_haves:
  truths:
    - "sanitizeEmailHtml remove <script> e conteúdo script"
    - "sanitizeEmailHtml remove event handlers (onclick, onload, onerror)"
    - "sanitizeEmailHtml remove href com protocolo javascript:"
    - "sanitizeEmailHtml remove tags não permitidas pela allowlist (ex: <table>, <img>)"
    - "sanitizeEmailHtml preserva tags permitidas e atributo style"
    - "sendEmail em emails.ts sanitiza htmlContent antes de enviar"
  artifacts:
    - path: "src/lib/email-sanitizer.ts"
      provides: "Helper sanitizeEmailHtml com allowlist canônica D-01..D-04"
      exports: ["sanitizeEmailHtml", "EMAIL_ALLOWED_TAGS", "EMAIL_ALLOWED_ATTRS"]
    - path: "src/lib/emails.ts"
      provides: "sendEmail com sanitização defensiva de htmlContent"
      contains: "sanitizeEmailHtml"
    - path: "src/__tests__/security/email-sanitizer.test.ts"
      provides: "Testes unitários de sanitização — documenta allowlist como código executável"
  key_links:
    - from: "src/lib/emails.ts"
      to: "src/lib/email-sanitizer.ts"
      via: "import sanitizeEmailHtml"
      pattern: "sanitizeEmailHtml"

user_setup:
  - service: npm
    why: "Instalar sanitize-html e seus tipos TypeScript"
    env_vars: []
    dashboard_config:
      - task: "Executar: npm install sanitize-html && npm install --save-dev @types/sanitize-html"
        location: "Terminal na raiz do projeto"
---

<objective>
Instalar `sanitize-html`, criar o helper `sanitizeEmailHtml()` com a allowlist canônica definida nas decisões D-01 a D-05 do CONTEXT.md, e aplicar como wrapper defensivo em `emails.ts` antes do envio via Brevo. Criar testes unitários que documentam a allowlist como contrato executável — estes testes servem também como referência para a Fase 13 (editor de templates).

Purpose: O sistema atual usa regex para sanitização com bypass confirmado (mencionado em REQUIREMENTS.md SEC-04). `sanitize-html` com allowlist explícita fecha vetores como encoding, nested tags e atributos codificados que regex não consegue tratar.

Output: `src/lib/email-sanitizer.ts` (novo), `emails.ts` modificado com sanitização, testes de regressão passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- API de sanitize-html verificada via Context7 -->
```typescript
import sanitizeHtml from "sanitize-html";

sanitizeHtml(html: string, options: {
  allowedTags: string[];
  allowedAttributes: Record<string, string[]> | false;
  allowedSchemes: string[];
  allowProtocolRelative: boolean;
}): string
```

<!-- Contrato de emails.ts a modificar -->
```typescript
// src/lib/emails.ts — assinatura atual de sendEmail
export async function sendEmail(
  params: SendEmailLegacy | SendEmailBranded
): Promise<void>
// params.htmlContent é o campo a sanitizar antes de enviar
```

<!-- Allowlist canônica CONTEXT.md D-01 a D-04 -->
```typescript
// D-01: Tags permitidas
const tags = ['p','br','strong','em','ul','ol','li','a','h1','h2','h3','span','div'];
// D-02: style em TODAS as tags (obrigatório para email clients)
// D-03: href apenas em <a>, http e https únicos
// D-04: todos os demais atributos removidos — allowlist explícita, não blocklist
// D-05: esta allowlist é canônica para Fase 13
```

<!-- Padrão de teste: src/lib/email-base.test.ts (padrão a seguir) -->
```typescript
import { describe, it, expect } from "vitest";
```
</interfaces>

<!-- ATENÇÃO sobre email-base.ts -->
<!-- email-base.ts usa escapeHtml() em strings de texto puro (title, greeting) -->
<!-- sanitizeEmailHtml é DISTINTO: é para HTML com algumas tags, não texto puro -->
<!-- NÃO modificar email-base.ts — os dois mecanismos coexistem sem conflito -->
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin input → emails.ts → Brevo | HTML inserido pelo admin (Fase 13) cruzará para envio de email — deve ser sanitizado antes de sair do sistema |
| Template TypeScript → emails.ts | Templates hardcoded já são seguros, mas sanitização defensiva é custo zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | HTML injection em corpo de email via admin (Fase 13) | mitigate | sanitizeEmailHtml com allowlist + allowProtocolRelative:false aplicado em sendEmail antes de enviar |
| T-12-09 | Tampering | Script injection via href javascript: | mitigate | allowedSchemes: ["http","https"] + allowProtocolRelative:false no sanitize-html |
| T-12-10 | Tampering | Event handler injection (onclick, onerror) | mitigate | allowedAttributes explícita exclui todos os handlers — apenas style e href permitidos |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Instalar sanitize-html e criar src/lib/email-sanitizer.ts</name>
  <files>src/lib/email-sanitizer.ts</files>
  <read_first>
    - src/lib/email-base.ts — entender como escapeHtml funciona para NÃO confundir com sanitizeEmailHtml; os dois são distintos e coexistem
    - package.json — verificar que sanitize-html ainda não está instalado
  </read_first>
  <action>
Passo 1 — Instalar dependências:
```bash
cd /Users/wesleyhudson/Documents/next-monarca
npm install sanitize-html
npm install --save-dev @types/sanitize-html
```

Verificar instalação: `npm ls sanitize-html` deve retornar `sanitize-html@2.x.x`.

Passo 2 — Criar src/lib/email-sanitizer.ts:

```typescript
/**
 * Sanitização de HTML para emails transacionais Monarca.
 *
 * Allowlist canônica para Fase 13 (editor de templates admin).
 * Ref: docs/sistema/SPEC_SECURITY_DATA_PROTECTION.md, CONTEXT.md D-01..D-05
 *
 * DISTINTO de escapeHtml (email-base.ts):
 * - escapeHtml: converte texto puro em texto seguro sem nenhuma tag
 * - sanitizeEmailHtml: HTML com tags permitidas, scripts/eventos removidos
 */
import sanitizeHtml from "sanitize-html";

/**
 * Tags HTML permitidas em corpos de email transacional.
 * D-01: suficientes para emails ricos sem expor vetores de ataque.
 */
export const EMAIL_ALLOWED_TAGS: string[] = [
  "p",
  "br",
  "strong",
  "em",
  "ul",
  "ol",
  "li",
  "a",
  "h1",
  "h2",
  "h3",
  "span",
  "div",
];

/**
 * Atributos permitidos por tag.
 * D-02: style em TODAS as tags (email clients exigem CSS inline).
 * D-03: href apenas em <a>.
 * D-04: todos os demais atributos removidos (class, id, data-*, onclick, etc.).
 */
export const EMAIL_ALLOWED_ATTRS: sanitizeHtml.IOptions["allowedAttributes"] = {
  "*": ["style"],
  a: ["href"],
};

/**
 * Sanitiza HTML para uso seguro em emails transacionais.
 *
 * Remove:
 * - Todas as tags não listadas em EMAIL_ALLOWED_TAGS
 * - Todos os atributos exceto style (todas tags) e href (<a>)
 * - href com protocolo não-http/https (bloqueia javascript:, data:, file:)
 * - Protocolos relativos (//evil.com)
 *
 * Preserva:
 * - Tags de formatação (p, strong, em, h1-h3, ul/ol/li, span, div)
 * - CSS inline via style (obrigatório para email clients)
 * - Links http/https via href em <a>
 *
 * @param html — HTML a sanitizar (pode conter input não-confiável)
 * @returns HTML sanitizado com apenas tags e atributos da allowlist
 */
export function sanitizeEmailHtml(html: string): string {
  return sanitizeHtml(html, {
    allowedTags: EMAIL_ALLOWED_TAGS,
    allowedAttributes: EMAIL_ALLOWED_ATTRS,
    allowedSchemes: ["http", "https"],
    allowProtocolRelative: false,
  });
}
```
  </action>
  <verify>
    <automated>test -f /Users/wesleyhudson/Documents/next-monarca/src/lib/email-sanitizer.ts && grep -c "sanitizeEmailHtml" /Users/wesleyhudson/Documents/next-monarca/src/lib/email-sanitizer.ts</automated>
  </verify>
  <done>
    - Arquivo src/lib/email-sanitizer.ts existe
    - Exporta `sanitizeEmailHtml`, `EMAIL_ALLOWED_TAGS`, `EMAIL_ALLOWED_ATTRS`
    - `npm ls sanitize-html` confirma pacote instalado
    - `npm ls @types/sanitize-html` confirma tipos instalados
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar sanitizeEmailHtml em emails.ts e criar testes de sanitização (D-11)</name>
  <files>src/lib/emails.ts, src/__tests__/security/email-sanitizer.test.ts</files>
  <read_first>
    - src/lib/emails.ts — ler estado atual completo; identificar onde htmlContent é passado para o payload (linha ~58); inserir sanitização ali
    - src/__tests__/security/rbac-regression.test.ts — padrão de estrutura de describe/it a seguir
    - src/lib/email-sanitizer.ts — confirmar exports antes de importar nos testes
  </read_first>
  <behavior>
    - Test 1: `<script>alert(1)</script>texto` → sanitize remove `<script>`, retorna `texto`
    - Test 2: `<p onclick="evil()">texto</p>` → remove onclick, retorna `<p>texto</p>`
    - Test 3: `<a href="javascript:alert(1)">link</a>` → remove href malicioso, retorna `<a>link</a>` (sem href)
    - Test 4: `<table><tr><td>dado</td></tr></table>` → remove table (não na allowlist), retorna `dado`
    - Test 5: `<p style="color:red">texto</p>` → preserva style (D-02), retorna `<p style="color:red">texto</p>`
    - Test 6: `<a href="https://monarca.com">link</a>` → preserva href https (D-03), retorna completo
    - Test 7: `<strong>negrito</strong>` → preserva tag permitida, retorna `<strong>negrito</strong>`
    - Test 8: `<img src="x" onerror="evil()">` → remove img (não na allowlist), retorna vazio
  </behavior>
  <action>
Passo 1 — Modificar src/lib/emails.ts:

Adicionar import no topo do arquivo (após o import existente do BrevoClient):
```typescript
import { sanitizeEmailHtml } from "@/lib/email-sanitizer";
```

Modificar a linha onde `htmlContent: params.htmlContent` é atribuída ao payload (linha ~58 do arquivo atual):
```typescript
// ANTES:
htmlContent: params.htmlContent,

// DEPOIS:
htmlContent: sanitizeEmailHtml(params.htmlContent),
```

O arquivo resultante deve ter apenas estas duas modificações — sem alterar nenhuma outra linha.

Passo 2 — Criar src/__tests__/security/email-sanitizer.test.ts:

```typescript
import { describe, it, expect } from "vitest";
import { sanitizeEmailHtml, EMAIL_ALLOWED_TAGS } from "@/lib/email-sanitizer";

/**
 * Testes de contrato da allowlist de sanitização de email.
 * Estes testes documentam e enforcement a allowlist D-01..D-04 (CONTEXT.md).
 * São usados também como referência pela Fase 13 (editor de templates admin).
 *
 * Ref: SEC-04, D-11
 */
describe("sanitizeEmailHtml — allowlist contrato", () => {
  describe("Remove vetores de ataque", () => {
    it("remove <script> e seu conteúdo", () => {
      const result = sanitizeEmailHtml("<script>alert(1)</script>texto");
      expect(result).not.toContain("<script>");
      expect(result).not.toContain("alert(1)");
      expect(result).toContain("texto");
    });

    it("remove event handlers em atributos (onclick)", () => {
      const result = sanitizeEmailHtml('<p onclick="evil()">texto</p>');
      expect(result).not.toContain("onclick");
      expect(result).toContain("<p>");
      expect(result).toContain("texto");
    });

    it("remove event handlers em atributos (onerror, onload)", () => {
      const result = sanitizeEmailHtml('<img src="x" onerror="evil()" onload="also()">');
      expect(result).not.toContain("onerror");
      expect(result).not.toContain("onload");
      expect(result).not.toContain("<img");
    });

    it("remove href com protocolo javascript:", () => {
      const result = sanitizeEmailHtml('<a href="javascript:alert(1)">clique</a>');
      expect(result).not.toContain("javascript:");
      expect(result).toContain("clique");
    });

    it("remove href com protocolo data:", () => {
      const result = sanitizeEmailHtml('<a href="data:text/html,<script>evil()</script>">link</a>');
      expect(result).not.toContain("data:");
      expect(result).toContain("link");
    });

    it("remove tags não na allowlist (<table>, <img>)", () => {
      const result = sanitizeEmailHtml("<table><tr><td>dado</td></tr></table>");
      expect(result).not.toContain("<table");
      expect(result).not.toContain("<tr");
      expect(result).not.toContain("<td");
      expect(result).toContain("dado");
    });

    it("remove atributos não permitidos (class, id, data-*)", () => {
      const result = sanitizeEmailHtml('<p class="danger" id="x" data-payload="evil">texto</p>');
      expect(result).not.toContain('class=');
      expect(result).not.toContain('id=');
      expect(result).not.toContain('data-payload');
      expect(result).toContain("texto");
    });
  });

  describe("Preserva conteúdo legítimo (D-01..D-03)", () => {
    it("preserva atributo style em qualquer tag (D-02)", () => {
      const result = sanitizeEmailHtml('<p style="color:red;font-weight:bold">texto</p>');
      expect(result).toContain('style="color:red;font-weight:bold"');
      expect(result).toContain("texto");
    });

    it("preserva href https em <a> (D-03)", () => {
      const result = sanitizeEmailHtml('<a href="https://monarcasemijoyas.com.py">link</a>');
      expect(result).toContain('href="https://monarcasemijoyas.com.py"');
      expect(result).toContain("link");
    });

    it("preserva href http em <a> (D-03)", () => {
      const result = sanitizeEmailHtml('<a href="http://example.com">link</a>');
      expect(result).toContain('href="http://example.com"');
    });

    it("preserva todas as tags de formatação (D-01)", () => {
      const html = "<p>p</p><strong>s</strong><em>e</em><ul><li>l</li></ul><h1>h1</h1><span>sp</span><div>d</div>";
      const result = sanitizeEmailHtml(html);
      EMAIL_ALLOWED_TAGS
        .filter(t => !["br"].includes(t)) // br não tem closing
        .forEach(tag => {
          expect(result).toContain(`<${tag}`);
        });
    });

    it("não altera string vazia", () => {
      expect(sanitizeEmailHtml("")).toBe("");
    });

    it("não altera texto puro sem HTML", () => {
      const text = "Olá João, sua maleta foi enviada.";
      expect(sanitizeEmailHtml(text)).toBe(text);
    });
  });
});

describe("sanitizeEmailHtml — EMAIL_ALLOWED_TAGS export", () => {
  it("contém as 13 tags canônicas D-01", () => {
    const expected = ["p", "br", "strong", "em", "ul", "ol", "li", "a", "h1", "h2", "h3", "span", "div"];
    expected.forEach(tag => {
      expect(EMAIL_ALLOWED_TAGS).toContain(tag);
    });
    expect(EMAIL_ALLOWED_TAGS).toHaveLength(13);
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && npx vitest run src/__tests__/security/email-sanitizer.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    - `grep "sanitizeEmailHtml" src/lib/emails.ts` retorna resultado (import e uso)
    - `npx vitest run src/__tests__/security/email-sanitizer.test.ts` — todos os testes passam (12+ testes)
    - `npx tsc --noEmit` — sem erros de TypeScript nos arquivos modificados
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Pacote instalado
cd /Users/wesleyhudson/Documents/next-monarca
npm ls sanitize-html 2>/dev/null | head -3

# 2. Helper criado e exporta função
grep -c "export function sanitizeEmailHtml" src/lib/email-sanitizer.ts

# 3. emails.ts usa sanitização
grep "sanitizeEmailHtml" src/lib/emails.ts

# 4. Testes passam
npx vitest run src/__tests__/security/email-sanitizer.test.ts 2>&1 | grep -E "Tests|passed|failed"

# 5. TypeScript limpo
npx tsc --noEmit 2>&1 | grep -E "email-sanitizer|emails.ts" | head -10

# 6. Allowlist tem 13 tags
grep "EMAIL_ALLOWED_TAGS" src/lib/email-sanitizer.ts | grep -c "p\|br\|strong"
```
</verification>

<success_criteria>
1. `src/lib/email-sanitizer.ts` existe e exporta `sanitizeEmailHtml`, `EMAIL_ALLOWED_TAGS`, `EMAIL_ALLOWED_ATTRS`
2. `grep -c "sanitizeEmailHtml" src/lib/emails.ts` retorna 2 (import + uso no payload)
3. `npx vitest run src/__tests__/security/email-sanitizer.test.ts` — todos os testes passam (zero falhas)
4. `EMAIL_ALLOWED_TAGS` tem exatamente 13 tags: p, br, strong, em, ul, ol, li, a, h1, h2, h3, span, div
5. `allowProtocolRelative: false` presente em email-sanitizer.ts
6. `npx tsc --noEmit` sem erros em email-sanitizer.ts ou emails.ts
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-seguranca-e-dependencias/12-03-SUMMARY.md`
</output>
