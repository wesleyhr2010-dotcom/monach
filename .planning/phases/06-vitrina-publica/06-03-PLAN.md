---
phase: 06-vitrina-publica
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-04
files_modified:
  - src/lib/vitrina.ts
  - src/app/vitrina/[slug]/[produtoId]/page.tsx
  - src/components/vitrina/ProductDetailView.tsx
  - src/components/vitrina/ProductCard.tsx
autonomous: true
requirements:
  - VITR-13
  - VITR-14
must_haves:
  truths:
    - "Product detail page accessible at /vitrina/{slug}/{produtoId}"
    - "Detail page displays product photo, name, price, and description"
    - "Agregar al carrito button adds product to localStorage cart"
    - "Invalid or non-maleta variant returns 404"
    - "Grid cards link to detail page with correct URL pattern"
  artifacts:
    - path: src/lib/vitrina.ts
      provides: getVitrinaProductDetail query
      exports:
        - getVitrinaProductDetail
    - path: src/app/vitrina/[slug]/[produtoId]/page.tsx
      provides: ISR detail page with metadata
      contains:
        - "revalidate = 300"
        - "generateMetadata"
        - "notFound()"
    - path: src/components/vitrina/ProductDetailView.tsx
      provides: Client interactivity for detail page
      contains:
        - "Agregar al carrito"
        - "addToVitrinaCart"
  key_links:
    - from: page.tsx
      to: getVitrinaProductDetail
      via: import from @/lib/vitrina
    - from: ProductDetailView
      to: addToVitrinaCart
      via: import from @/lib/cart-vitrina
    - from: ProductCard
      to: /vitrina/{slug}/{variantId}
      via: Link href
---

<objective>
Build the product detail page at `/vitrina/{slug}/{produtoId}` with ISR caching, product photos, description, price, and an "Agregar al carrito" button that adds the item to the localStorage cart.

Purpose: Enable customers to view product details before adding to cart, completing the grid → detail → cart flow.
Output: Working detail page with secure data fetching and cart integration.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-vitrina-publica/06-CONTEXT.md
@.planning/phases/06-vitrina-publica/06-RESEARCH.md
@docs/revendedoras/SPEC_VITRINE_PUBLICA.md

## Key patterns from codebase

**Vitrina page (Plan 06-01):**
- `src/app/vitrina/[slug]/page.tsx` — ISR `revalidate = 300`, `generateMetadata`, `notFound()` for missing data
- Layout at `src/app/vitrina/[slug]/layout.tsx` wraps children with CartProvider + CartShell

**Cart system (Plan 06-04):**
- `src/lib/cart-vitrina.ts` — `addToVitrinaCart(item, resellerSlug, quantity)`
- `src/components/vitrina/CartProvider.tsx` — `useVitrinaCart()` hook

**ProductCard (Plan 06-01):**
- Links to `/vitrina/${slug}/${variantId}`

**Locked decisions:**
- **D-12:** Grid leva para página de detalhe do produto (`/vitrina/{slug}/{produtoId}`)
- **D-09:** Preço exibido = preço atual do ProductVariant

**Security requirement:**
- Detail page must verify the variant belongs to the reseller's active maleta. Do NOT allow viewing arbitrary product variants by guessing IDs.

**Schema (relevant fields):**
```prisma
model ProductVariant {
  id        String @id @db.Uuid
  product_id String @db.Uuid
  price     Decimal? @db.Decimal(12, 2)
  image_url String @default("")
  product   Product @relation(fields: [product_id], references: [id])
}

model Product {
  id          String @id @db.Uuid
  name        String
  description String @default("")
  images      Json @default("[]")
}
```

**Prisma query for validation:**
```typescript
const maletaItem = await prisma.maletaItem.findFirst({
  where: {
    product_variant_id: variantId,
    maleta: { reseller_id: reseller.id, status: 'ativa' },
    quantidade_vendida: { lt: prisma.maleta_itens.fields.quantidade_enviada },
  },
});
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detail query and create detail page server component</name>
  <files>src/lib/vitrina.ts, src/app/vitrina/[slug]/[produtoId]/page.tsx</files>
  <read_first>
    - src/lib/vitrina.ts (existing getVitrinaData)
    - prisma/schema.prisma (ProductVariant, Product, MaletaItem models)
    - src/app/vitrina/[slug]/page.tsx (ISR and metadata pattern)
    - src/app/vitrina/[slug]/layout.tsx (shared layout)
  </read_first>
  <action>
    1. Append to `src/lib/vitrina.ts`:
       - Export `async function getVitrinaProductDetail(slug: string, variantId: string)`:
         - Query reseller: `prisma.reseller.findFirst({ where: { slug, is_active: true }, select: { id: true, name: true, whatsapp: true } })`
         - If no reseller, return `null`
         - Query variant: `prisma.productVariant.findUnique({ where: { id: variantId }, include: { product: true } })`
         - If no variant, return `null`
         - **Validate variant is in reseller's active maleta:**
           ```typescript
           const maletaItem = await prisma.maletaItem.findFirst({
             where: {
               product_variant_id: variantId,
               maleta: { reseller_id: reseller.id, status: 'ativa' },
               quantidade_vendida: { lt: prisma.maleta_itens.fields.quantidade_enviada },
             },
           });
           ```
         - If no `maletaItem`, return `null` (prevents enumeration)
         - Return `{ reseller, variant }`
       - Export type `VitrinaProductDetail = NonNullable<Awaited<ReturnType<typeof getVitrinaProductDetail>>>;`

    2. Create `src/app/vitrina/[slug]/[produtoId]/page.tsx`:
       - `export const revalidate = 300;`
       - `generateMetadata({ params })`:
         - Destructure `slug` and `produtoId` from `await params`
         - Call `getVitrinaProductDetail(slug, produtoId)`
         - If no data, return `{ title: 'Producto no encontrado' }`
         - Else return `{ title: \`${variant.product.name} | ${reseller.name}\`, description: (variant.product.description || '').slice(0, 160), robots: 'noindex' }`
       - Default export async function:
         - Destructure `slug` and `produtoId` from `await params`
         - Call `getVitrinaProductDetail(slug, produtoId)`
         - If null, call `notFound()`
         - Return `<ProductDetailView variant={variant} resellerSlug={slug} resellerWhatsapp={reseller.whatsapp} resellerName={reseller.name} resellerId={reseller.id} />`
       - Import `ProductDetailView` from `@/components/vitrina/ProductDetailView`
  </action>
  <verify>
    <automated>
      grep -c "getVitrinaProductDetail" src/lib/vitrina.ts | grep -q "1" && \
      grep -c "product_variant_id:" src/lib/vitrina.ts | grep -q "1" && \
      grep -c "maleta: { reseller_id:" src/lib/vitrina.ts | grep -q "1" && \
      grep -c "revalidate = 300" src/app/vitrina/[slug]/[produtoId]/page.tsx | grep -q "1" && \
      grep -c "notFound()" src/app/vitrina/[slug]/[produtoId]/page.tsx | grep -q "1" && \
      grep -c "generateMetadata" src/app/vitrina/[slug]/[produtoId]/page.tsx | grep -q "1"
    </automated>
  </verify>
  <done>
    - `getVitrinaProductDetail` validates variant belongs to reseller's active maleta
    - Detail page has ISR `revalidate = 300`, metadata, and 404 handling
    - Page renders `ProductDetailView` with variant and reseller data
  </done>
  <acceptance_criteria>
    - `src/lib/vitrina.ts` contains `export async function getVitrinaProductDetail(`
    - `src/lib/vitrina.ts` contains `product_variant_id: variantId`
    - `src/lib/vitrina.ts` contains `maleta: { reseller_id: reseller.id, status: 'ativa' }`
    - `src/lib/vitrina.ts` contains `quantidade_vendida: { lt: prisma.maleta_itens.fields.quantidade_enviada }`
    - `src/app/vitrina/[slug]/[produtoId]/page.tsx` exists and contains `export const revalidate = 300`
    - `src/app/vitrina/[slug]/[produtoId]/page.tsx` contains `notFound()`
    - `src/app/vitrina/[slug]/[produtoId]/page.tsx` contains `generateMetadata`
    - `src/app/vitrina/[slug]/[produtoId]/page.tsx` contains `robots: 'noindex'`
    - `src/app/vitrina/[slug]/[produtoId]/page.tsx` imports `ProductDetailView`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ProductDetailView client component</name>
  <files>src/components/vitrina/ProductDetailView.tsx</files>
  <read_first>
    - src/lib/cart-vitrina.ts (addToVitrinaCart signature)
    - src/lib/format.ts (formatGs)
    - docs/revendedoras/SPEC_VITRINE_PUBLICA.md (detail page layout)
    - src/components/vitrina/CartProvider.tsx (useVitrinaCart if needed for feedback)
  </read_first>
  <action>
    Create `src/components/vitrina/ProductDetailView.tsx`:
    - `"use client"`
    - Import `useState` from `react`
    - Import `Image` from `next/image`
    - Import `Link` from `next/link`
    - Import `addToVitrinaCart` from `@/lib/cart-vitrina`
    - Import `formatGs` from `@/lib/format`
    - Props: `{ variant: { id: string; product_id: string; price: Decimal | null; image_url: string; product: { name: string; description: string; images: string[]; } }; resellerSlug: string; resellerWhatsapp: string; resellerName: string; resellerId: string; }`
    - State: `const [added, setAdded] = useState(false);`
    - Render structure:
      1. Back link: `<Link href={/vitrina/${resellerSlug}} className="inline-flex items-center text-sm text-gray-500 hover:text-gray-800 mb-4">← Volver</Link>`
      2. Image: large product image in `relative aspect-square bg-gray-100 overflow-hidden rounded-lg`. Use `next/image` with `fill`, `object-cover`, `sizes="(max-width: 768px) 100vw, 600px"`. Source: `variant.image_url || (variant.product.images as string[])?.[0] || '/placeholder.svg'`
      3. Name: `<h1 className="text-2xl font-semibold mt-4">{variant.product.name}</h1>`
      4. Price: `{variant.price && Number(variant.price) > 0 && <p className="text-xl text-[#35605a] font-medium mt-2">{formatGs(Number(variant.price))}</p>}`
      5. Description: `<p className="text-gray-600 mt-4 whitespace-pre-wrap">{variant.product.description}</p>`
      6. Button: `<button onClick={handleAdd} className="w-full mt-6 py-3 bg-[#35605a] text-white rounded-lg font-medium hover:bg-[#2a4d47] transition active:scale-95">{added ? '¡Agregado!' : '📦 Agregar al carrito'}</button>`
    - `handleAdd` function:
      - `addToVitrinaCart({ product_variant_id: variant.id, product_id: variant.product_id, name: variant.product.name, price: Number(variant.price) || 0, image_url: variant.image_url || (variant.product.images as string[])?.[0] || '/placeholder.svg' }, resellerSlug);`
      - `setAdded(true);`
      - `setTimeout(() => setAdded(false), 1500);`
  </action>
  <verify>
    <automated>
      grep -c "Agregar al carrito" src/components/vitrina/ProductDetailView.tsx | grep -q "1" && \
      grep -c "addToVitrinaCart" src/components/vitrina/ProductDetailView.tsx | grep -q "1" && \
      grep -c "formatGs" src/components/vitrina/ProductDetailView.tsx | grep -q "1" && \
      grep -c "← Volver" src/components/vitrina/ProductDetailView.tsx | grep -q "1" && \
      grep -c "useState" src/components/vitrina/ProductDetailView.tsx | grep -q "1"
    </automated>
  </verify>
  <done>
    - ProductDetailView is a client component with photo, name, price, description, and add-to-cart button
    - Button calls `addToVitrinaCart` with correct item shape
    - Button shows "¡Agregado!" feedback for 1.5s after click
    - Back link returns to vitrina grid
  </done>
  <acceptance_criteria>
    - `src/components/vitrina/ProductDetailView.tsx` exists and contains `"use client"`
    - `src/components/vitrina/ProductDetailView.tsx` contains `Agregar al carrito`
    - `src/components/vitrina/ProductDetailView.tsx` contains `addToVitrinaCart`
    - `src/components/vitrina/ProductDetailView.tsx` contains `formatGs`
    - `src/components/vitrina/ProductDetailView.tsx` contains `← Volver`
    - `src/components/vitrina/ProductDetailView.tsx` contains `useState`
    - `src/components/vitrina/ProductDetailView.tsx` contains `next/image`
    - `src/components/vitrina/ProductDetailView.tsx` contains `aspect-square`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify ProductCard links and end-to-end build</name>
  <files>src/components/vitrina/ProductCard.tsx</files>
  <read_first>
    - src/components/vitrina/ProductCard.tsx (grid card from Plan 06-01)
    - src/app/vitrina/[slug]/[produtoId]/page.tsx (detail page from Task 1)
  </read_first>
  <action>
    1. Verify `src/components/vitrina/ProductCard.tsx` links to `/vitrina/${slug}/${variantId}` (should already exist from Plan 06-01). If the link is missing or uses a different pattern, update it to:
       - `<Link href={\`/vitrina/\${slug}/\${variantId}\`}>`
       - The `variantId` prop is the `product_variant_id` from the MaletaItem

    2. Run `npm run build` to verify the entire vitrina subsystem compiles without errors.

    3. Optional: Verify `next.config.ts` image remotePatterns include R2 domains for product images. If not, the images may not load — but this is existing config, do not modify unless broken.
  </action>
  <verify>
    <automated>
      grep -c "/vitrina/\${slug}/\${variantId}" src/components/vitrina/ProductCard.tsx | grep -q "1" && \
      npm run build
    </automated>
  </verify>
  <done>
    - ProductCard links to `/vitrina/{slug}/{variantId}`
    - `npm run build` passes with zero errors across all vitrina pages
  </done>
  <acceptance_criteria>
    - `src/components/vitrina/ProductCard.tsx` contains ``href={`/vitrina/${slug}/${variantId}`}`` or equivalent
    - `npm run build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js | Slug and produtoId params are untrusted |
| Next.js → Prisma | Must validate variant belongs to reseller's active maleta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Information Disclosure | Product variant enumeration | mitigate | `getVitrinaProductDetail` validates `maletaItem` exists for the reseller's active maleta before returning data; returns `null` otherwise → `notFound()` |
| T-06-13 | Tampering | Invalid produtoId | mitigate | Prisma `findUnique` with UUID validation via type system; missing variant returns `null` → `notFound()` |
| T-06-14 | Information Disclosure | Product description HTML | mitigate | Render description as plain text (`whitespace-pre-wrap`) or sanitize with `isomorphic-dompurify` if HTML is expected |
</threat_model>

<verification>
- [ ] `npm run build` passes
- [ ] `curl http://localhost:3000/vitrina/test-slug/invalid-id` returns 404
- [ ] Detail page renders image, name, price, description, and "Agregar al carrito" button
- [ ] Clicking "Agregar al carrito" updates localStorage and shows "¡Agregado!" feedback
- [ ] ProductCard in grid links to `/vitrina/{slug}/{variantId}`
</verification>

<success_criteria>
- `/vitrina/{slug}/{produtoId}` renders product detail for valid variants in active maleta
- Invalid or non-maleta variant returns 404
- Detail page shows product photo, name, price (via `formatGs`), and description
- "Agregar al carrito" button adds item to localStorage cart with correct structure
- Button shows "¡Agregado!" feedback for 1.5 seconds
- Grid cards link to detail page with URL pattern `/vitrina/{slug}/{variantId}`
</success_criteria>

<output>
After completion, create `.planning/phases/06-vitrina-publica/06-03-SUMMARY.md`
</output>
