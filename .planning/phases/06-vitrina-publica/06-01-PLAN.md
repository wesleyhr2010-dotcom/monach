---
phase: 06-vitrina-publica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/vitrina.ts
  - src/app/vitrina/[slug]/page.tsx
  - src/app/vitrina/[slug]/layout.tsx
  - src/components/vitrina/VitrinaHeader.tsx
  - src/components/vitrina/ProductCard.tsx
  - src/components/vitrina/ProductGrid.tsx
  - src/components/vitrina/EmptyState.tsx
  - scripts/rls-policies.sql
autonomous: true
requirements:
  - VITR-01
  - VITR-02
  - VITR-03
  - VITR-04
  - VITR-05
  - VITR-06
  - VITR-07
  - VITR-08
  - VITR-12
must_haves:
  truths:
    - "URL /vitrina/{slug} renders reseller profile and product grid"
    - "Inactive or missing slug returns 404 via notFound()"
    - "No active maleta shows profile + empty state message + WhatsApp CTA"
    - "SEO metadata includes OG tags with reseller avatar and robots:noindex"
    - "RLS policies allow anonymous SELECT on maletas and maleta_itens"
  artifacts:
    - path: src/lib/vitrina.ts
      provides: getVitrinaData query helper
      exports:
        - getVitrinaData
    - path: src/app/vitrina/[slug]/page.tsx
      provides: ISR vitrina page
      contains:
        - "revalidate = 300"
        - "generateMetadata"
        - "robots: 'noindex'"
        - "notFound()"
    - path: src/components/vitrina/VitrinaHeader.tsx
      provides: Hero with avatar, name, bio, WhatsApp CTA
    - path: scripts/rls-policies.sql
      provides: anon RLS policies for maletas/maleta_itens
      contains:
        - "maletas_anon_read_active"
        - "maleta_itens_anon_read_active"
  key_links:
    - from: page.tsx
      to: getVitrinaData
      via: import from @/lib/vitrina
    - from: getVitrinaData
      to: prisma
      via: Maleta + MaletaItem + ProductVariant query with nested include
    - from: ProductGrid
      to: ProductCard
      via: map over items array
    - from: page.tsx
      to: VitrinaHeader + ProductGrid + EmptyState
      via: conditional rendering based on items.length
---

<objective>
Build the public vitrina page at `/vitrina/{slug}` with ISR caching, dynamic SEO metadata, reseller profile hero, product grid from the active maleta, empty state handling, and anonymous RLS policies.

Purpose: This is the foundation of the public storefront — every downstream plan depends on the data layer, shared components, and layout created here.
Output: Working vitrina page with profile + grid, 404 handling, noindex SEO, and database policies for anonymous access.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-vitrina-publica/06-CONTEXT.md
@.planning/phases/06-vitrina-publica/06-RESEARCH.md
@docs/revendedoras/SPEC_VITRINE_PUBLICA.md

## Key patterns from codebase

**Catalog page (reference for vitrina structure):**
- `src/app/catalogo/[slug]/page.tsx` — hero with avatar (`bg-[#35605a]`, `rounded-full w-20 h-20`), grid with `aspect-[260/340]`, footer "Powered by Monarca Semijoyas"
- Uses `getResellerBySlug()` and `getResellerCatalogProducts()` from `src/app/actions.ts`
- ISR with `revalidate = 60`

**Data sanitization:**
- `src/lib/data-protection/vitrina-sanitizer.ts` — `getPublicVitrinaData()` returns `{ name, avatar_url, slug, whatsapp_link }`
- Never expose email, address, commission rate, or other PII

**Currency formatting:**
- `src/lib/format.ts` — `formatGs(value)` returns "G$ 1.850.000" using `es-PY` locale

**Prisma patterns:**
- NO nested `$transaction(async tx => {...})`
- Use `prisma.maleta_itens.fields.quantidade_enviada` for field references in where clauses
- `include` for relation loading to avoid N+1

**Schema models (relevant fields):**
```prisma
model Reseller {
  id         String @id @db.Uuid
  name       String
  avatar_url String @default("")
  bio        String @default("")
  whatsapp   String
  slug       String @unique
  is_active  Boolean @default(true)
}

model Maleta {
  id          String @id @db.Uuid
  reseller_id String @db.Uuid
  status      MaletaStatus @default(ativa)
  itens       MaletaItem[]
}

model MaletaItem {
  id                 String @id @db.Uuid
  maleta_id          String @db.Uuid
  product_variant_id String @db.Uuid
  quantidade_enviada Int
  quantidade_vendida Int @default(0)
  product_variant    ProductVariant @relation(fields: [product_variant_id], references: [id])
}

model ProductVariant {
  id        String  @id @db.Uuid
  product_id String @db.Uuid
  price     Decimal? @db.Decimal(12, 2)
  image_url String @default("")
  product   Product @relation(fields: [product_id], references: [id])
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data layer and RLS policies</name>
  <files>src/lib/vitrina.ts, scripts/rls-policies.sql</files>
  <read_first>
    - prisma/schema.prisma (Reseller, Maleta, MaletaItem, ProductVariant models)
    - src/app/actions.ts (getResellerBySlug pattern)
    - scripts/rls-policies.sql (existing policies format)
    - src/lib/data-protection/vitrina-sanitizer.ts (PII rules)
  </read_first>
  <action>
    1. Create `src/lib/vitrina.ts` with `getVitrinaData(slug: string)` function:
       - Query `prisma.reseller.findFirst({ where: { slug, is_active: true }, select: { id, name, avatar_url, bio, whatsapp } })`
       - If no reseller, return `null`
       - Query `prisma.maleta.findFirst({ where: { reseller_id: reseller.id, status: 'ativa' }, include: { itens: { where: { quantidade_vendida: { lt: prisma.maleta_itens.fields.quantidade_enviada } }, include: { product_variant: { include: { product: true } } } } } })`
       - Map items to flat array: `{ id: item.id, variantId: item.product_variant_id, name: item.product_variant.product.name, price: item.product_variant.price ? Number(item.product_variant.price) : null, image: item.product_variant.image_url || (item.product_variant.product.images as string[])?.[0] || '/placeholder.svg', productId: item.product_variant.product_id }`
       - Return `{ reseller, items }`
       - Export `VitrinaItem` and `VitrinaData` types

    2. Append to `scripts/rls-policies.sql` two new policies:
       ```sql
       -- --------------------------------------------
       -- 24. MALETAS (anon read active)
       -- --------------------------------------------
       CREATE POLICY IF NOT EXISTS "maletas_anon_read_active"
       ON maletas FOR SELECT TO anon
       USING (status = 'ativa');

       -- --------------------------------------------
       -- 25. MALETA_ITENS (anon read via active maleta)
       -- --------------------------------------------
       CREATE POLICY IF NOT EXISTS "maleta_itens_anon_read_active"
       ON maleta_itens FOR SELECT TO anon
       USING (
         EXISTS (
           SELECT 1 FROM maletas m
           WHERE m.id = maleta_itens.maleta_id
           AND m.status = 'ativa'
         )
       );
       ```

    3. [BLOCKING] After writing policies, apply them in Supabase SQL Editor. The executor must run the new policy blocks from `scripts/rls-policies.sql` in the Supabase dashboard. This is required before anonymous reads can succeed via the `anon` role.
  </action>
  <verify>
    <automated>
      grep -c "export async function getVitrinaData" src/lib/vitrina.ts | grep -q "1" && \
      grep -c "quantidade_vendida: { lt: prisma.maleta_itens.fields.quantidade_enviada }" src/lib/vitrina.ts | grep -q "1" && \
      grep -c "maletas_anon_read_active" scripts/rls-policies.sql | grep -q "1" && \
      grep -c "maleta_itens_anon_read_active" scripts/rls-policies.sql | grep -q "1"
    </automated>
  </verify>
  <done>
    - `src/lib/vitrina.ts` exports `getVitrinaData` with correct Prisma query and mapped item shape
    - `scripts/rls-policies.sql` contains `maletas_anon_read_active` and `maleta_itens_anon_read_active` policies
    - [BLOCKING] RLS policies applied in Supabase SQL Editor
  </done>
  <acceptance_criteria>
    - `src/lib/vitrina.ts` contains `export async function getVitrinaData(`
    - `src/lib/vitrina.ts` queries `prisma.reseller.findFirst({ where: { slug, is_active: true } })`
    - `src/lib/vitrina.ts` queries `prisma.maleta.findFirst({ where: { status: 'ativa' } })` with nested `include` for `itens → product_variant → product`
    - `src/lib/vitrina.ts` maps items with fields: `variantId`, `name`, `price`, `image`, `productId`
    - `scripts/rls-policies.sql` contains the text `maletas_anon_read_active`
    - `scripts/rls-policies.sql` contains the text `maleta_itens_anon_read_active`
    - `scripts/rls-policies.sql` contains `ON maletas FOR SELECT TO anon`
    - `scripts/rls-policies.sql` contains `ON maleta_itens FOR SELECT TO anon`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create shared vitrina components</name>
  <files>src/components/vitrina/VitrinaHeader.tsx, src/components/vitrina/ProductCard.tsx, src/components/vitrina/ProductGrid.tsx, src/components/vitrina/EmptyState.tsx</files>
  <read_first>
    - src/app/catalogo/[slug]/page.tsx (hero and grid patterns)
    - src/lib/format.ts (formatGs signature)
    - docs/design-system/tokens.md (token references)
  </read_first>
  <action>
    Create 4 components in `src/components/vitrina/`. All UI strings in Spanish (Paraguayan). Reuse visual patterns from `src/app/catalogo/[slug]/page.tsx`.

    1. `VitrinaHeader.tsx` — Server Component:
       - Props: `{ name: string; avatar_url: string; bio?: string; whatsapp: string; }`
       - Hero section: `bg-[#35605a] text-white py-12 px-6 md:px-12`
       - Avatar: `next/image` 80x80, `rounded-full object-cover w-20 h-20 border-2 border-white/30`. Fallback: `<div className="w-20 h-20 rounded-full bg-white/15 flex items-center justify-center text-3xl">🦋</div>`
       - Subtitle: `<p className="text-[12px] uppercase tracking-[0.15em] opacity-70 mb-1">Vitrina de</p>`
       - Title: `<h1 className="text-[28px] md:text-[36px] font-light tracking-[0.02em]">{name}</h1>`
       - Bio: `{bio && <p className="text-[14px] opacity-80 mt-2 max-w-[500px]">{bio}</p>}`
       - WhatsApp CTA: `<a href={\`https://wa.me/\${whatsapp.replace(/\\D/g, '')}\`} className="inline-block mt-4 px-4 py-2 bg-white/15 rounded-full text-sm hover:bg-white/25 transition">Consultar por WhatsApp</a>`
       - Top-right link to `/`: "Monarca Semijoyas" with `text-[12px] uppercase tracking-[0.1em] opacity-60 hover:opacity-100`

    2. `ProductCard.tsx` — Server Component:
       - Props: `{ variantId: string; name: string; price: number | null; image: string; slug: string; }`
       - Wrap in `<Link href={/vitrina/\${slug}/\${variantId}} className="group cursor-pointer flex flex-col">`
       - Image container: `relative overflow-hidden bg-gray-100 aspect-[260/340]`
       - `next/image` with `fill`, `object-cover`, `transition-transform duration-500 group-hover:scale-105`, `sizes="(max-width: 768px) 50vw, 260px"`
       - Name: `<h4 className="text-sm leading-5 text-darkslategray-200 truncate">{name}</h4>`
       - Price: `{price && price > 0 && <p className="text-sm font-medium leading-6 text-darkslategray-200">{formatGs(price)}</p>}`

    3. `ProductGrid.tsx` — Server Component:
       - Props: `{ items: VitrinaItem[]; slug: string; }`
       - Count label: `<p className="text-[13px] text-gray-400 mb-6">{items.length} {items.length === 1 ? 'artículo' : 'artículos'}</p>`
       - Grid: `<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-x-4 gap-y-8">`
       - Map items to `ProductCard`

    4. `EmptyState.tsx` — Server Component:
       - Props: `{ whatsapp: string; }`
       - Centered: `<div className="text-center py-20">`
       - Message: `<p className="text-gray-400 text-lg">No tiene artículos disponibles momentáneamente.</p>`
       - CTA: `<a href={\`https://wa.me/\${whatsapp.replace(/\\D/g, '')}\`} className="inline-block mt-4 text-[#35605a] underline">Consultar por WhatsApp</a>`
  </action>
  <verify>
    <automated>
      ls src/components/vitrina/*.tsx | wc -l | grep -q "4" && \
      grep -c "No tiene artículos disponibles momentáneamente" src/components/vitrina/EmptyState.tsx | grep -q "1" && \
      grep -c "formatGs" src/components/vitrina/ProductCard.tsx | grep -q "1" && \
      grep -c "aspect-\[260/340\]" src/components/vitrina/ProductCard.tsx | grep -q "1" && \
      grep -c "Vitrina de" src/components/vitrina/VitrinaHeader.tsx | grep -q "1"
    </automated>
  </verify>
  <done>
    - 4 components exist in `src/components/vitrina/`
    - ProductCard links to `/vitrina/{slug}/{variantId}`
    - EmptyState displays "No tiene artículos disponibles momentáneamente" + WhatsApp CTA
    - VitrinaHeader matches catalog hero visual pattern
  </done>
  <acceptance_criteria>
    - `src/components/vitrina/VitrinaHeader.tsx` exists and contains `Vitrina de`
    - `src/components/vitrina/VitrinaHeader.tsx` contains `bg-[#35605a]`
    - `src/components/vitrina/ProductCard.tsx` exists and contains `aspect-[260/340]`
    - `src/components/vitrina/ProductCard.tsx` contains `formatGs(price)`
    - `src/components/vitrina/ProductCard.tsx` contains ``href={`/vitrina/${slug}/${variantId}`}`` or equivalent link pattern
    - `src/components/vitrina/ProductGrid.tsx` exists and contains `grid-cols-2`
    - `src/components/vitrina/EmptyState.tsx` exists and contains `No tiene artículos disponibles momentáneamente`
    - `src/components/vitrina/EmptyState.tsx` contains `Consultar por WhatsApp`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build vitrina page with ISR and metadata</name>
  <files>src/app/vitrina/[slug]/page.tsx, src/app/vitrina/[slug]/layout.tsx</files>
  <read_first>
    - src/app/vitrina/[slug]/page.tsx (current stub — 15 lines with placeholder)
    - src/lib/vitrina.ts (getVitrinaData created in Task 1)
    - src/app/catalogo/[slug]/page.tsx (metadata and page structure reference)
  </read_first>
  <action>
    1. Replace `src/app/vitrina/[slug]/page.tsx`:
       - Add `export const revalidate = 300;` at top (per D-01)
       - Import `notFound` from `next/navigation`
       - Import `getVitrinaData` from `@/lib/vitrina`
       - Import `VitrinaHeader`, `ProductGrid`, `EmptyState` from `@/components/vitrina/*`
       - Import `Image` from `next/image` and `Link` from `next/link`
       - `generateMetadata({ params })`: async, destructure `slug` from `await params`, call `getVitrinaData(slug)`. If no reseller, return `{ title: 'Vitrina no encontrada' }`. Else return `{ title: \`${reseller.name} | Joyería Monarca\`, description: \`Descubrí las hermosas joyas de \${reseller.name}\`, openGraph: { title: \`\${reseller.name} | Joyería Monarca\`, description: \`Joyas exclusivas por \${reseller.name}\`, images: [reseller.avatar_url || '/og-default.jpg'], type: 'website' }, robots: 'noindex' }`
       - Default export async function: destructure `slug` from `await params`, call `getVitrinaData(slug)`. If null, call `notFound()`.
       - Return `<div className="bg-white min-h-screen flex flex-col">`
       - Render `<VitrinaHeader name={reseller.name} avatar_url={reseller.avatar_url} bio={reseller.bio} whatsapp={reseller.whatsapp} />`
       - Render `<main className="flex-1 max-w-[1200px] mx-auto w-full px-6 md:px-12 py-10">`
       - If `items.length === 0`, render `<EmptyState whatsapp={reseller.whatsapp} />`
       - Else render `<ProductGrid items={items} slug={slug} />`
       - Close main, render footer: `<footer className="border-t border-gray-100 py-6 text-center text-[12px] text-gray-400">Powered by <Link href="/" className="text-[#35605a] hover:underline">Monarca Semijoyas</Link></footer>`
       - Close div

    2. Create `src/app/vitrina/[slug]/layout.tsx`:
       - Export default async function `Layout({ children, params }: { children: React.ReactNode; params: Promise<{ slug: string }> })`
       - Return `<div className="bg-white min-h-screen flex flex-col">{children}</div>`
       - This is the shared layout for the vitrina and its detail pages

    3. Run `npm run build` to verify no TypeScript or build errors.
  </action>
  <verify>
    <automated>
      grep -c "revalidate = 300" src/app/vitrina/[slug]/page.tsx | grep -q "1" && \
      grep -c "robots: 'noindex'" src/app/vitrina/[slug]/page.tsx | grep -q "1" && \
      grep -c "notFound()" src/app/vitrina/[slug]/page.tsx | grep -q "1" && \
      grep -c "generateMetadata" src/app/vitrina/[slug]/page.tsx | grep -q "1" && \
      grep -c "max-w-\[1200px\]" src/app/vitrina/[slug]/page.tsx | grep -q "1" && \
      npm run build
    </automated>
  </verify>
  <done>
    - `src/app/vitrina/[slug]/page.tsx` has `revalidate = 300`, `generateMetadata`, `robots: 'noindex'`, and `notFound()`
    - Page renders VitrinaHeader, conditionally renders EmptyState or ProductGrid
    - `npm run build` passes with zero errors
  </done>
  <acceptance_criteria>
    - `src/app/vitrina/[slug]/page.tsx` contains `export const revalidate = 300`
    - `src/app/vitrina/[slug]/page.tsx` contains `robots: 'noindex'`
    - `src/app/vitrina/[slug]/page.tsx` contains `notFound()`
    - `src/app/vitrina/[slug]/page.tsx` contains `generateMetadata`
    - `src/app/vitrina/[slug]/page.tsx` contains `getVitrinaData`
    - `src/app/vitrina/[slug]/page.tsx` contains `VitrinaHeader`
    - `src/app/vitrina/[slug]/page.tsx` contains `ProductGrid`
    - `src/app/vitrina/[slug]/page.tsx` contains `EmptyState`
    - `src/app/vitrina/[slug]/layout.tsx` exists
    - `npm run build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js API | Untrusted input (slug, event payloads) crosses here |
| Next.js → Prisma/Supabase | Application-level filtering prevents unauthorized data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | getVitrinaData | mitigate | Select only public fields (name, avatar_url, bio, whatsapp); never expose email, commission, address |
| T-06-02 | Information Disclosure | 404 handling | mitigate | `notFound()` for both missing AND inactive slugs — no distinction leaks existence |
| T-06-03 | Tampering | RLS policies | mitigate | Add `maletas_anon_read_active` and `maleta_itens_anon_read_active` to enforce anon can only read active maletas |
| T-06-04 | Denial of Service | Product grid query | mitigate | Use single Prisma query with nested `include` to avoid N+1 |
</threat_model>

<verification>
- [ ] `npm run build` passes
- [ ] `curl http://localhost:3000/vitrina/test-slug` returns 200 or 404 (not 500)
- [ ] RLS policies for `maletas` and `maleta_itens` applied in Supabase
</verification>

<success_criteria>
- `/vitrina/{slug}` renders reseller profile and product grid for active resellers with active maletas
- Inactive or missing slug returns 404
- No active maleta shows profile + "No tiene artículos disponibles momentáneamente" + WhatsApp CTA
- SEO metadata includes OG title, description, image, and `robots: 'noindex'`
- RLS policies `maletas_anon_read_active` and `maleta_itens_anon_read_active` exist in Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/06-vitrina-publica/06-01-SUMMARY.md`
</output>
