---
phase: 03-visibility-analytics-reseller-admin-dashboards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/actions-dashboard.ts
  - src/app/admin/analytics/page.tsx
  - src/components/admin/dashboard/MetricCard.tsx
  - src/components/admin/dashboard/AlertasCard.tsx
  - src/components/admin/dashboard/RankingTable.tsx
  - src/components/admin/dashboard/DocsCard.tsx
  - src/app/admin/analytics/AnalyticsKpiCards.tsx
  - src/components/ui/skeleton-card.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08
must_haves:
  truths:
    - "Admin dashboard at /admin displays KPI cards with period filter (7d/30d/3m/12m)"
    - "COLABORADORA sees group-scoped KPIs only"
    - "Skeleton loading states display while data is loading"
    - "Main dashboard links to /admin/analytics for charts and deep data"
    - "All DASH requirements are verified as covered"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Admin dashboard with period filter and skeleton states"
      min_lines: 120
    - path: "src/app/admin/actions-dashboard.ts"
      provides: "Server actions accepting period parameter"
      exports: ["getDashboardMetricas", "getAlertasMaletas", "getRankingColaboradoras", "getRankingRevendedoras"]
    - path: "src/components/admin/dashboard/MetricCard.tsx"
      provides: "KPI card component with skeleton support"
    - path: "src/components/admin/dashboard/AlertasCard.tsx"
      provides: "Alerts list component with skeleton support"
    - path: "src/components/admin/dashboard/RankingTable.tsx"
      provides: "Ranking table with skeleton support"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/app/admin/actions-dashboard.ts"
      via: "import and call dashboard actions with period param"
      pattern: "getDashboardMetricas\\("
    - from: "src/app/admin/page.tsx"
      to: "/admin/analytics"
      via: "Link to analytics page for charts and deep data"
      pattern: "href=\"/admin/analytics\""
---

<objective>
Complete the Admin Dashboard by adding period filtering (7d/30d/3m/12m) to the main `/admin` page, adding skeleton loading states to all dashboard and analytics components, and verifying all DASH requirements are satisfied.

Purpose: Ensure admin and colaboradora have a polished, responsive dashboard experience with consistent loading patterns and time-based filtering.
Output: Updated `/admin` and `/admin/analytics` pages with period filters, skeleton states, and verified requirement coverage.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-visibility-analytics-reseller-admin-dashboards/03-RESEARCH.md

@docs/admin/SPEC_ADMIN_DASHBOARD.md
@docs/admin/SPEC_ADMIN_ANALYTICS_NOTIFICATIONS.md

# Existing admin dashboard
@src/app/admin/page.tsx
@src/app/admin/actions-dashboard.ts
@src/app/admin/analytics/page.tsx
@src/app/admin/actions-analytics.ts

# Existing components
@src/components/admin/dashboard/MetricCard.tsx
@src/components/admin/dashboard/AlertasCard.tsx
@src/components/admin/dashboard/RankingTable.tsx
@src/components/admin/dashboard/DocsCard.tsx
@src/app/admin/analytics/AnalyticsKpiCards.tsx

# Phase 1 UI components
@src/components/ui/skeleton-card.tsx
@src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add period filter to admin dashboard actions and page</name>
  <files>
    src/app/admin/actions-dashboard.ts
    src/app/admin/page.tsx
  </files>
  <action>
    1. Update `src/app/admin/actions-dashboard.ts`:
       - Modify `getDashboardMetricas(colaboradoraId?: string, periodDays?: number)` to accept an optional `periodDays` parameter (default: 30)
       - Replace the hardcoded `getMesAtual()` with a new helper `getPeriodRange(periodDays?: number)` that returns `{ start: Date; end: Date }`:
         - If periodDays is provided: start = now - periodDays days, end = now
         - If not provided: use current month (existing behavior for backward compatibility)
       - Update faturamento queries to use the period range instead of current month
       - Update revendedorasNovas query to use period range (count created within period)
       - Update `getRankingColaboradoras(periodDays?: number)` and `getRankingRevendedoras(colaboradoraId: string, periodDays?: number)` to accept period and filter maletas by `updated_at` within the period
       - Update `getMinhaComissao(resellerId: string, periodDays?: number)` similarly
       - Ensure all functions still call `requireAuth(["ADMIN", "COLABORADORA"])`
       - Keep the existing `getAlertasMaletas` unchanged (alerts are real-time, not period-dependent)
    2. Update `src/app/admin/page.tsx`:
       - Read `?period=` query param (default: "30")
       - Validate against allowed values: [7, 30, 90, 365]
       - Add period selector buttons in the header (same style as `/admin/analytics`):
         ```tsx
         <div style={{ display: "flex", gap: "6px" }}>
           {[{label:"7d",days:7},{label:"30d",days:30},{label:"3m",days:90},{label:"12m",days:365}].map(opt => (
             <Link key={opt.days} href={`/admin?period=${opt.days}`} className={...}>
               {opt.label}
             </Link>
           ))}
         </div>
         ```
       - Pass `periodDays` to all server action calls:
         - `getDashboardMetricas(colaboradoraId, periodDays)`
         - `getRankingColaboradoras(periodDays)` or `getRankingRevendedoras(colaboradoraId, periodDays)`
         - `getMinhaComissao(colaboradoraId, periodDays)` (if applicable)
       - Add a link or button near KPI cards: "Ver analytics detallado →" linking to `/admin/analytics?period={periodDays}`
       - Ensure the page still exports `const dynamic = "force-dynamic"`
  </action>
  <verify>
    <automated>
      grep -q "periodDays" src/app/admin/actions-dashboard.ts && \
      grep -q "getPeriodRange" src/app/admin/actions-dashboard.ts && \
      grep -q "period=" src/app/admin/page.tsx && \
      grep -q "/admin/analytics" src/app/admin/page.tsx && \
      grep -q "getDashboardMetricas.*periodDays" src/app/admin/page.tsx
    </automated>
  </verify>
  <done>
    - actions-dashboard.ts accepts periodDays in all metric functions
    - getPeriodRange helper exists
    - /admin page reads ?period query param
    - Period selector buttons render in header
    - Link to /admin/analytics present
  </done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton loading states to admin dashboard and analytics</name>
  <files>
    src/components/admin/dashboard/MetricCard.tsx
    src/components/admin/dashboard/AlertasCard.tsx
    src/components/admin/dashboard/RankingTable.tsx
    src/components/admin/dashboard/DocsCard.tsx
    src/app/admin/analytics/AnalyticsKpiCards.tsx
    src/app/admin/analytics/page.tsx
    src/app/admin/page.tsx
  </files>
  <action>
    1. Update `src/components/admin/dashboard/MetricCard.tsx`:
       - Add an optional `loading?: boolean` prop
       - When `loading` is true, render a skeleton placeholder instead of the value and subValue:
         - Use `SkeletonCard` or inline `div` with `animate-pulse bg-gray-800 rounded` (use admin theme colors)
         - Skeleton should match the visual size of the loaded content
    2. Update `src/components/admin/dashboard/AlertasCard.tsx`:
       - Add `loading?: boolean` prop
       - When loading, render 3 skeleton rows (each a thin bar with pulse animation)
    3. Update `src/components/admin/dashboard/RankingTable.tsx`:
       - Add `loading?: boolean` prop
       - When loading, render 4 skeleton rows
    4. Update `src/components/admin/dashboard/DocsCard.tsx`:
       - Add `loading?: boolean` prop
       - When loading, render a single skeleton bar
    5. Update `src/app/admin/analytics/AnalyticsKpiCards.tsx`:
       - Add `loading?: boolean` prop
       - When loading, render skeleton cards for each KPI
    6. Update `src/app/admin/page.tsx`:
       - Use React `Suspense` boundaries around data-fetching sections OR
       - Use a local `loading` state (but since it's a Server Component, prefer passing `loading` based on whether data is available)
       - Since Next.js Server Components don't have traditional loading states, implement a client-side loading pattern:
         - Wrap the dashboard body in a Client Component `DashboardShell` that receives promises from the server and shows skeletons while resolving
         - OR simpler: add a `loading.tsx` file in `src/app/admin/` that renders skeleton cards
       - Recommended approach: create `src/app/admin/loading.tsx`:
         ```tsx
         import { SkeletonCard } from "@/components/ui/skeleton-card";
         export default function AdminLoading() {
           return (
             <div style={{ padding: "28px 32px", display: "flex", flexDirection: "column", gap: 20 }}>
               <div style={{ display: "flex", gap: 16 }}>
                 {[1,2,3,4,5].map(i => <SkeletonCard key={i} height={100} />)}
               </div>
               <div style={{ display: "flex", gap: 16 }}>
                 <SkeletonCard height={240} style={{ flex: 1 }} />
                 <SkeletonCard height={240} style={{ flex: 1 }} />
               </div>
             </div>
           );
         }
         ```
    7. Update `src/app/admin/analytics/page.tsx`:
       - Create `src/app/admin/analytics/loading.tsx` with skeleton layouts matching the analytics page structure:
         - KPI cards row (6 skeletons)
         - Charts row (2 skeletons)
         - Tables row (2 skeletons)
         - Products card (1 skeleton)
  </action>
  <verify>
    <automated>
      test -f src/app/admin/loading.tsx && \
      test -f src/app/admin/analytics/loading.tsx && \
      grep -q "loading" src/components/admin/dashboard/MetricCard.tsx && \
      grep -q "loading" src/components/admin/dashboard/AlertasCard.tsx && \
      grep -q "loading" src/components/admin/dashboard/RankingTable.tsx && \
      grep -q "SkeletonCard" src/app/admin/loading.tsx && \
      grep -q "SkeletonCard" src/app/admin/analytics/loading.tsx
    </automated>
  </verify>
  <done>
    - src/app/admin/loading.tsx exists with skeleton layout
    - src/app/admin/analytics/loading.tsx exists with skeleton layout
    - All dashboard components accept loading prop
    - All analytics components accept loading prop
    - Skeleton layouts visually match the loaded content structure
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify DASH requirements and run quality checks</name>
  <files>
    docs/REQUIREMENTS.md
    .planning/ROADMAP.md
    src/app/admin/page.tsx
    src/app/admin/analytics/page.tsx
    src/app/admin/actions-dashboard.ts
    src/app/admin/actions-analytics.ts
  </files>
  <action>
    1. Verify DASH-01: Admin sees global KPIs at `/admin`
       - Confirm `/admin/page.tsx` renders faturamento, maletas, revendedoras, alertas cards for ADMIN role
    2. Verify DASH-02: COLABORADORA sees group-scoped KPIs at `/admin`
       - Confirm page checks `user.role === "COLABORADORA"` and passes `colaboradoraId` to actions
       - Confirm COLABORADORA sees "Minha Comissão" card and "Ranking das Minhas Revendedoras"
    3. Verify DASH-03: Period filter (7d/30d/3m/12m) reflects in all KPIs
       - Confirm `/admin?page=...` updates all metric queries
       - Confirm `/admin/analytics?period=...` updates all analytics queries
    4. Verify DASH-04: Gráfico de fluxo de maletas por status
       - Confirm `/admin/analytics` renders fluxo chart with enviadas/devolvidas/atrasadas bars
    5. Verify DASH-05: Ranking de produtos mais vendidos (top 10)
       - Confirm `/admin/analytics` renders produtos table with unidades vendidas and valor total
    6. Verify DASH-06: Alertas de prazo (maletas com ≤7 dias)
       - Confirm `/admin/analytics` renders alertas de prazo table
       - Confirm `/admin` alertas card shows maletas atrasadas, acertos pendentes, vencem amanhã
    7. Verify DASH-07: Scope seguro — COLABORADORA só vê dados do seu grupo
       - Confirm `actions-dashboard.ts` filters by `colaboradora_id` in all queries
       - Confirm `actions-analytics.ts` filters by `colaboradora_id` in all queries
    8. Verify DASH-08: Estados de loading com skeleton cards consistentes
       - Confirm `src/app/admin/loading.tsx` exists and uses SkeletonCard
       - Confirm `src/app/admin/analytics/loading.tsx` exists and uses SkeletonCard
    9. Run quality checks:
       - `npm run lint` — must pass
       - `npm run build` — must pass
       - `npm test` — existing tests must pass
    10. Update `docs/REQUIREMENTS.md`:
        - Mark DASH-01 through DASH-08 as `[x]` complete
        - Update the Traceability table at the bottom: change Phase 3 DASH items status from "Pending" to "Complete"
    11. Update `docs/next_steps.md`:
        - Mark Phase 3 items as completed
        - Add a dated entry for Phase 3 completion
    12. Update `.planning/ROADMAP.md`:
        - Update Phase 3 status: change "Not started" to "Ready to execute" → after execution, update to "Completed"
        - Update Plans count: change "TBD" to "2 plans"
        - Add plan list:
          ```
          Plans:
          - [x] `03-01-PLAN.md` — Reseller Performance Dashboard
          - [x] `03-02-PLAN.md` — Admin Dashboard Polish
          ```
  </action>
  <verify>
    <automated>
      npm run lint && \
      npm run build && \
      grep -q "\[x\] DASH-01" docs/REQUIREMENTS.md && \
      grep -q "\[x\] DASH-02" docs/REQUIREMENTS.md && \
      grep -q "\[x\] DASH-03" docs/REQUIREMENTS.md && \
      grep -q "\[x\] DASH-08" docs/REQUIREMENTS.md && \
      test -f src/app/admin/loading.tsx && \
      test -f src/app/admin/analytics/loading.tsx
    </automated>
  </verify>
  <done>
    - All DASH requirements verified as implemented
    - REQUIREMENTS.md updated with DASH-01..DASH-08 marked complete
    - Build and lint pass
    - ROADMAP.md updated with Phase 3 plan list
    - next_steps.md updated
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | Untrusted query params (period) |
| Server Action → Database | Must scope all queries by role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | period query param | mitigate | Validate against whitelist [7, 30, 90, 365]; default to 30; cast with parseInt and check isNaN |
| T-03-06 | Information Disclosure | COLABORADORA scope | mitigate | All queries in actions-dashboard.ts and actions-analytics.ts filter by colaboradora_id when role !== ADMIN |
| T-03-07 | Elevation of Privilege | requireAuth bypass | mitigate | Every server action calls requireAuth(["ADMIN", "COLABORADORA"]) at entry |
| T-03-08 | DoS | Long period queries | mitigate | Limit period to max 365 days; use indexes on date columns; paginate where applicable |
</threat_model>

<verification>
- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `/admin` renders with period filter buttons
- [ ] Changing period updates KPI values
- [ ] `/admin/analytics` renders with existing charts and new skeleton loading
- [ ] COLABORADORA login shows only group data
- [ ] ADMIN login shows global data
- [ ] Skeleton loading states display during navigation
- [ ] All DASH requirements marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
- DASH-01: KPIs globais visíveis para ADMIN em `/admin`
- DASH-02: KPIs por grupo visíveis para COLABORADORA em `/admin`
- DASH-03: Filtro de período (7d/30d/3m/12m) reflete em todos os KPIs do `/admin`
- DASH-04: Gráfico de fluxo de maletas presente em `/admin/analytics`
- DASH-05: Ranking de produtos mais vendidos (top 10) em `/admin/analytics`
- DASH-06: Alertas de prazo (≤7 dias) em `/admin/analytics`
- DASH-07: COLABORADORA scope enforced via colaboradora_id filter
- DASH-08: Skeleton loading states consistentes em `/admin` e `/admin/analytics`
</success_criteria>

<output>
After completion, create `.planning/phases/03-visibility-analytics-reseller-admin-dashboards/03-02-SUMMARY.md`
</output>
