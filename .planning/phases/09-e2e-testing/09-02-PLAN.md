---
phase: 09-e2e-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tests/e2e/login.spec.ts
  - tests/e2e/maleta.spec.ts
  - tests/e2e/venda.spec.ts
  - tests/e2e/devolucao.spec.ts
  - tests/e2e/fixtures/scenarios.ts
  - tests/e2e/helpers/maleta.ts
  - tests/e2e/helpers/navigation.ts
autonomous: true
requirements:
  - E2E-03
  - E2E-04
  - E2E-05
  - E2E-06
must_haves:
  truths:
    - Revendedora can log in with email/password and lands on /app
    - Revendedora can view her active maleta with correct items
    - Revendedora can register a sale and see updated quantities
    - Revendedora can initiate return flow and reach confirmation
    - Each test is independent (uses fresh seeded scenario)
  artifacts:
    - path: tests/e2e/login.spec.ts
      provides: Login golden path tests
      min_lines: 40
      contains:
        - "test('login com credenciais válidas"
        - "test('login com senha errada"
    - path: tests/e2e/maleta.spec.ts
      provides: Maleta viewing tests
      min_lines: 30
      contains:
        - "test('visualizar maleta ativa"
    - path: tests/e2e/venda.spec.ts
      provides: Sale registration tests
      min_lines: 40
      contains:
        - "test('registrar venda"
    - path: tests/e2e/devolucao.spec.ts
      provides: Return flow tests
      min_lines: 40
      contains:
        - "test('iniciar devolução"
    - path: tests/e2e/fixtures/scenarios.ts
      provides: Shared test scenarios using test.extend
      exports: ["test", "expect"]
  key_links:
    - from: tests/e2e/login.spec.ts
      to: tests/e2e/helpers/auth.ts
      via: loginAsReseller
    - from: tests/e2e/venda.spec.ts
      to: tests/e2e/helpers/seed.ts
      via: seedScenario + seedMaleta
    - from: tests/e2e/devolucao.spec.ts
      to: src/app/app/maleta/[id]/devolver/page.tsx
      via: page navigation and form submission
---

<objective>
Implement the 4 core golden path E2E tests covering the critical reseller business flow: login → maleta → sale → return.

Purpose: These tests verify the heart of the business — a reseller can log in, see her consignment bag, register a sale, and initiate a return. Without these, regressions in the core flow go undetected.
Output: 4 Playwright spec files with independent, seeded tests for login, maleta viewing, sale registration, and return initiation.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/sistema/SPEC_TESTING_STRATEGY.md
@docs/revendedoras/SPEC_MALETA.md
@docs/revendedoras/SPEC_DEVOLUCAO.md
@docs/revendedoras/SPEC_RECUPERAR_CONTRASENA.md

## Prior Plan Output (09-01)

From 09-01-PLAN.md:
- `tests/e2e/helpers/auth.ts` exports: `loginAsReseller`, `loginAsAdmin`, `loginAsColaboradora`, `logout`
- `tests/e2e/helpers/seed.ts` exports: `seedScenario`, `cleanupScenario`, `seedUser`, `seedMaleta`, `seedProduct`
- `tests/e2e/helpers/test-data.ts` exports: `createTestReseller`, `createTestProduct`, `createTestVariant`
- `tests/e2e/helpers/db.ts` exports: `prisma` instance

## Routes to Test (from existing codebase)

- `/app/login` — Reseller login page
- `/app` — Reseller home/dashboard after login
- `/app/maleta` — List of maletas
- `/app/maleta/[id]` — Maleta detail with items, sale/return buttons
- `/app/maleta/[id]/registrar-venta` — Sale registration form
- `/app/maleta/[id]/devolver` — Return flow (4 steps)

## Key UI Elements (must inspect actual pages)

Read these files before writing selectors:
- `src/app/app/login/page.tsx` — login form fields and submit button
- `src/app/app/page.tsx` — home page elements post-login
- `src/app/app/maleta/page.tsx` — maleta list items
- `src/app/app/maleta/[id]/page.tsx` — maleta detail, "Registrar Venta" button
- `src/app/app/maleta/[id]/registrar-venta/page.tsx` — sale form fields
- `src/app/app/maleta/[id]/devolver/page.tsx` — return flow steps

## SPEC_TESTING_STRATEGY.md — E2E Section

```ts
// Login example from SPEC
test('login com credenciais válidas redireciona para /app', async ({ page }) => {
  await page.goto('/app/login');
  await page.fill('[name=email]', 'revendedora@test.com');
  await page.fill('[name=password]', 'test123456');
  await page.click('[type=submit]');
  await expect(page).toHaveURL('/app');
  await expect(page.locator('h1')).toContainText('Hola');
});
```

## Business Rules (from SPECs)

- Maleta ativa: status = "ativa", has items with quantidade_enviada > 0
- Registrar venda: requires cliente_nome, at least one item selected, updates quantidade_vendida
- Devolução: 4 steps (Resumen → Foto → Revisión → Confirmación), requires comprobante upload
- Login error messages in Spanish Paraguayan ("Contraseña incorrecta")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Fixtures and Login/Maleta Tests</name>
  <files>
    tests/e2e/fixtures/scenarios.ts
    tests/e2e/helpers/navigation.ts
    tests/e2e/login.spec.ts
    tests/e2e/maleta.spec.ts
  </files>
  <action>
    1. Create `tests/e2e/fixtures/scenarios.ts` using Playwright's `test.extend`:
       - Define a fixture `seededReseller` that automatically calls `seedScenario()` in `beforeEach` and `cleanupScenario()` in `afterEach`
       - The fixture should expose: `reseller` (auth+profile objects), `maleta` (the created maleta), `products` (array of created products)
       - Export `test` and `expect` from this file so all spec files use the extended versions
       - Use `test.beforeEach` and `test.afterEach` at the fixture level, NOT in individual tests

    2. Create `tests/e2e/helpers/navigation.ts`:
       - `navigateToMaleta(page)` → clicks "Mi Maleta" or navigates to /app/maleta, waits for load
       - `navigateToHome(page)` → navigates to /app, waits for load
       - `waitForAppReady(page)` → waits for main content to be visible (use a data-testid like `data-testid="app-layout"` or wait for h1)
       - These helpers abstract navigation so tests don't break if URLs change

    3. Create `tests/e2e/login.spec.ts`:
       - Test "login com credenciais válidas redireciona para /app":
         * Seed a reseller
         * Navigate to /app/login
         * Fill email and password using `loginAsReseller` helper
         * Assert URL is /app
         * Assert page contains greeting text (e.g., "Hola" or reseller name)
       - Test "login com senha errada exibe erro":
         * Navigate to /app/login
         * Fill valid email, wrong password
         * Submit
         * Assert error message visible ("Contraseña incorrecta" or similar)
         * Assert URL stays on /app/login
       - Test "logout redireciona para login":
         * Log in as reseller
         * Click logout (use `logout` helper)
         * Assert redirect to /app/login

    4. Create `tests/e2e/maleta.spec.ts`:
       - Test "visualizar maleta ativa":
         * Log in as seeded reseller
         * Navigate to /app/maleta
         * Assert at least one maleta card/item is visible
         * Assert the maleta status shows "Activa" or equivalent
       - Test "acessar detalhes da maleta":
         * Log in, navigate to maleta list
         * Click on the active maleta
         * Assert URL contains /app/maleta/
         * Assert page shows product items
         * Assert "Registrar Venta" button is visible
         * Assert "Devolver" button is visible
  </action>
  <verify>
    <automated>
      test -f tests/e2e/fixtures/scenarios.ts && \
      test -f tests/e2e/helpers/navigation.ts && \
      test -f tests/e2e/login.spec.ts && \
      test -f tests/e2e/maleta.spec.ts && \
      grep -q "test.extend" tests/e2e/fixtures/scenarios.ts && \
      grep -q "seedScenario" tests/e2e/fixtures/scenarios.ts && \
      grep -q "login com credenciais válidas" tests/e2e/login.spec.ts && \
      grep -q "visualizar maleta ativa" tests/e2e/maleta.spec.ts
    </automated>
  </verify>
  <done>
    - `tests/e2e/fixtures/scenarios.ts` extends Playwright test with auto-seed/cleanup
    - Login tests cover valid credentials, wrong password, and logout
    - Maleta tests cover list view and detail view
    - All tests use Spanish Paraguayan assertions where applicable
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Venda and Devolução Tests</name>
  <files>
    tests/e2e/venda.spec.ts
    tests/e2e/devolucao.spec.ts
  </files>
  <action>
    1. Create `tests/e2e/venda.spec.ts`:
       - Use the extended test fixture from `fixtures/scenarios.ts`
       - Test "registrar venda incrementa quantidade vendida":
         * Seed scenario with reseller + active maleta with at least 1 item (quantity > 1 to allow sale)
         * Log in as reseller
         * Navigate to maleta detail
         * Click "Registrar Venta" button
         * Wait for navigation to /app/maleta/[id]/registrar-venta
         * Fill client name ("Cliente Teste E2E")
         * Select at least one item to sell (checkbox or quantity input)
         * Submit form
         * Assert redirect back to maleta detail or success message
         * Assert sold quantity increased by 1 (either by querying DB via Prisma or checking UI text)
       - Test "venda sem selecionar item mostra erro":
         * Navigate to sale form
         * Fill client name but don't select any item
         * Submit
         * Assert validation error visible ("Selecciona al menos un artículo" or similar)
       - Test "venda sem nombre de cliente mostra erro":
         * Select item but leave client name empty
         * Assert validation error for required field

    2. Create `tests/e2e/devolucao.spec.ts`:
       - Test "iniciar devolução e acessar passos":
         * Seed scenario with active maleta
         * Log in as reseller
         * Navigate to maleta detail
         * Click "Devolver" button
         * Assert URL contains /devolver
         * Assert Step 1 (Resumen) shows summary of sent/sold/to-return
       - Test "devolução com upload de comprovante":
         * Navigate through return flow to Step 2 (Foto)
         * Since camera is hard to automate in E2E, use file upload alternative:
           - Check if there's a file input fallback for the camera
           - If camera is the only option, mock with `page.setInputFiles()` on a hidden file input
           - Or skip camera step and test that the flow reaches Step 3 (Revisión) after some interaction
         * Complete Step 3 (Revisión final) — verify summary and commission estimate
         * Submit in Step 4 (Confirmación)
         * Assert redirect to /app with success state or maleta status changed
       - Note: The return flow involves camera access which is flaky in headless CI. Per SPEC_TESTING_STRATEGY.md, use `context.grantPermissions(['camera'])` in the test. If camera still fails, mark test with `test.skip()` and document the limitation in the test file comment.

    3. All test files must import `test` from `tests/e2e/fixtures/scenarios.ts`, not from `@playwright/test` directly.
  </action>
  <verify>
    <automated>
      test -f tests/e2e/venda.spec.ts && \
      test -f tests/e2e/devolucao.spec.ts && \
      grep -q "registrar venda" tests/e2e/venda.spec.ts && \
      grep -q "devolução" tests/e2e/devolucao.spec.ts && \
      grep -q "fixtures/scenarios" tests/e2e/venda.spec.ts && \
      grep -q "fixtures/scenarios" tests/e2e/devolucao.spec.ts
    </automated>
  </verify>
  <done>
    - Venda test registers a sale and verifies quantity update
    - Venda validation tests cover missing item and missing client name
    - Devolução test navigates through all 4 steps of return flow
    - Camera permission is granted via Playwright context
    - All 4 spec files use the shared seeded fixture
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E tests → Application | Tests exercise real UI; must not pollute production DB |
| Test fixtures → Database | Fixtures have full DB access via Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Test screenshots on failure may capture sensitive data | mitigate | Configure Playwright to only screenshot on failure; ensure test data uses fake PII |
| T-09-05 | Denial of Service | Tests running against production URL | mitigate | playwright.config.ts baseURL must point to localhost:3000 (dev server) |
| T-09-06 | Elevation of Privilege | Test seeds admin user with weak password | accept | Test DB only; documented in .env.test.example |
</threat_model>

<verification>
- Run `npm run test:e2e -- tests/e2e/login.spec.ts` → passes
- Run `npm run test:e2e -- tests/e2e/maleta.spec.ts` → passes
- Run `npm run test:e2e -- tests/e2e/venda.spec.ts` → passes (may require app dev server running)
- All tests complete in < 2 minutes total
</verification>

<success_criteria>
- 4 spec files exist: login.spec.ts, maleta.spec.ts, venda.spec.ts, devolucao.spec.ts
- Each test is independent (auto-seed/cleanup via fixture)
- Login test covers valid/invalid credentials and logout
- Maleta test covers list and detail views
- Venda test covers sale registration and validation errors
- Devolução test covers return flow steps
- `npm run test:e2e` executes all 4 spec files
- Total E2E runtime < 5 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/09-e2e-testing/09-02-SUMMARY.md`
</output>
