---
phase: 04-build-optimization-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/catalogo/[slug]/[productSlug]/page.tsx
  - src/app/carrinho/page.tsx
  - src/app/vitrina/[slug]/page.tsx
  - vercel.json
  - .env.example
autonomous: false
requirements:
  - TECH-07
  - TECH-08
  - TECH-09
user_setup:
  - service: vercel
    why: "DATABASE_URL must be configured as a build-time environment variable"
    env_vars:
      - name: DATABASE_URL
        source: "Vercel Dashboard → Project Settings → Environment Variables → Production/Preview/Development"
      - name: DIRECT_URL
        source: "Same as DATABASE_URL (used by Prisma for migrations)"
    dashboard_config:
      - task: "Add DATABASE_URL to all three environments (Production, Preview, Development)"
        location: "Vercel Dashboard → monarca → Settings → Environment Variables"
      - task: "Verify DIRECT_URL is also present (needed for Prisma migrate)"
        location: "Same as above"
must_haves:
  truths:
    - "DATABASE_URL is present in Vercel build environment for Production, Preview, and Development"
    - "Public pages no longer use force-dynamic (only revalidate = 60 or static)"
    - "Build completes successfully without Prisma connection errors on public pages"
    - "Authenticated pages continue to use force-dynamic for real-time data"
  artifacts:
    - path: "src/app/catalogo/[slug]/[productSlug]/page.tsx"
      provides: "Product detail page with ISR instead of force-dynamic"
      contains: "export const revalidate = 60"
    - path: "src/app/carrinho/page.tsx"
      provides: "Cart page with appropriate caching strategy"
    - path: "src/app/vitrina/[slug]/page.tsx"
      provides: "Public vitrine page with ISR"
      contains: "export const revalidate = 60"
  key_links:
    - from: "Vercel Dashboard"
      to: "src/app/*/page.tsx"
      via: "DATABASE_URL build env var enables static generation with Prisma"
      pattern: "revalidate = 60|dynamic = 'force-dynamic'"
---

<objective>
Configure Vercel build environment and migrate all public pages from force-dynamic to ISR (revalidate = 60). Ensure build succeeds with Prisma static generation.

Purpose: Eliminate the force-dynamic workaround on public pages so they benefit from edge caching and faster loads, while preserving real-time behavior on authenticated routes.
Output: Public pages using ISR, DATABASE_URL confirmed in Vercel, build passing.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

# Critical pitfall for this phase
@docs/sistema/SPEC_CACHING_STRATEGY.md
@docs/sistema/SPEC_DEPLOY_STRATEGY.md

# Existing public pages to audit
@src/app/page.tsx (already has revalidate = 60)
@src/app/catalogo/page.tsx (already has revalidate = 60)
@src/app/produto/[slug]/page.tsx (already has revalidate = 60)
@src/app/catalogo/[slug]/[productSlug]/page.tsx (HAS force-dynamic — must fix)
@src/app/carrinho/page.tsx (no directive — needs decision)
@src/app/vitrina/[slug]/page.tsx (does not exist yet — create with ISR)

# Prisma config
@src/lib/prisma.ts
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Configure DATABASE_URL in Vercel build environment</name>
  <what-built>
    This is a human-only step. The agent cannot access the Vercel dashboard.
  </what-built>
  <how-to-verify>
    1. Open Vercel Dashboard → monarca project → Settings → Environment Variables
    2. Verify DATABASE_URL exists in Production, Preview, and Development
    3. Verify DIRECT_URL exists in all three environments
    4. Trigger a redeploy (or run `vercel --prod` locally) and confirm build log shows no PrismaClientInitializationError
    5. Optional: run `vercel env ls` to confirm from CLI
  </how-to-verify>
  <resume-signal>Type "done" when DATABASE_URL and DIRECT_URL are configured in all three Vercel environments and a test build succeeds</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Audit and fix caching directives on all public pages</name>
  <files>
    src/app/catalogo/[slug]/[productSlug]/page.tsx
    src/app/carrinho/page.tsx
    src/app/vitrina/[slug]/page.tsx
    src/app/page.tsx
    src/app/catalogo/page.tsx
    src/app/produto/[slug]/page.tsx
    src/app/catalogo/[slug]/page.tsx
  </files>
  <read_first>
    - src/app/catalogo/[slug]/[productSlug]/page.tsx (current state)
    - src/app/carrinho/page.tsx (current state)
    - docs/sistema/SPEC_CACHING_STRATEGY.md §8 (ISR strategy per route)
  </read_first>
  <action>
    1. Read each public page file listed above.
    2. For `src/app/catalogo/[slug]/[productSlug]/page.tsx`:
       - Remove `export const dynamic = "force-dynamic"`
       - Add `export const revalidate = 60` at the top
       - Verify the page does NOT call `headers()` or `cookies()` (these would force dynamic regardless of the export). If it does, refactor to remove those calls or keep force-dynamic and document why.
    3. For `src/app/carrinho/page.tsx`:
       - If it exists and has no directive: check if it needs dynamic data. If it's a static page (no user-specific data on initial load), add `export const revalidate = 60`. If it reads cookies/headers for cart state, keep as-is or use `dynamic = 'force-dynamic'` and document.
    4. For `src/app/vitrina/[slug]/page.tsx`:
       - If the file does not exist, create it with `export const revalidate = 60` at the top. The vitrina page is a public page per SPEC_CACHING_STRATEGY §3.5 and §8.
       - If it already exists, ensure it has `revalidate = 60` and no `force-dynamic`.
    5. For all other public pages (`page.tsx`, `catalogo/page.tsx`, `produto/[slug]/page.tsx`, `catalogo/[slug]/page.tsx`):
       - Verify they already have `export const revalidate = 60`
       - Ensure they do NOT also have `export const dynamic = "force-dynamic"` (having both is a conflict — revalidate wins but it's confusing)
    6. Run `npm run build` locally with DATABASE_URL set to confirm no Prisma errors on public pages.
    7. If build fails with static generation error on any public page, investigate whether that page calls dynamic APIs (headers/cookies/redirect from server component). Fix by either removing the dynamic call or reverting to force-dynamic with a comment explaining why.
  </action>
  <verify>
    <automated>
      grep -r "force-dynamic" src/app/page.tsx src/app/catalogo/page.tsx src/app/produto/[slug]/page.tsx src/app/catalogo/[slug]/page.tsx src/app/catalogo/[slug]/[productSlug]/page.tsx src/app/carrinho/page.tsx src/app/vitrina/[slug]/page.tsx 2>/dev/null | wc -l | xargs test 0 -eq && \
      grep -q "revalidate = 60" src/app/catalogo/[slug]/[productSlug]/page.tsx && \
      grep -q "revalidate = 60" src/app/vitrina/[slug]/page.tsx && \
      npm run build 2>&1 | grep -v "PrismaClientInitializationError" | tail -5
    </automated>
  </verify>
  <done>
    - No public page uses `export const dynamic = "force-dynamic"`
    - All public pages have `export const revalidate = 60` (or are truly static)
    - `npm run build` passes without Prisma connection errors on public pages
    - Any page that must remain dynamic has a code comment explaining why
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Prisma pool limits for serverless</name>
  <files>
    src/lib/prisma.ts
  </files>
  <read_first>
    - src/lib/prisma.ts (current state)
  </read_first>
  <action>
    1. Read `src/lib/prisma.ts`.
    2. Verify the Prisma client instantiation includes connection pool limits appropriate for Vercel serverless:
       ```ts
       const prisma = new PrismaClient({
         datasources: {
           db: {
             url: process.env.DATABASE_URL,
           },
         },
         // Connection pool for serverless (Vercel)
         // Each instance gets its own pool; keep max low
       });
       ```
    3. If pool limits are NOT configured, add them:
       ```ts
       const prisma = new PrismaClient({
         datasources: { db: { url: process.env.DATABASE_URL } },
       });
       ```
       Note: Prisma 7 with PrismaPg driver adapter may not support `connectionLimit` in the constructor. If the project uses the adapter, pool config may need to be in the connection string or adapter config. Do NOT add unsupported options.
    4. Verify `max` pool size is documented or configured. If using a direct PostgreSQL connection string, ensure it does NOT override with a large pool (default Prisma pool is usually fine for serverless).
  </action>
  <verify>
    <automated>
      test -f src/lib/prisma.ts && \
      grep -q "PrismaClient" src/lib/prisma.ts && \
      grep -q "datasources" src/lib/prisma.ts
    </automated>
  </verify>
  <done>
    - Prisma client configured with proper datasource URL
    - No oversized connection pool that could exhaust Supabase limits
    - Build and runtime work correctly with serverless environment
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Build → Database | Build-time database access for static generation |
| Public Page → Prisma | Static generation must not leak sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | Build with DATABASE_URL | mitigate | DATABASE_URL configured BEFORE removing force-dynamic; test build locally before deploying (PITFALL CP-4) |
| T-04-02 | Information Disclosure | Static generation of public pages | mitigate | Verify no authenticated/user-specific data is rendered in ISR pages; public pages only show catalog/vitrine data |
| T-04-03 | Elevation of Privilege | Carrinho page caching | mitigate | If cart page reads auth cookies, it MUST remain dynamic; do NOT apply ISR to pages with auth-dependent renders |
</threat_model>

<verification>
- [ ] DATABASE_URL present in Vercel Production, Preview, and Development
- [ ] `npm run build` passes locally with DATABASE_URL configured
- [ ] No public page uses `force-dynamic`
- [ ] All public pages have `revalidate = 60` or are static
- [ ] Authenticated pages (`/app/*`, `/admin/*`) still use `force-dynamic`
- [ ] Build log shows no PrismaClientInitializationError on public routes
</verification>

<success_criteria>
- TECH-07: force-dynamic removed from all public pages
- TECH-08: ISR (revalidate = 60) configured on all public pages
- TECH-09: DATABASE_URL configured in Vercel build environment variables
- Build completes without static generation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-optimization-polish/04-01-SUMMARY.md`
</output>
