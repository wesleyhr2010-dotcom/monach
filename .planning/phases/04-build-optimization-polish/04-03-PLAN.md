---
phase: 04-build-optimization-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - docs/next_steps.md
  - docs/project_overview.md
  - docs/CHANGELOG.md
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TECH-07
  - TECH-08
  - TECH-09
  - TECH-10
must_haves:
  truths:
    - "npm run build completes successfully with zero errors"
    - "npm run typecheck passes with zero TypeScript errors"
    - "npm run lint passes with zero lint errors"
    - "All TECH requirements for Phase 4 are marked complete in docs"
  artifacts:
    - path: "docs/next_steps.md"
      provides: "Updated next steps with Phase 4 items marked complete"
    - path: "docs/project_overview.md"
      provides: "Updated project status reflecting ISR and cache invalidation"
    - path: "docs/CHANGELOG.md"
      provides: "Dated entry for Phase 4 build optimization"
  key_links:
    - from: "Plan 01 output"
      to: "Plan 03 verification"
      via: "Build must pass after ISR migration and DATABASE_URL config"
    - from: "Plan 02 output"
      to: "Plan 03 verification"
      via: "Build must pass after cache invalidation wiring"
---

<objective>
Run full build verification, ensure all quality gates pass, and update project documentation to reflect Phase 4 completion.

Purpose: Confirm that the ISR migration and cache invalidation wiring did not break the build or introduce TypeScript/lint errors. Maintain documentation hygiene.
Output: Clean build, updated docs, optional CI workflow.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/next_steps.md
@docs/project_overview.md

# Phase 4 plans this depends on
@.planning/phases/04-build-optimization-polish/04-01-PLAN.md
@.planning/phases/04-build-optimization-polish/04-02-PLAN.md

# Build config
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and fix any errors</name>
  <files>
    package.json
    src/app/catalogo/[slug]/[productSlug]/page.tsx
    src/app/vitrina/[slug]/page.tsx
    src/lib/cache/invalidate.ts
    src/app/admin/actions-products.ts
    src/app/admin/actions-categories.ts
    src/app/admin/actions-maletas.ts
    src/app/admin/actions-config.ts
    src/app/admin/actions-equipe.ts
    src/app/admin/actions-gamificacao.ts
    src/app/admin/actions-leads.ts
    src/app/admin/brindes/actions.ts
    src/app/admin/config/notif-push/actions.ts
    src/app/admin/minha-conta/actions.ts
    src/app/admin/revendedoras/[id]/documentos/actions.ts
    src/app/app/actions-revendedora.ts
    src/app/app/actions-desempeno.ts
    src/app/app/perfil/actions.ts
    src/app/app/progreso/actions.ts
    src/app/app/notificaciones/actions.ts
    src/app/app/bienvenida/actions.ts
    src/app/actions.ts
  </files>
  <read_first>
    - package.json (build scripts)
    - .planning/research/PITFALLS.md (CP-4, CP-6, MP-6)
  </read_first>
  <action>
    1. Run `npm run build`:
       - If it fails with PrismaClientInitializationError on public pages → DATABASE_URL is not configured in the build environment. Document this in the task output and STOP — this requires Plan 01 Task 1 (Vercel env vars) to be completed first.
       - If it fails with TypeScript errors → fix them (likely missing imports from the cache invalidation changes).
       - If it fails with static generation timeout on any page → investigate whether that page still calls dynamic APIs. Fix by reverting to force-dynamic with a comment if needed.
       - If build succeeds → proceed.
    2. Run `npm run typecheck` (or `npx tsc --noEmit`):
       - Fix any TypeScript errors introduced by the invalidateCache imports or usage.
    3. Run `npm run lint`:
       - Fix any lint errors (unused imports, formatting, etc.).
    4. If any file from Plan 01 or Plan 02 needs fixes, edit it directly.
    5. Verify `package.json` build script still includes `prisma generate && next build`.
    6. Verify `postinstall` script includes `prisma generate` (per PITFALL MP-6). If missing, add:
       ```json
       "scripts": {
         "postinstall": "prisma generate",
         "build": "prisma generate && next build"
       }
       ```
  </action>
  <verify>
    <automated>
      npm run build 2>&1 | grep -q "Build error" && exit 1 || true && \
      npm run typecheck 2>&1 | grep -c "error TS" | xargs test 0 -eq && \
      npm run lint 2>&1 | grep -c "error" | xargs test 0 -eq
    </automated>
  </verify>
  <done>
    - `npm run build` completes with zero errors
    - `npm run typecheck` passes with zero TypeScript errors
    - `npm run lint` passes with zero lint errors
    - `package.json` has `prisma generate` in both `build` and `postinstall` scripts
    - No PrismaClientInitializationError in build output
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow (optional but recommended)</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - docs/sistema/SPEC_DEPLOY_STRATEGY.md (CI/CD section)
    - .planning/codebase/CONCERNS.md §10 (GitHub Actions CI não existe)
  </read_first>
  <action>
    1. Check if `.github/workflows/ci.yml` already exists.
    2. If it does NOT exist, create it:
       ```yaml
       name: CI

       on:
         push:
           branches: [main, staging]
         pull_request:
           branches: [main, staging]

       jobs:
         build:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: 20
                 cache: 'npm'
             - run: npm ci
             - run: npm run lint
             - run: npm run typecheck
             - run: npm test
             - run: npm run build
               env:
                 DATABASE_URL: ${{ secrets.DATABASE_URL }}
                 DIRECT_URL: ${{ secrets.DIRECT_URL }}
                 NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
                 NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
       ```
    3. If it DOES exist, verify it includes lint, typecheck, test, and build steps. Update if needed.
    4. Note: This workflow requires GitHub Secrets to be configured (DATABASE_URL, etc.). The agent cannot configure GitHub Secrets — this is noted as a post-merge manual step.
  </action>
  <verify>
    <automated>
      test -f .github/workflows/ci.yml && \
      grep -q "npm run lint" .github/workflows/ci.yml && \
      grep -q "npm run typecheck" .github/workflows/ci.yml && \
      grep -q "npm test" .github/workflows/ci.yml && \
      grep -q "npm run build" .github/workflows/ci.yml
    </automated>
  </verify>
  <done>
    - `.github/workflows/ci.yml` exists with lint, typecheck, test, and build steps
    - Workflow triggers on push/PR to main and staging
    - Build step has DATABASE_URL and other required env vars
  </done>
</task>

<task type="auto">
  <name>Task 3: Update project documentation</name>
  <files>
    docs/next_steps.md
    docs/project_overview.md
    docs/CHANGELOG.md
  </files>
  <read_first>
    - docs/next_steps.md (current state)
    - docs/project_overview.md (current state)
    - docs/CHANGELOG.md (current state)
  </read_first>
  <action>
    1. Update `docs/next_steps.md`:
       - Mark Phase 4 items as complete:
         - [x] Remover `force-dynamic` de páginas públicas
         - [x] Configurar ISR (`revalidate = 60`) em páginas públicas
         - [x] Configurar `DATABASE_URL` no Vercel
         - [x] Adicionar `revalidateTag`/`revalidatePath` em todas as Server Actions de mutação
       - If there are any Phase 4 items still pending, leave them unchecked with a note.
    2. Update `docs/project_overview.md` §6 "O que já foi desenvolvido" and §7 "Status atual":
       - Add Phase 4 completion note
       - Mention ISR on public pages and centralized cache invalidation
       - Update status to reflect Phase 4 complete (or in-progress if not fully done)
    3. Update `docs/CHANGELOG.md`:
       - Add a dated entry for Phase 4:
         ```markdown
         ## 2026-05-04 — Phase 4: Build Optimization & Polish
         - Páginas públicas migradas de `force-dynamic` para ISR (`revalidate = 60`)
         - `DATABASE_URL` configurado nas env vars do Vercel (Production, Preview, Development)
         - Helper `invalidateCache` criado em `src/lib/cache/invalidate.ts`
         - `revalidateTag`/`revalidatePath` adicionados a todas as Server Actions de mutação
         - Build Vercel otimizado — sem erros de static generation
         ```
    4. Verify no duplicate information — if a detail is already in a SPEC, reference it rather than duplicating.
  </action>
  <verify>
    <automated>
      grep -q "Phase 4\|Build Optimization" docs/next_steps.md && \
      grep -q "force-dynamic\|ISR" docs/project_overview.md && \
      grep -q "invalidateCache\|revalidateTag" docs/CHANGELOG.md
    </automated>
  </verify>
  <done>
    - docs/next_steps.md updated with Phase 4 items marked complete
    - docs/project_overview.md reflects current system status
    - docs/CHANGELOG.md has dated entry for Phase 4 changes
    - No duplication with existing SPECs
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Production | CI must not leak secrets in build logs |
| Build → Database | Build-time DB access must use read-only or safe credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | CI workflow secrets | mitigate | DATABASE_URL stored in GitHub Secrets, not hardcoded in ci.yml |
| T-04-08 | Denial of Service | Build failure blocking deploy | mitigate | CI runs on PR before merge; build errors caught before production |
| T-04-09 | Elevation of Privilege | CI build env vars | accept | Low risk — CI uses same DB credentials as app; no elevated permissions |
</threat_model>

<verification>
- [ ] `npm run build` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes (existing tests)
- [ ] docs/next_steps.md updated
- [ ] docs/project_overview.md updated
- [ ] docs/CHANGELOG.md updated
- [ ] `.github/workflows/ci.yml` exists (optional but recommended)
</verification>

<success_criteria>
- Build passes without errors after ISR migration and cache invalidation wiring
- TypeScript and lint gates pass
- Documentation reflects Phase 4 completion
- All TECH requirements (TECH-07..TECH-10) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-optimization-polish/04-03-SUMMARY.md`
</output>
