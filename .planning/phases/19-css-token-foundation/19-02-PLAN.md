---
phase: "19"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/admin.css
autonomous: true
requirements:
  - TKN-03
must_haves:
  truths:
    - "admin.css :root contains light values for all --admin-* tokens (D-19-02)"
    - ".admin-layout[data-theme=\"dark\"] block in admin.css contains all current dark values"
    - "No visual change — admin stays dark by default because no data-theme attribute is set yet (Phase 21)"
    - "npm run build and npm run lint pass with no new errors"
  artifacts:
    - path: "src/app/admin/admin.css"
      provides: "Light :root defaults + .admin-layout[data-theme=\"dark\"] dark variants"
      contains:
        - ":root { --admin-bg: #ffffff;"
        - ".admin-layout[data-theme=\"dark\"]"
      min_lines: 1058
  key_links:
    - from: ":root { --admin-* }"
      to: ".admin-layout[data-theme=\"dark\"] { --admin-* }"
      via: "CSS cascade — dark selector overrides :root defaults when data-theme=\"dark\" is present"
      pattern: ".admin-layout\\[data-theme=\"dark\"\\]"
---

<objective>
Invert admin.css theme architecture: move current dark `--admin-*` values from `:root` to `.admin-layout[data-theme="dark"]`, and set light values as `:root` defaults per D-19-02.

Purpose: Establish dual-theme capability for admin panel. Light becomes the CSS default (:root), dark becomes the explicit variant ([data-theme="dark"]). No visual change because no data-theme attribute is set yet — :root light values are overridden by globals.css shadcn/ui tokens for admin (Plan 01 Task 3 keeps admin dark via .admin-layout[data-theme="dark"] in globals.css).

Output: admin.css with light :root defaults and .admin-layout[data-theme="dark"] block containing all current dark values.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-css-token-foundation/19-CONTEXT.md
@.planning/phases/19-css-token-foundation/19-RESEARCH.md
@src/app/admin/admin.css
@docs/design-system/tokens.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move dark --admin-* values from :root to .admin-layout[data-theme="dark"]</name>
  <files>src/app/admin/admin.css</files>
  <read_first>
    - src/app/admin/admin.css lines 1-50 — current :root block with all 34 dark --admin-* token declarations
  </read_first>
  <action>
    In admin.css, replace the current `:root { ... }` block (lines 5-50) that contains all dark --admin-* values with a `.admin-layout[data-theme="dark"] { ... }` block. Keep ALL the same token declarations inside — only change the selector from `:root` to `.admin-layout[data-theme="dark"]`. The tokens to preserve exactly as-is:

    --admin-bg: #0a0a0a
    --admin-surface: #171717
    --admin-surface-hover: #222222
    --admin-surface-row-atrasada: #1a1010
    --admin-surface-row-aguardando: #1a1a10
    --admin-border: #2a2a2a
    --admin-border-focus: #35605a
    --admin-text: #ededed
    --admin-text-muted: #888888
    --admin-text-dim: #444444
    --admin-accent: #35605a
    --admin-accent-hover: #2a4d48
    --admin-danger: #e05c5c
    --admin-danger-hover: #c44545
    --admin-success: #4ade80
    --admin-success-10: rgba(74, 222, 128, 0.1)
    --admin-success-15: rgba(74, 222, 128, 0.15)
    --admin-warning: #facc15
    --admin-warning-10: rgba(250, 204, 21, 0.1)
    --admin-warning-15: rgba(250, 204, 21, 0.15)
    --admin-danger (second declaration, line 26): #e05c5c
    --admin-danger-10: rgba(224, 92, 92, 0.1)
    --admin-danger-15: rgba(224, 92, 92, 0.15)
    --admin-muted-10: rgba(136, 136, 136, 0.1)
    --admin-muted-15: rgba(136, 136, 136, 0.15)
    --admin-info: #6677dd
    --admin-info-light: #60a5fa
    --admin-brown: #917961
    --admin-beige: #b4aba2
    --admin-purple: #8b5cf6
    --admin-purple-light: #a855f7
    --admin-orange: #f59e0b
    --admin-green-alt: #22c55e
    --admin-blue-alt: #3b82f6
    --admin-emerald: #10b981
    --admin-bg-success: #0f3d1c
    --admin-border-success: #1a5a2a
    --admin-bg-info: #1a1a2e
    --admin-radius: 8px
    --admin-radius-sm: 4px
    --admin-radius-lg: 12px
    --admin-radius-pill: 9px
    --admin-sidebar-width: 240px
    --admin-header-height: 80px

    Do NOT modify any other CSS rules in the file in this task — only the :root block selector changes.
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
    <source>grep -c '^\..admin-layout\[data-theme="dark"\]' src/app/admin/admin.css returns at least 1</source>
    <source>grep -c '\-\-admin-bg: #0a0a0a' src/app/admin/admin.css returns 1 (dark value now under .admin-layout[data-theme="dark"])</source>
  </verify>
  <done>Dark --admin-* tokens moved from :root to .admin-layout[data-theme="dark"], build passes</done>
</task>

<task type="auto">
  <name>Task 2: Add light :root defaults for all --admin-* tokens</name>
  <files>src/app/admin/admin.css</files>
  <read_first>
    - src/app/admin/admin.css — the .admin-layout[data-theme="dark"] block created in Task 1, to ensure all tokens have corresponding light values
    - docs/design-system/tokens.md lines 55-99 — reference for all admin token names and their current dark values
  </read_first>
  <action>
    Insert a new `:root { ... }` block at the top of admin.css (before the `.admin-layout[data-theme="dark"]` block from Task 1) containing light-mode defaults for ALL --admin-* tokens. Use these light values:

    --admin-bg: #ffffff
    --admin-surface: #f5f5f5
    --admin-surface-hover: #e5e5e5
    --admin-surface-row-atrasada: #fef2f2
    --admin-surface-row-aguardando: #fefce8
    --admin-border: #e5e7eb
    --admin-border-focus: #35605a
    --admin-text: #1a1a1a
    --admin-text-muted: #6b7280
    --admin-text-dim: #9ca3af
    --admin-accent: #35605a
    --admin-accent-hover: #2a4d48
    --admin-danger: #dc2626
    --admin-danger-hover: #b91c1c
    --admin-success: #16a34a
    --admin-success-10: rgba(22, 163, 74, 0.1)
    --admin-success-15: rgba(22, 163, 74, 0.15)
    --admin-warning: #ca8a04
    --admin-warning-10: rgba(202, 138, 4, 0.1)
    --admin-warning-15: rgba(202, 138, 4, 0.15)
    --admin-danger (second declaration): #dc2626
    --admin-danger-10: rgba(220, 38, 38, 0.1)
    --admin-danger-15: rgba(220, 38, 38, 0.15)
    --admin-muted-10: rgba(107, 114, 128, 0.1)
    --admin-muted-15: rgba(107, 114, 128, 0.15)
    --admin-info: #4f6dd4
    --admin-info-light: #3b82f6
    --admin-brown: #917961
    --admin-beige: #b4aba2
    --admin-purple: #8b5cf6
    --admin-purple-light: #a855f7
    --admin-orange: #f59e0b
    --admin-green-alt: #22c55e
    --admin-blue-alt: #3b82f6
    --admin-emerald: #10b981
    --admin-bg-success: #dcfce7
    --admin-border-success: #bbf7d0
    --admin-bg-info: #eff6ff
    --admin-radius: 8px
    --admin-radius-sm: 4px
    --admin-radius-lg: 12px
    --admin-radius-pill: 9px
    --admin-sidebar-width: 240px
    --admin-header-height: 80px

    Also add the alias tokens from the second :root block (lines 1018-1026 in current file) to this new :root block:
    --admin-primary: var(--admin-accent)
    --admin-text-primary: var(--admin-text)
    --admin-text-secondary: var(--admin-text-muted)
    --admin-bg-secondary: var(--admin-surface-hover)
    --admin-row-atrasada: var(--admin-surface-row-atrasada)
    --admin-row-aguardando: var(--admin-surface-row-aguardando)

    After inserting this new :root block, remove the duplicate alias :root block at lines 1018-1026 (the one starting with `/* -- Additional root aliases for inline styles -- */`) to avoid duplicate :root declarations. The alias tokens should only exist in the new :root block at the top.

    Per D-19-02: light values (#ffffff, #f5f5f5, #e5e7eb) are the :root defaults. Dark values stay in .admin-layout[data-theme="dark"].
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
    <source>grep -c ':root {' src/app/admin/admin.css returns 1 (only one :root block, the new light defaults)</source>
    <source>grep -c '\-\-admin-bg: #ffffff' src/app/admin/admin.css returns 1</source>
    <source>grep -c '\-\-admin-text: #1a1a1a' src/app/admin/admin.css returns 1</source>
  </verify>
  <done>:root block with light --admin-* defaults exists at top of admin.css, duplicate alias :root removed, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is pure CSS — no authentication, data handling, or network operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | N/A | admin.css | accept | Pure CSS token declarations — no security surface |
</threat_model>

<verification>
- `npm run build` passes without errors
- `npm run lint` passes without new errors
- `grep -c ':root {' src/app/admin/admin.css` returns 1 (single :root block)
- `grep -c '\-\-admin-bg: #ffffff' src/app/admin/admin.css` returns 1 (light default present)
- `grep -c '\-\-admin-bg: #0a0a0a' src/app/admin/admin.css` returns 1 (dark value under .admin-layout[data-theme="dark"])
- `grep -c '.admin-layout\[data-theme="dark"\]' src/app/admin/admin.css` returns at least 1
</verification>

<success_criteria>
1. :root block in admin.css contains light values for all --admin-* tokens (bg: #ffffff, surface: #f5f5f5, text: #1a1a1a, border: #e5e7eb)
2. .admin-layout[data-theme="dark"] block contains all current dark values (#0a0a0a, #171717, #ededed, etc.)
3. Duplicate alias :root block (lines 1018-1026) removed — aliases merged into single :root
4. All 34+ --admin-* tokens have both light and dark variants declared
5. npm run build and npm run lint pass with no new errors
6. No visual change — admin stays dark because globals.css .admin-layout[data-theme="dark"] (Plan 01) provides the dark shadcn/ui overrides that cascade
</success_criteria>

<output>
After completion, create `.planning/phases/19-css-token-foundation/19-02-SUMMARY.md`
</output>
