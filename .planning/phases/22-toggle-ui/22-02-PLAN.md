---
phase: 22-toggle-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/app/admin/minha-conta/page.tsx
  - src/components/theme/ThemeToggle.tsx
autonomous: true
requirements:
  - TOG-02
  - TOG-03
must_haves:
  truths:
    - "Admin/Colaboradora sees 'Apariencia' section in /admin/minha-conta with sun/moon toggle"
    - "Clicking toggle immediately switches /admin surface between dark and light"
    - "Sonner toast appears in the correct theme after manual toggle on either surface"
  artifacts:
    - path: "src/app/admin/minha-conta/page.tsx"
      provides: "Admin account page with Apariencia section"
      contains: "Apariencia"
    - path: "src/components/theme/ThemeToggle.tsx"
      provides: "Toggle component with admin variant support"
      contains: "variant.*admin"
  key_links:
    - from: "src/app/admin/minha-conta/page.tsx"
      to: "src/components/theme/ThemeToggle.tsx"
      via: "import and render with variant='admin'"
      pattern: "ThemeToggle.*variant.*admin"
    - from: "src/components/theme/SonnerThemer.tsx"
      to: "src/components/theme/useTheme.ts"
      via: "useTheme(storageKey) -> resolvedTheme"
      pattern: "resolvedTheme"
---

<objective>
Wire the ThemeToggle into the admin account page (`/admin/minha-conta`) and verify that Sonner toasts follow the manually selected theme on both surfaces.

Purpose: Deliver TOG-02 (admin toggle) and TOG-03 (Sonner theming verification).
Output: Updated /admin/minha-conta/page.tsx with Apariencia section + verification of Sonner toast theming.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-toggle-ui/22-CONTEXT.md
@.planning/phases/21-themeprovider-infrastructure/21-02-SUMMARY.md

## ThemeToggle Component (created in Plan 01)

Expected interface:
```typescript
interface ThemeToggleProps {
  variant?: "app" | "admin"; // default "app"
}
export function ThemeToggle({ variant = "app" }: ThemeToggleProps): JSX.Element;
```

The component consumes `useThemeContext()` and calls `setTheme("light" | "dark")`.

## Target Page: src/app/admin/minha-conta/page.tsx

Current structure (Server Component):
- AdminTopHeader
- Profile card (avatar, name, email, comissão)
- Stat cards grid (2 rows)
- "Acessos Rápidos" card with links

The "Apariencia" section should be added as a new card BELOW the "Acessos Rápidos" section.

## Admin Design System Tokens

From `src/app/admin/admin.css` (reference the actual file if needed during execution):
- Text: `--admin-text`, `--admin-text-muted`, `--admin-text-dim`
- Backgrounds: `--admin-bg`, `--admin-card-bg`
- Border: `--admin-border`
- Primary accent: `--admin-primary`

## Sonner Verification Context

From Phase 21, `SonnerThemer.tsx` already does:
```tsx
const { resolvedTheme } = useTheme(storageKey);
return <Toaster theme={resolvedTheme} ... />;
```

TOG-03 is a **verification** task: confirm that when the theme is toggled manually, a toast triggered afterward uses the correct `theme` prop. The implementation is already complete — we only need to verify.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Apariencia section to /admin/minha-conta</name>
  <files>src/app/admin/minha-conta/page.tsx, src/components/theme/ThemeToggle.tsx</files>
  <action>
    Modify `src/app/admin/minha-conta/page.tsx` to add an "Apariencia" section.

    1. In `src/components/theme/ThemeToggle.tsx`, ensure the `admin` variant is properly styled:
       - When `variant === "admin"`, use admin tokens:
         - Background: `var(--admin-card-bg)` or Tailwind class that maps to it
         - Text: `var(--admin-text)` or equivalent class
         - Border: `var(--admin-border)` or equivalent class
         - Icon color: `var(--admin-primary)` or equivalent class
       - The pill shape, size, and interaction behavior remain identical to the app variant.
       - If admin token Tailwind classes don't exist (e.g., no `text-admin-primary` utility), use inline style with CSS variable: `style={{ color: "var(--admin-primary)" }}`.

    2. In `src/app/admin/minha-conta/page.tsx`:
       - Add import: `import { ThemeToggle } from "@/components/theme/ThemeToggle";`
       - Add import: `import { Sun, Moon } from "lucide-react";` (if a decorative icon is needed)
       - After the "Acessos Rápidos" `admin-card` div (but still inside the main content div), add a new card:
         ```tsx
         <div className="admin-card">
           <h3
             className="text-sm font-semibold mb-3"
             style={{ color: "var(--admin-text-dim)", fontFamily: "Raleway, system-ui, sans-serif" }}
           >
             APARIENCIA
           </h3>
           <div className="flex items-center justify-between">
             <span className="text-sm" style={{ color: "var(--admin-text)" }}>Tema</span>
             <ThemeToggle variant="admin" />
           </div>
         </div>
         ```
       - Note: The exact card class and heading style should match the existing "Acessos Rápidos" section above it (copy the same heading style pattern).

    3. Ensure the page still builds and no lint errors are introduced.
  </action>
  <verify>
    <automated>grep -n "Apariencia" src/app/admin/minha-conta/page.tsx && grep -n "ThemeToggle" src/app/admin/minha-conta/page.tsx && grep -n "variant.*admin" src/components/theme/ThemeToggle.tsx</automated>
  </verify>
  <done>/admin/minha-conta page contains "Apariencia" section with ThemeToggle variant="admin", admin token styling is applied, page builds and lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Sonner theming follows manual toggle (TOG-03)</name>
  <files>src/components/theme/SonnerThemer.tsx, src/components/theme/useTheme.ts</files>
  <action>
    Verify TOG-03: Sonner Toaster follows the manually selected theme, not OS preference.

    This is a verification-only task — no new code should be needed if Phase 21 was implemented correctly.

    Verification steps:
    1. Read `src/components/theme/SonnerThemer.tsx` and confirm:
       - It uses `useTheme(storageKey)` (not a media query)
       - It passes `theme={resolvedTheme}` to `<Toaster>` where `resolvedTheme` is `"light" | "dark"` (never `"system"`)
       - The `resolvedTheme` comes from `useTheme.ts` which resolves "system" to the concrete theme

    2. Read `src/components/theme/useTheme.ts` and confirm:
       - `setTheme()` persists the chosen value to localStorage
       - `getInitialTheme()` reads from localStorage first, falling back to "system"
       - `getInitialResolvedTheme()` returns the concrete "light" | "dark" value

    3. Run the automated checks:
       - `grep -n 'theme="system"' src/components/theme/SonnerThemer.tsx` → must return 0 results
       - `grep -n "resolvedTheme" src/components/theme/SonnerThemer.tsx` → must return results
       - `grep -n "localStorage.setItem" src/components/theme/useTheme.ts` → must return results

    4. Build check: `npm run build` must pass with 0 errors.

    If any of the above checks fail, fix the issue. The most likely failure mode is SonnerThemer passing `"system"` to the `theme` prop, which Sonner may not handle correctly. If found, map it to `resolvedTheme`.

    Document verification results in the task output.
  </action>
  <verify>
    <automated>test $(grep -c 'theme="system"' src/components/theme/SonnerThemer.tsx) -eq 0 && grep -c "resolvedTheme" src/components/theme/SonnerThemer.tsx && grep -c "localStorage.setItem" src/components/theme/useTheme.ts && npm run build</automated>
  </verify>
  <done>SonnerThemer uses resolvedTheme (never "system"), useTheme.ts persists to localStorage, build passes. TOG-03 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → localStorage | Same as Plan 01 — user toggles own preference only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Information Disclosure | SonnerThemer.tsx | accept | Toast theme is visible UI state, not sensitive data |
</threat_model>

<verification>
- [ ] `/admin/minha-conta` page contains "Apariencia" text
- [ ] `/admin/minha-conta` imports and renders `<ThemeToggle variant="admin" />`
- [ ] ThemeToggle component supports `variant="admin"` with admin token styling
- [ ] SonnerThemer passes `resolvedTheme` (concrete light/dark) — never "system"
- [ ] `npm run lint` passes with 0 errors in modified files
- [ ] `npm run build` passes
- [ ] All three TOG requirements (TOG-01 from Plan 01, TOG-02, TOG-03) are satisfied
</verification>

<success_criteria>
- TOG-02 satisfied: Admin/Colaboradora can toggle theme from `/admin/minha-conta`
- TOG-03 satisfied: Sonner toasts follow the manually selected theme (verified, not new code)
- Toggle is binary (light/dark only) on both surfaces
- Preference persists across reloads on both surfaces
- No hardcoded hex values in new code
- Admin page uses admin design system tokens
</success_criteria>

<output>
After completion, create `.planning/phases/22-toggle-ui/22-02-SUMMARY.md`
</output>
