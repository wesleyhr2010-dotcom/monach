---
phase: 22-toggle-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/theme/ThemeToggle.tsx
  - src/app/app/perfil/page.tsx
autonomous: true
requirements:
  - TOG-01
must_haves:
  truths:
    - "Revendedora sees 'Apariencia' section in /app/perfil with sun/moon toggle"
    - "Clicking toggle immediately switches /app surface between dark and light"
    - "Theme preference persists after page reload"
  artifacts:
    - path: "src/components/theme/ThemeToggle.tsx"
      provides: "Reusable binary sun/moon toggle using useThemeContext"
      exports: ["ThemeToggle"]
    - path: "src/app/app/perfil/page.tsx"
      provides: "PWA profile page with Apariencia section"
      contains: "Apariencia"
  key_links:
    - from: "src/app/app/perfil/page.tsx"
      to: "src/components/theme/ThemeToggle.tsx"
      via: "import and render"
      pattern: "import.*ThemeToggle"
    - from: "src/components/theme/ThemeToggle.tsx"
      to: "src/components/theme/useTheme.ts"
      via: "useThemeContext()"
      pattern: "useThemeContext"
---

<objective>
Create the reusable ThemeToggle component and wire it into the PWA profile page (`/app/perfil`) under an "Apariencia" section.

Purpose: Deliver TOG-01 — the revendedora can manually switch between dark and light mode from her profile page.
Output: ThemeToggle.tsx component + updated /app/perfil/page.tsx with Apariencia section.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-toggle-ui/22-CONTEXT.md

## Existing Theme Infrastructure (from Phase 21)

From `src/components/theme/types.ts`:
```typescript
export type Theme = "light" | "dark" | "system";
export interface ThemeContextType {
  theme: Theme;
  resolvedTheme: "light" | "dark";
  setTheme: (theme: Theme) => void;
}
```

From `src/components/theme/useTheme.ts`:
```typescript
export function useThemeContext(): ThemeContextType {
  const context = useContext(ThemeContext);
  if (!context) throw new Error("useThemeContext must be used within a ThemeProvider");
  return context;
}
```

From `src/app/app/layout.tsx`:
- `<AppShell>` is wrapped in `<AppThemeProvider>` (inside AppShell)
- All children rendered inside AppShell have access to the theme context.

## Target Page: src/app/app/perfil/page.tsx

Current structure (Server Component):
- Header with avatar, name, stats
- Menu list (Link items): Mis Datos, Documentos, Datos Bancarios, Notificaciones, Soporte
- Logout button

The "Apariencia" section must be added to the menu list area as an interactive item (not a Link).

## Design System Constraints

- Use tokens from `globals.css` (app surface): `--app-text`, `--app-text-secondary`, `--app-card-bg`, `--app-surface-warm`, `--app-border`, `--app-accent-brown`
- Use `lucide-react` icons: `Sun`, `Moon`
- Font family for app: Raleway (already the default)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThemeToggle client component</name>
  <files>src/components/theme/ThemeToggle.tsx</files>
  <action>
    Create a client component at `src/components/theme/ThemeToggle.tsx` that renders a binary sun/moon toggle.

    Behavior:
    - Import `useThemeContext` from `@/components/theme/useTheme.ts`
    - Import `Sun` and `Moon` from `lucide-react`
    - The component reads `{ theme, setTheme }` from `useThemeContext()`
    - Render a button with:
      - `aria-label` in Spanish: "Cambiar tema" (español paraguayo)
      - When `theme === "dark"` (or resolved is dark): show Sun icon + text "Modo claro" (indicates what clicking will do)
      - When `theme === "light"` (or resolved is light): show Moon icon + text "Modo oscuro" (indicates what clicking will do)
    - On click: call `setTheme(theme === "dark" ? "light" : "dark")`
    - Important: the toggle must NOT offer a "system" option — it's strictly binary light/dark.
    - The component must be a "use client" component.
    - Use app surface tokens for colors (this component will be reused in admin, so accept an optional `variant: "app" | "admin"` prop with default "app", and map to the correct token set).
    - For app variant: bg uses `--app-card-bg`, text uses `--app-text`, icon uses `--app-accent-brown`, border uses `--app-border`.
    - The toggle should have a pill-shaped container (`rounded-full`) with subtle hover state.
    - Size: `h-10` (40px) with `px-4`, `gap-2` between icon and label.
    - Do NOT use hardcoded hex values — always use CSS custom properties or Tailwind token classes.

    Also verify that the export is named `ThemeToggle`.
  </action>
  <verify>
    <automated>grep -n "use client" src/components/theme/ThemeToggle.tsx && grep -n "useThemeContext" src/components/theme/ThemeToggle.tsx && grep -n "export function ThemeToggle" src/components/theme/ThemeToggle.tsx && grep -n "setTheme" src/components/theme/ThemeToggle.tsx</automated>
  </verify>
  <done>Component exists, is a client component, consumes useThemeContext, exports ThemeToggle, toggles between light and dark only, no hardcoded hex values.</done>
</task>

<task type="auto">
  <name>Task 2: Add Apariencia section to /app/perfil</name>
  <files>src/app/app/perfil/page.tsx</files>
  <action>
    Modify `src/app/app/perfil/page.tsx` to add an "Apariencia" section in the menu list area.

    Current menu items array ends with Soporte y Ayuda. After the `menuItems.map(...)` block (but still inside the menu container div), add:
    - A visual separator: `<div className="border-t border-app-border my-2" />`
    - Then the Apariencia interactive row. Since this is NOT a Link, it must be a separate `<div>` or `<button>` element rendered directly (not in the map).
    - The row must match the menu item styling:
      - `flex items-center gap-3 px-4 py-3.5 bg-app-card-bg rounded-xl text-app-text`
      - Left side: icon placeholder (the ThemeToggle component itself will be rendered here, but since ThemeToggle is a client component and the page is a server component, you must import and render `<ThemeToggle variant="app" />` inline)
      - Label: "Apariencia" as `<span className="flex-1 text-sm font-medium">Apariencia</span>`
      - Right side: the ThemeToggle component (which itself contains the icon and label)
    
    Wait — rethinking the layout. The ThemeToggle component IS the interactive control. It should replace the right-side chevron for this row. The row structure should be:
    - Left: a `Palette` or `Sun` icon from lucide-react (static, decorative)
    - Middle: "Apariencia" label
    - Right: `<ThemeToggle variant="app" />` (the actual interactive toggle button)

    Add import for ThemeToggle at top of file:
    `import { ThemeToggle } from "@/components/theme/ThemeToggle";`

    Also add `Palette` or `Sun` import from lucide-react to the existing icon import line.

    Ensure the page still builds and no lint errors are introduced.
  </action>
  <verify>
    <automated>grep -n "Apariencia" src/app/app/perfil/page.tsx && grep -n "ThemeToggle" src/app/app/perfil/page.tsx && grep -n "import.*Palette\|import.*Sun" src/app/app/perfil/page.tsx</automated>
  </verify>
  <done>/app/perfil page contains "Apariencia" section with ThemeToggle, menu styling is consistent, page builds and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → localStorage | Theme toggle writes to localStorage via setTheme, which is already trusted code from Phase 21 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | ThemeToggle.tsx | accept | User can only toggle their own preference; no security boundary crossed. localStorage is client-only. |
</threat_model>

<verification>
- [ ] `src/components/theme/ThemeToggle.tsx` exists and exports `ThemeToggle`
- [ ] ThemeToggle is a "use client" component
- [ ] ThemeToggle calls `useThemeContext()` and `setTheme("light" | "dark")` — never "system"
- [ ] `/app/perfil` page contains "Apariencia" text
- [ ] `/app/perfil` imports and renders `<ThemeToggle variant="app" />`
- [ ] `npm run lint` passes with 0 errors in modified files
- [ ] `npm run build` passes
</verification>

<success_criteria>
- TOG-01 satisfied: Revendedora can toggle theme from `/app/perfil`
- Toggle is binary (light/dark only)
- Preference persists across reloads
- No hardcoded hex values in new code
</success_criteria>

<output>
After completion, create `.planning/phases/22-toggle-ui/22-01-SUMMARY.md`
</output>
