---
phase: 14-analytics-periodo-personalizado
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified: [src/app/admin/analytics/VitrinaCsvDownload.tsx, src/app/admin/analytics/page.tsx]
autonomous: true
requirements: [ANLT-11]

must_haves:
  truths:
    - "O arquivo CSV exportado contém dados referentes ao período exato selecionado na UI"
    - "O nome do arquivo CSV inclui as datas do intervalo selecionado"
  artifacts:
    - path: "src/app/admin/analytics/VitrinaCsvDownload.tsx"
      provides: "Download de CSV com contexto de data"
---

<objective>
Garantir que a funcionalidade de exportação de dados (CSV) respeite o intervalo de datas selecionado e realizar a verificação final de integração de toda a fase.

Propósito: Integridade dos dados exportados.
Saída: Export CSV corrigido e fase validada.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-analytics-periodo-personalizado/14-02-SUMMARY.md
@src/app/admin/analytics/VitrinaCsvDownload.tsx
</context>

<interfaces>
From src/app/admin/actions-analytics.ts:
```typescript
export async function exportVitrinaAnalyticsCSV(from: Date, to: Date): Promise<string>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar Export CSV para usar Range</name>
  <files>src/app/admin/analytics/VitrinaCsvDownload.tsx, src/app/admin/analytics/page.tsx</files>
  <action>
    - Garantir que a chamada para `exportVitrinaAnalyticsCSV` no `AnalyticsPage` passe os objetos `from/to` calculados.
    - Atualizar o componente `VitrinaCsvDownload` para usar o range no nome do arquivo sugerido (ANLT-11).
    - Formatar as datas no filename como YYYYMMDD para brevidade (ex: `vitrina-20240415-20240420.csv`).
  </action>
  <verify>
    Baixar CSV após filtrar por um período customizado e conferir se o conteúdo está correto.
  </verify>
  <done>Export CSV respeita o filtro atual e tem nome de arquivo descritivo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fluxo completo de Analytics com Período Personalizado</what-built>
  <how-to-verify>
    1. Acessar /admin/analytics no ambiente de desenvolvimento.
    2. Selecionar um intervalo de 5 dias no DatePicker.
    3. Verificar se os KPIs e o gráfico de Fluxo mostram apenas esses 5 dias.
    4. Alterar a revendedora no seletor e verificar se o intervalo de datas persiste na URL e nos dados.
    5. Selecionar um intervalo de > 366 dias e ver se a mensagem de erro aparece (bloqueando a query).
    6. Exportar o CSV e validar se as métricas no arquivo correspondem ao período em tela.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- Requisitos ANLT-07 até ANLT-11 satisfeitos integralmente.
- Experiência de usuário fluida na navegação entre filtros e revendedoras.
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/14-analytics-periodo-personalizado/14-03-SUMMARY.md`
</output>
