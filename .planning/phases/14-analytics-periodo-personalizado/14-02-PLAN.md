---
phase: 14-analytics-periodo-personalizado
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: [src/components/ui/date-range-picker.tsx, src/app/admin/analytics/page.tsx, src/app/admin/analytics/ResellerSelect.tsx]
autonomous: true
requirements: [ANLT-07, ANLT-08, ANLT-09, ANLT-10]

must_haves:
  truths:
    - "Admin consegue selecionar um range no date picker e a URL atualiza com ?from=...&to=..."
    - "A página lê os parâmetros da URL e os utiliza para buscar os dados"
    - "Selecionar um range > 366 dias impede a query e mostra um aviso"
  artifacts:
    - path: "src/components/ui/date-range-picker.tsx"
      provides: "Componente visual de seleção de intervalo"
    - path: "src/app/admin/analytics/page.tsx"
      provides: "Dashboard integrado com filtros de data"
---

<objective>
Implementar a interface de seleção de período personalizado e integrá-la ao dashboard de admin, suportando estado via URL e mantendo os presets existentes.

Propósito: Oferecer flexibilidade de consulta para o Admin.
Saída: Dashboard funcional com DateRangePicker.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-analytics-periodo-personalizado/14-01-SUMMARY.md
@src/app/admin/analytics/page.tsx
@src/app/admin/analytics/ResellerSelect.tsx
</context>

<interfaces>
<!-- Key types and contracts from actions-analytics.ts after refactor -->
From src/app/admin/actions-analytics.ts:
```typescript
export async function getAnalyticsKPIs(from: Date, to: Date): Promise<AnalyticsKPIs>;
export async function getAnalyticsFluxoMaletas(from: Date, to: Date): Promise<FluxoDia[]>;
export function getRangeFromParams(period?: number, fromStr?: string, toStr?: string): { from: Date; to: Date };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implementar DateRangePicker (Shadcn)</name>
  <files>src/components/ui/date-range-picker.tsx</files>
  <action>
    - Criar o componente `DatePickerWithRange` usando `react-day-picker` e `date-fns`.
    - Deve suportar seleção de range e emitir evento de mudança (navegação via router.push).
    - Estilizar conforme o padrão do projeto (var(--admin-*) e tokens do design system).
    - Per D-14-02.
  </action>
  <verify>
    Componente renderiza e permite selecionar duas datas.
  </verify>
  <done>Componente DateRangePicker disponível.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar Filtros na Página de Analytics</name>
  <files>src/app/admin/analytics/page.tsx, src/app/admin/analytics/ResellerSelect.tsx</files>
  <action>
    - No `AnalyticsPage`:
      - Extrair `from` (YYYY-MM-DD) e `to` (YYYY-MM-DD) de `searchParams`.
      - Usar `getRangeFromParams` para obter objetos Date.
      - Se `from/to` presentes, calcular diff de dias; se > 366, renderizar mensagem de erro estilizada e não carregar dados (ANLT-10).
      - Passar os objetos `from` e `to` para todas as chamadas de Server Actions.
    - Atualizar UI de Presets (7d, 30d, etc): Links devem ser atualizados para limpar `from/to` da URL.
    - No `ResellerSelect`: Garantir que os parâmetros `from` e `to` sejam propagados como inputs hidden no form para não perder o filtro ao trocar de revendedora.
    - Per D-14-04.
  </action>
  <verify>
    - Trocar range no picker atualiza URL e recarrega dados.
    - Clicar em "7d" limpa range customizado e volta pro preset.
    - Trocar revendedora mantém o range customizado.
  </verify>
  <done>Dashboard totalmente funcional com filtros de data.</done>
</task>

</tasks>

<success_criteria>
- Admin consegue ver dados de períodos específicos (ex: 15 de Abril a 20 de Abril).
- URL amigável e persistente (?from=2024-04-15&to=2024-04-20).
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/14-analytics-periodo-personalizado/14-02-SUMMARY.md`
</output>
