---
phase: 14-analytics-periodo-personalizado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/admin/actions-analytics.ts]
autonomous: true
requirements: [SEC-05, ANLT-07, ANLT-11]

must_haves:
  truths:
    - "Todas as funções de analytics aceitam (from: Date, to: Date) como argumentos principais"
    - "As queries SQL/Prisma utilizam o range fornecido em vez de apenas um offset de dias"
    - "O limite superior 'to' inclui todo o dia selecionado no fuso do Paraguai (até 23:59:59)"
  artifacts:
    - path: "src/app/admin/actions-analytics.ts"
      provides: "Analytics API com suporte a range customizado"
---

<objective>
Refatorar as Server Actions de analytics para suportar intervalos de datas arbitrários (from/to) em vez de apenas um número fixo de dias passados, garantindo a correção do fuso horário para o Paraguai (UTC-3).

Propósito: Base técnica para permitir filtros personalizados no dashboard.
Saída: Funções refatoradas em actions-analytics.ts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/admin/actions-analytics.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar helpers de conversão de data e fuso</name>
  <files>src/app/admin/actions-analytics.ts</files>
  <action>
    - Definir `PY_OFFSET_MS = 3 * 60 * 60 * 1000` (UTC-3).
    - Criar helper `getRangeFromParams(period?: number, fromStr?: string, toStr?: string)` que retorna `{ from: Date, to: Date }`.
    - Se `fromStr/toStr` presentes, converter 'YYYY-MM-DD' para midnight (from) e 23:59:59 (to) no fuso -03:00.
    - Se apenas `period` presente, manter lógica de `getSinceDate` mas retornando o range `{ from: since, to: now }`.
    - Per D-14-01, D-14-03.
  </action>
  <verify>
    npm run test-prisma (ou script similar que invoque o helper)
  </verify>
  <done>Helper robusto que trata limites de dia corretamente no fuso PY.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar assinaturas das funções de Analytics</name>
  <files>src/app/admin/actions-analytics.ts</files>
  <action>
    - Alterar assinaturas de: `getAnalyticsKPIs`, `getAnalyticsFluxoMaletas`, `getAnalyticsTopRevendedoras`, `getAnalyticsProdutosMaisVendidos`, `getVitrinaKPIs`, `getVitrinaVisitasSeries`, `getVitrinaRankingRevendedoras`, `exportVitrinaAnalyticsCSV`.
    - Mudar de `(periodDays = 30)` para `(from: Date, to: Date)`.
    - Atualizar todas as queries Prisma e SQL `$queryRawUnsafe` para usar `BETWEEN $1 AND $2` ou `{ gte: from, lte: to }`.
    - Garantir que `getAnalyticsDistribuicaoStatus` (que não tinha período) agora também suporte filtro por data.
    - Per D-14-01.
  </action>
  <verify>
    Check de compilação: `npx tsc -p tsconfig.json --noEmit` (vai falhar no page.tsx, o que é esperado e corrigido no próximo plano).
  </verify>
  <done>Backend pronto para receber ranges arbitrários.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin → DB | Input de datas (strings) convertido em objetos Date seguros para queries |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information Disclosure | Analytics Queries | mitigate | Usar requireAuth em todas as actions refatoradas (já existente) |
| T-14-01-02 | Denial of Service | Custom Range Queries | mitigate | Limite de 366 dias imposto na camada de UI/Action (ANLT-10) |
</threat_model>

<success_criteria>
- Todas as funções de analytics compilam com a nova assinatura.
- Queries utilizam gte/lte com os objetos Date corretos.
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/14-analytics-periodo-personalizado/14-01-SUMMARY.md`
</output>
