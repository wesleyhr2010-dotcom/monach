---
phase: 15-admin-ui-consistencia-visual
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01-PLAN.md
files_modified:
  - src/components/admin/AdminStatusBadge.tsx
  - src/components/admin/AdminEmptyState.tsx
  - src/app/admin/maleta/page.tsx
  - src/app/admin/maleta/[id]/page.tsx
  - src/app/admin/maleta/[id]/editar/page.tsx
  - src/app/admin/maleta/nova/page.tsx
  - src/app/admin/revendedoras/page.tsx
  - src/app/admin/revendedoras/[id]/page.tsx
  - src/app/admin/revendedoras/[id]/documentos/page.tsx
  - src/app/admin/leads/page.tsx
  - src/app/admin/brindes/page.tsx
  - src/app/admin/brindes/solicitudes/page.tsx
  - src/app/admin/config/niveis/NiveisClient.tsx
  - src/app/admin/config/comissoes/ComissoesClient.tsx
  - src/app/admin/config/contratos/ContratosClient.tsx
  - src/app/admin/config/notif-push/NotifPushClient.tsx
  - src/app/admin/config/emails/page.tsx
  - src/app/admin/minha-conta/comissoes/page.tsx
  - src/app/admin/equipe/page.tsx
  - src/app/admin/consultoras/page.tsx
  - src/app/admin/relatorios/page.tsx
autonomous: true
requirements:
  - ADUI-03
  - ADUI-04
user_setup: []
must_haves:
  truths:
    - "Todos os status em rotas admin usam AdminStatusBadge — zero markup inline de status"
    - "Todos os empty states em rotas admin usam AdminEmptyState — zero markup inline de estado vazio"
    - "AdminEmptyState e AdminStatusBadge suportam todos os casos de uso encontrados"
  artifacts:
    - path: "src/components/admin/AdminStatusBadge.tsx"
      provides: "Badge universal para todos os status admin"
      exports: ["AdminStatusBadge", "getStatusRowStyle"]
    - path: "src/components/admin/AdminEmptyState.tsx"
      provides: "Empty state universal para todas as rotas admin"
      exports: ["AdminEmptyState"]
  key_links:
    - from: "src/app/admin/*/page.tsx"
      to: "src/components/admin/AdminStatusBadge.tsx"
      via: "import { AdminStatusBadge }"
      pattern: "import.*AdminStatusBadge"
    - from: "src/app/admin/*/page.tsx"
      to: "src/components/admin/AdminEmptyState.tsx"
      via: "import { AdminEmptyState }"
      pattern: "import.*AdminEmptyState"
---

<objective>
Padronizar todos os indicadores de status com AdminStatusBadge e todos os estados vazios com AdminEmptyState em todas as rotas admin.

Purpose: Eliminar inconsistências de UI onde status e empty states são implementados de formas diferentes em cada rota.
Output: Todos os status usam AdminStatusBadge; todos os empty states usam AdminEmptyState; nenhum markup inline permanece.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-admin-ui-consistencia-visual/15-CONTEXT.md
@.planning/phases/15-admin-ui-consistencia-visual/15-AUDIT.md

<interfaces>
From src/components/admin/AdminStatusBadge.tsx:
```tsx
type AdminStatusBadgeProps = {
  status: MaletaStatus;
  className?: string;
};
export function AdminStatusBadge({ status, className }: AdminStatusBadgeProps)
export function getStatusRowStyle(status: MaletaStatus): React.CSSProperties | undefined
```

From src/components/admin/AdminEmptyState.tsx:
```tsx
type AdminEmptyStateProps = {
  icon?: LucideIcon;
  title: string;
  description?: string;
  action?: React.ReactNode;
  className?: string;
};
export function AdminEmptyState({ icon: Icon, title, description, action, className }: AdminEmptyStateProps)
```

From src/lib/maleta-helpers.ts (maletaStatusConfig):
```typescript
export type MaletaStatus = 'ativa' | 'atrasada' | 'aguardando_revisao' | 'concluida';
export const maletaStatusConfig: Record<MaletaStatus, { label: string; color: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender AdminStatusBadge para novos domínios</name>
  <files>src/components/admin/AdminStatusBadge.tsx, src/lib/maleta-helpers.ts</files>
  <action>
Atualmente `AdminStatusBadge` aceita apenas `MaletaStatus` (ativa, atrasada, aguardando_revisao, concluida). Verificar no audit (15-AUDIT.md) se há status de outros domínios em rotas admin que precisam de badge:

Possíveis novos status:
- **Leads**: `novo`, `em_contato`, `convertido`, `descartado`
- **Contratos**: `ativo`, `inativo`, `pendente`
- **Brindes/Solicitações**: `pendente`, `aprovado`, `rejeitado`, `enviado`
- **Documentos**: `pendente`, `aprovado`, `rejeitado`
- **Equipe/Consultoras**: `ativo`, `inativo`

Para cada domínio encontrado no audit:
1. Criar tipo union (ex: `LeadStatus`, `ContratoStatus`) se ainda não existir
2. Adicionar config de estilos em `AdminStatusBadge.tsx` usando tokens `var(--admin-*)`
3. MAINTAIN backward compatibility — `MaletaStatus` continua funcionando exatamente como antes
4. Se o domínio já tem um componente de badge próprio (ex: `BrindesBadge`), avaliar se faz sentido migrar para `AdminStatusBadge` ou manter separado — documentar a decisão no SUMMARY

Se NENHUM novo domínio precisar de badge (todos os status já usam AdminStatusBadge ou não há status visuais em outras rotas), esta task reduz-se a: "Verificar que AdminStatusBadge cobre todos os casos existentes — nenhuma mudança necessária".
  </action>
  <verify>
    <automated>grep -rn "AdminStatusBadge" src/app/admin/ --include="*.tsx" | wc -l | awk '{print ($1 >= 5 ? "PASS: " $1 " usages" : "FAIL: only " $1 " usages")}'</automated>
  </verify>
  <done>AdminStatusBadge suporta todos os domínios de status encontrados em rotas admin, ou confirmação de que apenas maleta usa badges de status. Nenhum status inline permanece.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir todos os empty states inline por AdminEmptyState</name>
  <files>
    src/app/admin/maleta/page.tsx
    src/app/admin/maleta/[id]/page.tsx
    src/app/admin/maleta/[id]/editar/page.tsx
    src/app/admin/maleta/nova/page.tsx
    src/app/admin/revendedoras/page.tsx
    src/app/admin/revendedoras/[id]/page.tsx
    src/app/admin/revendedoras/[id]/documentos/page.tsx
    src/app/admin/leads/page.tsx
    src/app/admin/brindes/page.tsx
    src/app/admin/brindes/solicitudes/page.tsx
    src/app/admin/config/niveis/NiveisClient.tsx
    src/app/admin/config/comissoes/ComissoesClient.tsx
    src/app/admin/config/contratos/ContratosClient.tsx
    src/app/admin/config/notif-push/NotifPushClient.tsx
    src/app/admin/config/emails/page.tsx
    src/app/admin/minha-conta/comissoes/page.tsx
    src/app/admin/equipe/page.tsx
    src/app/admin/consultoras/page.tsx
    src/app/admin/relatorios/page.tsx
  </files>
  <action>
Substituir TODOS os empty states inline e usos de `EmptyState` (de `@/components/ui/empty-state`) por `AdminEmptyState` em rotas admin.

Casos a corrigir (baseado no audit):
1. **Markup inline com `<div className="admin-empty">`**: substituir por `<AdminEmptyState icon={...} title="..." description="..." />`
2. **`<p>Nenhum ... cadastrado.</p>` inline**: envolver com `<AdminEmptyState>` ou substituir completamente
3. **Uso de `EmptyState` de `@/components/ui/empty-state`**: trocar import e componente para `AdminEmptyState`
4. **`<p className="text-muted-foreground">Nenhum documento...</p>`**: substituir por `<AdminEmptyState>`
5. **`<div className="admin-empty-state">`**: substituir por `<AdminEmptyState>`

Mapeamento de exemplos:
| Rota | Atual | Correto |
|------|-------|---------|
| `maleta/page.tsx` | `EmptyState` de `@/components/ui/empty-state` | `AdminEmptyState` |
| `revendedoras/page.tsx` | `EmptyState` de `@/components/ui/empty-state` | `AdminEmptyState` |
| `config/niveis/NiveisClient.tsx` | `<p>Nenhum nivel cadastrado.</p>` | `<AdminEmptyState icon={Layers} title="Nenhum nível cadastrado" />` |
| `config/comissoes/ComissoesClient.tsx` | `<p>Nenhuma faixa cadastrada.</p>` | `<AdminEmptyState icon={BarChart3} title="Nenhuma faixa cadastrada" />` |
| `config/contratos/ContratosClient.tsx` | `<p>Nenhum contrato cadastrado.</p>` | `<AdminEmptyState icon={FileText} title="Nenhum contrato cadastrado" />` |
| `config/notif-push/NotifPushClient.tsx` | `<div className="admin-empty"><p>No hay...</p></div>` | `<AdminEmptyState icon={Users} title="No hay revendedoras" description="..." />` |
| `leads/page.tsx` | `<div className="admin-empty">Nenhuma lead...</div>` | `<AdminEmptyState icon={UserPlus} title="Nenhuma lead encontrada" />` |
| `revendedoras/[id]/page.tsx` | `<p style={{ color: "#555555" }}>Nenhuma maleta</p>` | `<AdminEmptyState icon={Package} title="Nenhuma maleta" />` |
| `minha-conta/comissoes/page.tsx` | `<div className="admin-empty-state">` | `<AdminEmptyState icon={DollarSign} title="Nenhuma comissão" />` |

Regras:
- Escolher ícone Lucide apropriado para cada contexto (nunca deixar sem ícone se o original tinha)
- Manter texto em **espanhol paraguaio** (não português)
- Se `AdminEmptyState` precisar de nova prop (ex: `variant`), estender o componente
- Preservar qualquer `action` (botão) que o empty state original tivesse
  </action>
  <verify>
    <automated>grep -rn 'EmptyState' src/app/admin/ --include="*.tsx" | grep -v "AdminEmptyState" | grep -v "node_modules" | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>Zero usages of `EmptyState` from `@/components/ui/empty-state` remain in admin routes. All empty states use `AdminEmptyState`. Zero inline `<p>` empty states remain. Text is in espanhol paraguaio.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar e padronizar uso de AdminStatusBadge em todas as rotas</name>
  <files>src/app/admin/*</files>
  <action>
Usando o audit report, verificar que TODOS os lugares que exibem status em rotas admin usam `AdminStatusBadge`.

Verificar rotas críticas:
1. `admin/maleta/page.tsx` — já usa ✓
2. `admin/maleta/[id]/page.tsx` — já usa ✓
3. `admin/leads/page.tsx` — verificar se há badges de status inline
4. `admin/revendedoras/page.tsx` — verificar se há badges de status inline
5. `admin/revendedoras/[id]/page.tsx` — verificar status de maleta, contrato, documentos
6. `admin/brindes/page.tsx` — verificar status de brindes
7. `admin/brindes/solicitudes/page.tsx` — verificar status de solicitações
8. `admin/config/emails/page.tsx` — verificar indicadores de override
9. `admin/equipe/page.tsx` — verificar status de equipe
10. `admin/consultoras/page.tsx` — verificar status

Para cada status inline encontrado:
- Identificar o tipo de status
- Se o tipo já é suportado por `AdminStatusBadge`, substituir
- Se o tipo é novo, adicionar suporte em `AdminStatusBadge` (Task 1) e depois substituir

Status inline comuns a procurar:
- `<span className="bg-green-*">` ou `bg-red-*`
- `<span style={{ color: "#..." }}>` usado como indicador de status
- `<div className="rounded-full bg-[#...]">` usado como dot/badge
- Texto colorido que comunica estado (ex: "Ativo", "Inativo", "Pendente")
  </action>
  <verify>
    <automated>grep -rn 'bg-green-\|bg-red-\|bg-yellow-\|bg-blue-' src/app/admin/ --include="*.tsx" | grep -v "node_modules" | grep -v "text-red-500" | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>Zero inline color badges (bg-green, bg-red, etc.) used for status communication in admin routes. All status indicators use AdminStatusBadge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Refatoração visual pura — não altera lógica de negócio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Denial of Service | AdminEmptyState | accept | Low risk — visual change only |
</threat_model>

<verification>
1. `grep -rn 'EmptyState' src/app/admin/ --include="*.tsx" | grep -v "AdminEmptyState" | grep -v "node_modules"` retorna vazio
2. `grep -rn 'bg-green-\|bg-red-\|bg-yellow-' src/app/admin/ --include="*.tsx" | grep -v "text-red-500"` retorna vazio
3. `grep -rn "AdminStatusBadge" src/app/admin/ --include="*.tsx" | wc -l` retorna >= 5
4. `npm run lint` passa
5. `npm run build` passa
</verification>

<success_criteria>
- Todos os empty states em rotas admin usam AdminEmptyState
- Zero usos de EmptyState de @/components/ui/empty-state em admin
- Zero inline <p> vazio ou <div className="admin-empty"> sem AdminEmptyState
- Todos os status visuais usam AdminStatusBadge
- Zero bg-green-*, bg-red-* usados para comunicar estado
- Build e lint passam
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-ui-consistencia-visual/15-02-SUMMARY.md`
</output>
