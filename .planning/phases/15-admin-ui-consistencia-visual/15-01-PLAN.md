---
phase: 15-admin-ui-consistencia-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/AdminStatusBadge.tsx
  - src/app/admin/admin.css
  - src/app/admin/page.tsx
  - src/app/admin/analytics/page.tsx
  - src/app/admin/analytics/VitrinaCsvDownload.tsx
  - src/app/admin/analytics/ResellerSelect.tsx
  - src/app/admin/login/reset-password/page.tsx
  - src/app/admin/maleta/page.tsx
  - src/app/admin/revendedoras/page.tsx
  - src/app/admin/revendedoras/[id]/page.tsx
  - src/app/admin/relatorios/page.tsx
autonomous: true
requirements:
  - ADUI-01
  - ADUI-02
user_setup: []
must_haves:
  truths:
    - "AdminStatusBadge usa apenas tokens CSS var(--admin-*) e zero hex hardcoded"
    - "Nenhum arquivo em src/app/admin/* contém valor hex hardcoded substituível por token admin"
    - "Audit report documenta todos os desvios encontrados antes das correções"
  artifacts:
    - path: "src/components/admin/AdminStatusBadge.tsx"
      provides: "Status badge com tokens admin"
      contains: "var(--admin-success)"
    - path: "src/app/admin/admin.css"
      provides: "Tokens admin de referência"
      contains: "--admin-danger: #e05c5c"
  key_links:
    - from: "AdminStatusBadge.tsx"
      to: "admin.css"
      via: "CSS variables"
      pattern: "var\\(--admin-\\w+\\)"
---

<objective>
Executar auditoria completa das rotas admin e substituir todos os valores hex hardcoded por tokens CSS `var(--admin-*)`.

Purpose: Eliminar inconsistências visuais causadas por cores hardcoded e estabelecer a base tokenizada para todos os componentes admin.
Output: AdminStatusBadge refatorado, hex values eliminados de rotas admin, audit report documentado.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-admin-ui-consistencia-visual/15-CONTEXT.md

<interfaces>
From src/app/admin/admin.css:
```css
.admin-layout {
  --admin-bg: #0a0a0a;
  --admin-surface: #171717;
  --admin-surface-hover: #222222;
  --admin-surface-row-atrasada: #1a1010;
  --admin-surface-row-aguardando: #1a1a10;
  --admin-border: #2a2a2a;
  --admin-border-focus: #35605a;
  --admin-text: #ededed;
  --admin-text-muted: #888888;
  --admin-text-dim: #444444;
  --admin-accent: #35605a;
  --admin-accent-hover: #2a4d48;
  --admin-danger: #e05c5c;
  --admin-danger-hover: #c44545;
  --admin-success: #4ade80;
  --admin-warning: #facc15;
  --admin-radius: 8px;
}
```

From src/components/admin/AdminStatusBadge.tsx (current):
```tsx
const statusStyles: Record<string, { dot: string; bg: string; text: string; rowBg?: string }> = {
  ativa:           { dot: "bg-[#4ADE80]", bg: "bg-[#4ADE801A]", text: "text-[#4ADE80]" },
  atrasada:        { dot: "bg-[#E05C5C]", bg: "bg-[#E05C5C26]", text: "text-[#E05C5C]", rowBg: "var(--admin-surface-row-atrasada)" },
  aguardando_revisao: { dot: "bg-[#FACC15]", bg: "bg-[#FACC151A]", text: "text-[#FACC15]", rowBg: "var(--admin-surface-row-aguardando)" },
  concluida:       { dot: "bg-[#555555]", bg: "bg-[#64646426]", text: "text-[#777777]" },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoria de desvios visuais por rota admin</name>
  <files>src/app/admin/*, src/components/admin/*</files>
  <action>
Executar scan automatizado em TODOS os arquivos `.tsx` e `.ts` em `src/app/admin/` e `src/components/admin/` para catalogar desvios visuais. Para cada arquivo, registrar:

1. **Hex hardcoded** que possui token admin equivalente:
   - `#0a0a0a` / `#0A0A0A` → `var(--admin-bg)`
   - `#171717` → `var(--admin-surface)`
   - `#1a1a1a` / `#1A1A1A` → `var(--admin-surface)`
   - `#2a2a2a` / `#2A2A2A` → `var(--admin-border)`
   - `#ededed` / `#EDEDED` → `var(--admin-text)`
   - `#888888` / `#888` → `var(--admin-text-muted)`
   - `#444444` / `#444` → `var(--admin-text-dim)`
   - `#35605a` / `#35605A` → `var(--admin-accent)`
   - `#e05c5c` / `#E05C5C` → `var(--admin-danger)`
   - `#4ade80` / `#4ADE80` → `var(--admin-success)`
   - `#facc15` / `#FACC15` → `var(--admin-warning)`
   - `#333333` / `#333` → `var(--admin-border)` (aproximação aceitável)

2. **Tailwind semantic color classes** usadas em admin:
   - `text-red-500`, `bg-red-500` → `var(--admin-danger)`
   - `text-blue-500`, `bg-blue-500` → `var(--admin-accent)`
   - `text-green-500`, `bg-green-500` → `var(--admin-success)`

3. **Empty states inline** (ver Task 2 do Plan 02)

4. **Status badges inline** (ver Task 1 do Plan 02)

Salvar o resultado em `.planning/phases/15-admin-ui-consistencia-visual/15-AUDIT.md` com uma seção por rota/componente.

Para cada rota que será MODIFICADA, consultar o artboard correspondente no Paper via MCP (`paper_get_screenshot`, `paper_get_jsx`) e registrar: "Artboard consultado: [sim/não] — link/id". Se não houver artboard, anotar "Sem artboard — usar design system existente".
  </action>
  <verify>
    <automated>test -f .planning/phases/15-admin-ui-consistencia-visual/15-AUDIT.md && grep -c "## Rota:" .planning/phases/15-admin-ui-consistencia-visual/15-AUDIT.md | grep -v '^0'</automated>
  </verify>
  <done>Audit report exists with at least 15 routes documented, listing all hex values and Paper consultation status per route.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar AdminStatusBadge para tokens CSS</name>
  <files>src/components/admin/AdminStatusBadge.tsx</files>
  <action>
Refatorar `src/components/admin/AdminStatusBadge.tsx` para usar tokens CSS `var(--admin-*)` em vez de valores hex hardcoded.

Mudanças obrigatórias:
1. Substituir TODOS os valores hex em `statusStyles`:
   - `bg-[#4ADE80]` → usar classe utilitária que aplica `background-color: var(--admin-success)` (ex: criar classe custom ou usar `style={{}}`)
   - `bg-[#4ADE801A]` → `style={{ backgroundColor: 'var(--admin-success)', opacity: 0.1 }}` ou similar
   - `text-[#4ADE80]` → `style={{ color: 'var(--admin-success)' }}`
   - `bg-[#E05C5C]` → `var(--admin-danger)`
   - `bg-[#E05C5C26]` → `var(--admin-danger)` com opacity
   - `text-[#E05C5C]` → `var(--admin-danger)`
   - `bg-[#FACC15]` → `var(--admin-warning)`
   - `bg-[#FACC151A]` → `var(--admin-warning)` com opacity
   - `text-[#FACC15]` → `var(--admin-warning)`
   - `bg-[#555555]` / `bg-[#64646426]` / `text-[#777777]` → `var(--admin-text-muted)`

2. Preservar `rowBg` que já usa `var(--admin-surface-row-*)` — não alterar.

3. Se necessário, adicionar novas variáveis CSS em `admin.css` para cores com opacity (ex: `--admin-success-10`, `--admin-danger-10`).

4. Manter a assinatura do componente e props inalteradas.

NÃO usar `bg-[var(--admin-success)]` no Tailwind — Tailwind v4 não resolve CSS variables em arbitrary values durante build. Use `style={{}}` ou classes CSS custom definidas em `admin.css`.
  </action>
  <verify>
    <automated>grep -c '#[0-9A-Fa-f]\{3,6\}' src/components/admin/AdminStatusBadge.tsx | grep -q '^0$'</automated>
  </verify>
  <done>AdminStatusBadge.tsx contains zero hex values and uses var(--admin-*) for all colors. Component renders identically in dev.</done>
</task>

<task type="auto">
  <name>Task 3: Substituir hex hardcoded por tokens em rotas admin</name>
  <files>
    src/app/admin/page.tsx
    src/app/admin/analytics/page.tsx
    src/app/admin/analytics/VitrinaCsvDownload.tsx
    src/app/admin/analytics/ResellerSelect.tsx
    src/app/admin/login/reset-password/page.tsx
    src/app/admin/maleta/page.tsx
    src/app/admin/revendedoras/page.tsx
    src/app/admin/revendedoras/[id]/page.tsx
    src/app/admin/relatorios/page.tsx
  </files>
  <action>
Usando o audit report (15-AUDIT.md) como guia, substituir TODOS os valores hex hardcoded nos arquivos de rota admin pelos tokens CSS equivalentes.

Mapeamento obrigatório:
| Hex | Token Admin | Notas |
|-----|-------------|-------|
| `#35605A`, `#35605a` | `var(--admin-accent)` | CTA primário, botões ativos |
| `#1a1a1a`, `#1A1A1A` | `var(--admin-surface)` | Fundos de cards/skeletons |
| `#2a2a2a`, `#2A2A2A` | `var(--admin-border)` | Bordas |
| `#0a0a0a`, `#0A0A0A` | `var(--admin-bg)` | Background geral |
| `#888`, `#888888` | `var(--admin-text-muted)` | Texto secundário |
| `#333`, `#333333` | `var(--admin-border)` | Bordas sutis |
| `#555`, `#555555` | `var(--admin-text-muted)` | Texto terciário |
| `#4ADE80`, `#4ade80` | `var(--admin-success)` | Sucesso |
| `#E05C5C`, `#e05c5c` | `var(--admin-danger)` | Erro/perigo |
| `#FACC15`, `#facc15` | `var(--admin-warning)` | Aviso |

Regras técnicas:
- NÃO usar `bg-[var(--admin-*)]` no Tailwind — usar `style={{ backgroundColor: 'var(--admin-*)' }}` ou classes CSS custom
- Para `className` com múltiplas condições (ex: `bg-[#35605A] text-white`), extrair para uma função helper ou objeto de estilos se necessário
- Preservar comportamento visual exato — verificar no browser que não houve regressão
- Não tocar em arquivos que NÃO estão na lista do audit
  </action>
  <verify>
    <automated>grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/ --include="*.tsx" | grep -v "admin.css" | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>Zero hex values remain in src/app/admin/* .tsx files (excluding admin.css which is the token source). All replaced with var(--admin-*) equivalents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Esta fase é puramente de refatoração visual — não altera lógica de negócio, auth ou dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | AdminStatusBadge | accept | Low risk — visual change only; no functional impact |
</threat_model>

<verification>
1. `grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/ --include="*.tsx" | grep -v "admin.css"` retorna vazio
2. `grep -rn 'var(--admin-)' src/components/admin/AdminStatusBadge.tsx` retorna matches para todas as cores
3. Build passa: `npm run build` sem erros
4. Lint passa: `npm run lint` sem erros novos
</verification>

<success_criteria>
- AdminStatusBadge usa apenas tokens CSS, zero hex
- Todos os arquivos em src/app/admin/* .tsx livres de hex hardcoded
- Audit report 15-AUDIT.md documenta desvios por rota
- Paper MCP consultado para cada rota modificada (registrado no audit)
- Build e lint passam
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-ui-consistencia-visual/15-01-SUMMARY.md`
</output>
