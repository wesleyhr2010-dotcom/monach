---
phase: 15-admin-ui-consistencia-visual
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01-PLAN.md
  - 15-02-PLAN.md
files_modified:
  - docs/design-system/tokens.md
  - docs/sistema/SPEC_DESIGN_MODULES.md
  - .planning/phases/15-admin-ui-consistencia-visual/15-VERIFICATION.md
autonomous: true
requirements:
  - ADUI-05
user_setup: []
must_haves:
  truths:
    - "Inspeção do CSS em produção não encontra valores hex hardcoded em arquivos de rota admin"
    - "Todos os status usam AdminStatusBadge em todas as rotas admin"
    - "Todos os empty states usam AdminEmptyState em todas as rotas admin"
    - "Documentação do design system reflete os padrões admin estabelecidos"
  artifacts:
    - path: "docs/design-system/tokens.md"
      provides: "Tokens admin documentados"
      contains: "--admin-"
    - path: "docs/sistema/SPEC_DESIGN_MODULES.md"
      provides: "Módulos admin registrados"
      contains: "AdminStatusBadge"
    - path: ".planning/phases/15-admin-ui-consistencia-visual/15-VERIFICATION.md"
      provides: "Verificação final da fase"
      contains: "## Verification Results"
  key_links:
    - from: "docs/sistema/SPEC_DESIGN_MODULES.md"
      to: "src/components/admin/AdminStatusBadge.tsx"
      via: "Referência de componente"
      pattern: "AdminStatusBadge"
---

<objective>
Verificar que zero desvios visuais permanecem em rotas admin e documentar os padrões estabelecidos no design system.

Purpose: Garantir que a fase atingiu os critérios de sucesso e que desenvolvedores futuros têm referência dos padrões admin.
Output: Verificação documentada, design system atualizado, fase pronta para merge.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-admin-ui-consistencia-visual/15-CONTEXT.md
@.planning/phases/15-admin-ui-consistencia-visual/15-AUDIT.md

<interfaces>
From 15-01-PLAN.md output:
- src/components/admin/AdminStatusBadge.tsx (tokenizado)
- src/app/admin/* (sem hex hardcoded)

From 15-02-PLAN.md output:
- src/app/admin/* (usando AdminEmptyState e AdminStatusBadge)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificação final — grep gates</name>
  <files>src/app/admin/*, src/components/admin/*</files>
  <action>
Executar bateria de verificações automatizadas (grep gates) para confirmar que nenhum desvio permanece.

Gates obrigatórios:

**Gate 1 — Hex hardcoded em rotas admin:**
```bash
grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/ --include="*.tsx" | grep -v "admin.css" | grep -v "node_modules"
```
Resultado esperado: VAZIO (zero matches)

**Gate 2 — Hex hardcoded em componentes admin:**
```bash
grep -rn '#[0-9A-Fa-f]\{3,6\}' src/components/admin/ --include="*.tsx" | grep -v "node_modules"
```
Resultado esperado: VAZIO, exceto se houver valor legítimo em comentário ou string de documentação

**Gate 3 — Semantic Tailwind colors para status:**
```bash
grep -rn 'bg-green-\|bg-red-\|bg-yellow-\|bg-blue-\|bg-gray-' src/app/admin/ --include="*.tsx" | grep -v "text-red-500" | grep -v "node_modules"
```
Resultado esperado: VAZIO (exceto `text-red-500` para ícones de ação como Trash2, que é aceitável)

**Gate 4 — AdminEmptyState universal:**
```bash
grep -rn 'EmptyState' src/app/admin/ --include="*.tsx" | grep -v "AdminEmptyState" | grep -v "node_modules"
```
Resultado esperado: VAZIO

**Gate 5 — Inline empty states:**
```bash
grep -rn 'className="admin-empty"' src/app/admin/ --include="*.tsx" | grep -v "node_modules"
```
Resultado esperado: VAZIO

**Gate 6 — AdminStatusBadge cobertura:**
```bash
grep -rn "AdminStatusBadge" src/app/admin/ --include="*.tsx" | wc -l
```
Resultado esperado: >= 5 (confirma que está sendo usado)

**Gate 7 — Build e lint:**
```bash
npm run lint
npm run typecheck
npm run build
```
Resultado esperado: Todos passam

Se QUALQUER gate falhar:
1. Documentar a falha no 15-VERIFICATION.md
2. Corrigir o desvio (pode exigir voltar ao Plan 01 ou 02)
3. Re-rodar todos os gates

Se TODOS os gates passarem:
1. Criar `15-VERIFICATION.md` com a seção `## Verification Results` listando cada gate como ✓ PASS
2. Registrar o hash do commit
  </action>
  <verify>
    <automated>
#!/bin/bash
FAIL=0
echo "## Verification Results" > /tmp/verify.md
echo "" >> /tmp/verify.md
HEX_ROUTES=$(grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/ --include="*.tsx" | grep -v "admin.css" | grep -v "node_modules" | wc -l | tr -d ' ')
if [ "$HEX_ROUTES" -eq 0 ]; then echo "- ✓ Gate 1 (hex in routes): PASS" >> /tmp/verify.md; else echo "- ✗ Gate 1 (hex in routes): FAIL ($HEX_ROUTES found)" >> /tmp/verify.md; FAIL=1; fi
HEX_COMP=$(grep -rn '#[0-9A-Fa-f]\{3,6\}' src/components/admin/ --include="*.tsx" | grep -v "node_modules" | wc -l | tr -d ' ')
if [ "$HEX_COMP" -eq 0 ]; then echo "- ✓ Gate 2 (hex in components): PASS" >> /tmp/verify.md; else echo "- ✗ Gate 2 (hex in components): FAIL ($HEX_COMP found)" >> /tmp/verify.md; FAIL=1; fi
INLINE_COLORS=$(grep -rn 'bg-green-\|bg-red-\|bg-yellow-\|bg-blue-\|bg-gray-' src/app/admin/ --include="*.tsx" | grep -v "text-red-500" | grep -v "node_modules" | wc -l | tr -d ' ')
if [ "$INLINE_COLORS" -eq 0 ]; then echo "- ✓ Gate 3 (inline status colors): PASS" >> /tmp/verify.md; else echo "- ✗ Gate 3 (inline status colors): FAIL ($INLINE_COLORS found)" >> /tmp/verify.md; FAIL=1; fi
EMPTY_STATE=$(grep -rn 'EmptyState' src/app/admin/ --include="*.tsx" | grep -v "AdminEmptyState" | grep -v "node_modules" | wc -l | tr -d ' ')
if [ "$EMPTY_STATE" -eq 0 ]; then echo "- ✓ Gate 4 (EmptyState from ui/): PASS" >> /tmp/verify.md; else echo "- ✗ Gate 4 (EmptyState from ui/): FAIL ($EMPTY_STATE found)" >> /tmp/verify.md; FAIL=1; fi
ADMIN_EMPTY=$(grep -rn 'className="admin-empty"' src/app/admin/ --include="*.tsx" | grep -v "node_modules" | wc -l | tr -d ' ')
if [ "$ADMIN_EMPTY" -eq 0 ]; then echo "- ✓ Gate 5 (inline admin-empty): PASS" >> /tmp/verify.md; else echo "- ✗ Gate 5 (inline admin-empty): FAIL ($ADMIN_EMPTY found)" >> /tmp/verify.md; FAIL=1; fi
BADGE_COUNT=$(grep -rn "AdminStatusBadge" src/app/admin/ --include="*.tsx" | wc -l | tr -d ' ')
if [ "$BADGE_COUNT" -ge 5 ]; then echo "- ✓ Gate 6 (AdminStatusBadge usage): PASS ($BADGE_COUNT usages)" >> /tmp/verify.md; else echo "- ✗ Gate 6 (AdminStatusBadge usage): FAIL ($BADGE_COUNT usages)" >> /tmp/verify.md; FAIL=1; fi
echo "" >> /tmp/verify.md
if [ "$FAIL" -eq 0 ]; then echo "## ALL GATES PASSED ✓" >> /tmp/verify.md; else echo "## SOME GATES FAILED ✗" >> /tmp/verify.md; fi
cat /tmp/verify.md
exit $FAIL
    </automated>
  </verify>
  <done>Todos os 6 gates passam. 15-VERIFICATION.md criado com resultados. Zero desvios encontrados.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar documentação do design system</name>
  <files>docs/design-system/tokens.md, docs/sistema/SPEC_DESIGN_MODULES.md</files>
  <action>
Atualizar a documentação do design system para refletir os padrões admin estabelecidos nesta fase.

**Em `docs/design-system/tokens.md`:**
Adicionar seção "Admin Tokens" listando todas as variáveis CSS do admin com seus valores e uso:

```markdown
## Admin Tokens (Dark Theme)

| Token | Valor | Uso |
|-------|-------|-----|
| `--admin-bg` | `#0a0a0a` | Background geral do painel |
| `--admin-surface` | `#171717` | Cards, tabelas, modais |
| `--admin-surface-hover` | `#222222` | Hover em cards |
| `--admin-border` | `#2a2a2a` | Bordas, divisores |
| `--admin-border-focus` | `#35605a` | Borda em foco |
| `--admin-text` | `#ededed` | Texto principal |
| `--admin-text-muted` | `#888888` | Texto secundário |
| `--admin-text-dim` | `#444444` | Texto terciário, placeholders |
| `--admin-accent` | `#35605a` | Botões primários, CTAs |
| `--admin-accent-hover` | `#2a4d48` | Hover em CTAs |
| `--admin-danger` | `#e05c5c` | Erros, deleções |
| `--admin-danger-hover` | `#c44545` | Hover em ações de perigo |
| `--admin-success` | `#4ade80` | Sucesso, status ativo |
| `--admin-warning` | `#facc15` | Avisos, status pendente |
| `--admin-radius` | `8px` | Border-radius padrão |
| `--admin-radius-sm` | `4px` | Border-radius pequeno |
| `--admin-radius-lg` | `12px` | Border-radius grande |
| `--admin-radius-pill` | `9px` | Pills e badges |

**Regra:** Nunca usar hex hardcoded em arquivos de rota admin. Sempre usar `var(--admin-*)`.
```

**Em `docs/sistema/SPEC_DESIGN_MODULES.md`:**
Adicionar seção "Admin Components" após a seção 15 (ou como apêndice) documentando:

```markdown
## Admin Components

### AdminStatusBadge
- **Uso:** Todos os status em rotas admin (maleta, lead, contrato, brinde, etc.)
- **Props:** `{ status: string; className?: string }`
- **Tokens:** Usa `var(--admin-success)`, `var(--admin-danger)`, `var(--admin-warning)`, `var(--admin-text-muted)`
- **Regra:** Nunca implementar badge de status inline — sempre usar este componente

### AdminEmptyState
- **Uso:** Todos os estados vazios em rotas admin
- **Props:** `{ icon?: LucideIcon; title: string; description?: string; action?: React.ReactNode; className?: string }`
- **Tokens:** Usa `var(--admin-text)` e `var(--admin-text-muted)`
- **Regra:** Nunca usar `<p>Nenhum...</p>` inline ou `EmptyState` de `@/components/ui/empty-state` em admin
```
  </action>
  <verify>
    <automated>grep -c "Admin Tokens" docs/design-system/tokens.md && grep -c "AdminStatusBadge" docs/sistema/SPEC_DESIGN_MODULES.md && grep -c "AdminEmptyState" docs/sistema/SPEC_DESIGN_MODULES.md</automated>
  </verify>
  <done>tokens.md contém seção "Admin Tokens" completa. SPEC_DESIGN_MODULES.md contém seção "Admin Components" com AdminStatusBadge e AdminEmptyState documentados.</done>
</task>

<task type="auto">
  <name>Task 3: Commit final e atualização de estado</name>
  <files>.planning/phases/15-admin-ui-consistencia-visual/15-VERIFICATION.md</files>
  <action>
1. Criar `15-VERIFICATION.md` na pasta da fase com o resultado dos gates (de Task 1)
2. Commitar TODAS as mudanças da fase com mensagem convencional:
   ```
   git add -A
   git commit -m "feat: admin UI consistência visual — tokens, badges, empty states [Phase 15]"
   ```
3. Push para remote `client`:
   ```
   git push client $(git branch --show-current)
   ```
4. Atualizar `docs/next_steps.md`: marcar Phase 15 como concluída
5. Atualizar `docs/project_overview.md` §7 "Status atual" e §6 "O que já foi desenvolvido"
6. Atualizar `docs/CHANGELOG.md` com entrada datada:
   ```markdown
   ## 2026-05-07 — Phase 15: Admin UI Consistência Visual
   - AdminStatusBadge refatorado para tokens CSS
   - Todos os empty states padronizados com AdminEmptyState
   - Zero hex hardcoded em rotas admin
   - Documentação do design system atualizada
   ```
7. Atualizar `.planning/ROADMAP.md`: marcar Phase 15 como completa
8. Atualizar `.planning/STATE.md`: registrar fase como completa
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -i "admin UI consistência\|Phase 15"</automated>
  </verify>
  <done>Commit criado e pushado para remote client. Documentação atualizada. Phase 15 marcada como completa em ROADMAP, STATE, next_steps, project_overview e CHANGELOG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Verificação e documentação — sem mudanças de lógica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | 15-VERIFICATION.md | accept | Documento de planejamento — não contém segredos |
</threat_model>

<verification>
1. `15-VERIFICATION.md` existe e lista todos os gates como PASS
2. `docs/design-system/tokens.md` contém seção "Admin Tokens"
3. `docs/sistema/SPEC_DESIGN_MODULES.md` contém seção "Admin Components"
4. Commit em git log com mensagem referenciando Phase 15
5. ROADMAP.md marca Phase 15 como completa
</verification>

<success_criteria>
- Todos os 6 gates de verificação passam
- Documentação do design system atualizada com tokens e componentes admin
- Commit final pushado para remote client
- Phase 15 marcada como completa em todos os arquivos de estado
- CHANGELOG.md tem entrada datada
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-ui-consistencia-visual/15-03-SUMMARY.md`
</output>
