---
phase: 20-app-hardcoded-color-migration
plan: 05
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
  - 20-03
  - 20-04
files_modified:
  - src/components/app/ActionButton.tsx
  - src/components/app/CommissionTiers.tsx
  - src/components/app/MaletaCard.tsx
  - src/components/app/MaletaItemRow.tsx
  - src/components/app/MaletaList.tsx
  - src/components/app/MenuHeader.tsx
  - src/components/app/MetricCardTrend.tsx
  - src/components/app/NotificacionItem.tsx
  - src/components/app/NotificacionesList.tsx
  - src/components/app/PreferenciasNotificacionesForm.tsx
  - src/components/app/ProductosPopularesList.tsx
  - src/components/app/SectionHeader.tsx
  - src/components/app/StatCard.tsx
  - src/components/app/TimeRangeSelector.tsx
  - src/components/app/VisitasDiariasChart.tsx
autonomous: true
requirements:
  - APM-01
  - APM-02
  - APM-03
must_haves:
  truths:
    - "All shared PWA components contain zero hardcoded hex values"
    - "grep across src/app/app and src/components/app returns zero hex matches"
    - "npm run build passes after all migrations"
    - "Applying data-theme=dark to .app-shell in DevTools shows dark colors without visual breaks"
  artifacts:
    - path: "src/components/app/*.tsx"
      provides: "All shared components with tokenized colors"
    - path: ".planning/phases/20-app-hardcoded-color-migration/20-05-SUMMARY.md"
      provides: "Verification record"
  key_links:
    - from: "Verification grep"
      to: "All migrated files"
      via: "grep -rn '\[#[0-9a-fA-F]\{3,6\}\]'"
      pattern: "0 matches"
---

<objective>
Migrate all remaining hardcoded hex color values in shared PWA components, perform a final verification sweep across the entire PWA codebase, and confirm the build passes with dark mode testable via DevTools.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/20-app-hardcoded-color-migration/20-01-PLAN.md
@.planning/phases/20-app-hardcoded-color-migration/20-02-PLAN.md
@.planning/phases/20-app-hardcoded-color-migration/20-03-PLAN.md
@.planning/phases/20-app-hardcoded-color-migration/20-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Data-Display Components</name>
  <files>
    src/components/app/MaletaCard.tsx
    src/components/app/MaletaItemRow.tsx
    src/components/app/MaletaList.tsx
    src/components/app/StatCard.tsx
    src/components/app/SectionHeader.tsx
    src/components/app/MetricCardTrend.tsx
    src/components/app/ProductosPopularesList.tsx
    src/components/app/VisitasDiariasChart.tsx
  </files>
  <read_first>
    src/components/app/MaletaCard.tsx
    src/components/app/MaletaItemRow.tsx
    src/components/app/MaletaList.tsx
    src/components/app/StatCard.tsx
    src/components/app/SectionHeader.tsx
    src/components/app/MetricCardTrend.tsx
    src/components/app/ProductosPopularesList.tsx
    src/components/app/VisitasDiariasChart.tsx
  </read_first>
  <action>
    Apply the standard hex-to-token mapping to all eight files.

    **Standard Mapping:**
    | Hex Pattern | Token Replacement |
    |-------------|-------------------|
    | bg-[#F5F2EF] | bg-app-bg |
    | text-[#1A1A1A] | text-app-text |
    | text-[#35605A] | text-app-primary |
    | bg-[#35605A] | bg-app-primary |
    | text-[#777777] | text-app-text-secondary |
    | text-[#2E5A4C] | text-app-primary |
    | bg-[#2E5A4C] | bg-app-primary |
    | text-[#6b7280] | text-app-text-secondary |
    | bg-[#F5F0E8] | bg-app-surface-warm |
    | bg-[#EBEBEB] | bg-app-surface |
    | border-[#E8E2D6] | border-app-border |
    | border-[#D9D6D2] | border-app-border-strong |
    | text-[#917961] | text-app-accent-brown |
    | text-[#4b5563] | text-app-text-dim |
    | text-[#B4ABA2] | text-app-muted |
    | text-[#6B6B6B] | text-app-text-secondary |
    | bg-[#FFFFFF] | bg-app-card-bg |
    | bg-white | bg-app-card-bg |
    | text-[#9B1C1C] | text-app-danger |
    | bg-[#FDE8E8] | bg-app-danger-bg |
    | text-[#C0392B] | text-app-danger |
    | bg-[#C0392B] | bg-app-danger |
    | text-[#9ca3af] | text-app-text-secondary |
    | text-[#999999] | text-app-text-secondary |
    | text-[#555555] | text-app-text-dim |
    | bg-[#D9D6D2] | bg-app-border-strong |
    | bg-[#E5E0DB] | bg-app-border |
    | text-[#6A9A8A] | text-app-primary |
    | border-[#D1C7B7] | border-app-border |
    | border-[#D9D6D2] | border-app-border-strong |

    **Component-specific notes:**

    1. **MaletaCard.tsx:**
       - Card background `bg-white` → `bg-app-card-bg`
       - Border colors → `border-app-border`
       - Status text colors → map to semantic tokens

    2. **MaletaItemRow.tsx:**
       - Item image placeholder `bg-[#F5F0E8]` → `bg-app-surface-warm`
       - Text colors → `text-app-text`, `text-app-text-secondary`
       - Price `text-[#35605A]` → `text-app-primary`

    3. **MaletaList.tsx:**
       - Empty state `bg-[#F5F2EF]` → `bg-app-bg`
       - Text colors → appropriate tokens

    4. **StatCard.tsx:**
       - Card `bg-white` → `bg-app-card-bg`
       - Trend colors (green/red) → `text-app-accent-green` / `text-app-danger`

    5. **SectionHeader.tsx:**
       - Title `text-[#1A1A1A]` → `text-app-text`
       - Subtitle `text-[#777777]` → `text-app-text-secondary`

    6. **MetricCardTrend.tsx:**
       - Card background `bg-white` → `bg-app-card-bg`
       - Trend arrow colors → `text-app-accent-green` / `text-app-danger`
       - Metric value `text-[#1A1A1A]` → `text-app-text`
       - Label `text-[#777777]` → `text-app-text-secondary`

    7. **ProductosPopularesList.tsx:**
       - Rank badge backgrounds and text colors
       - `bg-[#F5F2EF]` → `bg-app-bg`
       - `text-[#1A1A1A]` → `text-app-text`
       - `text-[#777777]` → `text-app-text-secondary`

    8. **VisitasDiariasChart.tsx:**
       - Chart bar colors — if using inline hex for recharts `fill` or `stroke`, map to tokens.
       - If the chart uses `var(--color-app-primary)` directly in JSX, that's already tokenized.
       - Check for any hardcoded hex in the chart configuration.
  </action>
  <verify>
    <automated>
      for f in src/components/app/MaletaCard.tsx src/components/app/MaletaItemRow.tsx src/components/app/MaletaList.tsx src/components/app/StatCard.tsx src/components/app/SectionHeader.tsx src/components/app/MetricCardTrend.tsx src/components/app/ProductosPopularesList.tsx src/components/app/VisitasDiariasChart.tsx; do
        count=$(grep -c '\[#[0-9a-fA-F]\{3,6\}\]' "$f" || true)
        if [ "$count" -gt 0 ]; then echo "FAIL: $f has $count hex values"; exit 1; fi
      done
      echo "PASS: Data-display components hex-free"
    </automated>
  </verify>
  <done>
    All data-display components contain zero hardcoded hex values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Notification, Form, and Action Components</name>
  <files>
    src/components/app/NotificacionItem.tsx
    src/components/app/NotificacionesList.tsx
    src/components/app/PreferenciasNotificacionesForm.tsx
    src/components/app/CommissionTiers.tsx
    src/components/app/TimeRangeSelector.tsx
    src/components/app/ActionButton.tsx
    src/components/app/MenuHeader.tsx
  </files>
  <read_first>
    src/components/app/NotificacionItem.tsx
    src/components/app/NotificacionesList.tsx
    src/components/app/PreferenciasNotificacionesForm.tsx
    src/components/app/CommissionTiers.tsx
    src/components/app/TimeRangeSelector.tsx
    src/components/app/ActionButton.tsx
    src/components/app/MenuHeader.tsx
  </read_first>
  <action>
    Apply the standard hex-to-token mapping to all seven files.

    **Component-specific notes:**

    1. **NotificacionItem.tsx:**
       - Unread dot `bg-[#35605A]` → `bg-app-primary`
       - Timestamp `text-[#777777]` → `text-app-text-secondary`
       - Card `bg-white` → `bg-app-card-bg`
       - Border → `border-app-border`

    2. **NotificacionesList.tsx:**
       - Section header `text-[#777777]` → `text-app-text-secondary`
       - Empty state colors → appropriate tokens

    3. **PreferenciasNotificacionesForm.tsx:**
       - This component has 20 hex matches — the most of any component.
       - Toggle switch backgrounds and track colors
       - `bg-white` → `bg-app-card-bg`
       - `border-[#E8E2D6]` → `border-app-border`
       - Active toggle `bg-[#35605A]` → `bg-app-primary`
       - Inactive toggle `bg-[#EBEBEB]` → `bg-app-surface`
       - Text labels → `text-app-text`, `text-app-text-secondary`
       - Description text → `text-app-text-secondary`

    4. **CommissionTiers.tsx:**
       - Tier card backgrounds and text
       - Active tier `bg-[#35605A]` → `bg-app-primary`
       - Inactive tier `bg-[#EBEBEB]` → `bg-app-surface`
       - Text colors → appropriate tokens

    5. **TimeRangeSelector.tsx:**
       - Active tab `bg-[#35605A]` → `bg-app-primary`
       - Inactive tab `bg-white` → `bg-app-card-bg`
       - Border `border-[#E8E2D6]` → `border-app-border`

    6. **ActionButton.tsx:**
       - Button backgrounds and text
       - Primary `bg-[#35605A]` → `bg-app-primary`
       - Secondary `bg-[#EBEBEB]` → `bg-app-surface`
       - Text colors → appropriate tokens

    7. **MenuHeader.tsx:**
       - Header text `text-[#1A1A1A]` → `text-app-text`
       - Subtitle `text-[#777777]` → `text-app-text-secondary`

    After replacement, verify each file with grep.
  </action>
  <verify>
    <automated>
      for f in src/components/app/NotificacionItem.tsx src/components/app/NotificacionesList.tsx src/components/app/PreferenciasNotificacionesForm.tsx src/components/app/CommissionTiers.tsx src/components/app/TimeRangeSelector.tsx src/components/app/ActionButton.tsx src/components/app/MenuHeader.tsx; do
        count=$(grep -c '\[#[0-9a-fA-F]\{3,6\}\]' "$f" || true)
        if [ "$count" -gt 0 ]; then echo "FAIL: $f has $count hex values"; exit 1; fi
      done
      echo "PASS: Notification, form, and action components hex-free"
    </automated>
  </verify>
  <done>
    All notification, form, and action components contain zero hardcoded hex values.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final Verification Sweep and Build Check</name>
  <files>Any remaining files in src/app/app and src/components/app</files>
  <read_first>None — this is a verification task</read_first>
  <action>
    1. **Run the comprehensive hex scan across ALL PWA files:**
       ```bash
       # Check for Tailwind arbitrary color values
       echo "=== Tailwind arbitrary values ==="
       grep -rn '\[#[0-9a-fA-F]\{3,6\}\]' src/app/app src/components/app || echo "None found"

       # Check for inline style backgroundColor with hex
       echo "=== Inline backgroundColor ==="
       grep -rn 'style={{.*backgroundColor.*#[0-9a-fA-F]\{3,6\}' src/app/app src/components/app || echo "None found"

       # Check for inline style color with hex
       echo "=== Inline color ==="
       grep -rn 'style={{.*color.*#[0-9a-fA-F]\{3,6\}' src/app/app src/components/app || echo "None found"

       # Check for inline style borderColor with hex
       echo "=== Inline borderColor ==="
       grep -rn 'style={{.*borderColor.*#[0-9a-fA-F]\{3,6\}' src/app/app src/components/app || echo "None found"
       ```

    2. **If any matches are found**, identify the files and fix them using the standard mapping.

    3. **Build verification:**
       ```bash
       npm run build
       ```
       The build MUST pass without new errors. Pre-existing lint errors are acceptable.

    4. **Dark mode spot check (manual verification steps for executor):**
       - Run `npm run dev` locally
       - Open the PWA in a browser
       - Open DevTools → Elements
       - Find the `.app-shell` div and manually add `data-theme="dark"`
       - Verify that:
         a. Background turns dark (`#1a1816`)
         b. Cards turn dark (`#242220`)
         c. Text turns light (`#f0ece8`)
         d. No "windows" of wrong color appear (e.g., white cards on dark bg)
         e. Primary buttons remain visible
         f. Borders are visible but not too strong
       - Remove `data-theme="dark"` and verify it returns to light mode correctly

    5. **Update docs:**
       - Add an entry to `docs/CHANGELOG.md`:
         ```markdown
         ## 2026-05-15 — Phase 20: /app Hardcoded Color Migration
         - Migrated all PWA pages and components from hardcoded hex values to CSS tokens
         - Added 9 new semantic tokens to globals.css with dark variants
         - Dark mode now testable via DevTools by setting `data-theme="dark"` on `.app-shell`
         ```

    6. **Update docs/next_steps.md:**
       - Mark Phase 20 as completed:
         `- [x] **Phase 20: /app Hardcoded Color Migration** — ...`
  </action>
  <verify>
    <automated>
      hex_count=$(grep -rnc '\[#[0-9a-fA-F]\{3,6\}\]' src/app/app src/components/app | grep -v ':0$' | wc -l)
      if [ "$hex_count" -gt 0 ]; then
        echo "FAIL: Found files with hex values:"
        grep -rn '\[#[0-9a-fA-F]\{3,6\}\]' src/app/app src/components/app | head -20
        exit 1
      fi
      echo "PASS: No hex values in PWA"
      npm run build > /dev/null 2>&1 && echo "PASS: Build succeeds" || echo "FAIL: Build failed"
    </automated>
  </verify>
  <done>
    Zero hardcoded hex values remain in src/app/app and src/components/app, build passes, and dark mode is verified in DevTools.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → UI | Final verification ensures no hex values were missed in any file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Denial of Service | Build | mitigate | Build must pass before marking complete |
| T-20-09 | Information Disclosure | DevTools test | accept | Dark mode test is local only, no data exposure |
</threat_model>

<verification>
1. `grep -rnc '\[#[0-9a-fA-F]\{3,6\}\]' src/app/app src/components/app | grep -v ':0$'` returns empty
2. `npm run build` passes
3. `docs/CHANGELOG.md` has Phase 20 entry
4. `docs/next_steps.md` marks Phase 20 as completed
</verification>

<success_criteria>
- grep for `\[#[0-9a-fA-F]\{3,6\}\]` across src/app/app and src/components/app returns zero matches
- All inline style color hex values are eliminated
- `npm run build` passes
- Dark mode testable in DevTools with no visual breaks
- CHANGELOG.md and next_steps.md updated
</success_criteria>

<output>
After completion, create `.planning/phases/20-app-hardcoded-color-migration/20-05-SUMMARY.md`
</output>
