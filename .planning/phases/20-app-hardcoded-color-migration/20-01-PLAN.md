---
phase: 20-app-hardcoded-color-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - docs/design-system/tokens.md
  - src/app/layout.tsx
  - src/components/app/AppShell.tsx
  - src/components/app/AppBottomNav.tsx
  - src/components/app/AppHeader.tsx
  - src/components/app/AppPageShell.tsx
autonomous: true
requirements:
  - APM-01
  - APM-02
must_haves:
  truths:
    - "All missing PWA color tokens exist in globals.css with dark variants"
    - "Root layout.tsx body background uses CSS variable instead of inline hex"
    - "AppShell.tsx contains zero hardcoded hex color values"
    - "AppBottomNav.tsx contains zero hardcoded hex color values"
    - "AppHeader.tsx contains zero hardcoded hex color values"
    - "AppPageShell.tsx contains zero hardcoded hex color values"
  artifacts:
    - path: "src/app/globals.css"
      provides: "PWA color tokens with dark variants"
      contains: "--color-app-text-secondary"
    - path: "src/app/layout.tsx"
      provides: "Root layout without inline backgroundColor"
      excludes: "backgroundColor: \"#F5F2EF\""
    - path: "src/components/app/AppShell.tsx"
      provides: "App shell with tokenized colors"
      excludes: "bg-[#F5F2EF]"
  key_links:
    - from: "globals.css @theme inline"
      to: ".app-shell[data-theme=dark]"
      via: "Token override for dark mode"
      pattern: "--color-app-.*: .*"
---

<objective>
Establish the complete token foundation for PWA dark mode by adding all missing semantic color tokens to globals.css, then migrate the root layout and all shell components to use tokens exclusively.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/design-system/tokens.md
@src/app/globals.css
@src/app/layout.tsx
@src/components/app/AppShell.tsx
@src/components/app/AppBottomNav.tsx
@src/components/app/AppHeader.tsx
@src/components/app/AppPageShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Missing PWA Color Tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css</read_first>
  <action>
    Inside the `@theme inline { ... }` block (after the existing `--color-app-danger: #D32F2F;` line), add the following new tokens:

    ```css
    --color-app-text-secondary: #777777;
    --color-app-text-dim: #4b5563;
    --color-app-accent-brown: #917961;
    --color-app-surface-warm: #F5F0E8;
    --color-app-surface: #EBEBEB;
    --color-app-border: #E8E2D6;
    --color-app-border-strong: #D9D6D2;
    --color-app-warning-bg: #FFF4E5;
    --color-app-primary-light: #6A9A8A;
    ```

    Then, inside the `.app-shell[data-theme="dark"] { ... }` block (after the existing `--color-app-danger: #f87171;` line), add the corresponding dark overrides:

    ```css
    --color-app-text-secondary: #a0a0a0;
    --color-app-text-dim: #888888;
    --color-app-accent-brown: #b4a080;
    --color-app-surface-warm: #2a2624;
    --color-app-surface: #363230;
    --color-app-border: #4a4540;
    --color-app-border-strong: #5a5550;
    --color-app-warning-bg: rgba(250, 204, 21, 0.2);
    --color-app-primary-light: #6A9A8A;
    ```

    Do NOT modify any existing tokens or their dark variants. Only append the new ones.
  </action>
  <verify>
    <automated>
      grep -c "color-app-text-secondary" src/app/globals.css | grep -q "2" && echo "PASS: Both light and dark tokens present" || echo "FAIL"
      grep -c "color-app-border" src/app/globals.css | grep -q "2" && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    globals.css contains all 9 new tokens in both `@theme inline` and `.app-shell[data-theme="dark"]` blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tokens.md Documentation</name>
  <files>docs/design-system/tokens.md</files>
  <read_first>docs/design-system/tokens.md</read_first>
  <action>
    Append the following table to `docs/design-system/tokens.md` under the "App PWA (monarca)" section, after the existing token list:

    ```markdown
    **Extended Tokens (Phase 20):**
    - appTextSecondary: #777777 — Secondary text (labels, descriptions)
    - appTextDim: #4b5563 — Dimmed text, form labels
    - appAccentBrown: #917961 — Brand brown/gold accent
    - appSurfaceWarm: #F5F0E8 — Warm hover/surface background
    - appSurface: #EBEBEB — Neutral light surface
    - appBorder: #E8E2D6 — Standard borders and dividers
    - appBorderStrong: #D9D6D2 — Stronger borders
    - appWarningBg: #FFF4E5 — Warning/notice background
    - appPrimaryLight: #6A9A8A — Lighter primary variant
    ```
  </action>
  <verify>
    <automated>
      grep -c "Extended Tokens (Phase 20)" docs/design-system/tokens.md | grep -q "1" && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    tokens.md documents all new tokens with their semantic purpose.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate Root Layout and Shell Components</name>
  <files>
    src/app/layout.tsx
    src/components/app/AppShell.tsx
    src/components/app/AppBottomNav.tsx
    src/components/app/AppHeader.tsx
    src/components/app/AppPageShell.tsx
  </files>
  <read_first>
    src/app/layout.tsx
    src/components/app/AppShell.tsx
    src/components/app/AppBottomNav.tsx
    src/components/app/AppHeader.tsx
    src/components/app/AppPageShell.tsx
  </read_first>
  <action>
    For each file listed, replace ALL hardcoded hex color values (both Tailwind arbitrary classes `-[#...]` and inline `style={{...}}` color properties) according to this mapping:

    **Mapping Table (use for ALL files in this task):**
    | Hex | Tailwind Replacement | Notes |
    |-----|---------------------|-------|
    | bg-[#F5F2EF] | bg-app-bg | |
    | text-[#1A1A1A] | text-app-text | |
    | text-[#35605A] | text-app-primary | |
    | bg-[#35605A] | bg-app-primary | |
    | text-[#777777] | text-app-text-secondary | |
    | text-[#2E5A4C] | text-app-primary | Unify with primary (very close) |
    | bg-[#2E5A4C] | bg-app-primary | Unify with primary |
    | text-[#6b7280] | text-app-text-secondary | |
    | bg-[#F5F0E8] | bg-app-surface-warm | |
    | text-[#1f2937] | text-app-text | Hover state in AppShell nav |
    | bg-[#EBEBEB] | bg-app-surface | |
    | border-[#E8E2D6] | border-app-border | |
    | text-[#917961] | text-app-accent-brown | |
    | text-[#4b5563] | text-app-text-dim | |
    | text-[#B4ABA2] | text-app-muted | |
    | bg-[#FFFFFF] | bg-app-card-bg | |
    | bg-white | bg-app-card-bg | Only when used as card/container bg |
    | text-white | text-white | Keep as-is (correct in both themes) |
    | style={{ backgroundColor: "#F5F2EF" }} | Remove inline style; ensure parent has bg-app-bg | |
    | style={{ color: "#..." }} | Replace with Tailwind class using mapping | |
    | style={{ borderColor: "#EBEBEB" }} | border-app-border | |
    | style={{ borderTopColor: "#35605A" }} | border-t-app-primary | |

    **File-specific instructions:**

    1. **src/app/layout.tsx**: 
       - Remove `style={{ backgroundColor: "#F5F2EF" }}` from `<body>`
       - Add `bg-app-bg` to the body className: `<body className="antialiased bg-app-bg">`
       - Change `themeColor: "#F5F2EF"` to `themeColor: "var(--color-app-bg)"` in the viewport export

    2. **src/components/app/AppShell.tsx**:
       - Line ~39: `className="flex bg-[#F5F2EF] text-[#1A1A1A] font-sans"` → `className="flex bg-app-bg text-app-text font-sans"`
       - Line ~43: `bg-white border-r border-[#E8E2D6]` → `bg-app-card-bg border-r border-app-border`
       - Line ~44: `border-b border-[#E8E2D6]` → `border-b border-app-border`
       - Line ~47: `text-[#917961]` → `text-app-accent-brown`
       - Line ~69: `bg-[#2E5A4C]/10 text-[#2E5A4C]` → `bg-app-primary/10 text-app-primary`
       - Line ~70: `text-[#6b7280] hover:bg-[#F5F0E8] hover:text-[#1f2937]` → `text-app-text-secondary hover:bg-app-surface-warm hover:text-app-text`
       - Line ~79: `border-t border-[#E8E2D6]` → `border-t border-app-border`
       - Keep `text-red-500 hover:text-red-700 hover:bg-red-50` as-is (Tailwind standard colors)

    3. **src/components/app/AppBottomNav.tsx**:
       - Replace any `bg-[#...]` or `text-[#...]` or `border-[#...]` with mapped tokens.

    4. **src/components/app/AppHeader.tsx**:
       - Replace all `bg-[#...]`, `text-[#...]`, `border-[#...]` with mapped tokens.

    5. **src/components/app/AppPageShell.tsx**:
       - Replace all `bg-[#...]`, `text-[#...]`, `border-[#...]` with mapped tokens.
       - Also replace inline `style={{ color: "#..." }}` or `style={{ backgroundColor: "#..." }}`.

    After all replacements, verify each file has zero remaining hex color hardcoding with:
    `grep -n '\[#[0-9a-fA-F]\{3,6\}\]' {file}`
  </action>
  <verify>
    <automated>
      for f in src/app/layout.tsx src/components/app/AppShell.tsx src/components/app/AppBottomNav.tsx src/components/app/AppHeader.tsx src/components/app/AppPageShell.tsx; do
        count=$(grep -c '\[#[0-9a-fA-F]\{3,6\}\]' "$f" || true)
        if [ "$count" -gt 0 ]; then echo "FAIL: $f has $count hex values"; exit 1; fi
      done
      echo "PASS: All shell files hex-free"
      grep -q 'bg-app-bg' src/app/layout.tsx && echo "PASS: layout uses bg-app-bg" || echo "FAIL"
    </automated>
  </verify>
  <done>
    Root layout and all shell components use tokens exclusively with zero remaining hardcoded hex values.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → UI | Incorrect token mapping could cause visual regressions in PWA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | globals.css | mitigate | Review all token values against existing design; dark values must be visually tested |
| T-20-02 | Denial of Service | layout.tsx | accept | Low risk — body background change is cosmetic |
</threat_model>

<verification>
1. `grep -c "color-app-text-secondary" src/app/globals.css` returns 2
2. `grep -c '\[#[0-9a-fA-F]\{3,6\}\]' src/components/app/AppShell.tsx` returns 0
3. `grep -q "bg-app-bg" src/app/layout.tsx` passes
4. `npm run build` passes without new errors
</verification>

<success_criteria>
- All 9 new PWA tokens exist in globals.css with light and dark variants
- tokens.md documents the new tokens
- src/app/layout.tsx uses `bg-app-bg` instead of inline `backgroundColor: "#F5F2EF"`
- All shell components (AppShell, AppBottomNav, AppHeader, AppPageShell) contain zero hardcoded hex values
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-app-hardcoded-color-migration/20-01-SUMMARY.md`
</output>
