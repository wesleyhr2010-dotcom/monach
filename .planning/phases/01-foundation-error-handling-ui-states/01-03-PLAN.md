---
phase: 01-foundation-error-handling-ui-states
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - package.json
  - src/app/app/layout.tsx
  - src/app/admin/layout.tsx
  - src/app/admin/actions-maletas.ts
  - src/app/admin/actions-leads.ts
  - src/app/admin/actions-equipe.ts
  - src/app/admin/actions-gamificacao.ts
  - src/app/admin/revendedoras/page.tsx
  - src/app/admin/revendedoras/[id]/documentos/page.tsx
  - src/app/admin/consultoras/page.tsx
  - src/app/admin/consultoras/[id]/page.tsx
  - src/app/admin/equipe/page.tsx
  - src/app/admin/produtos/ProductForm.tsx
  - src/app/admin/categorias/CategoryManager.tsx
autonomous: true
requirements:
  - TECH-01
  - TECH-06
must_haves:
  truths:
    - "sonner Toaster is mounted in both PWA and Admin root layouts"
    - "actions-maletas.ts and actions-leads.ts return ActionResult<T> instead of throwing BUSINESS errors"
    - "All inline admin-toast divs are replaced with sonner toast() calls"
    - "No throw new Error.*BUSINESS patterns remain in actions-maletas.ts or actions-leads.ts"
  artifacts:
    - path: "package.json"
      provides: "sonner dependency"
      contains: '"sonner"'
    - path: "src/app/app/layout.tsx"
      provides: "PWA Toaster mount"
      contains: "<Toaster"
    - path: "src/app/admin/layout.tsx"
      provides: "Admin Toaster mount"
      contains: "<Toaster"
    - path: "src/app/admin/actions-maletas.ts"
      provides: "Maleta actions with ActionResult"
      contains: "safeAction"
    - path: "src/app/admin/actions-leads.ts"
      provides: "Lead actions with ActionResult"
      contains: "safeAction"
  key_links:
    - from: "src/app/admin/layout.tsx"
      to: "sonner"
      via: "import { Toaster } from 'sonner'"
      pattern: "import.*Toaster.*sonner"
    - from: "src/app/admin/actions-maletas.ts"
      to: "src/lib/action-utils.ts"
      via: "import { safeAction }"
      pattern: "import.*safeAction.*action-utils"
---

<objective>
Unify toast notifications across PWA and Admin via `sonner`, migrate the remaining high-traffic admin Server Actions (`actions-maletas.ts`, `actions-leads.ts`) to the `ActionResult<T>` pattern, and replace all inline admin toast divs with `sonner` API calls.

Purpose: Completes the foundation layer by ensuring consistent toast UX and eliminating the last major ad-hoc error-throwing patterns in admin action files.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-error-handling-ui-states/01-01-PLAN.md

**Existing patterns to follow:**
- `src/lib/action-utils.ts` provides `safeAction` and `ActionResult<T>` (enhanced in Plan 01).
- `src/app/admin/layout.tsx` is a Server Component; `sonner`'s `<Toaster />` is a Client Component — it must be imported into a client wrapper or placed directly (Toaster itself is a client component).
- Inline toasts in admin use CSS classes `admin-toast`, `admin-toast-success`, `admin-toast-error` defined in `src/app/admin/admin.css`.
- `actions-products.ts` and `actions-categories.ts` already use `safeAction` as the reference pattern.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner and mount Toaster in both layouts</name>
  <files>
    package.json,
    src/app/app/layout.tsx,
    src/app/admin/layout.tsx
  </files>
  <action>
    1. Run `npm install sonner` to add the dependency.
    2. Read `src/app/app/layout.tsx` and `src/app/admin/layout.tsx`.
    3. In `src/app/app/layout.tsx`:
       - Add `import { Toaster } from "sonner";` at the top.
       - Place `<Toaster position="top-center" richColors />` inside the `<AppShell>` children or adjacent to it so toasts render above all content. Since AppShell is a Client Component, Toaster can be placed directly inside AppShell's JSX or in layout.tsx (layout is Server Component, but Toaster is a client component that works when imported into server components in Next.js App Router).
       - Actually, `layout.tsx` in `/app` is an async Server Component. The `Toaster` from sonner is a Client Component. You can import and use Client Components inside Server Components. Add `<Toaster position="top-center" richColors />` inside the returned JSX, as a sibling to `<AppShell>`.
    4. In `src/app/admin/layout.tsx`:
       - Add `import { Toaster } from "sonner";` at the top.
       - Place `<Toaster position="top-right" richColors />` inside the returned JSX, as a sibling to `<AdminLayoutClient>`.
    5. Run `npm run typecheck` to verify.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -q '"sonner"' package.json && echo "sonner installed"</automated>
    <automated>grep -q "<Toaster" src/app/app/layout.tsx && grep -q "<Toaster" src/app/admin/layout.tsx && echo "Toaster mounted in both layouts"</automated>
  </verify>
  <done>
    - `package.json` contains `"sonner"` dependency
    - `src/app/app/layout.tsx` renders `<Toaster position="top-center" richColors />`
    - `src/app/admin/layout.tsx` renders `<Toaster position="top-right" richColors />`
    - `npm run typecheck` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining high-traffic admin actions to safeAction</name>
  <files>
    src/app/admin/actions-maletas.ts,
    src/app/admin/actions-leads.ts,
    src/app/admin/actions-equipe.ts,
    src/app/admin/actions-gamificacao.ts
  </files>
  <action>
    1. Read each target file completely.
    2. For `src/app/admin/actions-maletas.ts`:
       - Wrap every exported async function in `return safeAction(async () => { ... });`.
       - Remove `throw new Error("BUSINESS:...")` patterns; return the error message string directly inside the wrapper.
       - Update return types to `Promise<ActionResult<T>>`.
       - Import `safeAction` and `ActionResult` from `@/lib/action-utils`.
       - Do NOT change Prisma queries or business logic.
    3. For `src/app/admin/actions-leads.ts`:
       - Apply the same safeAction wrapping.
       - Remove any inline try/catch blocks that manually format errors — safeAction handles that.
       - Update return types.
    4. For `src/app/admin/actions-equipe.ts`:
       - Apply safeAction wrapping.
       - Remove BUSINESS throws.
       - Note: lines 275 and 340 currently do inline `msg.includes("BUSINESS:")` parsing — after migration, the client will receive `ActionResult` and no longer needs to parse BUSINESS prefixes.
    5. For `src/app/admin/actions-gamificacao.ts`:
       - Apply safeAction wrapping.
       - Remove BUSINESS throws.
    6. Run `npm run typecheck` after each file to catch type errors incrementally.
    7. Verify with grep: `grep -r "throw new Error.*BUSINESS" src/app/admin/actions-*.ts` should return zero matches after migration (excluding any test files).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -c "throw new Error.*BUSINESS" src/app/admin/actions-maletas.ts src/app/admin/actions-leads.ts src/app/admin/actions-equipe.ts src/app/admin/actions-gamificacao.ts || echo "0"</automated>
    <automated>grep -l "safeAction" src/app/admin/actions-maletas.ts src/app/admin/actions-leads.ts src/app/admin/actions-equipe.ts src/app/admin/actions-gamificacao.ts | wc -l | grep -q "4"</automated>
  </verify>
  <done>
    - `actions-maletas.ts`, `actions-leads.ts`, `actions-equipe.ts`, `actions-gamificacao.ts` all import `safeAction` and `ActionResult`
    - Zero `throw new Error("BUSINESS:` patterns remain in these four files
    - All exported async functions return `Promise<ActionResult<T>>`
    - `npm run typecheck` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace inline toast divs with sonner across admin pages</name>
  <files>
    src/app/admin/revendedoras/page.tsx,
    src/app/admin/revendedoras/[id]/documentos/page.tsx,
    src/app/admin/consultoras/page.tsx,
    src/app/admin/consultoras/[id]/page.tsx,
    src/app/admin/equipe/page.tsx,
    src/app/admin/produtos/ProductForm.tsx,
    src/app/admin/categorias/CategoryManager.tsx
  </files>
  <action>
    1. Read each target file to locate inline toast markup:
       - Look for `admin-toast`, `admin-toast-success`, `admin-toast-error` class usage.
       - Look for local `useState` toast patterns (e.g., `const [toast, setToast] = ...`).
    2. For each file:
       - Add `import { toast } from "sonner";` at the top.
       - Replace `setToast({ type: "success", message: "..." })` or inline `<div className="admin-toast...">` with `toast.success("...")` or `toast.error("...")`.
       - Remove the local toast state (`useState` for toast) and any JSX that conditionally renders toast divs.
       - Keep all other logic unchanged.
    3. Specific replacements:
       - `src/app/admin/revendedoras/page.tsx`: lines 368-369 (success/error divs) → `toast.success()` / `toast.error()`.
       - `src/app/admin/revendedoras/[id]/documentos/page.tsx`: lines 125, 128 → sonner calls.
       - `src/app/admin/consultoras/[id]/page.tsx`: lines 115-116 → sonner calls.
       - `src/app/admin/consultoras/page.tsx`: lines 162-163 → sonner calls.
       - `src/app/admin/equipe/page.tsx`: lines 130-131 → sonner calls.
       - `src/app/admin/produtos/ProductForm.tsx`: lines 278-280 → replace `setToast` with `toast.success()` / `toast.error()`.
       - `src/app/admin/categorias/CategoryManager.tsx`: lines 290-294 → replace `setToast` with `toast.success()` / `toast.error()`.
    4. After replacement, run `npm run typecheck` to verify.
    5. Run `npm run lint` to catch any unused imports.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>grep -r "admin-toast" src/app/admin/ --include="*.tsx" --include="*.ts" | grep -v "admin.css" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    - Zero inline `admin-toast` divs remain in admin TSX/TS files (excluding `admin.css`)
    - All target files import `toast` from "sonner" and use `toast.success()` / `toast.error()`
    - No unused toast-related `useState` imports remain
    - `npm run typecheck` and `npm run lint` pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Client | ActionResult prevents raw error leakage; sonner displays only user-friendly messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | sonner toast | mitigate | toast.success/error only receive string messages; never pass Error objects or stack traces directly to toast() |
| T-03-02 | Denial of Service | sonner Toaster | accept | Toaster is a lightweight client component; no server-side rendering overhead |
</threat_model>

<verification>
- `grep -q '"sonner"' package.json`
- `grep -q "<Toaster" src/app/app/layout.tsx && grep -q "<Toaster" src/app/admin/layout.tsx`
- `grep -c "throw new Error.*BUSINESS" src/app/admin/actions-maletas.ts src/app/admin/actions-leads.ts src/app/admin/actions-equipe.ts src/app/admin/actions-gamificacao.ts` returns 0
- `grep -r "admin-toast" src/app/admin/ --include="*.tsx" --include="*.ts" | grep -v "admin.css"` returns empty
- `npm run typecheck` passes
- `npm run lint` passes
</verification>

<success_criteria>
- sonner is installed and `<Toaster />` is mounted in both PWA and Admin layouts
- `actions-maletas.ts` and `actions-leads.ts` return `ActionResult<T>` with zero BUSINESS throws
- `actions-equipe.ts` and `actions-gamificacao.ts` also migrated to safeAction
- All inline admin toast divs replaced with `toast.success()` / `toast.error()` calls
- `npm run typecheck` and `npm run lint` pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-error-handling-ui-states/01-03-SUMMARY.md`
</output>
