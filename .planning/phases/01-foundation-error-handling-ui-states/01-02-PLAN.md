---
phase: 01-foundation-error-handling-ui-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/empty-state.tsx
  - src/components/ui/error-state.tsx
  - src/components/ui/skeleton-card.tsx
  - src/app/app/maleta/page.tsx
  - src/app/app/catalogo/page.tsx
  - src/app/app/page.tsx
  - src/app/admin/produtos/page.tsx
  - src/app/admin/revendedoras/page.tsx
  - src/app/admin/maleta/page.tsx
autonomous: true
requirements:
  - TECH-03
  - TECH-04
  - TECH-05
must_haves:
  truths:
    - "EmptyState component renders icon, title and description in Spanish"
    - "ErrorState component renders icon, title, description and a retry button"
    - "SkeletonCard component wraps Skeleton with card-like styling"
    - "At least 3 PWA routes and 3 Admin routes use these components for loading/empty/error states"
  artifacts:
    - path: "src/components/ui/empty-state.tsx"
      provides: "EmptyState reusable component"
      exports: ["EmptyState"]
    - path: "src/components/ui/error-state.tsx"
      provides: "ErrorState reusable component"
      exports: ["ErrorState"]
    - path: "src/components/ui/skeleton-card.tsx"
      provides: "SkeletonCard reusable component"
      exports: ["SkeletonCard"]
  key_links:
    - from: "src/components/ui/skeleton.tsx"
      to: "src/components/ui/skeleton-card.tsx"
      via: "import { Skeleton }"
      pattern: "import.*Skeleton.*from.*skeleton"
---

<objective>
Create three reusable UI-state components (SkeletonCard, EmptyState, ErrorState) and integrate them into at least three PWA routes and three Admin routes, replacing ad-hoc loading spinners, empty messages, and error markup.

Purpose: Provides a consistent visual language for non-ideal states across both PWA and Admin surfaces, reducing duplication and improving UX.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

**Existing patterns to follow:**
- `src/components/ui/skeleton.tsx` — base `Skeleton` component from shadcn/ui (animate-pulse, rounded-md, bg-accent).
- Design system tokens: `--app-*` for PWA, `--admin-*` for Admin. Raleway font family for PWA.
- PWA routes already have some loading states (e.g., `MaletaCard` on `/app/maleta/`).
- Admin routes use `admin-toast` CSS classes for inline messages; empty states are often inline `<p>` tags.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmptyState, ErrorState and SkeletonCard components</name>
  <files>
    src/components/ui/empty-state.tsx,
    src/components/ui/error-state.tsx,
    src/components/ui/skeleton-card.tsx
  </files>
  <action>
    1. Read `src/components/ui/skeleton.tsx` to understand the existing Skeleton pattern.
    2. Create `src/components/ui/skeleton-card.tsx`:
       - Props: `className?: string`
       - Renders a card-shaped container (rounded-xl, border, bg-white for PWA / bg-[#1a1a1a] for admin context) containing 3-4 `Skeleton` lines of varying widths to simulate a card layout.
       - Export as `SkeletonCard`.
    3. Create `src/components/ui/empty-state.tsx`:
       - Props: `icon?: React.ReactNode`, `title: string`, `description?: string`, `action?: { label: string; onClick: () => void }`
       - Renders a vertically centered flex column with the icon (default `PackageOpen` from lucide-react), title in `text-lg font-semibold`, description in `text-sm text-muted-foreground`, and optional action button.
       - All text must be in Spanish (the component itself is generic, but default strings should be Spanish-friendly like "No hay datos" / "Parece que aún no hay nada por aquí").
    4. Create `src/components/ui/error-state.tsx`:
       - Props: `title?: string`, `description?: string`, `onRetry?: () => void`
       - Renders a vertically centered flex column with `AlertTriangle` icon, title default "Algo salió mal", description default "No pudimos cargar los datos. Intentá de nuevo.", and a retry button (if `onRetry` is provided) with `RotateCcw` icon.
    5. Run `npm run typecheck` to ensure the new components compile.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>test -f src/components/ui/empty-state.tsx && test -f src/components/ui/error-state.tsx && test -f src/components/ui/skeleton-card.tsx && echo "Components exist"</automated>
  </verify>
  <done>
    - `src/components/ui/empty-state.tsx` exports `EmptyState` with icon, title, description, action props
    - `src/components/ui/error-state.tsx` exports `ErrorState` with title, description, onRetry props
    - `src/components/ui/skeleton-card.tsx` exports `SkeletonCard` with card-like skeleton layout
    - `npm run typecheck` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate UI state components into PWA routes</name>
  <files>
    src/app/app/maleta/page.tsx,
    src/app/app/catalogo/page.tsx,
    src/app/app/page.tsx
  </files>
  <action>
    1. Read each target PWA route file completely.
    2. For `src/app/app/maleta/page.tsx`:
       - Replace any inline loading spinner or bare `<p>Cargando...</p>` with `<SkeletonCard />` while data is loading.
       - Replace any inline empty message with `<EmptyState title="No tenés maletas" description="Tus consignaciones aparecerán aquí." />`.
       - Wrap any error display in `<ErrorState onRetry={() => window.location.reload()} />`.
    3. For `src/app/app/catalogo/page.tsx`:
       - Add `<SkeletonCard />` for loading state.
       - Add `<EmptyState title="Catálogo vacío" description="Tu maleta activa no tiene productos disponibles." />` for empty state.
       - Add `<ErrorState />` for error state.
    4. For `src/app/app/page.tsx` (home):
       - Add `<SkeletonCard />` for loading state of metric cards.
       - Add `<EmptyState title="Bienvenida" description="Completá tu perfil para ver tus métricas." />` when profile is incomplete.
       - Add `<ErrorState />` for error state.
    5. Use design system tokens (e.g., `--app-bg`, `--app-text`) where applicable. Do not hard-code hex values.
    6. Run `npm run typecheck` after all changes.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -l "EmptyState\|ErrorState\|SkeletonCard" src/app/app/maleta/page.tsx src/app/app/catalogo/page.tsx src/app/app/page.tsx | wc -l | grep -q "3"</automated>
  </verify>
  <done>
    - All three PWA routes import and use at least one of SkeletonCard, EmptyState, or ErrorState
    - Loading, empty, and error states are visually consistent across the three routes
    - `npm run typecheck` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate UI state components into Admin routes</name>
  <files>
    src/app/admin/produtos/page.tsx,
    src/app/admin/revendedoras/page.tsx,
    src/app/admin/maleta/page.tsx
  </files>
  <action>
    1. Read each target Admin route file completely.
    2. For `src/app/admin/produtos/page.tsx`:
       - Replace inline loading markup with `<SkeletonCard className="admin-card" />` (adapt styling to admin dark theme if needed).
       - Replace empty product list message with `<EmptyState title="No hay productos" description="Agregá tu primer producto desde el botón "Nuevo"." />`.
       - Add `<ErrorState onRetry={refetch} />` for query errors.
    3. For `src/app/admin/revendedoras/page.tsx`:
       - Add `<SkeletonCard />` for loading state.
       - Add `<EmptyState title="No hay revendedoras" description="Aún no tenés revendedoras en el sistema." />` for empty state.
       - Add `<ErrorState />` for error state.
    4. For `src/app/admin/maleta/page.tsx`:
       - Add `<SkeletonCard />` for loading state.
       - Add `<EmptyState title="No hay maletas" description="Creá tu primera consignación desde el botón "Nueva Maleta"." />` for empty state.
       - Add `<ErrorState />` for error state.
    5. Use admin design system tokens (`--admin-bg`, `--admin-text`, etc.) where applicable. Maintain the dark theme consistency.
    6. Run `npm run typecheck` after all changes.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -l "EmptyState\|ErrorState\|SkeletonCard" src/app/admin/produtos/page.tsx src/app/admin/revendedoras/page.tsx src/app/admin/maleta/page.tsx | wc -l | grep -q "3"</automated>
  </verify>
  <done>
    - All three Admin routes import and use at least one of SkeletonCard, EmptyState, or ErrorState
    - Loading, empty, and error states are visually consistent with the admin dark theme
    - `npm run typecheck` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → User | ErrorState must not expose internal error details (stack traces, SQL) to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | ErrorState | mitigate | ErrorState only renders generic Spanish messages; no prop accepts raw Error objects or stack traces |
</threat_model>

<verification>
- `test -f src/components/ui/empty-state.tsx && test -f src/components/ui/error-state.tsx && test -f src/components/ui/skeleton-card.tsx`
- `grep -c "EmptyState\|ErrorState\|SkeletonCard" src/app/app/maleta/page.tsx src/app/app/catalogo/page.tsx src/app/app/page.tsx` — each file shows at least 1 match
- `grep -c "EmptyState\|ErrorState\|SkeletonCard" src/app/admin/produtos/page.tsx src/app/admin/revendedoras/page.tsx src/app/admin/maleta/page.tsx` — each file shows at least 1 match
- `npm run typecheck` passes
</verification>

<success_criteria>
- EmptyState, ErrorState, and SkeletonCard exist in `src/components/ui/`
- Each component has well-defined props and renders in Spanish
- At least 3 PWA routes and 3 Admin routes use the components
- `npm run typecheck` passes after all integrations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-error-handling-ui-states/01-02-SUMMARY.md`
</output>
