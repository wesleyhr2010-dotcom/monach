---
phase: 16-foundation-schema-clientes
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01-PLAN.md
files_modified:
  - src/app/admin/actions-clientes.ts
  - src/lib/types.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
user_setup: []
must_haves:
  truths:
    - "Admin pode cadastrar cliente com nome, RUC, cidade, telefone — retorna ActionResult<Cliente>"
    - "Sistema rejeita cadastro com RUC duplicado — retorna erro em espanhol"
    - "Admin pode editar dados de cliente existente"
    - "Server Action getClientes retorna lista unificada de clientes da loja e compradores de maleta"
    - "Filtro por origem (LOJA / REVENDEDORA) funciona via branch na Server Action"
    - "Todas as actions usam requireAuth([ADMIN]) e retornam ActionResult<T>"
  artifacts:
    - path: "src/app/admin/actions-clientes.ts"
      provides: "Server Actions de CRUD e busca de clientes"
      exports:
        - "criarCliente"
        - "editarCliente"
        - "buscarClientePorRuc"
        - "getClientes"
    - path: "src/lib/types.ts"
      provides: "Tipos TypeScript compartilhados"
      contains: "export interface ClienteItem"
  key_links:
    - from: "src/app/admin/actions-clientes.ts"
      to: "prisma/schema.prisma"
      via: "prisma.cliente"
      pattern: "prisma\\.cliente\\.(create|findMany|update|findUnique)"
    - from: "src/app/admin/actions-clientes.ts"
      to: "src/lib/user.ts"
      via: "requireAuth"
      pattern: "requireAuth\\(\\[\\s*\\\"ADMIN\\\"\\s*\\]\\)"
    - from: "src/app/admin/actions-clientes.ts"
      to: "src/lib/action-utils.ts"
      via: "safeAction, ActionResult, BusinessError"
      pattern: "safeAction|ActionResult|BusinessError"
---

<objective>
Implementar Server Actions para CRUD de clientes e lista unificada, seguindo os padrões do projeto (ActionResult, safeAction, requireAuth, $transaction array form).

Purpose: Fornecer a camada de API serverless que a UI do admin consome. A lista unificada é a decisão arquitetural central (D-16-02) desta fase.
Output: `actions-clientes.ts` com 4 actions exportadas e tipos compartilhados.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-foundation-schema-clientes/16-CONTEXT.md
@prisma/schema.prisma
@src/lib/user.ts
@src/lib/action-utils.ts

<interfaces>
From src/lib/action-utils.ts:
```typescript
export type ActionResult<T = void> =
    | { success: true; data: T }
    | { success: false; error: string };

export class BusinessError extends Error {
    constructor(message: string) {
        super(message);
        this.name = "BusinessError";
    }
}

export async function safeAction<T = void>(
    fn: () => Promise<T>,
    options?: { actionName?: string; userId?: string }
): Promise<ActionResult<T>> {
    try {
        const result = await fn();
        return { success: true, data: result };
    } catch (err: unknown) {
        if (options?.actionName) {
            captureServerActionError(err, options.actionName, options.userId);
        }
        return { success: false, error: mapError(err) };
    }
}
```

From src/lib/user.ts:
```typescript
export async function requireAuth(allowedRoles?: Role[]): Promise<CurrentUser> {
    const user = await getCurrentUser();
    if (!user) {
        throw new BusinessError("Sesión no válida. Inicia sesión nuevamente.");
    }
    if (!user.isActive) {
        throw new BusinessError("Tu cuenta no está activa. Contacta a tu consultora.");
    }
    if (allowedRoles && allowedRoles.length > 0 && !allowedRoles.includes(user.role)) {
        throw new BusinessError("No tienes permiso para realizar esta acción.");
    }
    return user;
}
```

From prisma/schema.prisma (new models — ver Plan 16-01):
```prisma
model Cliente {
  id        String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  nombre    String
  ruc       String   @unique
  ciudad    String   @default("")
  telefono  String   @default("")
  origen    ClienteOrigem @default(LOJA)
  created_at DateTime @default(now()) @db.Timestamptz()
  updated_at DateTime @default(now()) @updatedAt @db.Timestamptz()
  ventas    VentaLoja[]
  @@index([origen])
  @@index([ruc])
  @@map("clientes")
}

model VendaMaleta {
  id             String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  maleta_id      String   @db.Uuid
  maleta_item_id String   @db.Uuid
  reseller_id    String   @db.Uuid
  cliente_nome     String
  cliente_telefone String
  quantidade     Int     @default(1)
  preco_unitario Decimal @db.Decimal(12, 2)
  created_at     DateTime @default(now()) @db.Timestamptz()
  // ... relations
}
```

From STATE.md (D-16-02):
```
Lista unificada de clientes via two-query merge — prisma.cliente.findMany() + prisma.vendaMaleta.findMany({ distinct: [...] }); filtro por origem por branch na Server Action
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tipos compartilhados ClienteItem e ClienteFormData</name>
  <files>src/lib/types.ts</files>
  <action>
Adicionar ao final de `src/lib/types.ts` (ou criar se não existir) os seguintes tipos:

```typescript
// ============================================
// Clientes (PDV v1.4)
// ============================================

export interface ClienteItem {
  id: string;
  nombre: string;
  ruc: string;
  ciudad: string;
  telefono: string;
  origen: "LOJA" | "REVENDEDORA";
  created_at: string; // ISO string
  updated_at: string; // ISO string
}

export interface ClienteFormData {
  nombre: string;
  ruc: string;
  ciudad: string;
  telefono: string;
}
```

Regras:
- Se `src/lib/types.ts` já existe, adicionar ao final sem remover nenhum tipo existente.
- `created_at` e `updated_at` como `string` (serialização JSON das Server Actions).
- `origem` como union literal `"LOJA" | "REVENDEDORA"` em vez de `string` para type safety na UI.
  </action>
  <verify>
    <automated>grep -c "export interface ClienteItem" src/lib/types.ts && grep -c "export interface ClienteFormData" src/lib/types.ts</automated>
  </verify>
  <done>Tipos ClienteItem e ClienteFormData existem e são exportados.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar Server Actions em actions-clientes.ts</name>
  <files>src/app/admin/actions-clientes.ts</files>
  <action>
Criar o arquivo `src/app/admin/actions-clientes.ts` com as 4 Server Actions abaixo. Seguir rigorosamente os padrões do projeto.

```typescript
"use server";

import { prisma } from "@/lib/prisma";
import { safeAction, BusinessError } from "@/lib/action-utils";
import { requireAuth } from "@/lib/user";
import type { ActionResult } from "@/lib/action-utils";
import type { ClienteItem, ClienteFormData } from "@/lib/types";

// ============================================
// Helper: serializar Prisma Cliente → ClienteItem
// ============================================
function serializeCliente(c: {
  id: string;
  nombre: string;
  ruc: string;
  ciudad: string;
  telefono: string;
  origen: string;
  created_at: Date;
  updated_at: Date;
}): ClienteItem {
  return {
    id: c.id,
    nombre: c.nombre,
    ruc: c.ruc,
    ciudad: c.ciudad,
    telefono: c.telefono,
    origen: c.origen as "LOJA" | "REVENDEDORA",
    created_at: c.created_at.toISOString(),
    updated_at: c.updated_at.toISOString(),
  };
}

// ============================================
// Action: criarCliente
// ============================================
export async function criarCliente(
  data: ClienteFormData
): Promise<ActionResult<ClienteItem>> {
  return safeAction(async () => {
    await requireAuth(["ADMIN"]);

    // Validação: RUC obrigatório e único
    const rucLimpo = data.ruc.trim();
    if (!rucLimpo) {
      throw new BusinessError("El RUC es obligatorio.");
    }

    const existente = await prisma.cliente.findUnique({
      where: { ruc: rucLimpo },
    });
    if (existente) {
      throw new BusinessError("Ya existe un cliente con ese RUC.");
    }

    const cliente = await prisma.cliente.create({
      data: {
        nombre: data.nombre.trim(),
        ruc: rucLimpo,
        ciudad: data.ciudad.trim(),
        telefono: data.telefono.trim(),
      },
    });

    return serializeCliente(cliente);
  }, { actionName: "criarCliente" });
}

// ============================================
// Action: editarCliente
// ============================================
export async function editarCliente(
  id: string,
  data: ClienteFormData
): Promise<ActionResult<ClienteItem>> {
  return safeAction(async () => {
    await requireAuth(["ADMIN"]);

    if (!id) {
      throw new BusinessError("ID de cliente no proporcionado.");
    }

    const rucLimpo = data.ruc.trim();
    if (!rucLimpo) {
      throw new BusinessError("El RUC es obligatorio.");
    }

    // Verificar duplicidade de RUC em OUTRO cliente
    const existente = await prisma.cliente.findFirst({
      where: { ruc: rucLimpo, NOT: { id } },
    });
    if (existente) {
      throw new BusinessError("Ya existe otro cliente con ese RUC.");
    }

    const cliente = await prisma.cliente.update({
      where: { id },
      data: {
        nombre: data.nombre.trim(),
        ruc: rucLimpo,
        ciudad: data.ciudad.trim(),
        telefono: data.telefono.trim(),
      },
    });

    return serializeCliente(cliente);
  }, { actionName: "editarCliente" });
}

// ============================================
// Action: buscarClientePorRuc
// ============================================
export async function buscarClientePorRuc(
  ruc: string
): Promise<ActionResult<ClienteItem | null>> {
  return safeAction(async () => {
    await requireAuth(["ADMIN"]);

    const cliente = await prisma.cliente.findUnique({
      where: { ruc: ruc.trim() },
    });

    return cliente ? serializeCliente(cliente) : null;
  }, { actionName: "buscarClientePorRuc" });
}

// ============================================
// Action: getClientes (lista unificada)
// ============================================
export async function getClientes(params?: {
  origem?: "LOJA" | "REVENDEDORA" | "TODOS";
}): Promise<ActionResult<ClienteItem[]>> {
  return safeAction(async () => {
    await requireAuth(["ADMIN"]);

    const filtro = params?.origem ?? "TODOS";
    const resultado: ClienteItem[] = [];

    // Branch 1: clientes da loja (com RUC)
    if (filtro === "LOJA" || filtro === "TODOS") {
      const clientesLoja = await prisma.cliente.findMany({
        orderBy: { nombre: "asc" },
      });
      resultado.push(...clientesLoja.map(serializeCliente));
    }

    // Branch 2: compradores de maletas (sem RUC, nome + telefone)
    if (filtro === "REVENDEDORA" || filtro === "TODOS") {
      const compradores = await prisma.vendaMaleta.findMany({
        distinct: ["cliente_nome", "cliente_telefone"],
        select: {
          cliente_nome: true,
          cliente_telefone: true,
        },
        orderBy: { cliente_nome: "asc" },
      });

      // Mapear para ClienteItem com ID sintético
      const compradoresMapped: ClienteItem[] = compradores.map((c, idx) => ({
        id: `REV-${idx}`,
        nombre: c.cliente_nome,
        ruc: "",
        ciudad: "",
        telefono: c.cliente_telefone,
        origen: "REVENDEDORA",
        created_at: "",
        updated_at: "",
      }));

      resultado.push(...compradoresMapped);
    }

    // Ordenar alfabeticamente por nombre
    resultado.sort((a, b) => a.nombre.localeCompare(b.nombre, "es"));

    return resultado;
  }, { actionName: "getClientes" });
}
```

Regras obrigatórias:
1. `requireAuth(["ADMIN"])` em TODAS as actions.
2. Todas retornam `ActionResult<T>` via `safeAction`.
3. Erros de negócio usam `BusinessError` com mensagens em espanhol paraguaio.
4. `prisma.cliente.findFirst({ where: { ruc, NOT: { id } } })` para validação de duplicidade no editar.
5. `getClientes` implementa o two-query merge (D-16-02) com branch por `origem`.
6. IDs sintéticos para compradores de maleta usam prefixo `REV-` (não são UUIDs reais).
7. NÃO usar `$transaction(async tx)` — usar array form se precisar (neste arquivo, não há transactions multi-operação).
8. NÃO importar tipos do Prisma Client diretamente nas assinaturas exportadas — usar `ClienteItem` e `ClienteFormData` do `src/lib/types.ts`.
  </action>
  <verify>
    <automated>grep -c "export async function criarCliente" src/app/admin/actions-clientes.ts && grep -c "export async function editarCliente" src/app/admin/actions-clientes.ts && grep -c "export async function buscarClientePorRuc" src/app/admin/actions-clientes.ts && grep -c "export async function getClientes" src/app/admin/actions-clientes.ts && grep -c "requireAuth\\(\\[\\s*\\\"ADMIN\\\"\\s*\\]\\)" src/app/admin/actions-clientes.ts && grep -c "safeAction" src/app/admin/actions-clientes.ts</automated>
  </verify>
  <done>Arquivo actions-clientes.ts criado com 4 actions exportadas, requireAuth, safeAction, ActionResult, e two-query merge em getClientes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → Server Action | Input não confiável cruza aqui (RUC, nome, telefone) |
| Server Action → Prisma | Queries executam com privilégios do DB role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Spoofing | criarCliente | mitigate | `requireAuth(["ADMIN"])` impede criação não autorizada |
| T-16-04 | Tampering | editarCliente | mitigate | Validação de duplicidade de RUC + ownership implícito (clientes são globais) |
| T-16-05 | Information Disclosure | getClientes | mitigate | `requireAuth(["ADMIN"])` filtra acesso; lista não expõe dados sensíveis além de nome/telefone/RUC |
| T-16-06 | Denial of Service | getClientes | mitigate | `distinct` no Prisma limita resultados; sem paginação nesta fase (aceitável para volume atual) |
</threat_model>

<verification>
1. `grep -c "export async function" src/app/admin/actions-clientes.ts` → 4
2. `grep -c "requireAuth" src/app/admin/actions-clientes.ts` → 4
3. `grep -c "safeAction" src/app/admin/actions-clientes.ts` → 4
4. `grep -c "BusinessError" src/app/admin/actions-clientes.ts` → 4
5. `npm run lint` passa sem erros
6. `npm run typecheck` passa (types ClienteItem e ClienteFormData usados corretamente)
</verification>

<success_criteria>
- 4 Server Actions exportadas: criarCliente, editarCliente, buscarClientePorRuc, getClientes
- Todas usam requireAuth(["ADMIN"]) e retornam ActionResult<T>
- Duplicidade de RUC bloqueada com BusinessError em espanhol
- getClientes implementa two-query merge (clientes + compradores maleta) com filtro por origem
- Tipos ClienteItem e ClienteFormData criados em src/lib/types.ts
- Lint e typecheck passam
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-schema-clientes/16-02-SUMMARY.md`
</output>
