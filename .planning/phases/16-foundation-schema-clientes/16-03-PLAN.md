---
phase: 16-foundation-schema-clientes
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02-PLAN.md
files_modified:
  - src/app/admin/clientes/page.tsx
  - src/app/admin/clientes/ClientesClient.tsx
  - src/app/admin/clientes/ClienteFormModal.tsx
  - src/app/admin/clientes/ClienteRow.tsx
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - VIS-01
  - VIS-02
user_setup: []
must_haves:
  truths:
    - "Admin acessa /admin/clientes e vê lista de clientes com tabs Loja / Revendedoras / Todos"
    - "Lista exibe nome, RUC, cidade, telefone e origem (badge)"
    - "Botão 'Nuevo Cliente' abre modal com formulário de criação"
    - "Submit com RUC duplicado exibe mensagem de erro em espanhol sem fechar modal"
    - "Linha de cliente tem botão 'Editar' que abre modal pré-preenchido"
    - "Todas as cores usam tokens var(--admin-*) — zero hex/px hardcoded"
    - "Paper MCP foi consultado antes da implementação (registrado no audit)"
  artifacts:
    - path: "src/app/admin/clientes/page.tsx"
      provides: "Server Component que carrega dados iniciais"
      contains: "getClientes"
    - path: "src/app/admin/clientes/ClientesClient.tsx"
      provides: "Client Component com estado de tabs, lista e modal"
      contains: "useState"
    - path: "src/app/admin/clientes/ClienteFormModal.tsx"
      provides: "Modal reutilizável de criação/edição"
      contains: "criarCliente | editarCliente"
    - path: "src/app/admin/clientes/ClienteRow.tsx"
      provides: "Linha de cliente na lista"
      contains: "origen"
  key_links:
    - from: "src/app/admin/clientes/page.tsx"
      to: "src/app/admin/actions-clientes.ts"
      via: "getClientes()"
      pattern: "getClientes"
    - from: "src/app/admin/clientes/ClientesClient.tsx"
      to: "ClienteFormModal.tsx"
      via: "props onEdit / onCreate"
      pattern: "ClienteFormModal"
    - from: "ClienteFormModal.tsx"
      to: "actions-clientes.ts"
      via: "criarCliente / editarCliente"
      pattern: "criarCliente|editarCliente"
---

<objective>
Implementar a tela `/admin/clientes` com lista unificada, filtro por origem via tabs, formulário de criação/edição em modal, e feedback de erro de RUC duplicado.

Purpose: Interface principal para gestão de clientes do PDV — o ponto de entrada para todo o fluxo de venda de loja.
Output: 4 arquivos React (page + client + modal + row) usando tokens admin e componentes reutilizáveis.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wesleyhudson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-foundation-schema-clientes/16-CONTEXT.md
@src/app/admin/actions-clientes.ts
@src/components/admin/AdminPageHeader.tsx
@src/components/admin/AdminEmptyState.tsx
@src/components/admin/AdminFilterBar.tsx

<interfaces>
From src/lib/types.ts (criado no Plan 16-02):
```typescript
export interface ClienteItem {
  id: string;
  nombre: string;
  ruc: string;
  ciudad: string;
  telefono: string;
  origen: "LOJA" | "REVENDEDORA";
  created_at: string;
  updated_at: string;
}

export interface ClienteFormData {
  nombre: string;
  ruc: string;
  ciudad: string;
  telefono: string;
}
```

From src/app/admin/actions-clientes.ts (criado no Plan 16-02):
```typescript
export async function criarCliente(data: ClienteFormData): Promise<ActionResult<ClienteItem>>;
export async function editarCliente(id: string, data: ClienteFormData): Promise<ActionResult<ClienteItem>>;
export async function buscarClientePorRuc(ruc: string): Promise<ActionResult<ClienteItem | null>>;
export async function getClientes(params?: { origem?: "LOJA" | "REVENDEDORA" | "TODOS" }): Promise<ActionResult<ClienteItem[]>>;
```

From src/components/admin/AdminPageHeader.tsx:
```typescript
export function AdminPageHeader({ title, description, breadcrumb, backHref, action }: AdminPageHeaderProps);
```

From src/components/admin/AdminEmptyState.tsx:
```typescript
export function AdminEmptyState({ icon: Icon, title, description, action, className }: AdminEmptyStateProps);
```

From src/components/admin/AdminFilterBar.tsx:
```typescript
export function AdminFilterBar({ searchValue, onSearchChange, onSearchSubmit, searchPlaceholder, filters, onClear, className }: AdminFilterBarProps);
```

From src/app/admin/admin.css (tokens):
```css
--admin-bg: #0a0a0a;
--admin-surface: #171717;
--admin-surface-hover: #222222;
--admin-border: #2a2a2a;
--admin-border-focus: #35605a;
--admin-text: #ededed;
--admin-text-muted: #888888;
--admin-text-dim: #444444;
--admin-accent: #35605a;
--admin-accent-hover: #2a4d48;
--admin-danger: #e05c5c;
--admin-danger-hover: #c44545;
--admin-success: #4ade80;
--admin-warning: #facc15;
--admin-radius: 8px;
--admin-radius-pill: 9999px;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consultar Paper MCP para o artboard de /admin/clientes</name>
  <files>src/app/admin/clientes/</files>
  <action>
Antes de escrever qualquer JSX, consultar o artboard correspondente no Paper via MCP:

1. Usar `pencil_get_editor_state` para listar artboards disponíveis.
2. Buscar artboard com nome contendo "clientes" ou "Clientes".
3. Se encontrado: extrair `get_jsx`, `get_computed_styles`, e `get_screenshot` do artboard.
4. Se NÃO encontrado: usar o design system existente (`admin.css` tokens + padrões de `AdminPageHeader`, `AdminEmptyState`, `AdminFilterBar`) como referência visual.

Registrar o resultado da consulta em `.planning/phases/16-foundation-schema-clientes/16-PAPER-AUDIT.md`:
```markdown
## Paper Audit — /admin/clientes

**Data:** 2026-05-08
**Artboard encontrado:** [sim/não]
**ID do artboard:** [id ou "N/A"]
**Capturas consultadas:** [lista]
**Decisões visuais aplicadas:** [resumo]
```

Regras:
- NÃO criar nenhum arquivo de UI antes deste registro.
- Se o Paper estiver indisponível (erro de rede), anotar "Paper indisponível — usando design system existente" e prosseguir.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-foundation-schema-clientes/16-PAPER-AUDIT.md && grep -c "Artboard encontrado" .planning/phases/16-foundation-schema-clientes/16-PAPER-AUDIT.md</automated>
  </verify>
  <done>Arquivo 16-PAPER-AUDIT.md existe com registro da consulta ao Paper (ou justificativa de indisponibilidade).</done>
</task>

<task type="auto">
  <name>Task 2: Criar componentes ClienteRow e ClienteFormModal</name>
  <files>
    src/app/admin/clientes/ClienteRow.tsx
    src/app/admin/clientes/ClienteFormModal.tsx
  </files>
  <action>
Criar os componentes de linha e modal reutilizável.

**Arquivo: `src/app/admin/clientes/ClienteRow.tsx`**
```tsx
"use client";

import { Users } from "lucide-react";
import type { ClienteItem } from "@/lib/types";

interface ClienteRowProps {
  cliente: ClienteItem;
  onEdit: (cliente: ClienteItem) => void;
}

export function ClienteRow({ cliente, onEdit }: ClienteRowProps) {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        gap: 16,
        padding: "14px 18px",
        background: "var(--admin-surface)",
        borderRadius: "var(--admin-radius)",
        border: "1px solid var(--admin-border)",
      }}
    >
      <div
        style={{
          width: 40,
          height: 40,
          borderRadius: "var(--admin-radius)",
          background: "var(--admin-border)",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          flexShrink: 0,
        }}
      >
        <Users size={18} style={{ color: "var(--admin-text-muted)" }} />
      </div>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 4 }}>
          <span style={{ fontSize: 15, fontWeight: 600, color: "var(--admin-text)" }}>
            {cliente.nombre}
          </span>
          <span
            style={{
              fontSize: 11,
              fontWeight: 600,
              textTransform: "uppercase",
              letterSpacing: "0.5px",
              padding: "2px 8px",
              borderRadius: 4,
              background: cliente.origen === "LOJA" ? "var(--admin-accent)" : "var(--admin-border)",
              color: cliente.origen === "LOJA" ? "#fff" : "var(--admin-text-muted)",
            }}
          >
            {cliente.origen === "LOJA" ? "Loja" : "Revendedoras"}
          </span>
        </div>
        <div style={{ display: "flex", gap: 16, fontSize: 13, color: "var(--admin-text-muted)" }}>
          <span>RUC: {cliente.ruc || "—"}</span>
          <span>{cliente.ciudad || "—"}</span>
          <span>{cliente.telefono || "—"}</span>
        </div>
      </div>
      {cliente.origen === "LOJA" && (
        <button
          onClick={() => onEdit(cliente)}
          className="admin-btn admin-btn-secondary admin-btn-sm"
        >
          Editar
        </button>
      )}
    </div>
  );
}
```

Regras para ClienteRow:
- Ícone `Users` do lucide-react.
- Badge de origem com cor `var(--admin-accent)` para LOJA e `var(--admin-border)` para REVENDEDORA.
- Botão "Editar" APENAS para clientes de origem LOJA (REVENDEDORA são read-only).
- Zero hex hardcoded — todos os valores vêm de `var(--admin-*)`.

**Arquivo: `src/app/admin/clientes/ClienteFormModal.tsx`**
```tsx
"use client";

import { useState, useCallback } from "react";
import { X, Loader2 } from "lucide-react";
import { criarCliente, editarCliente } from "../actions-clientes";
import type { ClienteItem, ClienteFormData } from "@/lib/types";
import type { ActionResult } from "@/lib/action-utils";

interface ClienteFormModalProps {
  cliente?: ClienteItem | null;
  onClose: () => void;
  onSuccess: (cliente: ClienteItem) => void;
}

const initialForm: ClienteFormData = {
  nombre: "",
  ruc: "",
  ciudad: "",
  telefono: "",
};

export function ClienteFormModal({ cliente, onClose, onSuccess }: ClienteFormModalProps) {
  const isEdit = Boolean(cliente);
  const [form, setForm] = useState<ClienteFormData>({
    nombre: cliente?.nombre ?? "",
    ruc: cliente?.ruc ?? "",
    ciudad: cliente?.ciudad ?? "",
    telefono: cliente?.telefono ?? "",
  });
  const [error, setError] = useState<string>("");
  const [loading, setLoading] = useState(false);

  const handleChange = (field: keyof ClienteFormData, value: string) => {
    setForm((prev) => ({ ...prev, [field]: value }));
    if (error) setError("");
  };

  const handleSubmit = useCallback(
    async (e: React.FormEvent) => {
      e.preventDefault();
      setLoading(true);
      setError("");

      let result: ActionResult<ClienteItem>;
      if (isEdit && cliente) {
        result = await editarCliente(cliente.id, form);
      } else {
        result = await criarCliente(form);
      }

      setLoading(false);
      if (result.success) {
        onSuccess(result.data);
        onClose();
      } else {
        setError(result.error);
      }
    },
    [form, isEdit, cliente, onSuccess, onClose]
  );

  return (
    <div
      style={{
        position: "fixed",
        inset: 0,
        background: "rgba(0,0,0,0.6)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        zIndex: 50,
      }}
      onClick={onClose}
    >
      <div
        style={{
          background: "var(--admin-surface)",
          border: "1px solid var(--admin-border)",
          borderRadius: "var(--admin-radius)",
          width: "100%",
          maxWidth: 480,
          padding: 24,
          margin: 16,
        }}
        onClick={(e) => e.stopPropagation()}
      >
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 20 }}>
          <h2 style={{ fontSize: 18, fontWeight: 600, color: "var(--admin-text)", margin: 0 }}>
            {isEdit ? "Editar Cliente" : "Nuevo Cliente"}
          </h2>
          <button onClick={onClose} style={{ background: "none", border: "none", cursor: "pointer", padding: 4 }}>
            <X size={20} style={{ color: "var(--admin-text-muted)" }} />
          </button>
        </div>

        {error && (
          <div
            style={{
              padding: "10px 14px",
              background: "rgba(224,92,92,0.1)",
              border: "1px solid var(--admin-danger)",
              borderRadius: "var(--admin-radius)",
              color: "var(--admin-danger)",
              fontSize: 13,
              marginBottom: 16,
            }}
          >
            {error}
          </div>
        )}

        <form onSubmit={handleSubmit} style={{ display: "flex", flexDirection: "column", gap: 14 }}>
          <div>
            <label style={{ display: "block", fontSize: 12, fontWeight: 500, color: "var(--admin-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.5px" }}>
              Nombre completo
            </label>
            <input
              type="text"
              required
              value={form.nombre}
              onChange={(e) => handleChange("nombre", e.target.value)}
              style={{
                width: "100%",
                padding: "10px 12px",
                background: "var(--admin-bg)",
                border: "1px solid var(--admin-border)",
                borderRadius: "var(--admin-radius)",
                color: "var(--admin-text)",
                fontSize: 14,
              }}
            />
          </div>

          <div>
            <label style={{ display: "block", fontSize: 12, fontWeight: 500, color: "var(--admin-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.5px" }}>
              RUC
            </label>
            <input
              type="text"
              required
              value={form.ruc}
              onChange={(e) => handleChange("ruc", e.target.value)}
              style={{
                width: "100%",
                padding: "10px 12px",
                background: "var(--admin-bg)",
                border: "1px solid var(--admin-border)",
                borderRadius: "var(--admin-radius)",
                color: "var(--admin-text)",
                fontSize: 14,
              }}
            />
          </div>

          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 14 }}>
            <div>
              <label style={{ display: "block", fontSize: 12, fontWeight: 500, color: "var(--admin-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.5px" }}>
                Ciudad
              </label>
              <input
                type="text"
                value={form.ciudad}
                onChange={(e) => handleChange("ciudad", e.target.value)}
                style={{
                  width: "100%",
                  padding: "10px 12px",
                  background: "var(--admin-bg)",
                  border: "1px solid var(--admin-border)",
                  borderRadius: "var(--admin-radius)",
                  color: "var(--admin-text)",
                  fontSize: 14,
                }}
              />
            </div>
            <div>
              <label style={{ display: "block", fontSize: 12, fontWeight: 500, color: "var(--admin-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.5px" }}>
                Teléfono
              </label>
              <input
                type="tel"
                value={form.telefono}
                onChange={(e) => handleChange("telefono", e.target.value)}
                style={{
                  width: "100%",
                  padding: "10px 12px",
                  background: "var(--admin-bg)",
                  border: "1px solid var(--admin-border)",
                  borderRadius: "var(--admin-radius)",
                  color: "var(--admin-text)",
                  fontSize: 14,
                }}
              />
            </div>
          </div>

          <div style={{ display: "flex", gap: 10, justifyContent: "flex-end", marginTop: 8 }}>
            <button
              type="button"
              onClick={onClose}
              className="admin-btn admin-btn-secondary"
            >
              Cancelar
            </button>
            <button
              type="submit"
              disabled={loading}
              className="admin-btn admin-btn-primary"
              style={{ display: "inline-flex", alignItems: "center", gap: 6 }}
            >
              {loading && <Loader2 size={16} className="animate-spin" />}
              {isEdit ? "Guardar cambios" : "Crear cliente"}
            </button>
          </div>
        </form>
      </div>
    </div>
  );
}
```

Regras para ClienteFormModal:
- Props: `cliente?` (null para criar, ClienteItem para editar), `onClose`, `onSuccess`.
- Erro de duplicidade de RUC aparece em banner vermelho (`var(--admin-danger)`) dentro do modal — modal NÃO fecha.
- Loading state com spinner `Loader2` no botão de submit.
- Campos: nombre (obrigatório), ruc (obrigatório), ciudad, telefono.
- Labels em espanhol paraguaio.
- Zero hex hardcoded — todos os valores via `var(--admin-*)`.
- Usar classes `admin-btn`, `admin-btn-primary`, `admin-btn-secondary` do design system.
  </action>
  <verify>
    <automated>test -f src/app/admin/clientes/ClienteRow.tsx && test -f src/app/admin/clientes/ClienteFormModal.tsx && grep -c "var(--admin-)" src/app/admin/clientes/ClienteRow.tsx && grep -c "var(--admin-)" src/app/admin/clientes/ClienteFormModal.tsx && grep -c "criarCliente" src/app/admin/clientes/ClienteFormModal.tsx && grep -c "editarCliente" src/app/admin/clientes/ClienteFormModal.tsx</automated>
  </verify>
  <done>ClienteRow.tsx e ClienteFormModal.tsx criados com zero hex hardcoded, usando tokens admin, labels em espanhol, e integração com actions-clientes.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Criar ClientesClient.tsx e page.tsx</name>
  <files>
    src/app/admin/clientes/ClientesClient.tsx
    src/app/admin/clientes/page.tsx
  </files>
  <action>
Criar a página principal e o client component.

**Arquivo: `src/app/admin/clientes/page.tsx`**
```tsx
import { getClientes } from "../actions-clientes";
import { ClientesClient } from "./ClientesClient";

export const metadata = {
  title: "Clientes — Monarca Admin",
};

export default async function ClientesPage() {
  const result = await getClientes({ origem: "TODOS" });
  const clientesIniciais = result.success ? result.data : [];

  return <ClientesClient initialClientes={clientesIniciais} />;
}
```

**Arquivo: `src/app/admin/clientes/ClientesClient.tsx`**
```tsx
"use client";

import { useState, useCallback } from "react";
import { Users, Plus } from "lucide-react";
import { AdminPageHeader } from "@/components/admin/AdminPageHeader";
import { AdminEmptyState } from "@/components/admin/AdminEmptyState";
import { ClienteRow } from "./ClienteRow";
import { ClienteFormModal } from "./ClienteFormModal";
import { getClientes } from "../actions-clientes";
import type { ClienteItem } from "@/lib/types";

type Tab = "TODOS" | "LOJA" | "REVENDEDORA";

interface ClientesClientProps {
  initialClientes: ClienteItem[];
}

export function ClientesClient({ initialClientes }: ClientesClientProps) {
  const [clientes, setClientes] = useState<ClienteItem[]>(initialClientes);
  const [activeTab, setActiveTab] = useState<Tab>("TODOS");
  const [modalOpen, setModalOpen] = useState(false);
  const [editingCliente, setEditingCliente] = useState<ClienteItem | null>(null);
  const [loading, setLoading] = useState(false);

  const handleTabChange = useCallback(
    async (tab: Tab) => {
      setActiveTab(tab);
      setLoading(true);
      const result = await getClientes({ origem: tab });
      if (result.success) {
        setClientes(result.data);
      }
      setLoading(false);
    },
    []
  );

  const handleCreate = () => {
    setEditingCliente(null);
    setModalOpen(true);
  };

  const handleEdit = (cliente: ClienteItem) => {
    setEditingCliente(cliente);
    setModalOpen(true);
  };

  const handleSuccess = useCallback(
    async (cliente: ClienteItem) => {
      // Recarregar lista para refletir mudanças
      const result = await getClientes({ origem: activeTab });
      if (result.success) {
        setClientes(result.data);
      }
    },
    [activeTab]
  );

  const tabs: { key: Tab; label: string }[] = [
    { key: "TODOS", label: "Todos" },
    { key: "LOJA", label: "Loja" },
    { key: "REVENDEDORA", label: "Revendedoras" },
  ];

  const filteredClientes = clientes; // já filtrado pela action

  return (
    <>
      <AdminPageHeader
        breadcrumb="Admin / Clientes"
        title="Clientes"
        description="Gestión de clientes de la tienda y compradores de revendedoras"
        action={
          <button
            onClick={handleCreate}
            className="admin-btn admin-btn-primary"
            style={{ display: "inline-flex", alignItems: "center", gap: 6 }}
          >
            <Plus size={14} />
            Nuevo Cliente
          </button>
        }
      />

      <div style={{ padding: "28px 32px", display: "flex", flexDirection: "column", gap: 20 }}>
        {/* Tabs */}
        <div style={{ display: "flex", gap: 4, borderBottom: "1px solid var(--admin-border)" }}>
          {tabs.map((tab) => (
            <button
              key={tab.key}
              onClick={() => handleTabChange(tab.key)}
              style={{
                padding: "10px 16px",
                fontSize: 13,
                fontWeight: 500,
                background: "none",
                border: "none",
                borderBottom: activeTab === tab.key ? "2px solid var(--admin-accent)" : "2px solid transparent",
                color: activeTab === tab.key ? "var(--admin-text)" : "var(--admin-text-muted)",
                cursor: "pointer",
                transition: "color 0.15s, border-color 0.15s",
              }}
            >
              {tab.label}
              <span
                style={{
                  marginLeft: 8,
                  fontSize: 11,
                  fontWeight: 600,
                  color: activeTab === tab.key ? "var(--admin-accent)" : "var(--admin-text-dim)",
                }}
              >
                {tab.key === "TODOS"
                  ? clientes.length
                  : clientes.filter((c) => c.origen === tab.key).length}
              </span>
            </button>
          ))}
        </div>

        {/* Lista */}
        {loading ? (
          <div style={{ textAlign: "center", padding: 40, color: "var(--admin-text-muted)" }}>
            Cargando...
          </div>
        ) : filteredClientes.length === 0 ? (
          <AdminEmptyState
            icon={Users}
            title="Ningún cliente encontrado"
            description={
              activeTab === "LOJA"
                ? "No hay clientes registrados en la tienda."
                : activeTab === "REVENDEDORA"
                ? "No hay compradores de maletas registrados."
                : "No hay clientes ni compradores registrados."
            }
            action={
              activeTab !== "REVENDEDORA" && (
                <button
                  onClick={handleCreate}
                  className="admin-btn admin-btn-primary"
                  style={{ display: "inline-flex", alignItems: "center", gap: 6 }}
                >
                  <Plus size={16} />
                  Nuevo Cliente
                </button>
              )
            }
          />
        ) : (
          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            {filteredClientes.map((cliente) => (
              <ClienteRow key={cliente.id} cliente={cliente} onEdit={handleEdit} />
            ))}
          </div>
        )}
      </div>

      {modalOpen && (
        <ClienteFormModal
          cliente={editingCliente}
          onClose={() => setModalOpen(false)}
          onSuccess={handleSuccess}
        />
      )}
    </>
  );
}
```

Regras para ClientesClient:
- Tabs: "Todos", "Loja", "Revendedoras" — com contagem dinâmica.
- Ao trocar de tab, chama `getClientes({ origem })` e atualiza a lista.
- Botão "Nuevo Cliente" abre modal de criação.
- `ClienteRow` recebe `onEdit` para abrir modal de edição.
- Empty state usa `AdminEmptyState` com mensagem contextual por tab.
- Loading state simples (texto "Cargando...") durante fetch de tab.
- Zero hex hardcoded.
- `page.tsx` é Server Component que faz o fetch inicial e passa para `ClientesClient`.
  </action>
  <verify>
    <automated>test -f src/app/admin/clientes/page.tsx && test -f src/app/admin/clientes/ClientesClient.tsx && grep -c "getClientes" src/app/admin/clientes/page.tsx && grep -c "AdminPageHeader" src/app/admin/clientes/ClientesClient.tsx && grep -c "AdminEmptyState" src/app/admin/clientes/ClientesClient.tsx && grep -c "var(--admin-)" src/app/admin/clientes/ClientesClient.tsx && grep -c "var(--admin-)" src/app/admin/clientes/page.tsx</automated>
  </verify>
  <done>page.tsx e ClientesClient.tsx criados com tabs, lista, modal integration, AdminPageHeader, AdminEmptyState, e zero hex hardcoded.</done>
</task>

<task type="auto">
  <name>Task 4: Verificação de tokens e build</name>
  <files>src/app/admin/clientes/</files>
  <action>
1. Verificar que NENHUM arquivo em `src/app/admin/clientes/` contém hex hardcoded:
```bash
grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/clientes/ --include="*.tsx"
```
   - Esperado: saída vazia.

2. Verificar que TODOS os arquivos usam tokens admin:
```bash
grep -rn 'var(--admin-)' src/app/admin/clientes/ --include="*.tsx" | wc -l
```
   - Esperado: > 20 matches.

3. Rodar lint:
```bash
npm run lint
```

4. Rodar typecheck:
```bash
npm run typecheck
```

5. Rodar build:
```bash
npm run build
```

6. Se houver erro de tipo em `ClienteItem` ou `ClienteFormData`, verificar se `src/lib/types.ts` foi atualizado no Plan 16-02.

7. Verificar que `src/app/admin/clientes/16-PAPER-AUDIT.md` existe (do Task 1).
  </action>
  <verify>
    <automated>grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/clientes/ --include="*.tsx" | wc -l | grep -q '^0$' && npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <done>Zero hex hardcoded em src/app/admin/clientes/, lint/typecheck/build passam, Paper audit registrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → UI | Input de formulário (RUC, nome, telefone) cruza da interface não confiável |
| UI → Server Action | Chamadas `criarCliente` / `editarCliente` via Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Spoofing | ClienteFormModal | mitigate | Botão "Editar" só aparece para origem LOJA; REVENDEDORA é read-only |
| T-16-08 | Tampering | handleSubmit | mitigate | Erros de duplicidade de RUC exibidos no modal sem fechar — previne perda de dados |
| T-16-09 | Information Disclosure | ClienteRow | mitigate | Exibe apenas nome, RUC, cidade, telefone — nenhum dado sensível |
| T-16-10 | Denial of Service | handleTabChange | mitigate | Sem paginação (aceitável para volume atual); pode ser adicionada em v1.5 |
</threat_model>

<verification>
1. `grep -rn '#[0-9A-Fa-f]\{3,6\}' src/app/admin/clientes/ --include="*.tsx"` → vazio
2. `grep -rn 'var(--admin-)' src/app/admin/clientes/ --include="*.tsx" | wc -l` → > 20
3. `npm run lint` → passa
4. `npm run typecheck` → passa
5. `npm run build` → passa
6. A rota `/admin/clientes` renderiza sem erro 500
7. Paper audit registrado em `16-PAPER-AUDIT.md`
</verification>

<success_criteria>
- Rota `/admin/clientes` acessível com lista de clientes
- Tabs Loja / Revendedoras / Todos funcionam com contagem
- Modal de criação/edição funciona com validação de RUC duplicado
- Feedback de erro em espanhol sem fechar modal
- Zero hex/px hardcoded — 100% tokens admin
- AdminEmptyState usado para estado vazio
- Paper MCP consultado (registrado no audit)
- Lint, typecheck e build passam
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-schema-clientes/16-03-SUMMARY.md`
</output>
