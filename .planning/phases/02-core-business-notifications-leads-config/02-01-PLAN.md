---
phase: 02-core-business-notifications-leads-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/notifications.ts
  - src/__tests__/lib/notifications.test.ts
  - src/app/admin/config/notif-push/page.tsx
  - src/app/admin/config/notif-push/TemplateEditor.tsx
autonomous: true
requirements:
  - NOTF-01
  - NOTF-07
  - NOTF-08
  - NOTF-09
must_haves:
  truths:
    - "substituirVariaveis(template, contexto) replaces known placeholders and ignores unknown ones"
    - "Template editor displays 'Variables disponibles' hint per template type"
    - "HTML is sanitized for email variables; plain-text conversion for OneSignal push"
  artifacts:
    - path: "src/lib/notifications.ts"
      provides: "substituirVariaveis, htmlToPlainText, sanitizeTemplateVars"
      exports: ["substituirVariaveis", "htmlToPlainText", "sanitizeTemplateVars"]
    - path: "src/__tests__/lib/notifications.test.ts"
      provides: "Tests for variable substitution and sanitization"
      min_lines: 50
    - path: "src/app/admin/config/notif-push/TemplateEditor.tsx"
      provides: "Template editor with variable hints"
      contains: "Variables disponibles"
  key_links:
    - from: "src/lib/notifications.ts"
      to: "TemplateEditor.tsx"
      via: "variable whitelist per tipo"
      pattern: "VARIAVEIS_POR_TIPO|variables.*tipo"
---

<objective>
Build the notification template engine: `substituirVariaveis()` helper with per-type variable whitelists, HTML sanitization for email and plain-text for push, and enhance the existing admin template editor with "Variables disponibles" hints.

Purpose: Enables dynamic notification content with safe variable substitution, preparing the template system for integration with cron jobs and Server Actions.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-business-notifications-leads-config/02-CONTEXT.md

**Existing patterns to follow:**
- `src/lib/notifications.ts` already exports `criarNotificacao`, `podeEnviarPush`, `enviarPushSePermitido`, `notificarRevendedora`.
- `src/app/admin/config/notif-push/` already has `page.tsx`, `NotifPushClient.tsx`, `actions.ts` with template CRUD.
- `NotificacaoTemplate` Prisma model has fields: `id`, `tipo`, `titulo_es`, `body_es`, `ativo`, `updated_at`.
- The project uses `isomorphic-dompurify` or similar for sanitization (check `package.json`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create substituirVariaveis helper with whitelist and tests</name>
  <files>src/lib/notifications.ts, src/__tests__/lib/notifications.test.ts</files>
  <behavior>
    - Test: substituirVariaveis("Hola {nome}", { nome: "Ana" }) returns "Hola Ana"
    - Test: substituirVariaveis("Hola {nome}", { nome: "Ana" }, ["nome"]) returns "Hola Ana"
    - Test: substituirVariaveis("Hola {nome}", { nome: "Ana" }, ["outro"]) returns "Hola {nome}" (unknown var ignored)
    - Test: substituirVariaveis("Hola {nome}", { nome: "Ana" }) without whitelist returns "Hola Ana" (all vars allowed)
    - Test: substituirVariaveis("Hola {nome}", {}) returns "Hola {nome}" (missing var leaves placeholder)
    - Test: handles nested objects: substituirVariaveis("{maleta.id}", { maleta: { id: "123" } }) returns "123"
  </behavior>
  <action>
    1. Read `src/lib/notifications.ts` to see current exports and patterns.
    2. Add `substituirVariaveis(template: string, contexto: Record<string, unknown>, whitelist?: string[]): string`:
       - Replace all occurrences of `{key}` where key exists in contexto.
       - If whitelist is provided, only replace keys in the whitelist; leave others unchanged.
       - Support dot notation for nested objects (`{maleta.id}` → `contexto.maleta.id`).
       - Convert values to string via `String(value)`.
       - If key not found in contexto, leave placeholder as-is.
    3. Export `VARIAVEIS_POR_TIPO: Record<string, string[]>` mapping each `TipoNotificacao` to its allowed variable names:
       - `prazo_proximo`: `["maleta_id", "dias_restantes", "nome_revendedora"]`
       - `maleta_atrasada`: `["maleta_id", "nome_revendedora"]`
       - `pontos_ganhos`: `["pontos", "motivo", "nome_revendedora"]`
       - `acerto_confirmado`: `["maleta_id", "valor_comissao", "nome_revendedora"]`
       - `devolucao_recebida`: `["maleta_id", "nome_revendedora"]`
       - `nova_maleta`: `["maleta_id", "nome_revendedora"]`
       - `brinde_entregue`: `["nome_regalo", "nome_revendedora"]`
    4. Create `src/__tests__/lib/notifications.test.ts` with Vitest tests covering all behavior cases above.
    5. Run `npm test -- src/__tests__/lib/notifications.test.ts` to verify.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/lib/notifications.test.ts -x</automated>
  </verify>
  <done>
    - `src/lib/notifications.ts` exports `substituirVariaveis` and `VARIAVEIS_POR_TIPO`
    - All tests in `src/__tests__/lib/notifications.test.ts` pass
    - Unknown variables are left unchanged when whitelist is provided
    - Missing variables leave placeholder unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Add HTML sanitization and plain-text utilities</name>
  <files>src/lib/notifications.ts, package.json</files>
  <action>
    1. Check `package.json` for existing sanitization library. If `isomorphic-dompurify` or `dompurify` is present, use it. If not, run `npm install isomorphic-dompurify`.
    2. Add `sanitizeTemplateVars(input: string): string` to `src/lib/notifications.ts`:
       - Use DOMPurify to sanitize HTML, allowing only basic formatting tags (`b`, `i`, `strong`, `em`, `br`, `p`, `a` with href).
       - Strip all scripts, event handlers, and dangerous attributes.
    3. Add `htmlToPlainText(html: string): string` to `src/lib/notifications.ts`:
       - Replace `<br>`, `<p>` with `\n`.
       - Strip all remaining HTML tags.
       - Decode HTML entities (`&amp;` → `&`, etc.).
       - Trim extra whitespace.
    4. Export both functions from `src/lib/notifications.ts`.
    5. Run `npm run typecheck` to verify.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -q "sanitizeTemplateVars\|htmlToPlainText" src/lib/notifications.ts && echo "Utilities exported"</automated>
  </verify>
  <done>
    - `sanitizeTemplateVars` strips scripts and dangerous HTML while preserving safe formatting
    - `htmlToPlainText` converts HTML to clean plain text for OneSignal push
    - `npm run typecheck` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance template editor with variable hints</name>
  <files>src/app/admin/config/notif-push/TemplateEditor.tsx, src/app/admin/config/notif-push/page.tsx</files>
  <action>
    1. Read `src/app/admin/config/notif-push/TemplateEditor.tsx` to understand current editor structure.
    2. Import `VARIAVEIS_POR_TIPO` from `@/lib/notifications`.
    3. Add a "Variables disponibles" section below the template body input:
       - Display the list of allowed variables for the current template's `tipo`.
       - Show each variable as a clickable chip/badge that inserts `{variable_name}` at cursor position in the body textarea.
       - If the template type has no whitelist entry, show "Todas las variables están permitidas".
    4. Add validation before save:
       - Scan the template body for `{...}` placeholders.
       - If any placeholder is NOT in the whitelist for this type, show a warning (non-blocking): "Variable {x} no está en la lista permitida para este tipo. Será ignorada en el envío."
    5. Update `src/app/admin/config/notif-push/page.tsx` if needed to pass `tipo` to the editor.
    6. Run `npm run typecheck` and `npm run lint`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>grep -q "Variables disponibles" src/app/admin/config/notif-push/TemplateEditor.tsx && echo "Variable hints added"</automated>
  </verify>
  <done>
    - Template editor shows "Variables disponibles" hint with clickable variable chips
    - Clicking a chip inserts `{variable_name}` into the template body
    - Validation warns about variables not in the type's whitelist
    - `npm run typecheck` and `npm run lint` pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template input → Storage | Admin-entered template content crosses into DB |
| Template output → Client/Push | Substituted content reaches end users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | Template body | mitigate | sanitizeTemplateVars strips scripts and dangerous HTML before storage |
| T-02-02 | Information Disclosure | Variable substitution | accept | Variable context is controlled by server code, not user input |
| T-02-03 | Tampering | Whitelist bypass | mitigate | Whitelist is hardcoded in source (VARIAVEIS_POR_TIPO), not configurable at runtime |
</threat_model>

<verification>
- `npm test -- src/__tests__/lib/notifications.test.ts` passes
- `grep -q "export.*substituirVariaveis" src/lib/notifications.ts`
- `grep -q "export.*VARIAVEIS_POR_TIPO" src/lib/notifications.ts`
- `grep -q "Variables disponibles" src/app/admin/config/notif-push/TemplateEditor.tsx`
- `npm run typecheck` passes
</verification>

<success_criteria>
- `substituirVariaveis()` correctly replaces whitelisted variables and ignores unknown ones
- Template editor displays variable hints and allows insertion via click
- HTML sanitization and plain-text utilities are available for downstream plans
- All tests pass and TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-business-notifications-leads-config/02-01-SUMMARY.md`
</output>
