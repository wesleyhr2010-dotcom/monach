---
phase: 02-core-business-notifications-leads-config
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - supabase/functions/check-maleta-prazo/index.ts
  - supabase/functions/marcar-maletas-atrasadas/index.ts
  - src/app/app/actions-revendedora.ts
  - src/app/admin/actions-maletas.ts
autonomous: true
requirements:
  - NOTF-02
  - NOTF-03
  - NOTF-04
  - NOTF-05
  - NOTF-06
must_haves:
  truths:
    - "Cron jobs read active templates from NotificacaoTemplate and substitute variables"
    - "Server actions registrarVenda, conferirEFecharMaleta, submitDevolucao send templated notifications"
    - "Fallback to default text if template is inactive or missing"
  artifacts:
    - path: "supabase/functions/check-maleta-prazo/index.ts"
      provides: "Cron job with template integration"
      contains: "getTemplateAtivo|substituirVariaveis"
    - path: "supabase/functions/marcar-maletas-atrasadas/index.ts"
      provides: "Cron job with template integration"
      contains: "getTemplateAtivo|substituirVariaveis"
    - path: "src/app/app/actions-revendedora.ts"
      provides: "PWA actions with templated notifications"
      contains: "notificarRevendedora|substituirVariaveis"
    - path: "src/app/admin/actions-maletas.ts"
      provides: "Admin actions with templated notifications"
      contains: "notificarRevendedora|substituirVariaveis"
  key_links:
    - from: "supabase/functions/check-maleta-prazo/index.ts"
      to: "NotificacaoTemplate"
      via: "supabase.from('notificacao_templates')"
      pattern: "notificacao_templates"
---

<objective>
Wire the notification template system into existing cron jobs and Server Actions: replace hardcoded Spanish strings with dynamic template content from `NotificacaoTemplate`, applying variable substitution and respecting active/inactive status.

Purpose: Completes the notification system by making the template editor actually control the content sent to resellers.
</objective>

<execution_context>
@/Users/wesleyhudson/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-business-notifications-leads-config/02-CONTEXT.md
@.planning/phases/02-core-business-notifications-leads-config/02-01-PLAN.md

**Existing patterns to follow:**
- `substituirVariaveis` and `VARIAVEIS_POR_TIPO` created in Plan 01.
- `notificarRevendedora()` in `src/lib/notifications.ts` handles persistence + push.
- Cron jobs exist in `supabase/functions/check-maleta-prazo/index.ts` and `supabase/functions/marcar-maletas-atrasadas/index.ts`.
- `actions-revendedora.ts` and `actions-maletas.ts` contain `registrarVenda`, `conferirEFecharMaleta`, `submitDevolucao`.
- Edge Functions use Supabase JS client with `service_role` key.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor cron jobs to read templates and substitute variables</name>
  <files>supabase/functions/check-maleta-prazo/index.ts, supabase/functions/marcar-maletas-atrasadas/index.ts</files>
  <action>
    1. Read both cron job files completely.
    2. Create a shared helper in `supabase/functions/_shared/notifications.ts` (or inline if shared imports are problematic in Edge Functions):
       - `async function getTemplateAtivo(supabase, tipo: string)` → queries `notificacao_templates` where `tipo = ? AND ativo = true`, returns `{ titulo_es, body_es }` or null.
    3. Update `supabase/functions/check-maleta-prazo/index.ts`:
       - After fetching maletas, for each maleta:
         a. Call `getTemplateAtivo(supabase, 'prazo_proximo')`.
         b. If template found, use `substituirVariaveis(template.body_es, { maleta_id: maleta.id, dias_restantes: calcularDias(maleta.data_limite), nome_revendedora: maleta.resellers.name })`.
         c. If template NOT found, use existing fallback text.
         d. Send push with substituted text.
    4. Update `supabase/functions/marcar-maletas-atrasadas/index.ts`:
       - After marking maletas as atrasada, for each affected maleta:
         a. Call `getTemplateAtivo(supabase, 'maleta_atrasada')`.
         b. If found, substitute `{maleta_id}`, `{nome_revendedora}`.
         c. If not found, use fallback text.
         d. Send push with substituted text.
    5. Ensure `substituirVariaveis` logic is available in Edge Functions (copy function or import from shared).
    6. Run `npm run typecheck` (if TypeScript covers Edge Functions; otherwise verify syntax manually).
  </action>
  <verify>
    <automated>grep -q "getTemplateAtivo\|notificacao_templates" supabase/functions/check-maleta-prazo/index.ts && echo "Template integration present in D-3 cron"</automated>
    <automated>grep -q "getTemplateAtivo\|notificacao_templates" supabase/functions/marcar-maletas-atrasadas/index.ts && echo "Template integration present in atrasadas cron"</automated>
  </verify>
  <done>
    - Both cron jobs query `notificacao_templates` for active templates
    - Variable substitution applied to template body before sending push
    - Fallback to default text preserved when template is inactive/missing
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire server actions to send templated notifications</name>
  <files>src/app/app/actions-revendedora.ts, src/app/admin/actions-maletas.ts</files>
  <action>
    1. Read `src/app/app/actions-revendedora.ts` and identify `registrarVenda`, `submitDevolucao` functions.
    2. Read `src/app/admin/actions-maletas.ts` and identify `conferirEFecharMaleta` function.
    3. Import `notificarRevendedora`, `substituirVariaveis`, `VARIAVEIS_POR_TIPO` from `@/lib/notifications`.
    4. In `registrarVenda`:
       - After successful sale registration, fetch template `pontos_ganhos` from `NotificacaoTemplate`.
       - If active, substitute `{pontos}`, `{motivo}`, `{nome_revendedora}`.
       - Call `notificarRevendedora({ reseller_id, tipo: 'pontos_ganhos', titulo: 'Puntos ganados', mensagem: substitutedText, auth_user_id })`.
    5. In `conferirEFecharMaleta`:
       - After successful maleta closure, fetch template `acerto_confirmado`.
       - Substitute `{maleta_id}`, `{valor_comissao}`, `{nome_revendedora}`.
       - Call `notificarRevendedora` with tipo `acerto_confirmado`.
    6. In `submitDevolucao`:
       - After successful devolution submission, fetch template `devolucao_recebida`.
       - Substitute `{maleta_id}`, `{nome_revendedora}`.
       - Call `notificarRevendedora` with tipo `devolucao_recebida`.
    7. For each action: if template is inactive/missing, skip notification (do NOT send hardcoded fallback from action — the fallback lives in the template system).
    8. Ensure `ActionResult<T>` pattern from Phase 01 is maintained; notification failure should not fail the main action (wrap in try/catch).
    9. Run `npm run typecheck`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -q "notificarRevendedora" src/app/app/actions-revendedora.ts && echo "Notifications wired to PWA actions"</automated>
    <automated>grep -q "notificarRevendedora" src/app/admin/actions-maletas.ts && echo "Notifications wired to admin actions"</automated>
    <automated>grep -q "substituirVariaveis" src/app/app/actions-revendedora.ts && grep -q "substituirVariaveis" src/app/admin/actions-maletas.ts && echo "Variable substitution used"</automated>
  </verify>
  <done>
    - `registrarVenda`, `conferirEFecharMaleta`, `submitDevolucao` send templated notifications
    - Variable substitution uses `substituirVariaveis` with context data
    - Notification failures are caught and don't break main action flow
    - `npm run typecheck` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification of notification flows</name>
  <files>src/__tests__/integration/notifications.test.ts</files>
  <action>
    1. Create integration test file `src/__tests__/integration/notifications.test.ts` (or add to existing test structure).
    2. Test template substitution end-to-end:
       - Seed a `NotificacaoTemplate` with body "Hola {nome_revendedora}, tu maleta {maleta_id} vence en {dias_restantes} días.".
       - Call `substituirVariaveis` with context → verify output.
    3. Test cron job fallback:
       - When no active template exists, verify fallback text is used.
    4. Test action notification wiring (mock `notificarRevendedora` if needed):
       - Verify `registrarVenda` calls notification helper with correct tipo and substituted message.
    5. Run `npm test -- src/__tests__/integration/notifications.test.ts`.
    6. If integration tests are not feasible in current test setup, create a manual verification script in `scripts/verify-notifications.ts` that:
       - Reads templates from DB.
       - Substitutes variables for each tipo.
       - Prints results for human verification.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/integration/notifications.test.ts -x || echo "Integration tests run (may be skipped if not configured)"</automated>
    <automated>grep -q "substituirVariaveis.*notificarRevendedora\|notificarRevendedora.*substituirVariaveis" src/app/app/actions-revendedora.ts src/app/admin/actions-maletas.ts && echo "Wiring verified"</automated>
  </verify>
  <done>
    - Integration tests or verification script confirms template substitution works
    - All 3 server actions call `notificarRevendedora` with substituted content
    - Cron jobs use templates with fallback
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron job → OneSignal | Edge Function sends push via external API |
| Server Action → Notification | Action triggers notification to reseller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Template variables | accept | Variables are server-generated (maleta_id, pontos), not user input |
| T-05-02 | Denial of Service | Notification failure | mitigate | try/catch around notification calls prevents main action failure |
| T-05-03 | Tampering | Template substitution | accept | Whitelist from Plan 01 prevents injection of unexpected variables |
</threat_model>

<verification>
- `npm run typecheck` passes
- `grep -q "notificacao_templates" supabase/functions/check-maleta-prazo/index.ts`
- `grep -q "notificacao_templates" supabase/functions/marcar-maletas-atrasadas/index.ts`
- `grep -q "notificarRevendedora" src/app/app/actions-revendedora.ts`
- `grep -q "notificarRevendedora" src/app/admin/actions-maletas.ts`
- `grep -q "substituirVariaveis" src/app/app/actions-revendedora.ts`
</verification>

<success_criteria>
- Cron jobs `check-maleta-prazo` and `marcar-maletas-atrasadas` read `NotificacaoTemplate` by type
- Variable substitution applied before sending push notifications
- Fallback to default text when template is inactive or missing
- `registrarVenda`, `conferirEFecharMaleta`, `submitDevolucao` send templated notifications via `notificarRevendedora`
- Notification failures do not break the main business action
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-business-notifications-leads-config/02-05-SUMMARY.md`
</output>
