---
phase: 17
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/app/admin/config/cotizacion/page.tsx
  - src/app/admin/config/cotizacion/CotizacionClient.tsx
autonomous: true
requirements: [PDV-01]

must_haves:
  truths:
    - "Admin vê a rota /admin/config/cotizacion com formulário de BRL→Gs. e USD→Gs."
    - "Admin digita ambas as tasas e clica 'Guardar cotización' — novo registro CotizacionDia é criado"
    - "Após salvar, alerta de sucesso aparece e o histórico é atualizado mostrando o novo registro no topo"
    - "Histórico de cotizações exibe data/hora, BRL→Gs. e USD→Gs. das últimas 20 entradas"
    - "Empty state aparece quando não há cotizações configuradas"
    - "UI usa AdminPageHeader (NÃO o padrão legado admin-header)"
    - "Toda UI text em espanhol paraguaio per UI-SPEC"
  artifacts:
    - path: "src/app/admin/config/cotizacion/page.tsx"
      provides: "RSC que carrega cotização atual + histórico inicial"
      exports: ["default"]
    - path: "src/app/admin/config/cotizacion/CotizacionClient.tsx"
      provides: "Client Component com form + tabela de histórico"
      exports: ["default"]
  key_links:
    - from: "page.tsx"
      to: "actions-cotizacion.ts:getCotizacionAtual + getHistorialCotizaciones"
      via: "RSC pré-carrega dados; passa via props para o Client"
      pattern: "await getCotizacionAtual"
    - from: "CotizacionClient.tsx"
      to: "actions-cotizacion.ts:salvarCotizacion"
      via: "form submit dispara Server Action"
      pattern: "salvarCotizacion\\("
---

<objective>
Implementar a tela `/admin/config/cotizacion` (PDV-01) — formulário simples para o admin configurar as taxas BRL→Gs. e USD→Gs. + tabela com histórico das últimas 20 cotizações.

Purpose: Sem essa tela, o admin não tem como inserir cotação no sistema, e o PDV ficará bloqueado em produção (Pitfall — `criarVentaLoja` lança erro se não houver registro).

Output:
- `page.tsx` (RSC, force-dynamic) que pré-carrega cotização atual + histórico
- `CotizacionClient.tsx` (Client Component) com formulário e tabela de histórico
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md
@CLAUDE.md
@docs/design-system/tokens.md

<interfaces>
<!-- Tipos importados de Wave 1 (17-01) -->

From src/app/admin/actions-cotizacion.ts:
```typescript
export type CotizacionAtual = {
  id: string;
  brlToPyg: number;
  usdToPyg: number;
  createdAt: string; // ISO
};
export type CotizacionHistorialItem = { ... idêntico };
export async function salvarCotizacion(brlToPyg: number, usdToPyg: number): Promise<ActionResult<{ id: string }>>;
export async function getCotizacionAtual(): Promise<ActionResult<CotizacionAtual | null>>;
export async function getHistorialCotizaciones(limit?: number): Promise<ActionResult<CotizacionHistorialItem[]>>;
```

From src/components/admin/AdminPageHeader.tsx:
```typescript
type Props = {
  title: string;
  description?: string;
  breadcrumb?: string;
  backHref?: string;
  action?: ReactNode;
};
```

From src/components/admin/AdminEmptyState.tsx:
```typescript
type Props = {
  icon?: ComponentType<{ className?: string }>;
  title: string;
  description?: string;
  action?: ReactNode;
};
```
</interfaces>

@src/app/admin/actions-cotizacion.ts
@src/components/admin/AdminPageHeader.tsx
@src/components/admin/AdminEmptyState.tsx
@src/app/admin/config/comissoes/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Paper MCP review + RSC page.tsx</name>
  <files>src/app/admin/config/cotizacion/page.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (Copywriting Contract — /admin/config/cotizacion section; Component Inventory)
    - src/app/admin/config/comissoes/page.tsx (padrão de RSC config page; força-dynamic)
    - src/app/admin/actions-cotizacion.ts (assinaturas das funções importadas — criadas em 17-01)
    - src/components/admin/AdminPageHeader.tsx (props)
  </read_first>
  <action>
**STEP 0 — Paper MCP consultation (D-15-01, OBRIGATÓRIO):**

Antes de escrever JSX, consultar o Paper MCP via os comandos abaixo (ou nota se nenhum artboard existir):

1. Chamar `mcp__plugin_paper-desktop_paper__get_basic_info` para descobrir os artboards do projeto.
2. Procurar artboard correspondente a "Cotización" ou "Configuración del día".
3. Se encontrado: extrair tokens e verificar layout via `get_jsx` / `get_computed_styles`.
4. Se NÃO encontrado: registrar na SUMMARY que não há artboard Paper específico, e seguir o UI-SPEC textual como fonte visual.

Não inventar layouts — UI-SPEC é a fonte canônica de cópia/componentes desta fase. Paper, quando existir, refina visualmente.

**STEP 1 — Criar RSC `src/app/admin/config/cotizacion/page.tsx`:**

```typescript
import { getCotizacionAtual, getHistorialCotizaciones } from "@/app/admin/actions-cotizacion";
import CotizacionClient from "./CotizacionClient";

export const dynamic = "force-dynamic";

export default async function CotizacionPage() {
  const [atualResult, historialResult] = await Promise.all([
    getCotizacionAtual(),
    getHistorialCotizaciones(20),
  ]);

  const cotizacionAtual = atualResult.success ? atualResult.data : null;
  const historial = historialResult.success ? historialResult.data : [];

  return (
    <CotizacionClient
      cotizacionAtual={cotizacionAtual}
      historial={historial}
    />
  );
}
```

**Regras críticas:**
- `export const dynamic = "force-dynamic"` — obrigatório em rotas autenticadas `/admin/*` (CLAUDE.md §3.3).
- `Promise.all` para paralelizar as duas queries iniciais.
- NÃO usar try/catch aqui — `safeAction` já retorna `ActionResult<T>`; tratar success false como "sem dados" (passa null/[]).
- NÃO chamar `supabase.auth.getUser()` direto — `requireAuth` já é executado dentro das actions importadas.
  </action>
  <verify>
    <automated>test -f /Users/wesleyhudson/Documents/next-monarca/src/app/admin/config/cotizacion/page.tsx && grep -c 'force-dynamic' /Users/wesleyhudson/Documents/next-monarca/src/app/admin/config/cotizacion/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Paper MCP consultation registrada (artboard checado ou nota explícita de ausência)
    - File `src/app/admin/config/cotizacion/page.tsx` exists
    - File contains `export const dynamic = "force-dynamic"`
    - File contains `import { getCotizacionAtual, getHistorialCotizaciones } from "@/app/admin/actions-cotizacion"`
    - File contains `import CotizacionClient from "./CotizacionClient"`
    - File contains `Promise.all([` (parallel fetching)
    - File contains `export default async function`
    - File does NOT contain `"use client"` (RSC)
    - `npx tsc --noEmit` reports zero errors for this file
  </acceptance_criteria>
  <done>
    Paper consultado, `page.tsx` criado como RSC, força dinâmica configurada, dados pré-carregados em paralelo, props passados ao Client.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CotizacionClient.tsx — formulário + alertas + tabela de histórico</name>
  <files>src/app/admin/config/cotizacion/CotizacionClient.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (Copywriting Contract — /admin/config/cotizacion section completa; Color section para tokens)
    - src/app/admin/actions-cotizacion.ts (tipos CotizacionAtual, CotizacionHistorialItem; assinatura salvarCotizacion)
    - src/components/admin/AdminPageHeader.tsx
    - src/components/admin/AdminEmptyState.tsx
    - src/app/admin/config/comissoes/page.tsx (padrão de admin-card / admin-input / admin-btn — apenas para classes CSS reutilizáveis)
    - docs/design-system/tokens.md (tokens --admin-* disponíveis)
  </read_first>
  <action>
Criar Client Component com formulário (BRL + USD) e tabela de histórico.

**Conteúdo do arquivo `src/app/admin/config/cotizacion/CotizacionClient.tsx`:**

```typescript
"use client";

import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { Calendar, TrendingUp } from "lucide-react";
import AdminPageHeader from "@/components/admin/AdminPageHeader";
import AdminEmptyState from "@/components/admin/AdminEmptyState";
import { salvarCotizacion, type CotizacionAtual, type CotizacionHistorialItem } from "@/app/admin/actions-cotizacion";

type Props = {
  cotizacionAtual: CotizacionAtual | null;
  historial: CotizacionHistorialItem[];
};

const TZ = "America/Asuncion";

function formatGs(value: number): string {
  return new Intl.NumberFormat("es-PY", { maximumFractionDigits: 0, useGrouping: true }).format(value);
}

function formatTimestamp(iso: string): string {
  const date = new Date(iso);
  const datePart = new Intl.DateTimeFormat("es-PY", {
    timeZone: TZ,
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
  }).format(date);
  const timePart = new Intl.DateTimeFormat("es-PY", {
    timeZone: TZ,
    hour: "2-digit",
    minute: "2-digit",
    hour12: false,
  }).format(date);
  return `${datePart} a las ${timePart}`;
}

export default function CotizacionClient({ cotizacionAtual, historial }: Props) {
  const router = useRouter();
  const [brl, setBrl] = useState<string>(cotizacionAtual ? String(cotizacionAtual.brlToPyg) : "");
  const [usd, setUsd] = useState<string>(cotizacionAtual ? String(cotizacionAtual.usdToPyg) : "");
  const [feedback, setFeedback] = useState<{ kind: "success" | "error"; message: string } | null>(null);
  const [isPending, startTransition] = useTransition();

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setFeedback(null);

    const brlNum = Number(brl);
    const usdNum = Number(usd);
    if (!Number.isFinite(brlNum) || brlNum <= 0) {
      setFeedback({ kind: "error", message: "Ingresá un valor válido para BRL." });
      return;
    }
    if (!Number.isFinite(usdNum) || usdNum <= 0) {
      setFeedback({ kind: "error", message: "Ingresá un valor válido para USD." });
      return;
    }

    startTransition(async () => {
      const result = await salvarCotizacion(brlNum, usdNum);
      if (result.success) {
        setFeedback({ kind: "success", message: "Cotización guardada correctamente." });
        router.refresh();
      } else {
        setFeedback({
          kind: "error",
          message: result.error || "No se pudo guardar la cotización. Verificá los valores e intentá de nuevo.",
        });
      }
    });
  }

  return (
    <>
      <AdminPageHeader
        title="Cotización del Día"
        description="Ingresá las tasas de cambio vigentes. Cada guardado crea un nuevo registro — no se sobreescribe el historial."
        breadcrumb="Configuración"
      />

      <div className="admin-content">
        {/* Formulário */}
        <div className="admin-card">
          <form onSubmit={handleSubmit} className="flex flex-col gap-6">
            <div className="flex flex-col gap-3">
              <label htmlFor="cot-brl" className="text-sm" style={{ color: "var(--admin-text)" }}>
                1 Real (BRL) =
              </label>
              <div className="flex items-center gap-3">
                <input
                  id="cot-brl"
                  type="number"
                  className="admin-input"
                  value={brl}
                  onChange={(e) => setBrl(e.target.value)}
                  placeholder="Ej: 1400"
                  min="1"
                  step="0.01"
                  disabled={isPending}
                  required
                />
                <span className="text-sm" style={{ color: "var(--admin-text-muted)" }}>Gs.</span>
              </div>
            </div>

            <div className="flex flex-col gap-3">
              <label htmlFor="cot-usd" className="text-sm" style={{ color: "var(--admin-text)" }}>
                1 Dólar (USD) =
              </label>
              <div className="flex items-center gap-3">
                <input
                  id="cot-usd"
                  type="number"
                  className="admin-input"
                  value={usd}
                  onChange={(e) => setUsd(e.target.value)}
                  placeholder="Ej: 7500"
                  min="1"
                  step="0.01"
                  disabled={isPending}
                  required
                />
                <span className="text-sm" style={{ color: "var(--admin-text-muted)" }}>Gs.</span>
              </div>
            </div>

            <div className="flex items-center gap-4">
              <button
                type="submit"
                className="admin-btn admin-btn-primary"
                disabled={isPending}
              >
                {isPending ? "Guardando..." : "Guardar cotización"}
              </button>

              {cotizacionAtual && (
                <span className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
                  Última actualización: {formatTimestamp(cotizacionAtual.createdAt)}
                </span>
              )}
            </div>

            {feedback && (
              <div
                className={feedback.kind === "success" ? "admin-alert admin-alert-success" : "admin-alert admin-alert-error"}
                role="status"
                aria-live="polite"
              >
                {feedback.message}
              </div>
            )}
          </form>
        </div>

        {/* Histórico */}
        <section className="mt-8">
          <h2 className="text-base font-semibold mb-3" style={{ color: "var(--admin-text)" }}>
            Historial de cotizaciones
          </h2>

          {historial.length === 0 ? (
            <AdminEmptyState
              icon={Calendar}
              title="Sin registros anteriores."
              description="Todavía no hay cotizaciones registradas. Ingresá los valores actuales para empezar."
            />
          ) : (
            <div className="admin-card" style={{ padding: 0 }}>
              <table className="admin-table">
                <thead>
                  <tr>
                    <th>Fecha / hora</th>
                    <th>BRL → Gs.</th>
                    <th>USD → Gs.</th>
                  </tr>
                </thead>
                <tbody>
                  {historial.map((row) => (
                    <tr key={row.id}>
                      <td>{formatTimestamp(row.createdAt)}</td>
                      <td>{formatGs(row.brlToPyg)}</td>
                      <td>{formatGs(row.usdToPyg)}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </section>
      </div>
    </>
  );
}
```

**Regras críticas (CLAUDE.md §3.1, D-15-02):**
- ZERO hex hardcoded — toda cor via `var(--admin-*)` (CSS variables).
- ZERO px hardcoded — usar classes Tailwind (`p-4`, `gap-3`, `mt-8`) ou tokens.
- Idioma de UI: espanhol paraguaio EXATAMENTE conforme UI-SPEC (não traduzir, não substituir).
- `useTransition` para o submit (não bloqueia UI durante a Server Action).
- `router.refresh()` após sucesso — recarrega o RSC e atualiza histórico.
- `formatTimestamp` usa `timeZone: "America/Asuncion"` (UTC-3) — Open Question 3 do RESEARCH.md resolvida.
- Pre-popular campos com `cotizacionAtual` se existir (UX — admin pode ajustar do último valor sem retipar).
- `aria-live="polite"` no alerta de feedback.
- Empty state usa `AdminEmptyState` com cópia EXATA do UI-SPEC.

**Componentes/CSS classes do projeto que reaproveitar (sem criar novos):**
- `AdminPageHeader` (componente)
- `AdminEmptyState` (componente)
- `admin-content`, `admin-card`, `admin-input`, `admin-btn`, `admin-btn-primary`, `admin-alert`, `admin-alert-success`, `admin-alert-error`, `admin-table` (CSS classes existentes)
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "CotizacionClient" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/config/cotizacion/CotizacionClient.tsx` exists
    - File starts with `"use client";`
    - File contains `import AdminPageHeader from "@/components/admin/AdminPageHeader"`
    - File contains `import AdminEmptyState from "@/components/admin/AdminEmptyState"`
    - File contains `import { salvarCotizacion`
    - File contains exact text `Cotización del Día` (UI-SPEC)
    - File contains exact text `Guardar cotización`
    - File contains exact text `Cotización guardada correctamente.`
    - File contains exact text `1 Real (BRL) =`
    - File contains exact text `1 Dólar (USD) =`
    - File contains exact text `Historial de cotizaciones`
    - File contains exact text `Sin registros anteriores.`
    - File contains `useTransition` from react
    - File contains `salvarCotizacion(brlNum, usdNum)`
    - File contains `router.refresh()`
    - File contains `timeZone: "America/Asuncion"` (UTC-3 para timestamps)
    - File does NOT contain hex color literals: `grep -E '#[0-9a-fA-F]{3,8}' src/app/admin/config/cotizacion/CotizacionClient.tsx` returns 0 matches (excluding comments)
    - File contains `var(--admin-` (uses CSS variables)
    - `npx tsc --noEmit` reports zero errors for this file
    - `npm run lint` passes for this file
  </acceptance_criteria>
  <done>
    Tela de Cotización funcional: form salva, alerta aparece, histórico atualiza, empty state mostra quando vazio. Tokens CSS exclusivamente, espanhol paraguaio, AdminPageHeader.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → page.tsx (RSC) | Authenticated session passes through middleware + requireAuth in actions |
| browser → CotizacionClient form | Untrusted input (brl, usd values) crosses to Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | salvarCotizacion input | mitigate | Validação client-side (`Number.isFinite`, `> 0`) + validação server-side em `salvarCotizacion` (já implementada em 17-01) |
| T-17-09 | Elevation of Privilege | direct page access | mitigate | `requireAuth(["ADMIN"])` em todas as actions chamadas pela página |
| T-17-10 | Information Disclosure | console errors | accept | Sem PII na tela de cotização (apenas valores numéricos) |
</threat_model>

<verification>
- `/admin/config/cotizacion` carrega sem erros
- Submeter o formulário com valores válidos exibe alerta de sucesso e atualiza a tabela
- Submeter com valor inválido (negativo, vazio) mostra alerta de erro inline
- Tabela exibe os timestamps em formato `DD/MM/YYYY a las HH:mm` no timezone Assunção
- Empty state aparece quando não há registros
- `npx tsc --noEmit` zero erros
- `npm run lint` passa
- Zero hex hardcoded no JSX
</verification>

<success_criteria>
- Tela `/admin/config/cotizacion` funcional ponta-a-ponta
- Form salva via `salvarCotizacion`, alerta exibido, histórico atualiza
- AdminPageHeader usado (não padrão legado)
- Espanhol paraguaio em 100% da UI
- Tokens CSS `--admin-*` exclusivamente; zero hex/px hardcoded
- Paper MCP consultado e registrado
</success_criteria>

<output>
After completion, create `.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-02-SUMMARY.md`
</output>
