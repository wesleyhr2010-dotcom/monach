---
phase: 17
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified:
  - src/app/admin/pdv/PdvClient.tsx
  - src/app/admin/pdv/PdvStepMoneda.tsx
  - src/app/admin/pdv/PdvStepResumen.tsx
  - src/app/admin/pdv/PdvSplashSuccess.tsx
autonomous: true
requirements: [PDV-02, PDV-05, PDV-06]

must_haves:
  truths:
    - "Step 3 Moneda: admin escolhe entre Guaraní (default), Dólar, Real via grupo de cards radio"
    - "Step 3 exibe linha de cotização '1 USD = 7.800 Gs. · Actualizado 08/05 14:30' no formato canônico (UI-SPEC)"
    - "Step 3 exibe total estimado em PYG calculado client-side com snapshot da cotização"
    - "Step 3 alerta warning quando não há cotização registrada"
    - "Step 4 Resumen: tabela de itens (Producto/Cant./Precio unit./Subtotal), seção cliente, seção moneda, total final"
    - "Step 4 botão 'Confirmar venta' (var(--admin-success) bg) chama criarVentaLoja"
    - "Após sucesso, splash substitui os steps in-place — NÃO usa router.push (D-17-PDV-04)"
    - "Splash exibe ícone CheckCircle 64px, '¡Venta registrada!', total e cliente; botão 'Nueva venta' reseta o PDV"
  artifacts:
    - path: "src/app/admin/pdv/PdvStepMoneda.tsx"
      provides: "Step 3 — seleção de moeda + display de cotização"
      exports: ["default"]
    - path: "src/app/admin/pdv/PdvStepResumen.tsx"
      provides: "Step 4 — tabela de resumo + Confirmar venta"
      exports: ["default"]
    - path: "src/app/admin/pdv/PdvSplashSuccess.tsx"
      provides: "Splash de sucesso com Nueva venta + Ver detalles"
      exports: ["default"]
    - path: "src/app/admin/pdv/PdvClient.tsx"
      provides: "Atualizado: importa os 3 novos componentes; remove placeholders"
      exports: ["default"]
  key_links:
    - from: "PdvStepResumen.tsx"
      to: "actions-pdv.ts:criarVentaLoja"
      via: "submit do botão 'Confirmar venta'"
      pattern: "criarVentaLoja\\("
    - from: "PdvClient.tsx"
      to: "PdvSplashSuccess.tsx"
      via: "showSplash state truthy → renderiza splash em vez dos steps"
      pattern: "showSplash"
    - from: "PdvStepMoneda.tsx"
      to: "props.cotizacionAtual"
      via: "snapshot lido pelo RSC inicial em page.tsx (Wave 2)"
      pattern: "cotizacionAtual"
---

<objective>
Completar o PDV: Step 3 (Moneda + display de cotização), Step 4 (Resumen + Confirmar venta) e Splash de sucesso. Atualizar `PdvClient.tsx` para integrar os 3 novos componentes em substituição aos placeholders criados em 17-03.

Purpose: Sem este plan, o PDV está pela metade — a venda não pode ser confirmada e o ciclo de "Nueva venta" não fecha. Este plan entrega o fluxo PDV completo ponta-a-ponta.

Output:
- `PdvStepMoneda.tsx`: Step 3 — radio cards de PYG/USD/BRL + linha de cotização + total estimado
- `PdvStepResumen.tsx`: Step 4 — tabela de itens + total + botão "Confirmar venta"
- `PdvSplashSuccess.tsx`: tela de sucesso pós-confirmação
- `PdvClient.tsx`: atualizado para chamar `criarVentaLoja` e renderizar o splash
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Tipos importados de 17-03 -->

From src/app/admin/pdv/PdvClient.tsx (criado em 17-03):
```typescript
export type ClienteSeleccionado =
  | { tipo: "cliente"; id: string; nombre: string; ruc: string | null; ciudad: string | null }
  | { tipo: "consumidor_final" };

export type CarritoItem = {
  variantId: string;
  productId: string;
  productName: string;
  productImage: string | null;
  attributeName: string;
  attributeValue: string;
  precioPyg: number;
  cantidad: number;
  stockMaximo: number;
};

export type Moneda = "PYG" | "USD" | "BRL";
```

From src/app/admin/actions-pdv.ts (17-01):
```typescript
export type CriarVentaLojaInput = {
  clienteId: string | null;
  moneda: "PYG" | "USD" | "BRL";
  itens: Array<{ product_variant_id: string; cantidad: number }>;
};
export async function criarVentaLoja(input: CriarVentaLojaInput): Promise<ActionResult<{ id: string }>>;
```

From src/app/admin/actions-cotizacion.ts (17-01):
```typescript
export type CotizacionAtual = { id: string; brlToPyg: number; usdToPyg: number; createdAt: string };
```
</interfaces>

@src/app/admin/pdv/PdvClient.tsx
@src/app/admin/pdv/PdvStepCliente.tsx
@src/app/admin/pdv/PdvStepProductos.tsx
@src/app/admin/actions-pdv.ts
@src/app/admin/actions-cotizacion.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Paper MCP review + PdvStepMoneda.tsx (radio cards + display de cotização)</name>
  <files>src/app/admin/pdv/PdvStepMoneda.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (sections: Step 3 — Moneda completa; Interaction Specs — Exchange Rate Display)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md (D-17-04 — Math.round por linha)
    - src/app/admin/actions-cotizacion.ts (tipo CotizacionAtual)
    - src/app/admin/pdv/PdvClient.tsx (tipos CarritoItem, Moneda)
  </read_first>
  <action>
**STEP 0 — Paper MCP consultation (D-15-01):**

Verificar Paper para artboards "PDV Step 3" / "Moneda" / "Cambio". Registrar consulta na SUMMARY.

**STEP 1 — Criar `src/app/admin/pdv/PdvStepMoneda.tsx`:**

```typescript
"use client";

import type { CarritoItem, Moneda } from "./PdvClient";
import type { CotizacionAtual } from "@/app/admin/actions-cotizacion";

type Props = {
  carrito: CarritoItem[];
  moneda: Moneda;
  setMoneda: (m: Moneda) => void;
  cotizacionAtual: CotizacionAtual | null;
};

const TZ = "America/Asuncion";

function formatGs(value: number): string {
  return new Intl.NumberFormat("es-PY", { maximumFractionDigits: 0, useGrouping: true }).format(value);
}

function formatCotizacionDate(iso: string): string {
  const date = new Date(iso);
  const dd = new Intl.DateTimeFormat("es-PY", { timeZone: TZ, day: "2-digit" }).format(date);
  const mm = new Intl.DateTimeFormat("es-PY", { timeZone: TZ, month: "2-digit" }).format(date);
  const time = new Intl.DateTimeFormat("es-PY", { timeZone: TZ, hour: "2-digit", minute: "2-digit", hour12: false }).format(date);
  return `${dd}/${mm} ${time}`;
}

const MONEDAS: { value: Moneda; label: string }[] = [
  { value: "PYG", label: "Guaraní (Gs.)" },
  { value: "USD", label: "Dólar (USD)" },
  { value: "BRL", label: "Real (BRL)" },
];

export default function PdvStepMoneda({ carrito, moneda, setMoneda, cotizacionAtual }: Props) {
  const totalPyg = carrito.reduce((acc, c) => acc + Math.round(c.precioPyg * c.cantidad), 0);

  // Conversão para moeda selecionada (preserva PYG como fonte de verdade)
  let totalConvertido: number | null = totalPyg;
  if (moneda !== "PYG") {
    if (!cotizacionAtual) {
      totalConvertido = null;
    } else {
      const rate = moneda === "USD" ? cotizacionAtual.usdToPyg : cotizacionAtual.brlToPyg;
      totalConvertido = rate > 0 ? Math.round((totalPyg / rate) * 100) / 100 : null;
    }
  }

  return (
    <div className="admin-card">
      <h2 className="text-base font-semibold mb-3" style={{ color: "var(--admin-text)" }}>
        ¿En qué moneda se paga?
      </h2>

      {/* Radio cards */}
      <div className="grid grid-cols-1 gap-3 sm:grid-cols-3" role="radiogroup" aria-label="Moneda">
        {MONEDAS.map((m) => {
          const selected = moneda === m.value;
          return (
            <button
              key={m.value}
              type="button"
              role="radio"
              aria-checked={selected}
              onClick={() => setMoneda(m.value)}
              className="p-4 rounded text-left"
              style={{
                background: "var(--admin-surface)",
                border: `1px solid ${selected ? "var(--admin-accent)" : "var(--admin-border)"}`,
                color: "var(--admin-text)",
              }}
            >
              <span className="text-sm font-semibold">{m.label}</span>
            </button>
          );
        })}
      </div>

      {/* Display de cotização */}
      <div className="mt-6 p-4 rounded" style={{ background: "var(--admin-surface)", border: "1px solid var(--admin-border)" }}>
        {cotizacionAtual ? (
          <div className="flex flex-col gap-1">
            <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
              1 USD = {formatGs(cotizacionAtual.usdToPyg)} Gs. · Actualizado {formatCotizacionDate(cotizacionAtual.createdAt)}
            </p>
            <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
              1 BRL = {formatGs(cotizacionAtual.brlToPyg)} Gs.
            </p>
          </div>
        ) : (
          <div className="admin-alert admin-alert-warning">
            Sin cotización registrada hoy. Pedile al admin que la actualice.
          </div>
        )}
      </div>

      {/* Total estimado */}
      <div className="mt-6">
        <p className="text-sm" style={{ color: "var(--admin-text-muted)" }}>
          {moneda === "PYG" ? "Total estimado en Gs." : `Total estimado en ${moneda}`}
        </p>
        <p className="text-2xl font-semibold mt-1" style={{ color: "var(--admin-text)" }}>
          {totalConvertido != null
            ? moneda === "PYG"
              ? `${formatGs(totalConvertido)} Gs.`
              : `${totalConvertido.toFixed(2)} ${moneda}`
            : "—"}
        </p>
        <p className="text-xs mt-2" style={{ color: "var(--admin-text-muted)" }}>
          Los precios del sistema están en Gs. Esta cotización es orientativa.
        </p>
      </div>
    </div>
  );
}
```

**Regras críticas:**
- `Math.round(c.precioPyg * c.cantidad)` por linha (D-17-04).
- Conversão: `totalPyg / rate` para USD/BRL com 2 casas decimais.
- `role="radiogroup"` e `role="radio"` + `aria-checked` para acessibilidade.
- Cópia ES-PY EXATA conforme UI-SPEC (não traduzir).
- ZERO hex hardcoded (D-15-02).
- Borda do card selecionado: `var(--admin-accent)`; não selecionado: `var(--admin-border)`.
- Display de cotização sempre mostra USD e BRL (UI-SPEC: ambos quando moneda != PYG, mas para simplicidade do executor, sempre mostramos os dois — admin sempre quer saber).
- Fallback "—" no total se cotização ausente e moeda != PYG.
- Timezone: `America/Asuncion`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "PdvStepMoneda" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/pdv/PdvStepMoneda.tsx` exists, starts with `"use client";`
    - File contains `import type { CarritoItem, Moneda }`
    - File contains `import type { CotizacionAtual }`
    - File contains exact text `¿En qué moneda se paga?`
    - File contains exact text `Guaraní (Gs.)`
    - File contains exact text `Dólar (USD)`
    - File contains exact text `Real (BRL)`
    - File contains exact text `Sin cotización registrada hoy. Pedile al admin que la actualice.`
    - File contains exact text `Los precios del sistema están en Gs. Esta cotización es orientativa.`
    - File contains exact text `Total estimado en Gs.`
    - File contains `role="radiogroup"`
    - File contains `role="radio"`
    - File contains `aria-checked={selected}`
    - File contains `Math.round(c.precioPyg * c.cantidad)`
    - File contains `timeZone: TZ` and `TZ = "America/Asuncion"`
    - File contains `cotizacionAtual.usdToPyg` and `cotizacionAtual.brlToPyg`
    - File contains `var(--admin-accent)` (border do card selecionado)
    - No hex literals: `grep -E '#[0-9a-fA-F]{3,8}' src/app/admin/pdv/PdvStepMoneda.tsx` returns 0
    - `npx tsc --noEmit` reports zero errors for this file
  </acceptance_criteria>
  <done>
    Step 3 funcional: radio cards de moeda, display de cotização no formato canônico UI-SPEC, total estimado convertido client-side, alerta quando sem cotização.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PdvStepResumen.tsx + PdvSplashSuccess.tsx + integração final em PdvClient.tsx</name>
  <files>src/app/admin/pdv/PdvStepResumen.tsx, src/app/admin/pdv/PdvSplashSuccess.tsx, src/app/admin/pdv/PdvClient.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (sections: Step 4 — Resumen completa; Splash de sucesso; Interaction Specs — Success Splash D-17-PDV-04)
    - src/app/admin/pdv/PdvClient.tsx (estrutura atual com placeholders, exports de tipos)
    - src/app/admin/actions-pdv.ts (assinatura criarVentaLoja, tipo CriarVentaLojaInput)
  </read_first>
  <action>
**STEP 1 — Criar `src/app/admin/pdv/PdvSplashSuccess.tsx`:**

```typescript
"use client";

import Link from "next/link";
import { CheckCircle } from "lucide-react";

type Props = {
  ventaId: string;
  totalPyg: number;
  clienteLabel: string;
  onNuevaVenta: () => void;
};

function formatGs(value: number): string {
  return new Intl.NumberFormat("es-PY", { maximumFractionDigits: 0, useGrouping: true }).format(value);
}

export default function PdvSplashSuccess({ ventaId, totalPyg, clienteLabel, onNuevaVenta }: Props) {
  return (
    <div
      className="admin-card flex flex-col items-center text-center p-8"
      style={{ background: "var(--admin-success-10)", border: "1px solid var(--admin-success)" }}
    >
      <CheckCircle style={{ color: "var(--admin-success)", width: "64px", height: "64px" }} />

      <h2 className="text-2xl font-semibold mt-6" style={{ color: "var(--admin-text)" }}>
        ¡Venta registrada!
      </h2>
      <p className="text-sm mt-2" style={{ color: "var(--admin-text-muted)" }}>
        La venta fue guardada correctamente.
      </p>

      <div className="mt-6 flex flex-col gap-1">
        <p className="text-sm" style={{ color: "var(--admin-text)" }}>
          Total: {formatGs(totalPyg)} Gs.
        </p>
        <p className="text-sm" style={{ color: "var(--admin-text)" }}>
          Cliente: {clienteLabel}
        </p>
      </div>

      <div className="flex gap-3 mt-8">
        <button type="button" className="admin-btn admin-btn-primary" onClick={onNuevaVenta}>
          Nueva venta
        </button>
        <Link href={`/admin/ventas/${ventaId}`} className="admin-btn admin-btn-secondary">
          Ver detalles
        </Link>
      </div>
    </div>
  );
}
```

**STEP 2 — Criar `src/app/admin/pdv/PdvStepResumen.tsx`:**

```typescript
"use client";

import { useState, useTransition } from "react";
import { criarVentaLoja } from "@/app/admin/actions-pdv";
import type { ClienteSeleccionado, CarritoItem, Moneda } from "./PdvClient";
import type { CotizacionAtual } from "@/app/admin/actions-cotizacion";

type Props = {
  cliente: ClienteSeleccionado;
  carrito: CarritoItem[];
  moneda: Moneda;
  cotizacionAtual: CotizacionAtual | null;
  onSuccess: (info: { ventaId: string; totalPyg: number; clienteLabel: string }) => void;
};

function formatGs(value: number): string {
  return new Intl.NumberFormat("es-PY", { maximumFractionDigits: 0, useGrouping: true }).format(value);
}

function monedaLabel(m: Moneda): string {
  if (m === "PYG") return "Guaraní (Gs.)";
  if (m === "USD") return "Dólar (USD)";
  return "Real (BRL)";
}

export default function PdvStepResumen({ cliente, carrito, moneda, cotizacionAtual, onSuccess }: Props) {
  const [error, setError] = useState<string | null>(null);
  const [isPending, startTransition] = useTransition();

  const totalPyg = carrito.reduce((acc, c) => acc + Math.round(c.precioPyg * c.cantidad), 0);

  let totalConvertido: number = totalPyg;
  if (moneda !== "PYG" && cotizacionAtual) {
    const rate = moneda === "USD" ? cotizacionAtual.usdToPyg : cotizacionAtual.brlToPyg;
    if (rate > 0) totalConvertido = Math.round((totalPyg / rate) * 100) / 100;
  }

  const clienteLabel = cliente.tipo === "cliente" ? cliente.nombre : "Consumidor Final";

  function handleConfirm() {
    setError(null);

    if (carrito.length === 0) {
      setError("Agregá al menos un producto a la venta.");
      return;
    }
    if (moneda !== "PYG" && !cotizacionAtual) {
      setError("No hay cotización registrada. Configurá la cotización antes de registrar ventas.");
      return;
    }

    startTransition(async () => {
      const result = await criarVentaLoja({
        clienteId: cliente.tipo === "cliente" ? cliente.id : null,
        moneda,
        itens: carrito.map((c) => ({
          product_variant_id: c.variantId,
          cantidad: c.cantidad,
        })),
      });

      if (!result.success) {
        setError(result.error || "No se pudo registrar la venta. Intentá de nuevo o verificá el stock disponible.");
        return;
      }

      onSuccess({
        ventaId: result.data.id,
        totalPyg,
        clienteLabel,
      });
    });
  }

  return (
    <div className="admin-card">
      <h2 className="text-base font-semibold mb-3" style={{ color: "var(--admin-text)" }}>
        Revisá la venta antes de confirmar
      </h2>

      {/* Cliente */}
      <section className="mb-4">
        <p className="text-xs uppercase tracking-wider mb-2" style={{ color: "var(--admin-text-muted)" }}>Cliente</p>
        <div className="p-3 rounded" style={{ background: "var(--admin-bg)" }}>
          <p className="text-sm font-semibold" style={{ color: "var(--admin-text)" }}>
            {clienteLabel}
          </p>
          {cliente.tipo === "cliente" && (
            <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
              RUC: {cliente.ruc ?? "—"}{cliente.ciudad ? ` · ${cliente.ciudad}` : ""}
            </p>
          )}
        </div>
      </section>

      {/* Productos */}
      <section className="mb-4">
        <p className="text-xs uppercase tracking-wider mb-2" style={{ color: "var(--admin-text-muted)" }}>Productos</p>
        <div className="admin-card" style={{ padding: 0 }}>
          <table className="admin-table">
            <thead>
              <tr>
                <th>Producto</th>
                <th>Cant.</th>
                <th>Precio unit.</th>
                <th>Subtotal</th>
              </tr>
            </thead>
            <tbody>
              {carrito.map((c) => (
                <tr key={c.variantId}>
                  <td>
                    <span style={{ color: "var(--admin-text)" }}>{c.productName}</span>
                    <br />
                    <span className="text-xs" style={{ color: "var(--admin-text-muted)" }}>{c.attributeValue}</span>
                  </td>
                  <td>{c.cantidad}</td>
                  <td>{formatGs(c.precioPyg)} Gs.</td>
                  <td>{formatGs(Math.round(c.precioPyg * c.cantidad))} Gs.</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </section>

      {/* Moneda */}
      <section className="mb-4">
        <p className="text-xs uppercase tracking-wider mb-2" style={{ color: "var(--admin-text-muted)" }}>Moneda de pago</p>
        <p className="text-sm" style={{ color: "var(--admin-text)" }}>{monedaLabel(moneda)}</p>
      </section>

      {/* Total */}
      <div
        className="flex items-center justify-between p-4 rounded"
        style={{ background: "var(--admin-bg)", borderTop: "1px solid var(--admin-border)" }}
      >
        <span className="text-sm" style={{ color: "var(--admin-text)" }}>Total</span>
        <span className="text-base font-semibold" style={{ color: "var(--admin-accent)" }}>
          {moneda === "PYG"
            ? `${formatGs(totalConvertido)} Gs.`
            : `${totalConvertido.toFixed(2)} ${moneda}`}
        </span>
      </div>

      {/* Aviso */}
      <div className="admin-alert admin-alert-warning mt-4">
        Al confirmar, el stock de los productos seleccionados se actualizará automáticamente.
      </div>

      {/* Erro */}
      {error && <div className="admin-alert admin-alert-error mt-3">{error}</div>}

      {/* Confirmar */}
      <button
        type="button"
        className="admin-btn admin-btn-primary mt-6 w-full sm:w-auto"
        onClick={handleConfirm}
        disabled={isPending}
        style={{ background: "var(--admin-success)", color: "var(--admin-bg)" }}
      >
        {isPending ? "Procesando..." : "Confirmar venta"}
      </button>
    </div>
  );
}
```

**STEP 3 — Atualizar `src/app/admin/pdv/PdvClient.tsx`:**

Substituir os placeholders pelos componentes reais. Mudanças exatas:

1. Adicionar imports no topo:
```typescript
import PdvStepMoneda from "./PdvStepMoneda";
import PdvStepResumen from "./PdvStepResumen";
import PdvSplashSuccess from "./PdvSplashSuccess";
```

2. Remover as funções `PlaceholderStep` e `PlaceholderSplash` no final do arquivo.

3. Substituir o bloco de placeholder do step 2 (Moneda):
```typescript
{step === 2 && (
  <PdvStepMoneda
    carrito={carrito}
    moneda={moneda}
    setMoneda={setMoneda}
    cotizacionAtual={cotizacionAtual}
  />
)}
```

4. Substituir o bloco de placeholder do step 3 (Resumen):
```typescript
{step === 3 && cliente && (
  <PdvStepResumen
    cliente={cliente}
    carrito={carrito}
    moneda={moneda}
    cotizacionAtual={cotizacionAtual}
    onSuccess={(info) => setShowSplash(info)}
  />
)}
```

5. Substituir o bloco do splash placeholder:
```typescript
{showSplash ? (
  <PdvSplashSuccess
    ventaId={showSplash.ventaId}
    totalPyg={showSplash.totalPyg}
    clienteLabel={showSplash.clienteLabel}
    onNuevaVenta={resetPdv}
  />
) : (
  // ... resto do JSX existente
)}
```

6. Esconder o botão "Siguiente" no Step 3 (Resumen) — já é o último step navegável; o botão "Confirmar venta" dentro do PdvStepResumen substitui:
```typescript
{step < PDV_STEPS.length - 1 && step !== PDV_STEPS.length - 1 && (
  <button ... >Siguiente →</button>
)}
```

Atualizar `canAdvance` para Step 2 (Moneda):
```typescript
if (step === 2) {
  // pode avançar se PYG ou se há cotização registrada
  return moneda === "PYG" || cotizacionAtual !== null;
}
```

**Regras críticas:**
- Botão "Confirmar venta" usa `var(--admin-success)` bg + `var(--admin-bg)` text (UI-SPEC — único CTA com success bg, intencionalmente diferenciado).
- Mapeamento de `CarritoItem` para `CriarVentaLojaInput.itens`: somente `product_variant_id` e `cantidad` enviados (NUNCA enviar preço ou cotização — D-17-03).
- `clienteId: cliente.tipo === "cliente" ? cliente.id : null` (D-17-PDV-01).
- Splash substitui IN-PLACE — NÃO usar `router.push` (D-17-PDV-04).
- "Nueva venta" chama `resetPdv()` que reseta tudo: step=0, cliente=null, carrito=[], moneda="PYG", showSplash=null.
- "Ver detalles" usa `Link` para `/admin/ventas/{id}` (rota futura, OK ter 404 por enquanto — UI-SPEC permite).
- Cópia ES-PY EXATA conforme UI-SPEC.
- ZERO hex hardcoded (D-15-02).
- `aria-live` no alerta de erro (heredo via `admin-alert` se aplicável; se não, adicionar `role="alert"`).

**Importante:** O step 3 só renderiza se `cliente !== null` (guard via `step === 3 && cliente`). Se cliente foi resetado por algum motivo, o componente não monta.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "pdv/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/pdv/PdvSplashSuccess.tsx` exists, starts with `"use client";`
    - PdvSplashSuccess.tsx contains exact text `¡Venta registrada!`
    - PdvSplashSuccess.tsx contains exact text `La venta fue guardada correctamente.`
    - PdvSplashSuccess.tsx contains exact text `Nueva venta`
    - PdvSplashSuccess.tsx contains exact text `Ver detalles`
    - PdvSplashSuccess.tsx contains `import { CheckCircle }`
    - PdvSplashSuccess.tsx contains `width: "64px", height: "64px"` (icon size per UI-SPEC)
    - PdvSplashSuccess.tsx contains `Link` (next/link) with `/admin/ventas/`
    - File `src/app/admin/pdv/PdvStepResumen.tsx` exists, starts with `"use client";`
    - PdvStepResumen.tsx contains `import { criarVentaLoja }`
    - PdvStepResumen.tsx contains exact text `Revisá la venta antes de confirmar`
    - PdvStepResumen.tsx contains exact text `Confirmar venta`
    - PdvStepResumen.tsx contains exact text `Procesando...`
    - PdvStepResumen.tsx contains exact text `Al confirmar, el stock de los productos seleccionados se actualizará automáticamente.`
    - PdvStepResumen.tsx contains exact text `No se pudo registrar la venta. Intentá de nuevo o verificá el stock disponible.`
    - PdvStepResumen.tsx contains exact text `Producto`, `Cant.`, `Precio unit.`, `Subtotal` (table headers)
    - PdvStepResumen.tsx contains exact text `Moneda de pago`
    - PdvStepResumen.tsx contains exact text `Total`
    - PdvStepResumen.tsx contains exact text `Consumidor Final`
    - PdvStepResumen.tsx contains `clienteId: cliente.tipo === "cliente" ? cliente.id : null`
    - PdvStepResumen.tsx contains `product_variant_id: c.variantId`
    - PdvStepResumen.tsx does NOT pass `brlToPyg` or `usdToPyg` to criarVentaLoja (D-17-03)
    - PdvStepResumen.tsx contains `background: "var(--admin-success)"` for confirm button
    - File `src/app/admin/pdv/PdvClient.tsx` updated:
      - imports `PdvStepMoneda`, `PdvStepResumen`, `PdvSplashSuccess`
      - does NOT contain `PlaceholderStep` or `PlaceholderSplash` anymore (`grep -c "Placeholder" src/app/admin/pdv/PdvClient.tsx` returns 0)
      - contains `<PdvStepMoneda` and `<PdvStepResumen` and `<PdvSplashSuccess`
      - canAdvance step 2 returns true if `moneda === "PYG" || cotizacionAtual !== null`
    - No hex literals in any new files: `grep -rE '#[0-9a-fA-F]{3,8}' src/app/admin/pdv/PdvStepMoneda.tsx src/app/admin/pdv/PdvStepResumen.tsx src/app/admin/pdv/PdvSplashSuccess.tsx` returns 0
    - `npx tsc --noEmit` reports zero errors for all pdv files
    - `npm run lint` passes for all new files
  </acceptance_criteria>
  <done>
    Step 4 funcional: tabela de resumo, total final em destaque, botão Confirmar venta chama criarVentaLoja, erro tratado, splash de sucesso aparece in-place. Nueva venta reseta o PDV. Ver detalles linka para /admin/ventas/{id}. Toda cópia ES-PY EXATA. Zero hex hardcoded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → criarVentaLoja | Untrusted input (clienteId, moneda, itens) crosses to Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Tampering | criarVentaLoja payload (rate, price) | mitigate | UI envia APENAS `clienteId`, `moneda`, `itens[].product_variant_id`, `itens[].cantidad`. Server lê preço e cotização do DB (D-17-03). Confirmado em 17-01 |
| T-17-16 | Tampering | clienteId injection | mitigate | Server valida existência do cliente antes de criar venda (já implementado em 17-01) |
| T-17-17 | Repudiation | venda confirmada | mitigate | `created_by: user.profileId` salvo na VentaLoja; `cotizacion_snapshot` é JSON imutável (D-17-02) |
| T-17-18 | Information Disclosure | erro genérico | mitigate | Mensagem de erro server-side genérica ("verificá el stock disponible") sem expor detalhes internos |
</threat_model>

<verification>
- Step 3 renderiza radio cards de moeda com PYG selecionado por padrão; admin troca para USD/BRL
- Display de cotização mostra "1 USD = 7.800 Gs. · Actualizado 08/05 14:30" no formato canônico
- Total estimado em PYG é calculado client-side e atualiza ao trocar moeda
- Quando sem cotização, alerta warning aparece e total mostra "—"
- Step 4: tabela exibe itens, totais batem com soma de subtotais
- Clicar "Confirmar venta" chama `criarVentaLoja` e exibe splash em caso de sucesso
- Splash exibe ícone 64px, "¡Venta registrada!", total, cliente label
- "Nueva venta" reseta o PDV completamente (step=0, sem cliente, carrito vazio)
- Erro retornado pelo server é exibido inline; admin pode tentar de novo
- Nenhum hex hardcoded
- `npx tsc --noEmit` zero erros
</verification>

<success_criteria>
- 3 novos componentes criados: PdvStepMoneda, PdvStepResumen, PdvSplashSuccess
- PdvClient.tsx atualizado para integrar os 3 e remover placeholders
- Fluxo PDV completo funcional ponta-a-ponta
- `criarVentaLoja` chamado com payload mínimo (apenas IDs e cantidad)
- Splash in-place (sem router.push)
- "Nueva venta" reseta o PDV
- ES-PY EXATO; zero hex; touch targets corretos; aria adequado
- Paper MCP consultado e registrado
</success_criteria>

<output>
After completion, create `.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-04-SUMMARY.md`
</output>
