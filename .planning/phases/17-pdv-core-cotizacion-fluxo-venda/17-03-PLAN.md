---
phase: 17
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/app/admin/pdv/page.tsx
  - src/app/admin/pdv/PdvClient.tsx
  - src/app/admin/pdv/PdvStepCliente.tsx
  - src/app/admin/pdv/PdvStepProductos.tsx
autonomous: true
requirements: [PDV-03, PDV-04]

must_haves:
  truths:
    - "Admin acessa /admin/pdv e vê AdminStepIndicator com 4 steps (Cliente / Productos / Moneda / Resumen)"
    - "Step 1 Cliente: admin digita RUC → busca por debounce 400ms → encontra cliente OU vê opção de registrar inline OU pula como Consumidor Final"
    - "Step 1 mini-form inline cria cliente novo via criarCliente sem sair do PDV"
    - "Step 2 Productos: admin busca por nome (debounce) e/ou filtra por categoria → lista de variantes carregada via getVariantsParaPdv"
    - "Step 2 carrinho lateral: admin pode adicionar/remover/incrementar/decrementar produtos; bloqueio +/- ao atingir stock_quantity"
    - "Estado do PDV mantido em PdvClient.tsx (cliente, carrito, step atual); steps 3-4 são adicionados em 17-04"
    - "Botões de navegação 'Anterior'/'Siguiente' funcionam entre os steps existentes; 'Siguiente' bloqueado se step atual incompleto"
  artifacts:
    - path: "src/app/admin/pdv/page.tsx"
      provides: "RSC: pré-carrega cotização atual + categorias"
      exports: ["default"]
    - path: "src/app/admin/pdv/PdvClient.tsx"
      provides: "Client Component com state global do PDV (cliente, carrito, step, moneda)"
      exports: ["default"]
    - path: "src/app/admin/pdv/PdvStepCliente.tsx"
      provides: "Step 1 — busca de RUC + mini-form de criação inline + Consumidor Final"
      exports: ["default"]
    - path: "src/app/admin/pdv/PdvStepProductos.tsx"
      provides: "Step 2 — busca/filtro de produtos + carrinho lateral"
      exports: ["default"]
  key_links:
    - from: "PdvStepCliente.tsx"
      to: "actions-clientes.ts:buscarClientePorRuc + criarCliente"
      via: "debounce 400ms para busca; submit do mini-form para criação inline"
      pattern: "buscarClientePorRuc\\(|criarCliente\\("
    - from: "PdvStepProductos.tsx"
      to: "actions-pdv.ts:getVariantsParaPdv"
      via: "debounce 400ms ou troca de categoria"
      pattern: "getVariantsParaPdv\\("
    - from: "PdvClient.tsx state"
      to: "PdvStepProductos / PdvStepCliente"
      via: "props (cliente, setCliente, carrito, setCarrito)"
      pattern: "setCarrito|setCliente"
---

<objective>
Implementar a base do PDV em `/admin/pdv` (PDV-03 + PDV-04): page.tsx (RSC), PdvClient.tsx (estado e navegação) e os dois primeiros steps (Cliente, Productos).

Purpose: O PDV é o coração da Fase 17. Steps 3 (Moneda), 4 (Resumen) e Splash de sucesso ficam em 17-04 — esta divisão permite que cada plan caiba em ~50% de contexto sem degradar a qualidade.

Output:
- `page.tsx` (RSC): pré-carrega cotização + categorias
- `PdvClient.tsx`: state global + AdminStepIndicator + roteamento entre os 4 steps + botões Anterior/Siguiente (com placeholders para steps 3-4)
- `PdvStepCliente.tsx`: Step 1 completo (busca, mini-form, Consumidor Final)
- `PdvStepProductos.tsx`: Step 2 completo (busca, filtro, lista, carrinho)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md
@CLAUDE.md
@docs/design-system/tokens.md

<interfaces>
<!-- DTOs e actions importadas (criados em 17-01 + Phase 16) -->

From src/app/admin/actions-pdv.ts (criado em 17-01):
```typescript
export type VariantParaPdv = {
  id: string;
  attribute_name: string;
  attribute_value: string;
  price: number | null;
  stock_quantity: number;
  product: { id: string; name: string; images: string[] };
};
export async function getVariantsParaPdv(params?: { search?: string; categoryId?: string }): Promise<ActionResult<VariantParaPdv[]>>;
```

From src/app/admin/actions-cotizacion.ts (criado em 17-01):
```typescript
export type CotizacionAtual = { id: string; brlToPyg: number; usdToPyg: number; createdAt: string };
export async function getCotizacionAtual(): Promise<ActionResult<CotizacionAtual | null>>;
```

From src/app/admin/actions-clientes.ts (Phase 16 — JÁ EXISTE):
```typescript
export type ClienteResumo = { id: string; nome: string; ruc: string | null; cidade: string | null; ... };
export async function buscarClientePorRuc(ruc: string): Promise<ActionResult<ClienteResumo | null>>;
export async function criarCliente(data: { nome: string; ruc?: string | null; cidade?: string | null; telefone?: string | null }): Promise<ActionResult<{ id: string }>>;
```

From src/app/admin/actions-categories.ts (JÁ EXISTE):
```typescript
export async function getCategories(): Promise<ActionResult<Array<{ id: string; name: string }>>>;
```

From src/components/admin/AdminStepIndicator.tsx:
```typescript
type Props = { steps: { label: string }[]; currentStep: number; className?: string };
```

<!-- Tipos compartilhados que PdvClient.tsx EXPORTA para uso de 17-04 (steps 3-4) -->
```typescript
// src/app/admin/pdv/PdvClient.tsx — exports for downstream consumers
export type ClienteSeleccionado =
  | { tipo: "cliente"; id: string; nombre: string; ruc: string | null; ciudad: string | null }
  | { tipo: "consumidor_final" };

export type CarritoItem = {
  variantId: string;
  productId: string;
  productName: string;
  productImage: string | null;
  attributeName: string;
  attributeValue: string;
  precioPyg: number;
  cantidad: number;
  stockMaximo: number;
};
```
</interfaces>

@src/app/admin/actions-pdv.ts
@src/app/admin/actions-cotizacion.ts
@src/app/admin/actions-clientes.ts
@src/app/admin/actions-categories.ts
@src/components/admin/AdminPageHeader.tsx
@src/components/admin/AdminStepIndicator.tsx
@src/components/admin/AdminEmptyState.tsx
@src/components/admin/AdminFilterBar.tsx
@src/app/admin/maleta/nova/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Paper MCP review + page.tsx + PdvClient.tsx (skeleton com state e navegação)</name>
  <files>src/app/admin/pdv/page.tsx, src/app/admin/pdv/PdvClient.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (sections: Component Inventory /admin/pdv, Layout geral, Copywriting Contract Global/navegação, Splash de sucesso, Interaction Specs — Focal Point por Step)
    - src/app/admin/maleta/nova/page.tsx (padrão de uso de AdminStepIndicator e Client Component multi-step)
    - src/components/admin/AdminStepIndicator.tsx (props exatas)
    - src/components/admin/AdminPageHeader.tsx
    - src/app/admin/actions-pdv.ts (criado em 17-01 — getVariantsParaPdv, criarVentaLoja)
    - src/app/admin/actions-cotizacion.ts (criado em 17-01)
    - src/app/admin/actions-categories.ts (getCategories)
  </read_first>
  <action>
**STEP 0 — Paper MCP consultation (D-15-01, OBRIGATÓRIO):**

1. `mcp__plugin_paper-desktop_paper__get_basic_info` — listar artboards.
2. Procurar artboard "PDV" / "Punto de Venta" / "Carrito".
3. Se encontrado: `get_jsx` + `get_computed_styles` para extrair tokens visuais. Confrontar com UI-SPEC; UI-SPEC prevalece em divergências.
4. Se NÃO encontrado: registrar nota de ausência e usar UI-SPEC como única fonte.

**STEP 1 — Criar `src/app/admin/pdv/page.tsx` (RSC):**

```typescript
import { getCotizacionAtual } from "@/app/admin/actions-cotizacion";
import { getCategories } from "@/app/admin/actions-categories";
import PdvClient from "./PdvClient";

export const dynamic = "force-dynamic";

export default async function PdvPage() {
  const [cotResult, catResult] = await Promise.all([
    getCotizacionAtual(),
    getCategories(),
  ]);

  const cotizacionAtual = cotResult.success ? cotResult.data : null;
  const categorias = catResult.success ? catResult.data : [];

  return <PdvClient cotizacionAtual={cotizacionAtual} categorias={categorias} />;
}
```

**STEP 2 — Criar `src/app/admin/pdv/PdvClient.tsx` (Client Component skeleton + state + navegação):**

```typescript
"use client";

import { useState } from "react";
import AdminPageHeader from "@/components/admin/AdminPageHeader";
import AdminStepIndicator from "@/components/admin/AdminStepIndicator";
import type { CotizacionAtual } from "@/app/admin/actions-cotizacion";
import PdvStepCliente from "./PdvStepCliente";
import PdvStepProductos from "./PdvStepProductos";

export type ClienteSeleccionado =
  | { tipo: "cliente"; id: string; nombre: string; ruc: string | null; ciudad: string | null }
  | { tipo: "consumidor_final" };

export type CarritoItem = {
  variantId: string;
  productId: string;
  productName: string;
  productImage: string | null;
  attributeName: string;
  attributeValue: string;
  precioPyg: number;
  cantidad: number;
  stockMaximo: number;
};

export type Moneda = "PYG" | "USD" | "BRL";

const PDV_STEPS = [
  { label: "Cliente" },
  { label: "Productos" },
  { label: "Moneda" },
  { label: "Resumen" },
];

type Props = {
  cotizacionAtual: CotizacionAtual | null;
  categorias: Array<{ id: string; name: string }>;
};

export default function PdvClient({ cotizacionAtual, categorias }: Props) {
  const [step, setStep] = useState<number>(0);
  const [cliente, setCliente] = useState<ClienteSeleccionado | null>(null);
  const [carrito, setCarrito] = useState<CarritoItem[]>([]);
  const [moneda, setMoneda] = useState<Moneda>("PYG");
  const [showSplash, setShowSplash] = useState<{ ventaId: string; totalPyg: number; clienteLabel: string } | null>(null);

  // Validação para avançar
  function canAdvance(): boolean {
    if (step === 0) return cliente !== null;        // Step 1 — cliente identificado ou Consumidor Final
    if (step === 1) return carrito.length > 0;      // Step 2 — pelo menos um item
    if (step === 2) return cotizacionAtual !== null || moneda === "PYG"; // Step 3 — em 17-04
    return true;
  }

  function handleNext() {
    if (!canAdvance()) return;
    setStep((s) => Math.min(s + 1, PDV_STEPS.length - 1));
  }

  function handleBack() {
    setStep((s) => Math.max(s - 1, 0));
  }

  function resetPdv() {
    setStep(0);
    setCliente(null);
    setCarrito([]);
    setMoneda("PYG");
    setShowSplash(null);
  }

  return (
    <>
      <AdminPageHeader
        title="Punto de Venta"
        description="Registrá una venta en la tienda física."
        breadcrumb="Ventas"
      />

      <div className="admin-content">
        {showSplash ? (
          // Splash será implementado em 17-04
          <PlaceholderSplash splash={showSplash} onReset={resetPdv} />
        ) : (
          <>
            <AdminStepIndicator steps={PDV_STEPS} currentStep={step} />

            <div className="mt-8">
              {step === 0 && (
                <PdvStepCliente
                  cliente={cliente}
                  onSelect={setCliente}
                />
              )}
              {step === 1 && (
                <PdvStepProductos
                  carrito={carrito}
                  setCarrito={setCarrito}
                  categorias={categorias}
                />
              )}
              {step === 2 && (
                // PdvStepMoneda — implementado em 17-04
                <PlaceholderStep label="Step 3 — Moneda (a ser implementado em 17-04)" />
              )}
              {step === 3 && (
                // PdvStepResumen — implementado em 17-04
                <PlaceholderStep label="Step 4 — Resumen (a ser implementado em 17-04)" />
              )}
            </div>

            <div className="flex items-center justify-between mt-8">
              <button
                type="button"
                className="admin-btn admin-btn-secondary"
                onClick={handleBack}
                disabled={step === 0}
              >
                ← Anterior
              </button>

              {step < PDV_STEPS.length - 1 && (
                <button
                  type="button"
                  className="admin-btn admin-btn-primary"
                  onClick={handleNext}
                  disabled={!canAdvance()}
                >
                  Siguiente →
                </button>
              )}
            </div>
          </>
        )}
      </div>
    </>
  );
}

function PlaceholderStep({ label }: { label: string }) {
  return (
    <div className="admin-card">
      <p className="text-sm" style={{ color: "var(--admin-text-muted)" }}>{label}</p>
    </div>
  );
}

function PlaceholderSplash({ splash, onReset }: { splash: { ventaId: string; totalPyg: number; clienteLabel: string }; onReset: () => void }) {
  return (
    <div className="admin-card">
      <p className="text-base" style={{ color: "var(--admin-text)" }}>¡Venta registrada! ({splash.ventaId.slice(0, 8)})</p>
      <button type="button" className="admin-btn admin-btn-primary mt-4" onClick={onReset}>
        Nueva venta
      </button>
    </div>
  );
}
```

**Regras críticas:**
- `export const dynamic = "force-dynamic"` em page.tsx.
- Estado global do PDV vive em PdvClient.tsx; passa via props para os steps.
- TIPOS exportados (`ClienteSeleccionado`, `CarritoItem`, `Moneda`) — 17-04 vai importá-los.
- Placeholders para steps 3-4 e Splash — 17-04 substitui por implementações reais.
- `canAdvance()` define quando "Siguiente" é habilitado. Step 0: cliente !== null. Step 1: carrito.length > 0.
- ZERO hex hardcoded (D-15-02).
- Cópia ES-PY conforme UI-SPEC: "Punto de Venta", "Anterior", "Siguiente →", "Registrá una venta...".
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "pdv/page|PdvClient" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Paper MCP consultation registrada
    - File `src/app/admin/pdv/page.tsx` exists with `force-dynamic` and `Promise.all`
    - File `src/app/admin/pdv/PdvClient.tsx` starts with `"use client";`
    - PdvClient.tsx contains `export type ClienteSeleccionado`
    - PdvClient.tsx contains `export type CarritoItem`
    - PdvClient.tsx contains `export type Moneda`
    - PdvClient.tsx contains `import AdminStepIndicator`
    - PdvClient.tsx contains `const PDV_STEPS = [` with 4 entries: Cliente, Productos, Moneda, Resumen
    - PdvClient.tsx contains exact text `Punto de Venta`
    - PdvClient.tsx contains exact text `← Anterior` and `Siguiente →`
    - PdvClient.tsx contains `useState` for `step`, `cliente`, `carrito`, `moneda`
    - PdvClient.tsx contains `canAdvance()` function
    - PdvClient.tsx imports `PdvStepCliente` and `PdvStepProductos`
    - No hex literals: `grep -E '#[0-9a-fA-F]{3,8}' src/app/admin/pdv/PdvClient.tsx` returns 0
    - `npx tsc --noEmit` reports zero errors for pdv files
  </acceptance_criteria>
  <done>
    page.tsx + PdvClient.tsx skeleton funcionais; estado e navegação operam entre os 4 steps; placeholders para 17-04.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PdvStepCliente.tsx — busca RUC + mini-form inline + Consumidor Final</name>
  <files>src/app/admin/pdv/PdvStepCliente.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (sections: Step 1 — Identificación del cliente; Interaction Specs — RUC Search + Mini-form)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md (D-17-PDV-01 — cliente opcional + mini-form inline)
    - src/app/admin/actions-clientes.ts (assinaturas EXATAS de buscarClientePorRuc e criarCliente — campos do retorno)
    - src/app/admin/pdv/PdvClient.tsx (tipo ClienteSeleccionado exportado)
  </read_first>
  <action>
Criar componente do Step 1. Estados conforme UI-SPEC tabela "Estados do campo RUC".

**Conteúdo do arquivo `src/app/admin/pdv/PdvStepCliente.tsx`:**

```typescript
"use client";

import { useState, useRef, useTransition } from "react";
import { UserCheck, UserX } from "lucide-react";
import { buscarClientePorRuc, criarCliente } from "@/app/admin/actions-clientes";
import type { ClienteSeleccionado } from "./PdvClient";

type Props = {
  cliente: ClienteSeleccionado | null;
  onSelect: (cliente: ClienteSeleccionado) => void;
};

type BuscaState =
  | { kind: "idle" }
  | { kind: "buscando" }
  | { kind: "encontrado"; cliente: { id: string; nombre: string; ruc: string | null; ciudad: string | null } }
  | { kind: "no_encontrado"; rucBuscado: string }
  | { kind: "error"; message: string };

export default function PdvStepCliente({ cliente, onSelect }: Props) {
  const [ruc, setRuc] = useState<string>("");
  const [busca, setBusca] = useState<BuscaState>({ kind: "idle" });
  const [mostrarMiniForm, setMostrarMiniForm] = useState(false);
  const [nombre, setNombre] = useState("");
  const [ciudad, setCiudad] = useState("");
  const [telefono, setTelefono] = useState("");
  const [miniFormError, setMiniFormError] = useState<string | null>(null);
  const [isPendingBusca, startBusca] = useTransition();
  const [isPendingCreate, startCreate] = useTransition();
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  function handleRucChange(value: string) {
    setRuc(value);
    setMostrarMiniForm(false);
    setMiniFormError(null);

    if (debounceRef.current) clearTimeout(debounceRef.current);
    if (value.trim().length < 3) {
      setBusca({ kind: "idle" });
      return;
    }
    debounceRef.current = setTimeout(() => {
      runBusca(value.trim());
    }, 400);
  }

  function runBusca(rucValue: string) {
    setBusca({ kind: "buscando" });
    startBusca(async () => {
      const result = await buscarClientePorRuc(rucValue);
      if (!result.success) {
        setBusca({ kind: "error", message: result.error || "Error al buscar." });
        return;
      }
      if (result.data) {
        setBusca({
          kind: "encontrado",
          cliente: {
            id: result.data.id,
            nombre: result.data.nome,
            ruc: result.data.ruc,
            ciudad: result.data.cidade,
          },
        });
      } else {
        setBusca({ kind: "no_encontrado", rucBuscado: rucValue });
      }
    });
  }

  function handleSelectCliente(c: { id: string; nombre: string; ruc: string | null; ciudad: string | null }) {
    onSelect({ tipo: "cliente", id: c.id, nombre: c.nombre, ruc: c.ruc, ciudad: c.ciudad });
  }

  function handleConsumidorFinal() {
    onSelect({ tipo: "consumidor_final" });
  }

  function handleAbrirMiniForm() {
    setMostrarMiniForm(true);
    setNombre("");
    setCiudad("");
    setTelefono("");
    setMiniFormError(null);
  }

  function handleCriarClienteInline(e: React.FormEvent) {
    e.preventDefault();
    setMiniFormError(null);

    if (!nombre.trim()) {
      setMiniFormError("El nombre es obligatorio.");
      return;
    }
    const rucValue = busca.kind === "no_encontrado" ? busca.rucBuscado : ruc.trim();

    startCreate(async () => {
      const result = await criarCliente({
        nome: nombre.trim(),
        ruc: rucValue || null,
        cidade: ciudad.trim() || null,
        telefone: telefono.trim() || null,
      });
      if (!result.success) {
        // Tratamento específico para RUC duplicado
        if (result.error?.toLowerCase().includes("ruc") || result.error?.toLowerCase().includes("existe")) {
          setMiniFormError("Ya existe un cliente con ese RUC.");
        } else {
          setMiniFormError(result.error || "No se pudo crear el cliente.");
        }
        return;
      }
      // Cliente criado: selecionar imediatamente
      onSelect({
        tipo: "cliente",
        id: result.data.id,
        nombre: nombre.trim(),
        ruc: rucValue || null,
        ciudad: ciudad.trim() || null,
      });
    });
  }

  function handleCambiarCliente() {
    setRuc("");
    setBusca({ kind: "idle" });
    setMostrarMiniForm(false);
    setMiniFormError(null);
    onSelect(null as unknown as ClienteSeleccionado); // signal: clear
    // Note: parent treats this as null (allowed); do NOT advance.
  }

  // Se já há cliente selecionado, mostra resumo + botão "Cambiar cliente"
  if (cliente) {
    return (
      <div className="admin-card">
        <h2 className="text-base font-semibold mb-3" style={{ color: "var(--admin-text)" }}>
          ¿Quién compra?
        </h2>

        {cliente.tipo === "cliente" ? (
          <div className="flex items-center justify-between gap-4 p-4 rounded" style={{ background: "var(--admin-bg)" }}>
            <div className="flex items-center gap-3">
              <UserCheck className="h-5 w-5" style={{ color: "var(--admin-success)" }} />
              <div>
                <p className="text-sm font-semibold" style={{ color: "var(--admin-text)" }}>{cliente.nombre}</p>
                <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
                  RUC: {cliente.ruc ?? "—"}{cliente.ciudad ? ` · ${cliente.ciudad}` : ""}
                </p>
              </div>
            </div>
            <button type="button" className="admin-btn admin-btn-secondary admin-btn-sm" onClick={handleCambiarCliente}>
              Cambiar cliente
            </button>
          </div>
        ) : (
          <div className="flex items-center justify-between gap-4 p-4 rounded" style={{ background: "var(--admin-bg)" }}>
            <div className="flex items-center gap-3">
              <UserX className="h-5 w-5" style={{ color: "var(--admin-text-muted)" }} />
              <div>
                <p className="text-sm font-semibold" style={{ color: "var(--admin-text)" }}>Consumidor Final</p>
                <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>Sin identificación registrada</p>
              </div>
            </div>
            <button type="button" className="admin-btn admin-btn-secondary admin-btn-sm" onClick={handleCambiarCliente}>
              Cambiar cliente
            </button>
          </div>
        )}
      </div>
    );
  }

  return (
    <div className="admin-card">
      <h2 className="text-base font-semibold mb-3" style={{ color: "var(--admin-text)" }}>
        ¿Quién compra?
      </h2>

      <div className="flex flex-col gap-3">
        <label htmlFor="pdv-ruc" className="text-sm" style={{ color: "var(--admin-text)" }}>
          RUC / Cédula
        </label>
        <input
          id="pdv-ruc"
          type="text"
          className="admin-input"
          placeholder="Buscá por RUC o cédula"
          value={ruc}
          onChange={(e) => handleRucChange(e.target.value)}
          autoFocus
          disabled={isPendingBusca || isPendingCreate}
        />
      </div>

      {/* Estados da busca */}
      {busca.kind === "buscando" && (
        <p className="text-xs mt-3" style={{ color: "var(--admin-text-muted)" }}>Buscando...</p>
      )}

      {busca.kind === "encontrado" && (
        <div className="mt-4 flex items-center justify-between gap-4 p-4 rounded" style={{ background: "var(--admin-success-10)" }}>
          <div>
            <p className="text-sm font-semibold" style={{ color: "var(--admin-text)" }}>{busca.cliente.nombre}</p>
            <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
              RUC: {busca.cliente.ruc ?? "—"}{busca.cliente.ciudad ? ` · ${busca.cliente.ciudad}` : ""}
            </p>
            <span
              className="inline-block mt-2 px-2 py-1 text-xs font-semibold rounded"
              style={{ background: "var(--admin-success-10)", color: "var(--admin-success)" }}
            >
              Cliente encontrado
            </span>
          </div>
          <button type="button" className="admin-btn admin-btn-primary" onClick={() => handleSelectCliente(busca.cliente)}>
            Usar cliente
          </button>
        </div>
      )}

      {busca.kind === "no_encontrado" && !mostrarMiniForm && (
        <div className="mt-4">
          <p className="text-sm" style={{ color: "var(--admin-text-muted)" }}>
            No encontramos a nadie con ese RUC. ¿Querés registrarlo?
          </p>
          <div className="flex gap-3 mt-3">
            <button type="button" className="admin-btn admin-btn-primary" onClick={handleAbrirMiniForm}>
              Registrar cliente
            </button>
            <button type="button" className="admin-btn admin-btn-secondary" onClick={handleConsumidorFinal}>
              Continuar como Consumidor Final
            </button>
          </div>
        </div>
      )}

      {busca.kind === "error" && (
        <div className="admin-alert admin-alert-error mt-3">{busca.message}</div>
      )}

      {/* Skip direto para Consumidor Final (estado idle) */}
      {busca.kind === "idle" && (
        <div className="mt-4">
          <button type="button" className="admin-btn admin-btn-secondary" onClick={handleConsumidorFinal}>
            Continuar como Consumidor Final
          </button>
        </div>
      )}

      {/* Mini-form inline */}
      {mostrarMiniForm && (
        <form onSubmit={handleCriarClienteInline} className="mt-4 flex flex-col gap-3 p-4 rounded" style={{ background: "var(--admin-bg)" }}>
          <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
            RUC: {busca.kind === "no_encontrado" ? busca.rucBuscado : ruc}
          </p>

          <div className="flex flex-col gap-2">
            <label className="text-sm" style={{ color: "var(--admin-text)" }}>Nombre completo</label>
            <input type="text" className="admin-input" placeholder="Ej: María González" value={nombre} onChange={(e) => setNombre(e.target.value)} required />
          </div>

          <div className="flex flex-col gap-2">
            <label className="text-sm" style={{ color: "var(--admin-text)" }}>Ciudad</label>
            <input type="text" className="admin-input" placeholder="Ej: Asunción" value={ciudad} onChange={(e) => setCiudad(e.target.value)} />
          </div>

          <div className="flex flex-col gap-2">
            <label className="text-sm" style={{ color: "var(--admin-text)" }}>Teléfono (opcional)</label>
            <input type="tel" className="admin-input" placeholder="Ej: 0981 000 000" value={telefono} onChange={(e) => setTelefono(e.target.value)} />
          </div>

          {miniFormError && <div className="admin-alert admin-alert-error">{miniFormError}</div>}

          <button type="submit" className="admin-btn admin-btn-primary" disabled={isPendingCreate}>
            {isPendingCreate ? "Guardando..." : "Guardar y continuar"}
          </button>
        </form>
      )}
    </div>
  );
}
```

**Regras críticas:**
- Debounce 400ms via `setTimeout` + `clearTimeout` (padrão do projeto, sem hook externo).
- `autoFocus` no input de RUC (UI-SPEC: focal point Step 1).
- Estados da busca conforme tabela do UI-SPEC (idle/buscando/encontrado/no_encontrado/error).
- Cópia ES-PY EXATA conforme UI-SPEC (não traduzir).
- Erro RUC duplicado: detecta heuristicamente via `error?.includes("ruc")` / `"existe"`.
- ZERO hex hardcoded — `var(--admin-*)` em todo lugar.
- Após criar cliente inline, chamar `onSelect` com tipo `"cliente"` — parent avança via "Siguiente".
- `handleCambiarCliente` reseta a busca; usar `null as unknown as ClienteSeleccionado` apenas como sinal de reset (parent já trata `cliente === null`).

**IMPORTANTE — verificação de campos retornados por `buscarClientePorRuc`:** A action retorna `nome`, `ruc`, `cidade` (português, conforme schema Phase 16). Mapear para `nombre`, `ruc`, `ciudad` no DTO local de UI.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "PdvStepCliente" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/pdv/PdvStepCliente.tsx` exists, starts with `"use client";`
    - File contains `import { buscarClientePorRuc, criarCliente }`
    - File contains `import type { ClienteSeleccionado }`
    - File contains `setTimeout(` with `400` as delay (debounce)
    - File contains `clearTimeout(`
    - File contains `autoFocus` on the RUC input
    - File contains exact text `¿Quién compra?`
    - File contains exact text `Buscá por RUC o cédula`
    - File contains exact text `Cliente encontrado`
    - File contains exact text `No encontramos a nadie con ese RUC. ¿Querés registrarlo?`
    - File contains exact text `Continuar como Consumidor Final`
    - File contains exact text `Registrar cliente`
    - File contains exact text `Guardar y continuar`
    - File contains exact text `Guardando...`
    - File contains exact text `Buscando...`
    - File contains exact text `Consumidor Final`
    - File contains exact text `El nombre es obligatorio.`
    - File contains exact text `Ya existe un cliente con ese RUC.`
    - File contains exact text `Cambiar cliente`
    - File contains exact text `Sin identificación registrada`
    - File contains exact text `Nombre completo`, `Ciudad`, `Teléfono (opcional)`
    - File contains `useTransition`
    - No hex literals: `grep -E '#[0-9a-fA-F]{3,8}' src/app/admin/pdv/PdvStepCliente.tsx` returns 0
    - File contains `var(--admin-success-10)` (badge "Cliente encontrado")
    - `npx tsc --noEmit` reports zero errors for this file
  </acceptance_criteria>
  <done>
    Step 1 funcional: busca por RUC com debounce, exibe cliente encontrado, mini-form inline cria novo cliente, Consumidor Final pula identificação, "Cambiar cliente" reseta. Espanhol paraguaio EXATO conforme UI-SPEC.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PdvStepProductos.tsx — busca de produtos + carrinho lateral com controles</name>
  <files>src/app/admin/pdv/PdvStepProductos.tsx</files>
  <read_first>
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-UI-SPEC.md (sections: Step 2 — Productos completa; Interaction Specs — Stock Validation D-17-PDV-03)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md (D-17-PDV-02 — busca + categoria; D-17-PDV-03 — bloqueio de stock)
    - src/app/admin/actions-pdv.ts (assinatura getVariantsParaPdv, tipo VariantParaPdv — criados em 17-01)
    - src/app/admin/pdv/PdvClient.tsx (tipo CarritoItem)
    - src/components/admin/AdminEmptyState.tsx
  </read_first>
  <action>
Criar Step 2: layout grid 2 colunas (busca à esquerda, carrinho à direita).

**Conteúdo do arquivo `src/app/admin/pdv/PdvStepProductos.tsx`:**

```typescript
"use client";

import { useState, useEffect, useRef, useTransition } from "react";
import Image from "next/image";
import { Package, Minus, Plus, Trash2, Search } from "lucide-react";
import { getVariantsParaPdv, type VariantParaPdv } from "@/app/admin/actions-pdv";
import AdminEmptyState from "@/components/admin/AdminEmptyState";
import type { CarritoItem } from "./PdvClient";

type Props = {
  carrito: CarritoItem[];
  setCarrito: (items: CarritoItem[]) => void;
  categorias: Array<{ id: string; name: string }>;
};

function formatGs(value: number): string {
  return new Intl.NumberFormat("es-PY", { maximumFractionDigits: 0, useGrouping: true }).format(value);
}

export default function PdvStepProductos({ carrito, setCarrito, categorias }: Props) {
  const [search, setSearch] = useState<string>("");
  const [categoryId, setCategoryId] = useState<string>("all");
  const [variants, setVariants] = useState<VariantParaPdv[]>([]);
  const [isPendingLoad, startLoad] = useTransition();
  const [stockError, setStockError] = useState<string | null>(null);
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  // Carregar variantes inicialmente e quando categoria muda imediatamente
  useEffect(() => {
    runLoad(search, categoryId);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [categoryId]);

  function handleSearchChange(value: string) {
    setSearch(value);
    if (debounceRef.current) clearTimeout(debounceRef.current);
    debounceRef.current = setTimeout(() => {
      runLoad(value, categoryId);
    }, 400);
  }

  function runLoad(searchValue: string, catValue: string) {
    startLoad(async () => {
      const result = await getVariantsParaPdv({
        search: searchValue.trim() || undefined,
        categoryId: catValue !== "all" ? catValue : undefined,
      });
      if (result.success) setVariants(result.data);
    });
  }

  function isInCart(variantId: string): boolean {
    return carrito.some((c) => c.variantId === variantId);
  }

  function handleAdd(variant: VariantParaPdv) {
    setStockError(null);
    if (isInCart(variant.id)) return;
    if (variant.stock_quantity < 1) {
      setStockError(`Stock insuficiente. Solo hay ${variant.stock_quantity} unidad(es) disponible(s).`);
      return;
    }
    const precio = variant.price ?? 0;
    setCarrito([
      ...carrito,
      {
        variantId: variant.id,
        productId: variant.product.id,
        productName: variant.product.name,
        productImage: variant.product.images[0] ?? null,
        attributeName: variant.attribute_name,
        attributeValue: variant.attribute_value,
        precioPyg: precio,
        cantidad: 1,
        stockMaximo: variant.stock_quantity,
      },
    ]);
  }

  function handleIncrement(variantId: string) {
    setStockError(null);
    setCarrito(
      carrito.map((c) =>
        c.variantId === variantId
          ? { ...c, cantidad: c.cantidad + 1 > c.stockMaximo ? c.cantidad : c.cantidad + 1 }
          : c
      )
    );
  }

  function handleDecrement(variantId: string) {
    setCarrito(
      carrito
        .map((c) =>
          c.variantId === variantId ? { ...c, cantidad: Math.max(1, c.cantidad - 1) } : c
        )
    );
  }

  function handleRemove(variantId: string) {
    setCarrito(carrito.filter((c) => c.variantId !== variantId));
  }

  const totalCarritoPyg = carrito.reduce((acc, c) => acc + Math.round(c.precioPyg * c.cantidad), 0);

  return (
    <div className="grid grid-cols-1 gap-6 lg:grid-cols-[1fr_360px]">
      {/* Coluna esquerda: busca + lista */}
      <div className="admin-card">
        <h2 className="text-base font-semibold mb-3" style={{ color: "var(--admin-text)" }}>
          Agregá productos a la venta
        </h2>

        <div className="flex flex-col gap-3 sm:flex-row sm:items-center mb-4">
          <div className="flex items-center gap-2 flex-1">
            <Search className="h-4 w-4" style={{ color: "var(--admin-text-muted)" }} />
            <input
              type="text"
              className="admin-input"
              placeholder="Buscar por nombre..."
              value={search}
              onChange={(e) => handleSearchChange(e.target.value)}
              autoFocus
            />
          </div>

          <div className="flex items-center gap-2">
            <label htmlFor="pdv-cat" className="text-sm" style={{ color: "var(--admin-text-muted)" }}>Categoría</label>
            <select
              id="pdv-cat"
              className="admin-input"
              value={categoryId}
              onChange={(e) => setCategoryId(e.target.value)}
            >
              <option value="all">Todas las categorías</option>
              {categorias.map((cat) => (
                <option key={cat.id} value={cat.id}>{cat.name}</option>
              ))}
            </select>
          </div>
        </div>

        {stockError && <div className="admin-alert admin-alert-error mb-3">{stockError}</div>}

        <div style={{ maxHeight: "480px", overflowY: "auto" }}>
          {isPendingLoad && variants.length === 0 ? (
            <p className="text-sm" style={{ color: "var(--admin-text-muted)" }}>Cargando...</p>
          ) : variants.length === 0 ? (
            search.trim().length > 0 ? (
              <AdminEmptyState
                icon={Package}
                title={`No encontramos productos con "${search.trim()}".`}
              />
            ) : (
              <AdminEmptyState
                icon={Package}
                title="Sin productos con stock disponible."
              />
            )
          ) : (
            <ul className="flex flex-col gap-2">
              {variants.map((v) => {
                const inCart = isInCart(v.id);
                const isLast = v.stock_quantity === 1;
                return (
                  <li
                    key={v.id}
                    className="flex items-center justify-between gap-3 p-3 rounded"
                    style={{ background: "var(--admin-bg)" }}
                  >
                    <div className="flex items-center gap-3 flex-1 min-w-0">
                      <div className="admin-table-thumb">
                        {v.product.images[0] ? (
                          <Image src={v.product.images[0]} alt="" width={40} height={40} />
                        ) : (
                          <Package className="h-5 w-5" style={{ color: "var(--admin-text-muted)" }} />
                        )}
                      </div>
                      <div className="min-w-0 flex-1">
                        <p className="text-sm font-semibold truncate" style={{ color: "var(--admin-text)" }}>{v.product.name}</p>
                        <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
                          {v.attribute_name}: {v.attribute_value} · Stock: {v.stock_quantity}
                          {v.price != null ? ` · ${formatGs(v.price)} Gs.` : ""}
                        </p>
                      </div>
                      {isLast && (
                        <span
                          className="px-2 py-1 text-xs font-semibold rounded"
                          style={{ background: "var(--admin-warning-10)", color: "var(--admin-warning)" }}
                        >
                          Última unidad
                        </span>
                      )}
                    </div>

                    {inCart ? (
                      <span
                        className="px-3 py-1 text-xs font-semibold rounded"
                        style={{ background: "var(--admin-muted-10)", color: "var(--admin-text-muted)" }}
                      >
                        En carrito
                      </span>
                    ) : (
                      <button
                        type="button"
                        className="admin-btn admin-btn-primary admin-btn-sm"
                        onClick={() => handleAdd(v)}
                      >
                        Agregar al carrito
                      </button>
                    )}
                  </li>
                );
              })}
            </ul>
          )}
        </div>
      </div>

      {/* Coluna direita: carrinho */}
      <aside className="admin-card">
        <div className="flex items-center justify-between mb-3">
          <h3 className="text-base font-semibold" style={{ color: "var(--admin-text)" }}>
            Carrito ({carrito.length} ítems)
          </h3>
          {carrito.length > 0 && (
            <span className="text-sm font-semibold" style={{ color: "var(--admin-accent)" }}>
              Total: {formatGs(totalCarritoPyg)} Gs.
            </span>
          )}
        </div>

        {carrito.length === 0 ? (
          <AdminEmptyState
            icon={Package}
            title="El carrito está vacío"
            description="Buscá y agregá productos para continuar."
          />
        ) : (
          <ul className="flex flex-col gap-2">
            {carrito.map((c) => (
              <li
                key={c.variantId}
                className="flex flex-col gap-2 p-3 rounded"
                style={{ background: "var(--admin-bg)" }}
              >
                <div className="flex items-center gap-3">
                  <div className="admin-table-thumb">
                    {c.productImage ? (
                      <Image src={c.productImage} alt="" width={40} height={40} />
                    ) : (
                      <Package className="h-5 w-5" style={{ color: "var(--admin-text-muted)" }} />
                    )}
                  </div>
                  <div className="min-w-0 flex-1">
                    <p className="text-sm font-semibold truncate" style={{ color: "var(--admin-text)" }}>{c.productName}</p>
                    <p className="text-xs" style={{ color: "var(--admin-text-muted)" }}>
                      {c.attributeValue} · {formatGs(c.precioPyg)} Gs.
                    </p>
                  </div>
                  <button
                    type="button"
                    className="admin-btn admin-btn-icon"
                    aria-label="Quitar del carrito"
                    onClick={() => handleRemove(c.variantId)}
                    style={{ color: "var(--admin-danger)" }}
                  >
                    <Trash2 className="h-4 w-4" />
                  </button>
                </div>

                <div className="flex items-center justify-between">
                  <div className="flex items-center gap-2">
                    <button
                      type="button"
                      className="admin-btn admin-btn-icon"
                      aria-label="Reducir cantidad"
                      onClick={() => handleDecrement(c.variantId)}
                      disabled={c.cantidad <= 1}
                      style={{ minHeight: "44px", minWidth: "44px" }}
                    >
                      <Minus className="h-4 w-4" />
                    </button>
                    <span className="text-sm font-semibold" style={{ color: "var(--admin-text)", minWidth: "24px", textAlign: "center" }}>
                      {c.cantidad}
                    </span>
                    <button
                      type="button"
                      className="admin-btn admin-btn-icon"
                      aria-label="Aumentar cantidad"
                      onClick={() => handleIncrement(c.variantId)}
                      disabled={c.cantidad >= c.stockMaximo}
                      style={{ minHeight: "44px", minWidth: "44px", opacity: c.cantidad >= c.stockMaximo ? 0.4 : 1 }}
                    >
                      <Plus className="h-4 w-4" />
                    </button>
                  </div>
                  <span className="text-sm font-semibold" style={{ color: "var(--admin-text)" }}>
                    {formatGs(Math.round(c.precioPyg * c.cantidad))} Gs.
                  </span>
                </div>
              </li>
            ))}
          </ul>
        )}
      </aside>
    </div>
  );
}
```

**Regras críticas:**
- Layout: `grid-cols-1 lg:grid-cols-[1fr_360px]` — stack em mobile, lado-a-lado em desktop.
- Debounce 400ms na busca; categoria recarrega imediatamente.
- `stock_quantity` é a fonte de verdade (Pitfall 2 — não recalcular com reservas).
- `Math.round(precioPyg * cantidad)` por linha (D-17-04).
- Botões `+` desabilitados quando `cantidad >= stockMaximo`; `-` desabilitado quando `cantidad <= 1`.
- `aria-label` em botões icon-only ("Reducir cantidad", "Aumentar cantidad", "Quitar del carrito").
- Touch target mínimo 44x44px nos botões `+/-` (UI-SPEC: Spacing).
- Badge "Última unidad" quando `stock_quantity === 1`.
- Badge "En carrito" (cinza, disabled) quando produto já está no carrinho.
- Empty states diferenciados: busca sem resultados vs. sem stock vs. carrinho vazio.
- Cópia ES-PY EXATA conforme UI-SPEC.
- ZERO hex hardcoded (D-15-02).
- `next/image` para thumbnails (40x40), fallback `Package` icon.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "PdvStepProductos" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/pdv/PdvStepProductos.tsx` exists, starts with `"use client";`
    - File contains `import { getVariantsParaPdv, type VariantParaPdv }`
    - File contains `import type { CarritoItem }`
    - File contains exact text `Agregá productos a la venta`
    - File contains exact text `Buscar por nombre...`
    - File contains exact text `Todas las categorías`
    - File contains exact text `Categoría`
    - File contains exact text `Agregar al carrito`
    - File contains exact text `En carrito`
    - File contains exact text `Última unidad`
    - File contains exact text `El carrito está vacío`
    - File contains exact text `Buscá y agregá productos para continuar.`
    - File contains exact text `Sin productos con stock disponible.`
    - File contains exact text `Stock:`
    - File contains exact text `Quitar del carrito` (aria-label)
    - File contains exact text `Reducir cantidad` (aria-label)
    - File contains exact text `Aumentar cantidad` (aria-label)
    - File contains exact text `Stock insuficiente. Solo hay`
    - File contains `setTimeout(` and `400` (debounce)
    - File contains `Math.round(c.precioPyg * c.cantidad)`
    - File contains `c.cantidad >= c.stockMaximo` (block + when at max)
    - File contains `minHeight: "44px"` (touch target)
    - File contains `grid-cols-[1fr_360px]`
    - File contains `var(--admin-warning-10)` (badge última unidad)
    - File contains `var(--admin-muted-10)` (badge en carrito)
    - No hex literals: `grep -E '#[0-9a-fA-F]{3,8}' src/app/admin/pdv/PdvStepProductos.tsx` returns 0
    - `npx tsc --noEmit` reports zero errors for this file
  </acceptance_criteria>
  <done>
    Step 2 funcional ponta-a-ponta: busca com debounce, filtro por categoria, lista de produtos com badges (Última unidad, En carrito), carrinho lateral com controles +/-/remover, bloqueio de stock client-side. ES-PY EXATO. Touch targets 44px.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → page.tsx (RSC) | Authenticated session validated by middleware + requireAuth in actions |
| browser → PdvStepCliente form | Untrusted input (RUC, nombre, ciudad, telefono) crosses to Server Actions |
| browser → PdvStepProductos | Untrusted input (search, categoryId, variantId, cantidad) crosses to Server Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Tampering | client-side stock validation | mitigate | Defense in depth: client bloqueia +/- (`disabled` quando `cantidad >= stockMaximo`), Server Action `criarVentaLoja` re-valida antes do INSERT (já implementado em 17-01) |
| T-17-12 | Tampering | RUC search input | mitigate | `buscarClientePorRuc` é uma query read-only com `requireAuth`; sem chance de injection (Prisma usa parametrized queries) |
| T-17-13 | Information Disclosure | console.error de erros | mitigate | Não logar nome/RUC/telefone — apenas IDs e códigos de erro |
| T-17-14 | Spoofing | criarCliente RUC duplicado | mitigate | Server Action `criarCliente` valida unicidade no DB (constraint `@unique` já existente em Phase 16); UI exibe mensagem genérica |
</threat_model>

<verification>
- `/admin/pdv` carrega com AdminStepIndicator no Step 0 (Cliente)
- Step 1: digitar RUC dispara busca após 400ms; cliente encontrado / não encontrado / mini-form / Consumidor Final funcionam
- Step 1: criar cliente inline avança o estado do PDV
- "Siguiente" desabilitado quando cliente é null; habilitado após selecionar
- Step 2: busca por nome com debounce; filtro de categoria funciona
- Step 2: adicionar/remover produtos; +/- com bloqueio de stock e touch targets de 44px
- Steps 3-4 mostram placeholder; "Siguiente"/"Anterior" navega
- ES-PY EXATO em toda UI
- Zero hex hardcoded
- `npx tsc --noEmit` zero erros
</verification>

<success_criteria>
- page.tsx + PdvClient.tsx + PdvStepCliente.tsx + PdvStepProductos.tsx criados
- Estado global do PDV operacional (cliente, carrito, step, moneda)
- Steps 1 e 2 ponta-a-ponta funcionais
- Tipos `ClienteSeleccionado`, `CarritoItem`, `Moneda` exportados de PdvClient.tsx para uso em 17-04
- Paper MCP consultado e registrado
- Zero hex hardcoded; ES-PY EXATO; AdminPageHeader; touch targets 44px
- Defense in depth de stock (client bloqueia, server re-valida em 17-01)
</success_criteria>

<output>
After completion, create `.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-03-SUMMARY.md`
</output>
