---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/admin/actions-cotizacion.ts
  - src/app/admin/actions-pdv.ts
autonomous: true
requirements: [PDV-01, PDV-02, PDV-05, PDV-06]

must_haves:
  truths:
    - "VentaLoja.cliente_id aceita null no banco (consumidor final)"
    - "Admin pode salvar cotização BRL→PYG e USD→PYG via salvarCotizacion"
    - "Admin pode ler cotização atual (mais recente) via getCotizacionAtual"
    - "Admin pode listar histórico de cotizações via getHistorialCotizaciones"
    - "Admin pode buscar variantes para PDV (filtradas por stock>0, ativo, search, categoria) via getVariantsParaPdv"
    - "Admin pode criar VentaLoja via criarVentaLoja com decremento atômico de estoque, snapshot de cotização imutável e clienteId opcional"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "VentaLoja.cliente_id nullable"
      contains: "cliente_id         String?  @db.Uuid"
    - path: "src/app/admin/actions-cotizacion.ts"
      provides: "salvarCotizacion, getCotizacionAtual, getHistorialCotizaciones"
      exports: ["salvarCotizacion", "getCotizacionAtual", "getHistorialCotizaciones"]
    - path: "src/app/admin/actions-pdv.ts"
      provides: "getVariantsParaPdv, criarVentaLoja"
      exports: ["getVariantsParaPdv", "criarVentaLoja"]
  key_links:
    - from: "actions-pdv.ts:criarVentaLoja"
      to: "prisma.cotizacionDia.findFirst"
      via: "pré-leitura de cotização — NUNCA do payload do client (D-17-03)"
      pattern: "findFirst.*orderBy.*created_at.*desc"
    - from: "actions-pdv.ts:criarVentaLoja"
      to: "prisma.productVariant.update"
      via: "decremento sequencial com compensação (padrão criarMaleta)"
      pattern: "stock_quantity.*decrement"
    - from: "actions-pdv.ts:criarVentaLoja"
      to: "prisma.estoqueMovimento.create"
      via: "registro de movimento tipo venda_loja"
      pattern: 'tipo:.*"venda_loja"'
---

<objective>
Implementar a base backend completa da Fase 17: a migration que torna `VentaLoja.cliente_id` nullable (suporte a Consumidor Final, D-17-PDV-01) e os dois arquivos de Server Actions que serão consumidos pelas duas telas (Wave 2).

Purpose: Sem este plan, qualquer tentativa de criar VentaLoja sem cliente falha com NOT NULL constraint. As actions também são pré-requisito de toda UI subsequente.

Output:
- Migration Prisma aplicada e Prisma Client regenerado
- `actions-cotizacion.ts` com 3 funções (`salvarCotizacion`, `getCotizacionAtual`, `getHistorialCotizaciones`)
- `actions-pdv.ts` com 2 funções (`getVariantsParaPdv`, `criarVentaLoja`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md
@.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Tipos e contratos que executores das Waves 2-3 vão consumir -->

From src/lib/action-utils.ts:
```typescript
export type ActionResult<T> = { success: true; data: T } | { success: false; error: string };
export function safeAction<T>(fn: () => Promise<T>, opts: { actionName: string }): Promise<ActionResult<T>>;
export class BusinessError extends Error {}
```

From src/lib/user.ts:
```typescript
export function requireAuth(roles: string[]): Promise<{ profileId: string | null; role: string; ... }>;
```

From src/lib/cache/invalidate.ts:
```typescript
export const invalidateCache: { path: { admin: (path: string) => void } };
```

DTOs criados neste plan (consumidos pela Wave 2):
```typescript
// src/app/admin/actions-cotizacion.ts
export type CotizacionAtual = {
  id: string;
  brlToPyg: number;
  usdToPyg: number;
  createdAt: string; // ISO
};

export type CotizacionHistorialItem = {
  id: string;
  brlToPyg: number;
  usdToPyg: number;
  createdAt: string; // ISO
};

// src/app/admin/actions-pdv.ts
export type VariantParaPdv = {
  id: string;
  attribute_name: string;
  attribute_value: string;
  price: number | null; // PYG
  stock_quantity: number;
  product: { id: string; name: string; images: string[] };
};

export type CriarVentaLojaInput = {
  clienteId: string | null;          // null = Consumidor Final
  moneda: "PYG" | "USD" | "BRL";
  itens: Array<{
    product_variant_id: string;
    cantidad: number;
  }>;
};
```

Schema Prisma relevante (verificado em prisma/schema.prisma):
```prisma
model CotizacionDia {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  brl_to_py  Decimal  @db.Decimal(12, 2)
  usd_to_py  Decimal  @db.Decimal(12, 2)
  created_at DateTime @default(now()) @db.Timestamptz()
  @@map("cotizacion_dia")
}

model VentaLoja {
  id                  String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  cliente_id          String   @db.Uuid    // ⚠️ DEVE virar String? NESTE PLAN
  total               Decimal  @db.Decimal(12, 2)
  moneda              Moneda
  total_pyg           Decimal  @db.Decimal(12, 2)
  cotizacion_snapshot Json     @default("{}")
  created_by          String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz()
  cliente             Cliente  @relation(fields: [cliente_id], references: [id])  // ⚠️ deve virar Cliente?
  itens               VentaLojaItem[]
}

enum EstoqueMovimentoTipo {
  reserva_maleta
  devolucao_maleta
  ajuste_manual
  venda_direta
  venda_loja  // ✅ já existe
}
```
</interfaces>

@src/app/admin/actions-maletas.ts
@src/app/admin/actions-clientes.ts
@src/app/admin/actions-categories.ts
@src/lib/action-utils.ts
@src/lib/user.ts
@src/lib/cache/invalidate.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 [BLOCKING]: Migration — VentaLoja.cliente_id nullable</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (estado atual completo do model VentaLoja, linhas ~706-727)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md (Pitfall 1 — schema gap)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md (D-17-PDV-01 — cliente opcional)
  </read_first>
  <action>
Per D-17-PDV-01, `VentaLoja.cliente_id` must accept null for "Consumidor Final" sales.

**Step 1 — Edit `prisma/schema.prisma`:**

Localizar o bloco `model VentaLoja {` (próximo à linha 706) e fazer EXATAMENTE as duas mudanças abaixo:

ANTES:
```prisma
model VentaLoja {
  id                  String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  cliente_id          String   @db.Uuid
  total               Decimal  @db.Decimal(12, 2)
  moneda              Moneda
  total_pyg           Decimal  @db.Decimal(12, 2)
  talonario           String?
  numero_factura      String?
  tipo_operacion      String?
  cotizacion_snapshot Json     @default("{}")
  created_by          String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz()
  updated_at          DateTime @default(now()) @updatedAt @db.Timestamptz()
  cliente      Cliente         @relation(fields: [cliente_id], references: [id])
  itens        VentaLojaItem[]
  movimentos   EstoqueMovimento[]
  @@index([cliente_id])
  @@map("ventas_loja")
}
```

DEPOIS (apenas duas linhas mudam):
```prisma
model VentaLoja {
  id                  String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  cliente_id          String?  @db.Uuid
  total               Decimal  @db.Decimal(12, 2)
  moneda              Moneda
  total_pyg           Decimal  @db.Decimal(12, 2)
  talonario           String?
  numero_factura      String?
  tipo_operacion      String?
  cotizacion_snapshot Json     @default("{}")
  created_by          String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz()
  updated_at          DateTime @default(now()) @updatedAt @db.Timestamptz()
  cliente      Cliente?        @relation(fields: [cliente_id], references: [id])
  itens        VentaLojaItem[]
  movimentos   EstoqueMovimento[]
  @@index([cliente_id])
  @@map("ventas_loja")
}
```

Mudanças exatas:
1. `cliente_id          String   @db.Uuid` → `cliente_id          String?  @db.Uuid`
2. `cliente      Cliente         @relation(...)` → `cliente      Cliente?        @relation(...)`

**Não tocar em mais nada do model.** Não mexer em `VentaLojaItem`, `Cliente`, ou outros models.

**Step 2 — Atualizar lado oposto da relação no model `Cliente`:**

Localizar `model Cliente {` (próximo à linha 685-705). A linha `ventas    VentaLoja[]` permanece igual — Prisma aceita `Cliente?` no lado VentaLoja sem mudar a coleção em Cliente.

**Step 3 — Rodar migration:**
```bash
npx prisma migrate dev --name "make-venta-loja-cliente-nullable"
```

Quando solicitado, aceitar a migration. Isso cria `prisma/migrations/{timestamp}_make_venta_loja_cliente_nullable/migration.sql` contendo `ALTER TABLE "ventas_loja" ALTER COLUMN "cliente_id" DROP NOT NULL;`.

**Step 4 — Regenerar Prisma Client:**
```bash
npx prisma generate
```

Isso garante que o tipo `VentaLojaCreateInput` aceita `cliente_id?: string` para uso na Task 3.
  </action>
  <verify>
    <automated>grep -E "cliente_id\s+String\?\s+@db\.Uuid" /Users/wesleyhudson/Documents/next-monarca/prisma/schema.prisma | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains exact line: `cliente_id          String?  @db.Uuid` (com `?` após String)
    - `prisma/schema.prisma` contains exact substring: `cliente      Cliente?        @relation(fields: [cliente_id]`
    - Diretório `prisma/migrations/` contém uma nova subpasta com sufixo `_make_venta_loja_cliente_nullable`
    - Arquivo `migration.sql` da nova migration contém exatamente o texto `ALTER COLUMN "cliente_id" DROP NOT NULL`
    - Comando `npx prisma generate` completa com exit code 0
    - Após generate, comando `grep -r "cliente_id?" node_modules/.prisma/client/index.d.ts` retorna pelo menos 1 match (tipo gerado refletindo nullable)
    - Não há outras alterações em `prisma/schema.prisma` (diff mostra apenas 2 linhas modificadas no model VentaLoja)
  </acceptance_criteria>
  <done>
    Migration aplicada localmente, Prisma Client regenerado, schema reflete `cliente_id String?` e relação `Cliente?`. Esta task DEVE ser concluída ANTES das Tasks 2 e 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: actions-cotizacion.ts — salvarCotizacion + getCotizacionAtual + getHistorialCotizaciones</name>
  <files>src/app/admin/actions-cotizacion.ts</files>
  <read_first>
    - src/app/admin/actions-clientes.ts (padrão completo de safeAction + requireAuth + BusinessError; usar como template para estrutura do arquivo)
    - src/lib/action-utils.ts (assinaturas exatas de safeAction, BusinessError, ActionResult)
    - src/lib/user.ts (assinatura de requireAuth)
    - src/lib/cache/invalidate.ts (assinatura de invalidateCache.path.admin)
    - prisma/schema.prisma (model CotizacionDia — campos brl_to_py e usd_to_py em snake_case)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md (Pattern 1, Pattern 2; Pitfall 7 sobre snake_case vs camelCase)
  </read_first>
  <action>
Criar o arquivo NOVO `src/app/admin/actions-cotizacion.ts` com TRÊS exports nomeados. Usar `safeAction` para todas as funções (D-17-01, padrão do projeto).

**Conteúdo COMPLETO do arquivo (todas as funções):**

```typescript
"use server";

import { prisma } from "@/lib/prisma";
import { requireAuth } from "@/lib/user";
import { safeAction, BusinessError, type ActionResult } from "@/lib/action-utils";
import { invalidateCache } from "@/lib/cache/invalidate";

export type CotizacionAtual = {
  id: string;
  brlToPyg: number;
  usdToPyg: number;
  createdAt: string;
};

export type CotizacionHistorialItem = {
  id: string;
  brlToPyg: number;
  usdToPyg: number;
  createdAt: string;
};

/**
 * Salva uma nova cotização (insert-por-update — D-17-01).
 * Cada save cria um registro novo; o histórico nunca é sobrescrito.
 */
export async function salvarCotizacion(
  brlToPyg: number,
  usdToPyg: number
): Promise<ActionResult<{ id: string }>> {
  return safeAction(
    async () => {
      await requireAuth(["ADMIN"]);

      if (!Number.isFinite(brlToPyg) || brlToPyg <= 0) {
        throw new BusinessError("La tasa BRL → Gs. debe ser mayor a cero.");
      }
      if (!Number.isFinite(usdToPyg) || usdToPyg <= 0) {
        throw new BusinessError("La tasa USD → Gs. debe ser mayor a cero.");
      }

      const cotizacion = await prisma.cotizacionDia.create({
        data: {
          brl_to_py: brlToPyg,
          usd_to_py: usdToPyg,
        },
      });

      invalidateCache.path.admin("/config/cotizacion");
      invalidateCache.path.admin("/pdv");

      return { id: cotizacion.id };
    },
    { actionName: "salvarCotizacion" }
  );
}

/**
 * Lê a cotização mais recente (D-17-01).
 * Retorna null se nenhuma cotização foi configurada ainda.
 */
export async function getCotizacionAtual(): Promise<ActionResult<CotizacionAtual | null>> {
  return safeAction(
    async () => {
      await requireAuth(["ADMIN"]);

      const cotizacion = await prisma.cotizacionDia.findFirst({
        orderBy: { created_at: "desc" },
      });

      if (!cotizacion) return null;

      return {
        id: cotizacion.id,
        brlToPyg: Number(cotizacion.brl_to_py),
        usdToPyg: Number(cotizacion.usd_to_py),
        createdAt: cotizacion.created_at.toISOString(),
      };
    },
    { actionName: "getCotizacionAtual" }
  );
}

/**
 * Histórico de cotizações (mais recentes primeiro). Default: 20 registros.
 */
export async function getHistorialCotizaciones(
  limit = 20
): Promise<ActionResult<CotizacionHistorialItem[]>> {
  return safeAction(
    async () => {
      await requireAuth(["ADMIN"]);

      const safeLimit = Math.min(Math.max(1, Math.floor(limit)), 100);

      const registros = await prisma.cotizacionDia.findMany({
        orderBy: { created_at: "desc" },
        take: safeLimit,
      });

      return registros.map((r) => ({
        id: r.id,
        brlToPyg: Number(r.brl_to_py),
        usdToPyg: Number(r.usd_to_py),
        createdAt: r.created_at.toISOString(),
      }));
    },
    { actionName: "getHistorialCotizaciones" }
  );
}
```

**Regras críticas:**
- Schema usa snake_case (`brl_to_py`, `usd_to_py`); DTOs exportados usam camelCase (`brlToPyg`, `usdToPyg`) — mapeamento explícito (Pitfall 7).
- `Number()` converte `Decimal` do Prisma para `number` no DTO (UI consome `number`).
- `requireAuth(["ADMIN"])` em TODAS as funções — guard centralizado, nunca chamar `supabase.auth.getUser()` direto.
- `invalidateCache.path.admin("/pdv")` no save: a próxima carga do PDV pega cotização nova.
- NÃO adicionar `created_by` (schema não tem esse campo — Assumption A2 do RESEARCH.md confirma).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "actions-cotizacion" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/actions-cotizacion.ts` exists
    - File starts with exact line `"use server";`
    - File contains `export async function salvarCotizacion(`
    - File contains `export async function getCotizacionAtual(`
    - File contains `export async function getHistorialCotizaciones(`
    - File contains `export type CotizacionAtual`
    - File contains `export type CotizacionHistorialItem`
    - File contains `safeAction(` (used in all 3 functions)
    - File contains `requireAuth(["ADMIN"])` (3 occurrences — one per function)
    - File contains `prisma.cotizacionDia.create({`
    - File contains `prisma.cotizacionDia.findFirst({` with `orderBy: { created_at: "desc" }`
    - File contains `prisma.cotizacionDia.findMany({` with `orderBy: { created_at: "desc" }`
    - File contains `BusinessError("La tasa BRL` and `BusinessError("La tasa USD`
    - File contains `invalidateCache.path.admin("/config/cotizacion")` AND `invalidateCache.path.admin("/pdv")`
    - File does NOT contain `created_by` (CotizacionDia schema has no such field)
    - `npx tsc --noEmit` reports zero errors for `actions-cotizacion.ts`
  </acceptance_criteria>
  <done>
    Arquivo criado, TypeScript compila sem erros, três funções exportadas e tipadas, todas wrapadas em safeAction.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: actions-pdv.ts — getVariantsParaPdv + criarVentaLoja (espelho de criarMaleta)</name>
  <files>src/app/admin/actions-pdv.ts</files>
  <read_first>
    - src/app/admin/actions-maletas.ts (linhas 152-300 — `criarMaleta` completo: pré-leitura, $transaction array form, sequential decrement com compensação. ESPELHAR este padrão exatamente)
    - src/app/admin/actions-maletas.ts (linhas ~972-1030 — `getAvailableVariants` para basear `getVariantsParaPdv`)
    - src/app/admin/actions-clientes.ts (padrão de safeAction)
    - prisma/schema.prisma (models VentaLoja, VentaLojaItem, EstoqueMovimento; enum Moneda; enum EstoqueMovimentoTipo com valor `venda_loja`)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-RESEARCH.md (Pattern 3 — código completo de criarVentaLoja; Pitfalls 2, 3, 4, 5)
    - .planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-CONTEXT.md (D-17-02, D-17-03, D-17-04, D-17-PDV-01, D-17-PDV-03)
  </read_first>
  <action>
Criar o arquivo NOVO `src/app/admin/actions-pdv.ts` com DOIS exports nomeados. ESTRUTURA OBRIGATÓRIA seguindo o padrão exato de `criarMaleta`.

**Conteúdo COMPLETO do arquivo:**

```typescript
"use server";

import { Prisma } from "@prisma/client";
import { prisma } from "@/lib/prisma";
import { requireAuth } from "@/lib/user";
import { safeAction, BusinessError, type ActionResult } from "@/lib/action-utils";
import { invalidateCache } from "@/lib/cache/invalidate";

export type VariantParaPdv = {
  id: string;
  attribute_name: string;
  attribute_value: string;
  price: number | null;
  stock_quantity: number;
  product: {
    id: string;
    name: string;
    images: string[];
  };
};

export type CriarVentaLojaInput = {
  clienteId: string | null;
  moneda: "PYG" | "USD" | "BRL";
  itens: Array<{
    product_variant_id: string;
    cantidad: number;
  }>;
};

/**
 * Lista variantes disponíveis para o PDV.
 * Filtros: stock_quantity > 0, ativo: true, busca por nome de produto, categoria opcional.
 * stock_quantity já é o estoque líquido (decrementado em criarMaleta) — D-17-PDV-02 + Pitfall 2.
 */
export async function getVariantsParaPdv(params?: {
  search?: string;
  categoryId?: string;
}): Promise<ActionResult<VariantParaPdv[]>> {
  return safeAction(
    async () => {
      await requireAuth(["ADMIN"]);

      const productWhere: Prisma.ProductWhereInput = { ativo: true };
      if (params?.search && params.search.trim().length > 0) {
        productWhere.name = { contains: params.search.trim(), mode: "insensitive" };
      }
      if (params?.categoryId && params.categoryId !== "all") {
        productWhere.categories = { some: { category_id: params.categoryId } };
      }

      const variants = await prisma.productVariant.findMany({
        where: {
          stock_quantity: { gt: 0 },
          ativo: true,
          product: productWhere,
        },
        include: {
          product: {
            select: { id: true, name: true, images: true, price: true },
          },
        },
        orderBy: { product: { name: "asc" } },
        take: 50,
      });

      return variants.map((v) => ({
        id: v.id,
        attribute_name: v.attribute_name,
        attribute_value: v.attribute_value,
        price: v.price != null ? Number(v.price) : v.product.price != null ? Number(v.product.price) : null,
        stock_quantity: v.stock_quantity,
        product: {
          id: v.product.id,
          name: v.product.name,
          images: (v.product.images as string[]) ?? [],
        },
      }));
    },
    { actionName: "getVariantsParaPdv" }
  );
}

/**
 * Cria VentaLoja (D-17-02 — espelho de criarMaleta).
 * - Cotização SEMPRE relida do DB (D-17-03), nunca aceita do payload do client.
 * - Pré-leitura de stock fora de transação (fail fast).
 * - Cliente opcional (D-17-PDV-01) — clienteId: null = Consumidor Final.
 * - Decremento sequencial com compensação (PrismaPg não suporta interactive transactions).
 * - Math.round() por linha; soma inteiros (D-17-04).
 */
export async function criarVentaLoja(
  input: CriarVentaLojaInput
): Promise<ActionResult<{ id: string }>> {
  return safeAction(
    async () => {
      const user = await requireAuth(["ADMIN"]);
      if (!user.profileId) {
        throw new BusinessError("Tu perfil de usuario no está configurado.");
      }

      // 1) Validação básica do input
      if (!input.itens || input.itens.length === 0) {
        throw new BusinessError("Agregá al menos un producto a la venta.");
      }
      const monedasValidas = ["PYG", "USD", "BRL"] as const;
      if (!monedasValidas.includes(input.moneda)) {
        throw new BusinessError("Moneda inválida.");
      }
      for (const item of input.itens) {
        if (!Number.isInteger(item.cantidad) || item.cantidad < 1) {
          throw new BusinessError("Cantidad inválida en uno de los productos.");
        }
      }

      // 2) Validar clienteId (se passado, deve existir)
      if (input.clienteId) {
        const exists = await prisma.cliente.findUnique({
          where: { id: input.clienteId },
          select: { id: true },
        });
        if (!exists) {
          throw new BusinessError("Cliente no encontrado.");
        }
      }

      // 3) Pré-leitura: cotização do DB (D-17-03 — NUNCA do payload)
      const cotizacion = await prisma.cotizacionDia.findFirst({
        orderBy: { created_at: "desc" },
      });
      if (!cotizacion) {
        throw new BusinessError(
          "No hay cotización registrada. Configurá la cotización antes de registrar ventas."
        );
      }
      const brlToPyg = Number(cotizacion.brl_to_py);
      const usdToPyg = Number(cotizacion.usd_to_py);

      // 4) Pré-leitura: variantes para validação de stock + cálculo de subtotais
      const variantIds = input.itens.map((i) => i.product_variant_id);
      const variants = await prisma.productVariant.findMany({
        where: { id: { in: variantIds } },
        include: { product: { select: { name: true, price: true } } },
      });
      const variantMap = new Map(variants.map((v) => [v.id, v]));

      for (const item of input.itens) {
        const variant = variantMap.get(item.product_variant_id);
        if (!variant) {
          throw new BusinessError("Una de las variantes seleccionadas no existe.");
        }
        if (variant.stock_quantity < item.cantidad) {
          throw new BusinessError(
            `Stock insuficiente para "${variant.product.name} — ${variant.attribute_value}": disponible ${variant.stock_quantity}.`
          );
        }
      }

      // 5) Cálculo de totais (D-17-04 — Math.round por linha, soma inteiros)
      let totalPyg = 0;
      const itensConSubtotal = input.itens.map((item) => {
        const variant = variantMap.get(item.product_variant_id)!;
        const precioUnitPyg =
          variant.price != null
            ? Number(variant.price)
            : variant.product.price != null
              ? Number(variant.product.price)
              : 0;
        const subtotalPyg = Math.round(precioUnitPyg * item.cantidad);
        totalPyg += subtotalPyg;
        return {
          product_variant_id: item.product_variant_id,
          cantidad: item.cantidad,
          precio_unitario: precioUnitPyg,
          subtotal: subtotalPyg,
        };
      });

      // Conversão do total para a moeda de pagamento (preserva PYG como fonte de verdade)
      const total =
        input.moneda === "PYG"
          ? totalPyg
          : input.moneda === "USD"
            ? Math.round((totalPyg / usdToPyg) * 100) / 100
            : Math.round((totalPyg / brlToPyg) * 100) / 100;

      // 6) Criar VentaLoja + itens (cascata via relation)
      const venta = await prisma.ventaLoja.create({
        data: {
          cliente_id: input.clienteId ?? null,
          total,
          moneda: input.moneda,
          total_pyg: totalPyg,
          cotizacion_snapshot: {
            brl_to_py: brlToPyg,
            usd_to_py: usdToPyg,
            cotizacion_id: cotizacion.id,
          },
          created_by: user.profileId,
          itens: {
            create: itensConSubtotal,
          },
        },
      });

      // 7) Decremento sequencial com compensação (padrão criarMaleta — Pitfall 3)
      const stockErrors: { variantId: string; qty: number }[] = [];
      for (const item of input.itens) {
        try {
          await prisma.productVariant.update({
            where: { id: item.product_variant_id },
            data: { stock_quantity: { decrement: item.cantidad } },
          });
        } catch (err) {
          console.error(
            "[criarVentaLoja] Error decrementing stock for variant",
            item.product_variant_id,
            err
          );
          stockErrors.push({ variantId: item.product_variant_id, qty: item.cantidad });
        }
      }
      if (stockErrors.length > 0) {
        // Compensação: deletar VentaLoja (cascade remove os itens)
        await prisma.ventaLoja.delete({ where: { id: venta.id } }).catch(() => {});
        throw new BusinessError(
          "No se pudo actualizar el stock. Intentá de nuevo."
        );
      }

      // 8) Registrar movimentos de estoque (best-effort — não bloqueia a venda)
      for (const item of input.itens) {
        await prisma.estoqueMovimento
          .create({
            data: {
              product_variant_id: item.product_variant_id,
              quantidade: item.cantidad,
              tipo: "venda_loja",
              motivo: `Venta en loja #${venta.id.slice(0, 8)}`,
              venta_loja_id: venta.id,
            },
          })
          .catch((err) =>
            console.error("[criarVentaLoja] Movement log failed:", err)
          );
      }

      invalidateCache.path.admin("/pdv");

      return { id: venta.id };
    },
    { actionName: "criarVentaLoja" }
  );
}
```

**Regras CRÍTICAS:**
- NUNCA aceitar `brlToPyg`/`usdToPyg` no input — sempre `findFirst` no DB (D-17-03 / Pitfall 4).
- NÃO usar `prisma.$transaction(async (tx) => ...)` — driver PrismaPg não suporta (Pitfall 3). O padrão é create + sequential update + compensação manual.
- Subtotal por linha: `Math.round(precioUnitPyg * cantidad)` — somar inteiros (Pitfall 5).
- `cotizacion_snapshot` é JSON imutável; uma vez salvo, não recalcular nunca.
- Filtro `where: { stock_quantity: { gt: 0 }, ativo: true }` em `getVariantsParaPdv` — produtos sem stock não devem aparecer (Pitfall 2 — `stock_quantity` JÁ é o estoque líquido).
- `invalidateCache.path.admin("/pdv")` ao final — para refletir stock atualizado em loads subsequentes.
- NUNCA logar PII (nome de cliente, RUC, telefone) em `console.error` — usar apenas IDs.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/wesleyhudson/Documents/next-monarca/tsconfig.json 2>&1 | grep -E "actions-pdv" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/admin/actions-pdv.ts` exists
    - File starts with `"use server";`
    - File contains `export async function getVariantsParaPdv(`
    - File contains `export async function criarVentaLoja(`
    - File contains `export type VariantParaPdv`
    - File contains `export type CriarVentaLojaInput`
    - File contains `safeAction(` (2 occurrences — one per function)
    - File contains `requireAuth(["ADMIN"])` (2 occurrences)
    - File contains `prisma.cotizacionDia.findFirst({` with `orderBy: { created_at: "desc" }`
    - File contains `cotizacion_snapshot:` with `brl_to_py`, `usd_to_py`, `cotizacion_id` fields
    - File contains `cliente_id: input.clienteId ?? null` (literal — D-17-PDV-01)
    - File contains `Math.round(precioUnitPyg * item.cantidad)`
    - File contains `tipo: "venda_loja"` (string literal exactly)
    - File contains `stock_quantity: { decrement:` (sequential decrement)
    - File contains `prisma.ventaLoja.delete({ where: { id: venta.id } })` (compensation)
    - File contains `stock_quantity: { gt: 0 }` (filter in getVariantsParaPdv)
    - File contains `ativo: true` (filter in variant query)
    - File does NOT contain `prisma.$transaction(async` (interactive transaction prohibited — Pitfall 3) — `grep -c '$transaction(async' src/app/admin/actions-pdv.ts` returns 0
    - File does NOT accept `brlToPyg` or `usdToPyg` in CriarVentaLojaInput type (D-17-03)
    - `npx tsc --noEmit` reports zero errors for `actions-pdv.ts`
  </acceptance_criteria>
  <done>
    Arquivo criado, TypeScript compila sem erros, duas funções exportadas, padrão de criarMaleta espelhado fielmente, todas as decisões locked respeitadas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action `criarVentaLoja` | Untrusted input (clienteId, moneda, itens) crosses here |
| client → Server Action `salvarCotizacion` | Untrusted input (brlToPyg, usdToPyg) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | criarVentaLoja — exchange rate | mitigate | Cotização SEMPRE relida do DB via `findFirst({ orderBy: { created_at: "desc" } })`; payload nunca aceita `brlToPyg`/`usdToPyg` (D-17-03 / Pitfall 4) |
| T-17-02 | Tampering | criarVentaLoja — price | mitigate | `precio_unitario` lido de `ProductVariant.price` ou `Product.price` no servidor; payload só carrega IDs e cantidad |
| T-17-03 | Tampering | criarVentaLoja — stock | mitigate | Pré-leitura de `stock_quantity` antes de criar venda; falha fast se `cantidad > stock_quantity`; compensação via `prisma.ventaLoja.delete` em caso de race condition no decrement |
| T-17-04 | Elevation of Privilege | qualquer action | mitigate | `requireAuth(["ADMIN"])` em TODAS as funções como primeira linha de execução |
| T-17-05 | Information Disclosure | console.error | mitigate | Logs registram apenas IDs (variantId, vendaId); nunca nome/RUC/telefone de cliente (CLAUDE.md §3 — PII) |
| T-17-06 | Denial of Service | getHistorialCotizaciones | mitigate | `limit` clampado entre 1 e 100 via `Math.min(Math.max(1, ...), 100)` |
| T-17-07 | Tampering | clienteId injection | mitigate | `prisma.cliente.findUnique({ where: { id: input.clienteId } })` valida existência antes de criar venda |
</threat_model>

<verification>
- `npx prisma migrate dev` aplicou a migration; `prisma/migrations/{timestamp}_make_venta_loja_cliente_nullable/migration.sql` existe
- `npx prisma generate` regenerou os tipos
- `npx tsc --noEmit` compila os 3 arquivos sem erros
- `grep -E "cliente_id\s+String\?\s+@db\.Uuid" prisma/schema.prisma` retorna match
- Os tipos `CotizacionAtual`, `CotizacionHistorialItem`, `VariantParaPdv`, `CriarVentaLojaInput` estão exportados e podem ser importados pelos plans 17-02 e 17-03
- Nenhuma das funções aceita taxas de câmbio do payload do client
</verification>

<success_criteria>
- Migration aplicada com `cliente_id` nullable e relação `Cliente?`
- 3 funções de cotização (`salvarCotizacion`, `getCotizacionAtual`, `getHistorialCotizaciones`) implementadas com `safeAction`
- 2 funções de PDV (`getVariantsParaPdv`, `criarVentaLoja`) implementadas, espelhando o padrão de `criarMaleta`
- Cotização sempre relida do DB; nunca do payload do client
- Decremento de stock sequencial com compensação manual
- Cliente opcional (null = Consumidor Final) suportado
- TypeScript compila zero erros
</success_criteria>

<output>
After completion, create `.planning/phases/17-pdv-core-cotizacion-fluxo-venda/17-01-SUMMARY.md`
</output>
