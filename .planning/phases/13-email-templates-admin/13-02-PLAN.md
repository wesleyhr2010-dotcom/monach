---
phase: 13-email-templates-admin
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [src/app/admin/config/emails/actions.ts, src/app/admin/config/emails/page.tsx, src/app/admin/config/emails/TemplateEditor.tsx, src/app/admin/config/emails/[tipo]/page.tsx]
autonomous: false
requirements: [ETML-01, ETML-02, ETML-03, ETML-04, ETML-05]

must_haves:
  truths:
    - "Admin can see the list of 7 email templates in /admin/config/emails"
    - "Admin can save subject, HTML body, and Plain Text body overrides via Server Actions"
    - "Editor displays chips for template-specific variables"
  artifacts:
    - path: "src/app/admin/config/emails/actions.ts"
      provides: "Server Actions for CRUD operations"
    - path: "src/app/admin/config/emails/TemplateEditor.tsx"
      provides: "Reusable editor component with variable chips and plain text field"
  key_links:
    - from: "src/app/admin/config/emails/[tipo]/page.tsx"
      to: "src/app/admin/config/emails/actions.ts"
      via: "Server Action call on submit"
---

<objective>
Implement the administrative interface for managing email template overrides.
Purpose: Allow administrators to visualize status and edit templates via a safe, variable-aware UI.
Output: Server Actions for CRUD, a list view with status indicators, and a specialized template editor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<interfaces>
From src/lib/emails-shared.ts:
```typescript
export interface EmailTemplateOption {
  value: string; // e.g., 'acerto_confirmado'
  label: string; // e.g., 'Confirmação de Acerto'
}
```

From src/app/admin/config/notif-push/TemplateEditor.tsx:
(Pattern reference for chip insertion and textarea handling)
</interfaces>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-email-templates-admin/13-CONTEXT.md
@.planning/phases/13-email-templates-admin/13-01-PLAN.md
@src/app/admin/config/notif-push/TemplateEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions for Email Templates</name>
  <files>src/app/admin/config/emails/actions.ts</files>
  <action>
    Implement Server Actions:
    - `saveEmailTemplate(data: EmailTemplateInput): Promise<ActionResult<EmailTemplate>>`: 
      - Validates variables against whitelist (D-05, D-08).
      - Uses `upsert` in Prisma.
      - **ETML-04**: Must handle `body_text` (Plain Text version of the body) if provided.
    - `deleteEmailTemplate(tipo: string): Promise<ActionResult<void>>`: Deletes the override from DB (D-02).
    - `toggleEmailTemplate(tipo: string, active: boolean): Promise<ActionResult<void>>`: Toggles `ativo` field (D-04).
    
    All actions must return `ActionResult<T>` and use `requireAuth(["ADMIN"])`.
  </action>
  <verify>
    <automated>ls src/app/admin/config/emails/actions.ts</automated>
  </verify>
  <done>Server Actions implemented with validation and plain text support</done>
</task>

<task type="auto">
  <name>Task 2: Template List View</name>
  <files>src/app/admin/config/emails/page.tsx</files>
  <action>
    Create the main management page at `/admin/config/emails`.
    - Fetch all overrides from DB.
    - Display the 7 templates (using `TIPO_EMAIL_OPTIONS` as source of truth).
    - **Language Compliance**: Show status badge using Paraguayan Spanish: 
      - "Estándar (Código)" if no DB record.
      - "Personalizado (BD)" if record exists.
    - Add a toggle for "Ativo/Inativo" per template.
    - Add a link/button to "Editar" which leads to `[tipo]` route.
    
    Use `AdminHeader`, `AdminCard`, and `AdminStatusBadge` for visual consistency.
  </action>
  <verify>
    <automated>ls src/app/admin/config/emails/page.tsx</automated>
  </verify>
  <done>List view with status indicators created using correct Spanish terminology</done>
</task>

<task type="auto">
  <name>Task 3: Template Editor Component and Page</name>
  <files>src/app/admin/config/emails/TemplateEditor.tsx, src/app/admin/config/emails/[tipo]/page.tsx</files>
  <action>
    1. Create `TemplateEditor.tsx` adapted from `notif-push/TemplateEditor.tsx`.
       - Fields: Subject (input), Greeting (input), Body HTML (textarea), **Body Text (Plain Text)** (textarea per ETML-04), Preview Text (input).
       - Variable chips: Clicking a chip inserts `{variable}` at cursor position.
       - Syntax check: Basic validation of closed braces.
    2. Create dynamic route `src/app/admin/config/emails/[tipo]/page.tsx`.
       - Fetch existing override if any.
       - Render `TemplateEditor`.
       - Implement "**Restablecer a estándar**" button (calls delete action).
  </action>
  <verify>
    <automated>ls src/app/admin/config/emails/TemplateEditor.tsx</automated>
  </verify>
  <done>Specialized editor implemented with plain text field and correct Spanish labels</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Admin Email Templates Management UI</what-built>
  <how-to-verify>
    1. Navigate to `/admin/config/emails`.
    2. Verify list of 7 templates is visible with "Estándar (Código)" badges.
    3. Click "Editar" on one template.
    4. Verify both "Body HTML" and "Body Text (Plain Text)" fields are present.
    5. Alter the subject and both bodies, insert a variable using a chip.
    6. Save and verify the record is created in the database.
    7. Return to list and verify the badge changed to "Personalizado (BD)".
    8. Click "Restablecer a estándar" and verify it returns to code fallback.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Elevation of Privilege | Server Actions | mitigate | All actions wrapped in `requireAuth(["ADMIN"])` |
| T-13-04 | Tampering | HTML Input | mitigate | Validation on save ensures no forbidden variables are used |
</threat_model>

<success_criteria>
Administrator can list, edit, save, and reset email templates via the web interface. All UI labels use Paraguayan Spanish.
</success_criteria>

<output>
After completion, create `.planning/phases/13-email-templates-admin/13-02-SUMMARY.md`
</output>
