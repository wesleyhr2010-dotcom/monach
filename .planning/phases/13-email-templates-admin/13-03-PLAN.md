---
phase: 13-email-templates-admin
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified: [src/lib/email-templates/acerto-confirmado.ts, src/lib/email-templates/candidatura-aprovada.ts, src/lib/email-templates/candidatura-rechazada.ts, src/lib/email-templates/convite-usuario.ts, src/lib/email-templates/documento-aprovado.ts, src/lib/email-templates/documento-pendente.ts, src/lib/email-templates/documento-rejeitado.ts, tests/integration/email-override.test.ts]
autonomous: true
requirements: [ETML-07]

must_haves:
  truths:
    - "Transactional email functions check for overrides before sending"
    - "System falls back to TS templates if no override exists"
    - "Integration tests prove that creating a DB override changes the sent email content"
  artifacts:
    - path: "tests/integration/email-override.test.ts"
      provides: "E2E verification of the override system"
  key_links:
    - from: "src/lib/email-templates/*.ts"
      to: "src/lib/email-logic.ts"
      via: "getEmailContent call"
---

<objective>
Integrate the override logic into existing email templates and verify system behavior.
Purpose: Finalize the loop from Admin UI -> DB -> Send Logic.
Output: Refactored templates and comprehensive integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<interfaces>
From src/lib/email-logic.ts:
```typescript
/**
 * Resolves email content (Subject, HTML, Text) from DB override or returns null.
 * Internally handles renderEmailBase and variable interpolation.
 */
export async function getEmailContent(
  tipo: string, 
  context: Record<string, any>
): Promise<{ subject: string; html: string; text: string } | null>;
```
</interfaces>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-email-templates-admin/13-01-PLAN.md
@.planning/phases/13-email-templates-admin/13-02-PLAN.md
@src/lib/email-templates/acerto-confirmado.ts
@src/lib/email-templates/documento-aprovado.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Transactional Email Templates</name>
  <files>src/lib/email-templates/acerto-confirmado.ts, src/lib/email-templates/candidatura-aprovada.ts, src/lib/email-templates/candidatura-rechazada.ts, src/lib/email-templates/convite-usuario.ts, src/lib/email-templates/documento-aprovado.ts, src/lib/email-templates/documento-pendente.ts, src/lib/email-templates/documento-rejeitado.ts</files>
  <action>
    Update each of the 7 email template functions to support overrides.
    Pattern:
    1. Assemble `context` object containing all data needed for interpolation (names, IDs, amounts, URLs).
    2. Call `const override = await getEmailContent("tipo_template", context)`.
    3. If `override` is not null:
       - Call `sendEmail` using `override.subject`, `override.html`, and `override.text`.
       - Return early.
    4. If `override` is null:
       - Proceed with existing hardcoded TS logic.
       
    Ensure `getEmailContent` handles the `renderEmailBase` wrapping if it finds an override.
  </action>
  <verify>
    <automated>grep "getEmailContent" src/lib/email-templates/acerto-confirmado.ts</automated>
  </verify>
  <done>All 7 templates integrated with the override system</done>
</task>

<task type="auto">
  <name>Task 2: Integration Testing</name>
  <files>tests/integration/email-override.test.ts</files>
  <action>
    Create a new integration test file.
    Tests should cover:
    1. Send email without DB record -> uses TS fallback (verify via mock sendEmail).
    2. Create DB override -> call email function -> verify sent content matches DB override.
    3. Deactivate DB override (ativo: false) -> call email function -> verify fallback is used again.
    4. Delete DB override -> verify fallback is used.
    5. Test variable interpolation: Verify `{nome_revendedora}` in DB override is correctly replaced in the sent email.
  </action>
  <verify>
    <automated>npm test tests/integration/email-override.test.ts</automated>
  </verify>
  <done>Integration tests passing and confirming override behavior</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | Template Logic | mitigate | Regression tests ensure fallback always works if DB fails or is empty |
</threat_model>

<success_criteria>
All 7 transactional emails can be customized via Admin UI without code changes. Fallback mechanism is robust and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/13-email-templates-admin/13-03-SUMMARY.md`
</output>
