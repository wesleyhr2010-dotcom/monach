---
phase: quick
plan: 260525-rg8
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/YYYYMMDD_add_pontos_por_guarani/migration.sql
  - src/lib/gamificacao.ts
  - src/app/app/actions-revendedora.ts
  - src/app/admin/actions-gamificacao.ts
  - src/app/admin/gamificacao/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Regra venda_maleta com pontos_por_guarani definido gera pontos proporcionais ao valor da venda"
    - "Regra venda_maleta com pontos_por_guarani null gera pontos fixos (retrocompativel)"
    - "Admin pode configurar pontos_por_guarani via UI"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Campo pontos_por_guarani em GamificacaoRegra"
      contains: "pontos_por_guarani"
    - path: "src/lib/gamificacao.ts"
      provides: "Calculo proporcional em awardPoints"
      contains: "pontos_por_guarani"
    - path: "src/app/admin/gamificacao/page.tsx"
      provides: "Coluna e edicao de Pts/Gs na tabela admin"
      contains: "pontos_por_guarani"
  key_links:
    - from: "src/app/app/actions-revendedora.ts"
      to: "src/lib/gamificacao.ts"
      via: "awardPoints com valorVenda"
      pattern: "awardPoints.*venda_maleta.*preco_fixado"
    - from: "src/app/admin/actions-gamificacao.ts"
      to: "prisma/schema.prisma"
      via: "CRUD do campo pontos_por_guarani"
      pattern: "pontos_por_guarani"
---

<objective>
Implementar pontos de gamificacao proporcionais ao valor da venda em Guaranis.

Purpose: Permitir que regras de gamificacao concedam pontos proporcionais (ex: 1 pt / 1.000 Gs) em vez de fixos, incentivando vendas de maior valor.
Output: Migration aplicada, logica de calculo atualizada, UI admin para configurar o fator.
</objective>

<execution_context>
@CLAUDE.md
</execution_context>

<context>
@prisma/schema.prisma (model GamificacaoRegra, linhas 403-421)
@src/lib/gamificacao.ts (funcao awardPoints, linhas 96-158)
@src/app/app/actions-revendedora.ts (chamada awardPoints em venda, linha 340)
@src/app/admin/actions-gamificacao.ts (schema + CRUD de regras)
@src/app/admin/gamificacao/page.tsx (tabela de regras admin)

<interfaces>
From src/lib/gamificacao.ts:
```typescript
export async function awardPoints(
  resellerId: string,
  acao: string,
  tx?: Omit<ExtendedPrismaClient, "$connect" | "$disconnect" | "$on" | "$transaction" | "$use" | "$extends">
): Promise<{ pontos: number; descricao: string } | null>
```

From src/app/admin/actions-gamificacao.ts:
```typescript
const updateRegraSchema = z.object({
  nome: z.string().min(3).max(80),
  descricao: z.string().max(200),
  pontos: z.number().int().min(1).max(10000),
  ativo: z.boolean(),
  icone: z.string(),
  ordem: z.number().int(),
  limite_diario: z.number().int().min(1).nullable(),
  meta_valor: z.number().positive().nullable(),
});

export async function getRegras(): Promise<Array<GamificacaoRegra & { meta_valor: number | null }>>
export async function atualizarRegra(id: string, rawData: z.infer<typeof updateRegraSchema>): Promise<...>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + lib calculo proporcional</name>
  <files>prisma/schema.prisma, src/lib/gamificacao.ts</files>
  <action>
1. Em prisma/schema.prisma, no model GamificacaoRegra (apos o campo `meta_valor`), adicionar:
   `pontos_por_guarani Decimal? @db.Decimal(10, 6)`

2. Gerar e aplicar migration: `npx prisma migrate dev --name add_pontos_por_guarani`

3. Em src/lib/gamificacao.ts, alterar a assinatura de `awardPoints` para aceitar 4o parametro opcional:
   ```
   awardPoints(resellerId: string, acao: string, tx?: ..., valorVenda?: number)
   ```

4. Dentro de awardPoints, APOS buscar a regra e antes de criar o pontosExtrato, calcular os pontos efetivos:
   - Declarar `let pontosEfetivos = regra.pontos;`
   - Se `regra.pontos_por_guarani != null && valorVenda != null && valorVenda > 0`:
     - `pontosEfetivos = Math.max(1, Math.floor(valorVenda * Number(regra.pontos_por_guarani)))`
   - Usar `pontosEfetivos` no lugar de `regra.pontos` no create e no return

5. Manter a guarda `if (!regra || !regra.ativo || regra.pontos <= 0) return null;` — mas quando pontos_por_guarani esta definido, a guarda de `regra.pontos <= 0` deve ser relaxada: mudar para `if (!regra || !regra.ativo || (regra.pontos <= 0 && regra.pontos_por_guarani == null)) return null;`

6. Todas as chamadas existentes (sem 4o argumento) continuam funcionando identicamente — regra com pontos_por_guarani null usa pontos fixo.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && npx prisma validate && grep -c "pontos_por_guarani" prisma/schema.prisma && grep -c "pontosEfetivos" src/lib/gamificacao.ts</automated>
  </verify>
  <done>Campo pontos_por_guarani existe no schema, migration aplicada, awardPoints calcula proporcionalmente quando campo definido e retrocompativel quando null.</done>
</task>

<task type="auto">
  <name>Task 2: Passar valorVenda na chamada de venda + atualizar actions admin</name>
  <files>src/app/app/actions-revendedora.ts, src/app/admin/actions-gamificacao.ts</files>
  <action>
1. Em src/app/app/actions-revendedora.ts, linha ~340 onde esta:
   `const pontosVenda = await awardPoints(resellerId, 'venda_maleta', tx);`
   Trocar para:
   `const pontosVenda = await awardPoints(resellerId, 'venda_maleta', tx, Number(item.preco_fixado ?? 0));`
   (item.preco_fixado e Decimal, converter com Number())

2. Em src/app/admin/actions-gamificacao.ts:
   a) No updateRegraSchema, adicionar campo:
      `pontos_por_guarani: z.number().positive().nullable(),`
   
   b) Em getRegras(), no map, adicionar conversao:
      `pontos_por_guarani: r.pontos_por_guarani != null ? Number(r.pontos_por_guarani) : null,`
   
   c) Em atualizarRegra(), o campo ja sera incluido automaticamente via spread do `data` validado pelo schema. Nenhuma mudanca adicional necessaria alem do schema.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && grep "awardPoints.*venda_maleta.*preco_fixado" src/app/app/actions-revendedora.ts && grep "pontos_por_guarani" src/app/admin/actions-gamificacao.ts | wc -l</automated>
  </verify>
  <done>Venda passa valor em Gs para awardPoints. Admin actions incluem pontos_por_guarani no schema zod, leitura e escrita.</done>
</task>

<task type="auto">
  <name>Task 3: UI admin — coluna Pts/Gs e edicao inline</name>
  <files>src/app/admin/gamificacao/page.tsx</files>
  <action>
1. Na tabela de Reglas de Puntos, adicionar coluna "Pts/Gs" entre "Puntos" e "Activo":
   - Header: `<th style={{ textAlign: "right" }}>Pts/Gs</th>`
   - Cell: mostra `r.pontos_por_guarani` formatado (ex: "0,001") quando definido, ou "—" quando null

2. Adicionar estado local para edicao inline do pontos_por_guarani:
   - `const [editingPtsGs, setEditingPtsGs] = useState<string | null>(null)` (id da regra)
   - `const [ptsGsValue, setPtsGsValue] = useState("")`

3. Na celula Pts/Gs de cada regra:
   - Se NAO editando: mostrar valor + botao pequeno "Editar" (icone Pencil de lucide-react, tamanho 12)
   - Se editando (editingPtsGs === r.id): mostrar input numerico + botoes Guardar/Cancelar
   - Input: `type="number" step="0.000001" min="0"` com estilo inline usando tokens admin
   - Guardar: chamar `atualizarRegra(r.id, { ...campos_existentes, pontos_por_guarani: ptsGsValue ? parseFloat(ptsGsValue) : null })` e reload
   - Para limpar (voltar a pontos fixos): permitir campo vazio → salva null

4. Textos em espanhol: "Pts/Gs", "Guardar", "Cancelar"

5. Estilo: usar APENAS tokens CSS variables (`--admin-*`), font-family Raleway. Zero hex hard-coded.
   - Input border: `var(--admin-border)`
   - Input background: `var(--admin-bg-input)` ou `var(--admin-bg-elevated)`
   - Botao guardar: `var(--admin-accent)`
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && grep -c "pontos_por_guarani" src/app/admin/gamificacao/page.tsx && grep -c "Pts/Gs" src/app/admin/gamificacao/page.tsx && ! grep -E "#[0-9A-Fa-f]{3,8}" src/app/admin/gamificacao/page.tsx</automated>
  </verify>
  <done>Coluna Pts/Gs visivel na tabela admin, edicao inline funcional, sem hex hard-coded, textos em espanhol.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` — schema valido
2. `npx prisma migrate status` — migration aplicada
3. `npm run lint` — sem erros de lint
4. Abrir /admin/gamificacao — tabela mostra coluna Pts/Gs
5. Editar pontos_por_guarani de venda_maleta para 0.001 (1 pt / 1.000 Gs)
6. Registrar venda de item com preco 50.000 Gs → deve ganhar 50 pontos
7. Limpar pontos_por_guarani (null) → venda deve voltar a dar pontos fixos
</verification>

<success_criteria>
- Regra com pontos_por_guarani=0.001 e venda de 100.000 Gs gera 100 pontos
- Regra com pontos_por_guarani=null gera pontos fixos (valor do campo `pontos`)
- Venda de valor muito baixo (ex: 500 Gs com fator 0.001) gera minimo 1 ponto
- Admin consegue configurar e limpar o fator via UI inline
- Zero breaking changes nas chamadas existentes de awardPoints sem 4o argumento
</success_criteria>

<output>
Commit: `feat(gamificacao): pontos proporcionais ao valor da venda em Guaranis`
</output>
