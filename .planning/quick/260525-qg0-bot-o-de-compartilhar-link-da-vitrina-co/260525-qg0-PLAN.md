---
phase: quick-260525-qg0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/app/actions-revendedora.ts
  - src/app/app/catalogo/page.tsx
autonomous: true
must_haves:
  truths:
    - "Revendedora ve un boton 'Compartir mi vitrina' en la pagina de catalogo"
    - "Al tocar el boton se abre el Web Share sheet con el link de la vitrina"
    - "Si Web Share no esta soportado, el link se copia al clipboard"
    - "Despues de compartir exitosamente, se registran puntos de gamificacion"
  artifacts:
    - path: "src/app/app/actions-revendedora.ts"
      provides: "Server action registrarPuntosCompartirLinkVitrina + getSlugRevendedora"
      contains: "compartilhou_link_vitrina"
    - path: "src/app/app/catalogo/page.tsx"
      provides: "Boton compartir vitrina + logica Web Share + fallback clipboard"
      contains: "Compartir mi vitrina"
  key_links:
    - from: "src/app/app/catalogo/page.tsx"
      to: "src/app/app/actions-revendedora.ts"
      via: "import { registrarPuntosCompartirLinkVitrina, getSlugRevendedora }"
      pattern: "registrarPuntosCompartirLinkVitrina"
    - from: "src/app/app/actions-revendedora.ts"
      to: "src/lib/gamificacao.ts"
      via: "awardPoints(resellerId, 'compartilhou_link_vitrina')"
      pattern: "compartilhou_link_vitrina"
---

<objective>
Add a "Compartir mi vitrina" button to the reseller catalog page that shares the vitrina link via Web Share API (with clipboard fallback) and awards gamification points.

Purpose: Allow resellers to easily share their personal storefront link to drive traffic and earn points.
Output: Working share button in catalog page + server action for point registration.
</objective>

<execution_context>
@.planning/STATE.md
</execution_context>

<context>
@src/app/app/actions-revendedora.ts (registrarPuntosCompartirCatalogo pattern at line 587-598)
@src/app/app/catalogo/page.tsx (full page — sticky bottom bar at line 222-237)
@src/lib/gamificacao.ts (awardPoints signature)
@src/app/app/mas/page.tsx (slug fetch pattern at lines 28-31)

<interfaces>
From src/app/app/actions-revendedora.ts:
```typescript
// Existing pattern to replicate:
export async function registrarPuntosCompartirCatalogo() {
  return safeAction(async () => {
    const user = await requireAuth(["REVENDEDORA"]);
    if (!user.profileId) throw new Error("Perfil no encontrado.");
    const resellerId = user.profileId;
    await awardPoints(resellerId, "compartilhou_catalogo");
    return { success: true };
  });
}
```

From src/lib/gamificacao.ts:
```typescript
export async function awardPoints(
  resellerId: string,
  acao: string,
  tx?: Omit<ExtendedPrismaClient, ...>
): Promise<{ pontos: number; descricao: string } | null>;
```

From prisma schema — Reseller model:
```
slug String @unique
```

Env var available: `NEXT_PUBLIC_SITE_URL` (e.g., "http://localhost:3000" / "https://monarcasemijoyas.com.py")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server actions for slug retrieval and vitrina link point registration</name>
  <files>src/app/app/actions-revendedora.ts</files>
  <action>
Add two new exported server actions at the end of `actions-revendedora.ts` (after `registrarPuntosCompartirCatalogo`):

1. `getSlugRevendedora()` — Calls `requireAuth(["REVENDEDORA"])`, gets `user.profileId`, queries `prisma.reseller.findUnique({ where: { id: resellerId }, select: { slug: true } })`. Returns `{ slug: string }`. Wrap in `safeAction`.

2. `registrarPuntosCompartirLinkVitrina()` — Identical pattern to `registrarPuntosCompartirCatalogo` but calls `awardPoints(resellerId, "compartilhou_link_vitrina")`. The acao string MUST be exactly `compartilhou_link_vitrina` — the admin configures the gamification rule in the database with this key.
  </action>
  <verify>
    <automated>grep -c "compartilhou_link_vitrina" src/app/app/actions-revendedora.ts | grep -q "1" && grep -c "getSlugRevendedora" src/app/app/actions-revendedora.ts | grep -q "1" && echo "PASS"</automated>
  </verify>
  <done>Both server actions exist, export correctly, use safeAction + requireAuth pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Compartir mi vitrina" button with Web Share + clipboard fallback + point awarding</name>
  <files>src/app/app/catalogo/page.tsx</files>
  <action>
Modify `src/app/app/catalogo/page.tsx`:

1. Add imports: `import { registrarPuntosCompartirLinkVitrina, getSlugRevendedora } from "../actions-revendedora"` and `import { Link2 } from "lucide-react"` (icon for the vitrina share button — distinct from Share2 used for photos).

2. Add state: `const [vitrinaSlug, setVitrinaSlug] = useState<string | null>(null)` and `const [shareMsg, setShareMsg] = useState<string | null>(null)`.

3. In the existing `useEffect` that calls `fetchCatalogo`, after the catalogo fetch completes successfully, also call `getSlugRevendedora()` and if successful set `setVitrinaSlug(result.data.slug)`.

4. Add handler function `handleShareVitrina`:
   - Build the vitrina URL: `const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || window.location.origin; const vitrinaUrl = \`${siteUrl}/vitrina/${vitrinaSlug}\`;`
   - Share text: `"Visita mi vitrina de joyas Monarca \u{1f48e}"` (this is the only emoji allowed in the share text per business pattern).
   - Try `navigator.share({ title: "Mi Vitrina Monarca", text: shareText, url: vitrinaUrl })`.
   - If `navigator.share` succeeds (no throw, no AbortError): call `await registrarPuntosCompartirLinkVitrina()` and show feedback via `setShareMsg("¡Puntos ganados!")`.
   - If AbortError (user cancelled): do nothing, return early.
   - If `navigator.share` is not available OR `canShare` is false: use `navigator.clipboard.writeText(vitrinaUrl)`, show `setShareMsg("¡Link copiado!")`, and still call `registrarPuntosCompartirLinkVitrina()`.
   - Clear `shareMsg` after 3 seconds via `setTimeout(() => setShareMsg(null), 3000)`.

5. In the sticky bottom section (the existing `{itens.length > 0 && (...)}` block at line 223), wrap both buttons in a `flex flex-col gap-2` container:
   - FIRST: The new "Compartir mi vitrina" button — only render if `vitrinaSlug` is not null. Style: `w-full py-3.5 rounded-full text-sm font-semibold flex items-center justify-center gap-2 shadow-lg` with `background: "var(--app-accent, #35605A)"` and `color: "#fff"`, fontFamily: `"var(--font-raleway)"`. Icon: `Link2 className="w-4 h-4"`. Text: `"Compartir mi vitrina"`.
   - SECOND: The existing "Seleccionar varias fotos" button (unchanged, keep its `background: "#1A1A1A"` style).

6. Below the button container (still inside the sticky div), render the feedback toast: `{shareMsg && <p className="text-center text-xs font-medium text-app-accent mt-1" style={{ fontFamily: "var(--font-raleway)" }}>{shareMsg}</p>}`.

Do NOT use any hard-coded hex values for the vitrina button background — use `var(--app-accent, #35605A)` CSS variable with fallback. The existing "Seleccionar varias fotos" button uses `#1A1A1A` which is acceptable as it matches the existing code convention for that specific dark button.
  </action>
  <verify>
    <automated>cd /Users/wesleyhudson/Documents/next-monarca && npx tsc --noEmit src/app/app/catalogo/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Page compiles without TS errors. "Compartir mi vitrina" button visible in sticky bar when vitrinaSlug is loaded. Web Share opens on tap (or clipboard copy on unsupported devices). Points awarded on successful share.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit src/app/app/catalogo/page.tsx`
2. Lint passes: `npm run lint -- --no-warn-ignored`
3. Manual: Open /app/catalogo on mobile — "Compartir mi vitrina" button appears above "Seleccionar varias fotos"
4. Manual: Tap button — share sheet opens with vitrina URL
5. Manual: On desktop (no Web Share) — link copies to clipboard, feedback shown
</verification>

<success_criteria>
- Reseller sees "Compartir mi vitrina" button in catalog page sticky bar
- Web Share API opens with correct vitrina URL format: `{SITE_URL}/vitrina/{slug}`
- Clipboard fallback works when Web Share is unavailable
- Server action calls awardPoints with acao "compartilhou_link_vitrina"
- No TypeScript errors, no lint errors
- Zero new hex/px magic values for the new button (uses CSS variable)
</success_criteria>

<output>
Create `.planning/quick/260525-qg0-bot-o-de-compartilhar-link-da-vitrina-co/260525-qg0-SUMMARY.md` when done
</output>
